# 饮食管家总功能开发计划 0.4

> 文档性质：产品需求、业务约束、工程路线、开发任务、验证证据与发布条件的总控文档
> 文档位置：仓库根目录 `总功能开发计划0.4.md`
> 编制日期：2026-08-13（Asia/Shanghai）；基线：`DOC-0.3`提交`d8e07366383d1685184e80bffddf16e1d2fbea3c`、文件SHA-256 `9914C27DD653AF757DDF58FAC3273C11E91AAC7C9D5D1D95583FF27CDACC8C79`
> 当前状态：用户已明确批准`DOC-0.4`成为唯一现行计划入口；B仍是唯一产品主线。`CHG-20260813-002`触发的`SH-TRACE-001`重开已由`EV-20260813-035`完成确定性镜像刷新与独立复核，现由`X-GATE-002`作为唯一`进行中`工作。仓库路径合同采用可移植相对模板，机器绝对map是忽略的本地门产物；只有有界预检全部通过才可原子生成，不得据模板或候选map提前授权产品任务。0.4只把来源路由/Doctor纳入0.1营养实现，把证据研究/资料变更监测纳入0.2；不把通用爬虫、社交平台登录、Cookie管理或自动推送提前塞入核心记录链
> 产品状态：PRODUCT-0.1与PRODUCT-0.2仍不可安装。`X-GATE-002`已知冻结契约`contract_id=diet-manager/contract-v2`、`contract_version=2`、`contract_sha256=632B2BBF8D0E6C655F4C0A47958828A86C67B3240065984CCC78A808E6F7072E`；追踪前置已由`EV-20260813-035`关闭，现允许重新运行有界预检、生成本地map并进入两轮门复核，但在门正式判定`bind_b_ready`前仍不得开始`SEL-*`产品实现。0.3保留为只读历史基线，不被覆盖，也不是现行权威

---

## 0. 文档使命与使用规则

### 0.1 为什么需要这份总计划

本项目同时存在产品规则、自然语言交互、库存、营养、进度、纠错、存储以及 A/B/C 三条工程路线。如果没有一份具有约束力的总计划，极易发生以下问题：

- 只知道某个 foundation 能构建，却误以为产品已经可以使用；
- 功能在不同文档中重复定义，修改一处后其他位置继续保留旧规则；
- A/B/C 各自实现了不同语义，最终无法公平比较；
- 任务只有“做功能”的描述，没有依赖、交付物、验收案例和证据；
- 已发生的饮食事实因为库存或营养问题而丢失；
- 验证和测试意外接触正式 JSONL 或 SQLite 业务数据；
- 用户无法从文档看出当前进度、下一目标、阻塞原因和正式安装条件。

因此，本文件必须同时回答：

1. 产品最终要解决什么问题；
2. PRODUCT-0.1 与 PRODUCT-0.2 分别包含什么；
3. 每项业务行为采用什么不可退化的规则；
4. A/B/C历史候选如何收敛为B唯一主线并保留必要安全约束；
5. 当前完成了什么、尚未完成什么；
6. 下一步应该做什么；
7. 什么证据才能证明一项任务真正完成；
8. 达到哪些硬门后才能称为可安装产品。

### 0.2 本文件不是聊天摘要

0.3按初始会话原文逐轮处理；0.4在该已核准基线上继续读取新增用户轮次和用户提供的两份技术说明。不得只依赖模型压缩后的上下文或二手摘要生成全文，也不得用技术说明中的示例产品范围替代饮食管家自己的产品边界。

文档末尾必须保留“会话覆盖台账”，逐轮记录：

- 原始轮次标识；
- 用户提出的目标、纠正或批准；
- 对应的最终生效规则；
- 落入本文件的章节；
- 是否被后续决定替代。

会话中的临时建议如果后来被用户否决，不能继续作为现行规则；但必须在决定台账中保留被替代关系，防止旧规则复活。

### 0.3 唯一事实来源原则

本文件采用以下分工，避免在多个位置重复维护同一状态：

- 产品和交互规则：只在带稳定需求编号的需求章节定义；
- 架构决定：只在决定台账定义，其他章节只引用决定编号；
- 开发状态：只在总任务台账维护；
- 任务完成证据：只在证据台账维护；
- 里程碑和路线矩阵：由任务、决定和证据编号派生，不另写第二份人工状态；
- 会话覆盖：只在会话覆盖台账维护。

任何规则变更都必须同时完成：需求更新、受影响案例更新、任务重开或新增、决定记录和新鲜验证证据。只改说明文字不算完成变更。

### 0.4 规范用语

本文件中的关键词具有约束含义：

- **必须**：发布前不可省略；
- **不得**：任何路线都不能违反；
- **应该**：默认实现，偏离需要决定记录和证据；
- **可以**：允许但不强制；
- **已完成**：交付物存在、验收通过并有新鲜证据；
- **未开始**：尚无满足完成条件的实现和证据；
- **进行中**：已经开始但尚未满足完成条件；
- **阻塞**：存在明确外部条件或前置决定，当前无法继续；
- **已取消**：任务因范围变更或路线停止而终止，历史仍保留。

禁止使用“基本完成”“外壳完成”“差不多可用”等自由状态代替正式状态。

### 0.5 不同对象使用不同状态体系

任务状态只允许：

```text
未开始
进行中
阻塞
已完成
已取消
```

需求处置只允许：

```text
0.1 必须
0.2 必须
后续候选
明确排除
已取代
```

路线状态只允许：

```text
实验中
有限对照
暂停
停止
发布候选
已发布
```

命令结果、持久记录生命周期、问题生命周期和批次生命周期分别使用各自章节规定的枚举，不得把不同对象状态混成一个自由短语。

### 0.6 信息的唯一可写位置

| 信息 | 唯一可写位置 | 其他位置的规则 |
|---|---|---|
| 产品与交互要求 | `REQ-*`需求台账及其规范章节 | 只引用需求ID |
| 实施状态 | 唯一任务台账 | 控制台、里程碑和路线矩阵只派生展示 |
| 验证事实 | `EV-*`证据台账 | 任务只引用证据ID，不复制“已通过”结论 |
| 生效决定 | `DEC-*`决策台账 | 正文和任务引用决定ID |
| 尚未决定问题 | `Q-*`待决台账 | 未决事项不得写成产品事实 |
| 风险与技术债 | `RISK-*`、`DEBT-*`台账 | 不用任务自由文本替代 |
| 变更与重开 | `CHG-*`变更台账 | 机器文件只作镜像，不独立维护状态 |
| 可执行案例 | `shared/acceptance-cases/`及案例台账 | 正文保存规则和覆盖关系 |
| 会话迁移证据 | 附录A | 不承担业务实施状态 |

### 0.7 任务完成定义

任务只有同时满足以下条件才可标记`已完成`：

1. 指定交付物存在于计划路径；
2. 任务台账`C[]`引用的局部案例中，由该任务brief精确声明的`case_assertion_paths`全部通过；同时，该任务的`full_case_set`若不是`none`，其展开后的每个案例都必须在规定的同一候选上完整通过。`full_case_set`独立补充`C[]`，无须把阶段全集复制进C[]；组件任务只可认领自身交付物或已完成上游依赖能够产生的断言；`R[]`只表示需求责任，不自动继承该需求全部发布案例；
3. 相关回归测试通过；
4. 验证发生在相关文件最后一次修改之后；
5. 至少引用一条有效`EV-*`证据；
6. 验证使用隔离测试数据，正式数据前后不变；
7. 没有未登记的接口、范围或迁移变化；
8. 需要独立复核的任务已经由复核角色确认。

### 0.8 计划维护纪律

- 全局共享任务WIP上限为1；路线收敛后只允许B有1个路线实现任务处于`进行中`，A/C不再占用完整实现WIP；
- 开发前确认依赖、交付物、验证命令、正式数据根和隔离数据根；
- 开发后只在唯一任务、证据、风险、决定和变更台账更新状态；
- 需求冻结后新增`0.1 必须`，必须登记`CHG-*`并说明价值、成本、依赖和被推迟工作；
- 被路线门停止的后续任务改为`已取消`，不删除历史、不冒充完成；
- 摘要、路线矩阵和里程碑是派生视图，与唯一任务台账冲突时必须修正视图。

### 0.9 新需求进入范围的最低材料

任何新能力只有同时具备以下内容，才能从候选池进入0.1或0.2：

- 至少一条真实用户话术；
- 所需数据字段和来源；
- 写入与不写入规则；
- 降级和失败策略；
- 至少一个正常案例和一个边界/失败案例；
- 任务依赖、交付物和验收证据；
- 对现有范围、迁移和发布门的影响。

`明确排除`项不进入产品范围，因此只要求至少一个能够证明拒绝边界的案例，不要求为被排除能力虚构“正常成功”案例；所有`0.1 必须`和`0.2 必须`仍必须同时具备正常与边界/失败覆盖。

### 0.10 DOC-0.4 的资料吸收原则

本版读取并冻结以下两份输入的文件身份；它们是设计参考，不是可执行依赖，也不自动获得产品权威：

| 资料 | SHA-256 | 直接吸收 | 改造后吸收 | 本版不纳入 |
|---|---|---|---|---|
| `Agent Reach Skill 架构深度拆解.md` | `6ED21AF1FCBF89CC07013EED5C07C216CFB238198BF8219BC50429F593E0D12A` | 来源适配器、主/备后端、真实Probe、Doctor、分级授权、安全配置 | 只用于营养资料和受控研究来源，不建设通用平台聚合层 | 大量社交平台、Cookie登录、一键安装全部渠道、内部自动调度 |
| `last30days Skill 架构深度拆解.md` | `B5704527720402B914AB728304491903598749CC574480F62F7A1A1CE5BFED20` | 证据先行、部分覆盖、可信度门、历史去重、空结果 | 用于营养资料更新研究和可审阅简报，不把热度当营养真值 | 自动写饮食/库存、自动采用研究结论、强制每日推送、首版全网趋势发现 |

吸收后的固定边界：

1. 核心记录链永远不依赖网络研究成功；网络失败时饮食事实照常提交，营养使用可靠缓存/模板或保持`unknown`；
2. 0.1来源能力只回答“当前可用哪一个受信来源、如何安全降级、为什么不可用”，不做趋势研究；
3. 0.2研究能力只产生带来源、覆盖和置信度的候选发现/更新建议，默认`preview`，不得直接修改饮食事实、库存、历史快照或已确认Profile；
4. 外部调度决定何时运行，研究引擎决定是否有新证据，交付层决定是否展示；Skill内部不常驻自建调度器；
5. 任一新增来源、后端、凭据、写入或推送能力都必须走独立CHG、隐私评审和失败案例，不能因“技术资料建议”自动启用。

---

## 1. 当前项目真实状态

### 1.1 当前目录和三条 foundation

项目根目录：

```text
<repository-root>
```

当前存在三套 0.1 foundation：

| 路线 | 目录 | 当前具备 | 当前不具备 |
|---|---|---|---|
| A：纯 Skill | `version-a-skill-only` | Skill 结构、固定 JSONL 边界、追加式存储约定、结构验证 | 经验证的口语业务闭环、可靠跨文件事务、完整业务实现 |
| B：轻量插件 | `version-b-lite-plugin` | TypeScript 插件外壳、`diet_manager` 工具边界、构建与测试入口 | 正式业务处理、SQLite 持久化、库存与营养事务 |
| C：强约束插件 | `version-c-strict-plugin` | TypeScript 插件外壳、显式命令、预览/提交状态边界、构建与测试入口 | 正式业务处理、SQLite 持久化、完整状态事务 |

三者都是工程基底，不是三个已经完成的产品。

当前代码现实必须写死，防止“外壳可构建”被解释成“功能已实现”：

- A仓库没有确定性解析器、JSONL Schema、写入器、读取投影、锁、事务、幂等或恢复harness；现有测试只做文件/文本结构检查；
- B的八个可移植action当前统一返回`foundation_not_implemented`和`committed:false`；已完成Node 24内置`node:sqlite`的test-owned兼容spike，但没有业务表、迁移或正式持久化；
- C的合法状态转换同样返回`foundation_not_implemented`和`writes:false`；状态由调用者直接传入，没有服务端预览记录或token，且工具参数当前无法传入Skill要求保留的`source_text`；
- `business-contract.md/CONTRACT-v2`候选已通过专用机器校验，但尚未被B运行时或共同业务Oracle绑定；共同案例仍未执行业务Oracle。

### 1.2 foundation 状态派生视图

本节只展示`§28.2`证据台账的派生结论，不是第二份证据源。原始会话首次执行形成历史证据`EV-20260809-001`、`EV-20260809-002`；本版又在2026-08-09使用隔离Node 24.15.0完成新鲜重验`EV-20260809-009`，结果为：

```text
Version A structure：通过
Version B：7/7 测试通过，构建通过，OpenClaw 2026.7.1插件验证通过
Version C：7/7 测试通过，构建通过，OpenClaw 2026.7.1插件验证通过
```

`shared\validate-foundations.ps1`已经具备以下基础保护：

- 为 B/C 插件验证创建唯一临时 `OPENCLAW_STATE_DIR`；
- 不复用调用者原有 OpenClaw 状态目录；
- 执行结束恢复调用者环境变量；
- 清理临时 OpenClaw 状态目录；
- 运行结束扫描项目内 `.jsonl`、`.sqlite`、`.sqlite3` 和 `.db` 文件。

新增的状态隔离回归测试验证：

- 两次插件验证均使用隔离目录；
- 不使用调用者状态目录；
- 验证后临时目录不存在；
- 原环境变量被恢复。

本次独立业务数据与辅助文件前后扫描结果为：

```text
BUSINESS_DATA_BEFORE=0
BUSINESS_DATA_AFTER=0
BUSINESS_DATA_CREATED=0
BUSINESS_DATA_CHANGED=0
OPENCLAW_TEMP_LEFTOVERS=0
```

扫描覆盖`.jsonl/.sqlite/.sqlite3/.db`以及WAL、SHM、journal和JSONL临时/日志辅助文件。本轮没有创建JSONL或SQLite业务数据。证据同时记录了两个环境故障及修复：失效的`node_modules`链接由pnpm重建；OpenClaw要求Node至少24.15.0，最终使用校验过官方SHA-256的隔离Node 24.15.0通过。

证据边界必须保留：它只证明“执行前业务类文件为0”的基线，且当前共享脚本仍缺少预置正式数据哈希不变、失败路径照常扫描、临时状态删除前审计和源码外构建等能力。`SH-SAFE-BASE-001`必须在G1前关闭这些缺口；foundation通过不代表任一业务handler已经实现。

### 1.3 当前没有开发完成的功能

当前 foundation 验证不证明以下能力已经实现：

- 可靠理解用户口语中的饮食、饮水、购买、查询和纠正；
- 把一餐中的多项食物规范化保存；
- 商品身份、包装、批次、位置、开封和保质期生命周期；
- 默认读取家庭库存并安全扣减；
- 数量模糊、库存不足和多候选时的事实优先降级；
- 权威营养数据、联网检索、缓存、来源版本和营养快照；
- 白水与营养饮品分离；
- 固定六项日进度；
- 异常中心和快捷回复；
- 追加式纠正、撤销、恢复和库存补偿；
- 日查询、周回顾、导出和个人模板；
- A 的可证明 JSONL 事务；
- B/C 的正式 SQLite Schema、迁移和事务；
- 面向普通用户的安装、升级、备份、恢复和卸载。

### 1.4 当前不得对外作出的承诺

在正式发布门通过前，不得声称：

- “饮食管家已经开发完成”；
- “现在可以正式安装并用于日常记账”；
- “A 版已经可以可靠保存业务数据”；
- “B/C 插件已经具备正式饮食功能”；
- “foundation 的 `0.1.0` 就是 PRODUCT-0.1”；
- “验证通过说明库存、营养和进度业务正确”。

当前正确表述是：三套foundation的历史工程结构、构建、基础测试、插件声明、状态隔离和空数据基线下零业务数据创建已有2026-08-09证据；2026-08-10用户已直接指定B为唯一产品主线，但B完整业务、正式存储与安全门仍未实现，产品仍不可安装。该裁决没有产生新的G1/G2评分或EV。

---

## 2. 产品版本边界

### 2.1 FOUNDATION-0.1

FOUNDATION-0.1 是工程比较阶段，只用于证明：

- 目录与组件边界清晰；
- A 的固定 JSONL 契约可以被静态验证；
- B/C 可以构建和通过 OpenClaw 插件验证；
- foundation handler 不进行业务写入；
- 验证过程不污染用户 OpenClaw 状态；
- 验证过程不创建 JSONL 或 SQLite 业务数据。

它不包含日常产品承诺。

### 2.2 PRODUCT-0.1：可靠日常饮食账本

PRODUCT-0.1 的目标不是“拥有最多功能”，而是做到：

```text
低摩擦记录已经发生的饮食事实
→ 安全关联家庭库存与营养
→ 允许即时纠错
→ 返回可靠的当天进度与明细
```

PRODUCT-0.1 至少包括：

- 本人饮食和白水记录；
- 商品入库与库存批次；
- 营养来源和进度；
- 异常保存与后续补全；
- 追加式纠正、撤销和恢复；
- 当日记录与进度查询；
- 幂等、事务、隐私和正式数据隔离；
- 正式安装、备份、恢复和安全卸载。

### 2.3 PRODUCT-0.2：记录与回顾助手

PRODUCT-0.2 在不削弱 0.1 可靠性的前提下增加：

- 个人菜品模板与常用记录复用；
- 更完整的周回顾和条件查询；
- 临期提醒；
- 更丰富的库存生命周期操作；
- 用户可读导出与匿名化选项；
- 批量补录和经过确认的便利规则。
- 营养资料多来源研究、覆盖说明与可信度门；
- 已关注食品/商品的资料变更监测、历史去重和可审阅更新建议。

PRODUCT-0.2的研究能力不是健康资讯推荐器。它只研究与食品身份、包装标签、营养基准、权威数据库版本和来源变化有关的证据；默认输出预览，不自动改变饮食、库存、目标或历史营养快照。

### 2.4 明确排除的产品层

本项目不建设“0.3 非医疗健康管理助手”。不纳入：

- 诊断；
- 治疗建议；
- 药物建议；
- 疾病风险判断；
- 自动减重、增肌或医疗营养方案；
- 未经用户明确请求的健康评价和饮食推荐。

六项目标和进度只用于用户自行配置的记录追踪，不构成健康建议。

---

## 3. 当前安装边界

### 3.1 现在能够做的是开发验证

当前可执行：

```powershell
Set-Location '<repository-root>'
powershell -ExecutionPolicy Bypass -File .\shared\validate-foundations.ps1
```

该命令只验证 A/B/C foundation，不安装正式产品，不创建可用于日常记录的业务数据库。

命令前置条件必须明确：PATH中有可用`npm`，或同时设置`DIET_MANAGER_NODE`、`DIET_MANAGER_TOOL_MODULES`和`DIET_MANAGER_OPENCLAW_ENTRY`。当前OpenClaw 2026.7.1拒绝Node 24.14.0，EV-009使用隔离Node 24.15.0通过；后续`SEL-INSTALL-001`必须把支持版本和运行时获取方式固化，不能要求普通用户临时拼环境变量。

### 3.2 当前不建议把任何路线用于正式日常使用

- A 只有规则和存储约定，跨文件原子性尚未通过故障测试；
- B 的操作仍返回 `foundation_not_implemented`；
- C 的正式业务同样未实现；
- 同时启用三条路线会带来工具冲突、重复写入和错误比较风险。

原始会话中曾出现“优先安装 A”的临时建议，后续已经被“当前尚未开发完成，不应作为正式产品安装”的事实替代。本文件以替代后的结论为准。

### 3.3 正式安装的前置条件

路线已由`DEC-027`固定为B；只有B依次通过安全、纵切/路径绑定与PRODUCT-0.1发布门后，才提供一个统一安装入口。普通用户不需要安装三套foundation，也不需要自行决定A/B/C。

详细安装、升级、备份、恢复和卸载约束见后续专章。

---

## 4. 总计划的组织方式与治理原则

### 4.1 一份总计划统管全局

本项目采用“一份总计划 + 领域章节 + 唯一任务台账 + 派生进度视图”的结构，同时吸收以下三种管理需要：

1. 一眼看到产品目标、当前阶段、下一步和发布条件；
2. 能按领域查看数据模型、交互规则、测试和交付物；
3. 能查看A/B/C历史比较、当前B-only收敛和取消/合并去向，但不把路线凌驾于产品目标之上。

不把功能拆散成互不约束的多份主计划，也不为 A/B/C 分别维护三套产品需求。专项契约、Schema、案例和证据可以独立成文件，但必须由本总计划通过稳定 ID 管理其职责、状态和依赖。

### 4.2 产品目标优先，B为唯一产品主线

自2026-08-10起，用户通过`DEC-027`直接指定B承担PRODUCT-0.1/0.2完整实现。该范围裁决替代“先三路全量比较再选路”，不表示B已经安全、就绪或可安装：

交付形态唯一引用 `DEC-018`；所有路线、安装与发布不得偏离该决定。

- 产品需求先冻结；
- 共同 Schema 和验收 Oracle 先建立；
- B用共同输入、时钟、fixture和期望结果验证；
- `SH-SAFE-BASE-001`先完成，随后B依次通过存储硬安全、纵向闭环/故障和路径绑定门；
- A停止后续完整实现，只保留只读/无插件降级规范；
- C停止独立完整开发，其服务端预览与状态安全控制通过`B-MERGE-C-001`明确并入B。

“B是主线”不能取代“用户最终得到什么”。B只要出现错误正式写入、重复副作用、负库存、半事务或路径越界，就不得进入正式发布；用户裁决不能跳过任何硬安全门。

### 4.3 A/B/C 不是产品版本

必须始终分开：

```text
PRODUCT-0.1 / PRODUCT-0.2 = 面向用户的能力阶段
A / B / C                 = 面向工程的实现路线
```

每个产品阶段只维护一份业务契约和案例集合。A/B/C 不得各自发明不同的产品含义，也不得借用另一条路线的证据标记本路线完成。

### 4.4 采用结构性重构，不做尾部堆叠

总计划后续优化采用结构性重构：

- 保留原计划中仍然有效的事实、任务和证据；
- 删除重复状态和已失效规则；
- 围绕日常使用闭环重新组织功能；
- 按 PRODUCT 阶段而不是按目录顺序安排开发；
- 建立稳定需求 ID、案例 ID、决定 ID、任务 ID 和证据 ID；
- 把新增推理需求放入明确阶段，不能直接膨胀 0.1。

禁止只在旧文档末尾不断追加新章节，因为这会造成同一规则在多个位置互相冲突。

### 4.5 一项任务的最低字段

正式任务记录采用规范化组合，不要求把所有字段横向挤进一张超宽表：§31逐任务行保存身份、范围、依赖、交付物、直接验收、根代码、证据、责任人与状态；§31.1把根代码展开为`official_data_roots`和`isolated_test_roots`；任务进入`进行中`前，`docs/work-items/<task_id>-brief.md`必须补齐精确命令、风险/决定/变更引用和其余执行字段。三者按`task_id`连接后，每个开发任务至少包含：

```text
task_id
product_scope
milestone
task_kind
route
objective
requirement_ids
case_ids
case_assertion_paths
full_case_set
dependencies
deliverables
verification_commands
acceptance_oracle
required_evidence_types
actual_evidence_ids
risk_ids / decision_ids / change_ids
official_data_roots
isolated_test_roots
owner
reviewer
status
blocker
next_action
reopen_condition
```

`未开始`任务可以暂时只有§31紧凑行，但没有精确`verification_commands`、`case_assertion_paths`、`full_case_set`、完整`acceptance_oracle`、`risk_ids/decision_ids/change_ids`和绝对根解析的任务不得转为`进行中`。已完成的历史任务若没有独立brief，必须由其EV结构化元数据承担同等字段并明确遗留限制；其EV没有断言级信息时，只能在EV确实执行完整Oracle的案例上按`*`认定。没有依赖、交付物、验收Oracle和证据要求的条目只能作为候选事项，不能进入执行中。

### 4.6 完成判定

完成判定只采用`§0.7`的八项定义；本节不再维护第二套条件。额外解释只有一条：“写完代码”“测试曾经通过”或“结构看起来正确”都不能单独构成完成证据。正式状态只写入`§31`唯一任务台账。

### 4.7 文档版本关系

| 文档 | 定位 | 处理原则 |
|---|---|---|
| `总功能开发计划.md` | 最初总计划，建立了 M0—M10、阶段任务和路线矩阵 | 作为历史输入，不继续直接堆叠 |
| `总功能开发计划0.2.md` | 大规模结构化增强稿，包含需求、案例、决定、任务和证据 | 作为重要基线逐项核对，不自动视为全部现行规则 |
| `总功能开发计划0.3.md` | 按完整原始会话重建并承载B纵切/故障闭环的总控版本 | 作为0.4冻结基线，不再继续追加新功能 |
| `总功能开发计划0.4.md` | 在最新0.3上吸收来源能力和证据研究边界的候选总控版本 | 独立复核和用户批准后成为新的计划入口；批准前不得替代0.3 |

0.3 不通过简单复制 0.2 生成；0.4也不是简单改标题。0.4必须冻结0.3的Git/文件身份，保留其完成证据和门状态，再把新增资料逐项映射为需求、案例、任务、风险和明确排除边界。

---

## 5. 全局产品硬约束

### 5.1 事实优先，而不是附加效果优先

核心处理顺序固定为：

```text
确认是否已经发生
→ 保存能够确定的本人饮食或入库事实
→ 计算能够证明的营养
→ 尝试安全库存关联
→ 将无法确定部分登记为待补充
→ 返回实际提交结果
```

以下问题不得导致已经发生的饮食事实丢失：

- 数量模糊或缺失；
- 营养资料未知；
- 库存没有候选；
- 不同商品存在多个候选；
- 库存不足；
- 单位无法换算；
- 组合食品的部分配料未知。

只有“无法确认是否发生、无法识别任何食品、核心事实互相矛盾或存储本身不可用”才允许阻止对应事实提交。

### 5.2 只记录本人

- 产品不建立家庭成员档案；
- 中文省略主语的饮食表达默认指用户本人；
- 明确属于孩子、朋友或其他人的饮食不写入本人记录；
- 多人共同进食但本人份量不明时，只保存本人确实参与的事实，个人数量保持未知或询问；
- 不提供“切换家庭成员”或“为他人代记”的功能。

### 5.3 不建设健康建议层

- PRODUCT-0.1 和 PRODUCT-0.2 都以记录、数据质量、查询和回顾为边界；
- 用户目标只用于展示用户自行设置的进度；
- 不主动评价饮食好坏；
- 不主动给出剩余热量预算、下一餐建议、减重建议或医疗建议；
- 用户明确要求分析时，只能在产品边界允许的范围内描述记录事实和数据覆盖，不能诊断或治疗。

### 5.4 原话、未知和依据永久保留

- 用户原始表达必须保存；
- 规范化值不能覆盖原始值；
- 未知不能写成 0；
- 估算不能伪装成用户明确事实；
- 每次换算、模板和来源都必须可追溯；
- 后续资料更新不能静默改变历史记录；
- 纠正只能通过追加事件形成新有效视图。

### 5.5 非家庭场景才跳过家庭库存

默认行为是读取并尝试关联家庭库存。只有存在以下可靠证据时才跳过：

- 用户明确说在外面、餐厅、食堂、公司、朋友家等；
- 用户明确说明外卖或食品不来自家庭库存；
- 用户明确要求“只记录”“不要扣库存”；
- 当前对话已有可靠且仍有效的非家庭场景。

用户没有说“在家”不能成为跳过库存的理由。

### 5.6 用餐场景的结构化来源

```text
home_default
external_explicit
delivery_explicit
skip_inventory_explicit
unknown
```

`home_default`表示“没有发现非家庭证据，因此按产品默认策略尝试库存”，不表示用户明确说了在家。`unknown`只表示场景证据不足、互相冲突或无法归一，不是跳过家庭库存的理由；只要没有可靠`external_explicit/delivery_explicit/skip_inventory_explicit`证据且饮食事实本身可提交，`unknown`仍必须读取并尝试库存，无法安全匹配时按库存异常降级。场景值、原始证据和采用规则版本必须同时保存。

`MealContext`最低字段为`scene`、`evidence_kind`、`source_message_id`、`source_text_fragment`、`scope`、`generated_at`、`valid_until`、`context_revision`和`rule_version`。跨餐次、跨日期、用户改口或达到`valid_until`后不得继续借用“在公司/只记录”等旧场景；对应过期案例进入`REQ-CONTEXT-003`。

---

## 6. 日常饮食记录规范

### 6.1 支持的日常表达

PRODUCT-0.1 必须支持：

- 早餐、午餐、晚餐、加餐、夜宵和未知餐次；
- 一餐多个食品和饮品；
- 单一食物、包装商品、普通菜品、组合菜品；
- 精确、近似、范围、模糊、分数和缺失数量；
- 当前记录与过去补录；
- 用户明确要求只记录、不扣库存或不计算营养；
- 白水与营养饮品在同一句话中分别处理。

示例：

```text
早餐吃了两个鸡蛋、一片面包，喝了250ml牛奶和300ml白水。
```

应形成同一餐次中的三个 `diet_meal` 项和一个独立 `diet_water` 项，而不是把牛奶当作白水。

### 6.2 饮食事件最低字段

```text
event_id
meal_id
schema_version
event_type
source_text
source_message_id
conversation_id
received_at
committed_at
occurred_at_text
occurred_time
meal_slot
meal_slot_source
context
items[]
lifecycle_status
provenance_refs[]
```

时间解析还必须能够表达一个范围而不是伪造精确时刻：

```text
OccurredTime
├─ raw_text
├─ resolved_start
├─ resolved_end
├─ precision: exact | date | meal_period | approximate | unknown
├─ timezone
├─ resolution_basis
├─ resolution_anchor
└─ resolver_version
```

日查询采用用户时区下的`[自然日开始, 次日自然日开始)`半开区间。`中午`、`早餐`等只保存时间段精度；解析规则升级不得静默重算历史时间。

`resolved_occurred_at`只允许作为`precision=exact`时的兼容派生字段，不能替代`OccurredTime.resolved_start/resolved_end`；日期、餐段、近似和未知时间必须保留区间语义。

### 6.3 食物项目最低字段

```text
item_id
raw_name
normalized_name
item_type
raw_amount_text
amount_kind
stated_amount
stated_unit
normalized_amount
normalized_unit
nutrition_amount
inventory_amount
conversion_basis
conversion_source
inventory_effect_intent
nutrition_effect_intent
quality_flags[]
```

FactCommit中的`MealItem`只保存效果意图和已知输入，不内嵌尚未提交的实际库存效果或营养快照。`EffectBundle`另行追加`InventoryEffect/NutritionSnapshot`并由当前投影暴露引用；不得回写原MealItem制造“历史一直完整”的假象。

其中 `amount_kind` 至少支持：

```text
exact
approximate
range
vague
missing
```

用户描述量、营养计算量和库存扣减量必须分开，不能共用一个数值字段。

完整份量结构至少支持：

```text
Amount
├─ raw_text
├─ kind: exact | approximate | range | vague | missing
├─ value
├─ min_value
├─ max_value
├─ unit_raw
├─ unit_normalized
├─ qualifier
├─ conversion_basis
├─ conversion_formula
├─ conversion_source
└─ evidence_kind
```

以下四个值必须分别存储，不得默认相等：用户实际描述的摄入量、营养计算采用量、库存实际扣减量、商品包装/单件规格。

### 6.4 数量不完整

- 数量模糊或缺失时仍保存饮食事实；
- 保存原始描述，例如“半碗”“一点”“两三口”；
- 能够合理估算时保存估算值和依据；
- 无法形成有限合理范围时保持未知；
- 不得为了减少追问而虚构 1 份、100g 或其他标准份量；
- 数量问题进入待补充中心；
- 营养和库存分别判断是否具备安全计算条件。

### 6.5 组合菜品

组合菜品必须避免整道菜与配料重复统计。处理层级为：

1. 用户明确组成：按明确配料记录；
2. 已确认个人模板：按个人模板记录；
3. 适合透明通用模板的常见菜品：按模板估算并标明组成；
4. 配方差异很大的套餐、自选或自制混合食品：保留整体事实，等待配料或标签。

同一项不能同时把整道菜营养和全部配料营养相加。

### 6.6 命令结果和记录生命周期必须分开

命令结果描述本次操作：

```text
committed
committed_with_issues
needs_clarification
ignored
failed
```

- `committed`：事实和本轮要求的附加效果已经终态提交；
- `committed_with_issues`：事实已经提交，但存在业务待补充项或技术附加效果仍待重试；
- `needs_clarification`：连最小事实或安全目标都无法确定，本次未写该子操作；
- `ignored`：计划、否定、纯查询、他人摄入或其他明确不应写入的内容；
- `failed`：事实提交本身失败或结果未知，不能声称已记录。

混合结果中的中文`已完成/已记录但待补充/未执行`必须分别映射为`committed`、`committed_with_issues`、`needs_clarification|ignored|failed`，并保存稳定原因码，禁止只返回自由中文状态。

持久化生命周期描述历史事件：

```text
active
superseded
voided
```

不得用“preview”或“failed”充当已经持久化饮食记录的生命周期状态。

---

## 7. 商品入库最低数据规范

### 7.1 入库事件

每次购买、制作或入库至少保存：

```text
stock_event_id
schema_version
event_type
source_text
source_message_id
received_at
committed_at
occurred_at_text
occurred_time
idempotency_key
items[]
lifecycle_status: active | superseded | voided
```

同一消息可以包含多个商品；不同购买或制作事件默认形成不同批次。

### 7.2 每个入库商品的最低字段

```text
raw_name
normalized_name
product_type
quantity
quantity_kind
unit
batch_id
remaining_quantity
remaining_unit
data_quality
issues[]
```

字段要求：

- `raw_name`必须保留用户原名；
- `normalized_name`用于匹配，不能替代原名；
- `product_type`无法确认时保存 `unknown`；
- `quantity`支持精确、近似、范围、模糊和缺失；
- `remaining_quantity`无法可靠计算时保持未知，不能写 0；
- `batch_id`用于区分同一商品的不同入库批次；
- 数据不完整不等于整个入库失败。

### 7.3 包装层级

包装商品至少支持：

```text
package_count
package_unit
units_per_package
inner_unit
per_item_amount
per_item_unit
derived_inner_count
derived_total_amount
derivation_formula
```

例如“两箱牛奶，每箱12盒，每盒250ml”应同时保存：

```text
2箱
12盒/箱
250ml/盒
24盒
6000ml
2 × 12 × 250
```

不能只保留最终 6000ml。

### 7.4 包装内容未知

“买了一袋鸡蛋”表示：

- 外包装数量为 1；
- 外包装单位为袋；
- 商品为鸡蛋；
- 袋内枚数未知。

系统可以按“1袋”保存，但不能无依据回答剩余枚数，也不能按“个”扣减。应登记 `missing_package_content`，后续允许补充袋内数量或确认只按袋管理。

### 7.5 商品资料关联

入库商品可以关联：

```text
brand
variant_or_flavor
barcode
purchase_location
storage_location
production_date
expiration_date
shelf_life_text
opened_at
nutrition_profile_id
nutrition_source_type
nutrition_source_version
note
```

未提供的信息不得虚构。没有营养表不阻止入库；后续可以补充并关联既有商品或批次。

### 7.6 入库提交行为

| 场景 | 行为 |
|---|---|
| 名称、数量和单位完整 | 正常入库 |
| 数量模糊或缺失 | 保存已知事实，剩余量保持未知或部分可用，登记问题 |
| 包装内容未知 | 按外包装入库，登记包装内容问题 |
| 商品类型不明确 | 保存为 `unknown`，登记资料问题 |
| 没有营养表 | 正常入库，营养资料可后补 |
| 同一消息多个商品 | 分别建立商品项和批次 |
| 包装规格相互冲突 | 不提交矛盾规范量，只澄清冲突部分 |
| 完全没有商品名称 | 对该项要求最小澄清，不影响同消息其他事件 |
| 相同幂等键重试 | 返回原结果，不重复创建批次 |

---

## 8. 待补充与异常中心

### 8.1 目的

```text
先保存确定事实
→ 标出缺失、估算或矛盾部分
→ 不执行无法证明的附加操作
→ 集中进入待补充中心
→ 用户选择现在处理、部分处理、稍后处理或不再处理
```

面向用户统一称为“待补充”；内部保留稳定问题代码。异常不等于写入失败。

### 8.2 问题分类

#### A. 真正阻止本次事实记录的问题

```text
reference_ambiguous
consumption_state_ambiguous
negation_scope_conflict
food_identity_unrecognized
```

只有连“用户已经吃了什么”这一最小事实都无法确认时，才把它作为可持久Issue的阻塞问题。系统一次只问阻止安全提交的最小问题。

`transaction_failed`、`storage_unavailable`和`commit_outcome_unknown`属于命令失败原因，不属于持久Issue：FactCommit本身失败时没有可靠的`entity_id`，系统不得声称已经保存Issue。只有FactCommit已经成功、随后附加效果或最终投影失败时，才允许创建`effect_processing_failed`或`progress_projection_pending`，并引用已提交事实和对应outbox；该Issue本身也必须在可证明的提交边界内成功，否则只返回稳定命令失败原因。

#### B. 不阻止饮食事实的待补充项

```text
quantity_missing
quantity_estimated
quantity_vague_unresolved
composition_unknown
nutrition_missing
nutrition_estimated
missing_package_content
inventory_no_match
inventory_multiple_candidates
inventory_insufficient
inventory_unit_unconvertible
inventory_batch_uncertain
occurred_time_defaulted
meal_slot_inferred
storage_location_uncertain
shelf_life_unknown
possible_duplicate
effect_processing_failed
progress_projection_pending
```

这些问题只影响数量精度、营养覆盖、库存附加效果或展示精度，不得反向阻止已经发生的饮食事实落库。

#### C. 通常不主动追问的可选资料

```text
optional_brand
optional_variant
optional_note
optional_storage_confirmation
optional_common_food_label
```

普通原始食材已有可信资料时，不要求用户上传标签；位置已有可靠规则时，不为了形式完整反复追问。阻塞问题只阻止无法确定的子操作，不能让同一消息中其他已明确事件一起丢失。

### 8.3 问题最低模型

```text
issue_id
issue_code
issue_type: blocking_fact | non_blocking_business | non_blocking_technical | optional
priority: critical | high | normal | low
entity_type
entity_id
field_path
detected_at
source_message_id
source_text
known_facts
missing_or_conflicting_facts
impact
candidate_values[]
candidate_actions[]
status: IssueStatus（唯一枚举见§8.12）
revision
last_presented_at
resolved_at
resolution_source
resolution_reason
resolution_event_id
```

`issue_id`是唯一标识；不得再建立内容等价的`anomaly_id`。`resolution_reason`当前只允许以下完整集合：

```text
user_supplied
user_confirmed
reliable_evidence_resolved
kept_estimate
deferred_by_user
dismissed_by_user
event_superseded
effect_retry_succeeded
```

`deferred_by_user`记录`awaiting_user → open`的延期原因，其余值分别对应补充、确认、可靠证据自动解决、保留估算、明确不补、上游事件失效和技术效果重试成功。未发生转换时该字段为`null`，不得填自由文本；面向用户的自然语言说明另存展示字段。增加、重命名或删除枚举值必须通过`SH-CONTRACT-004`的契约版本变更和迁移，不允许实现临场扩展。

`resolution_source`只允许`user/reliable_context/system_retry/event_lifecycle`。`open`或`awaiting_user`时，除延期后的`resolution_reason=deferred_by_user`外，`resolved_at`、`resolution_event_id`和其他关闭原因必须为`null`；`resolved`或`dismissed`时必须同时具备`resolved_at`、允许的`resolution_source`和匹配状态的`resolution_reason`，需要追加事件的转换还必须有`resolution_event_id`。

### 8.4 统一提示而不是连续盘问

一轮记录产生多个问题时，先完成能够提交的事实，再统一展示重点问题。不得连续追问数量、商品、批次、营养表和餐次，把一次记录变成问卷。

快捷选项示例：

```text
米饭数量：
A. 按100g估算
B. 按200g估算

牛奶库存：
C. 扣减全脂牛奶1盒
D. 扣减低脂牛奶1盒

也可以直接说明实际情况，不必选择以上选项。
```

用户可以回复字母组合，也可以直接说“米饭150克，牛奶扣低脂的”。快捷选项只是高效模板，不是唯一合法输入。

### 8.5 快捷选项执行规则

- 选项必须来自现有数据、可靠换算或明确标注的估算；
- 每个选项说明会修改什么或扣减什么；
- 每个问题只给少量最有用候选；
- 必须存在保持原样、稍后处理或不关联的出口；
- 选项与具体提示、问题和目标记录绑定；
- 执行前重新检查记录、库存和换算依据；
- 旧选项过期或状态变化后不得直接执行；
- 自然语言提供的新事实优先于字母指代；
- 已经明确的部分先处理，剩余歧义只问一个最小问题；
- 同一问题重复解决不得重复扣减或重复修改。

每一组面向用户的快捷选项最后一行都必须明确允许自由文本回复，例如：

```text
也可以直接说明实际情况，不必选择以上选项。
```

可以根据对象自然化，但不得省略“不是只能回复字母”的含义。

### 8.6 问题解决也是追加事件

```text
IssueResolutionEvent
├─ resolution_id
├─ issue_id
├─ target_entity_id
├─ supplied_facts
├─ source_text
├─ created_at
├─ resulting_correction_id
├─ resulting_inventory_effect
├─ resolution_reason
├─ application_outcome: applied | no_change | rejected
└─ rejection_reason
```

`application_outcome`不是`IssueStatus`，唯一含义如下：

| application_outcome | `resolution_reason` / `rejection_reason` | IssueStatus结果 |
|---|---|---|
| `applied` | `resolution_reason`必须直接使用§8.3唯一枚举中与动作匹配的`user_supplied/user_confirmed/reliable_evidence_resolved/kept_estimate/deferred_by_user/dismissed_by_user/effect_retry_succeeded`；`rejection_reason=null` | 按§8.12变为`resolved`、`dismissed`，或延期后回到`open` |
| `no_change` | 两个原因字段均为`null` | 保持原IssueStatus，不生成无意义纠正或库存效果 |
| `rejected` | `resolution_reason=null`；`rejection_reason`只允许`stale_revision/expired_prompt/conflicting_selection/prompt_target_revision_stale/business_validation_failed` | 保持原IssueStatus；返回新候选或稳定拒绝原因 |

不存在第二套`outcome_reason`同义枚举；例如“保持估算”只能写`kept_estimate`，“补充事实”只能写`user_supplied`。目标领域事件确已被纠正或撤销时，不写`rejected`，而按§8.12把Issue转为`resolved + event_superseded`；`prompt_target_revision_stale`仅表示本次快捷候选绑定的旧revision失效、目标事件本身仍有效。

解决问题时不能静默覆盖原始记录。涉及库存变化时，问题解决带来的纠正、库存效果、Issue终态和投影必须在同一个`EffectBundle`内成功或回滚；不得回滚更早已经提交的原饮食事实。技术提交失败时整个Resolution效果包回滚，不得持久化`IssueResolutionEvent(application_outcome=failed)`；只保留可重试outbox或返回§8.2规定的稳定命令失败原因。`CASE-INVENTORY-006`、`CASE-ISSUE-002`和`CASE-ISSUE-003`分别覆盖陈旧拒绝、自然语言成功应用和延期转换。

### 8.7 提示频率

- 当前记录结束后只集中提示一次新产生的重要问题；
- 用户继续记录时不强制先处理旧问题；
- 用户延期后不在每条消息重复打扰；
- 用户主动查询、回顾或再次提到相关对象时可以重新展示；
- 用户明确不再处理的项目不自动提醒；
- 未解决问题仍影响数据覆盖说明，不能被当成已经完整。

### 8.8 数量异常的具体处理

有可信有限范围时，例如`半碗米饭`的合理范围为100—150g：

- 默认采用较大合理值150g计算营养；
- 回执公开写`米饭半碗｜按熟重150g偏高估算`；
- 内部保存原话、范围、范围来源、采用值与估算方法；
- 建立低优先级`quantity_estimated`；
- 只有库存身份和单位换算同时可靠时，才允许按估算量扣库存。

数量完全缺失时，例如`吃了米饭`：

- 保存吃了米饭的事实；
- 数量保持`unknown`，禁止伪造1份或100g；
- 无法计算的营养保持未知；
- 只有存在少量现实候选时才给快捷选项，否则询问大致数量。

### 8.9 多候选与库存不足

多个不同商品均可能匹配时，饮食先提交，不猜测扣减任一商品，建立`inventory_multiple_candidates`，并返回品牌、变体、规格和可读入库日期。用户可用字母或`低脂那瓶`等自然语言解决。

库存不足时：

- 饮食数量以用户真实表达为准；
- 库存绝不变为负数；
- 默认不进行不完整的部分扣减；
- 不把饮食数量擅自缩小到库存现有量；
- 允许用户随后补录库存、确认外食或保留异常；
- 未来若提供部分扣减，必须由用户明确选择。

### 8.10 快捷选项显示门槛与容量

仅在以下条件成立时显示快捷选项：存在2—4个高可信候选；选择会明显影响库存、营养或保质期；当前上下文能够可靠绑定；每个选项都能表达完整安全的动作。

以下情况不显示：数据已明确；只有一个可靠结果；普通低影响估算；候选过多；选项过期；用户已说稍后处理。

单次回执原则上最多展示4个主要快捷操作。待补充项更多时写`另有N项低优先级信息可稍后补充`。一个问题内选项互斥，不同问题的选项允许组合回复。

### 8.11 快捷回复绑定与执行前复核

每次展示的快捷组至少持久化：

```text
prompt_id
issue_id
option_ids[]
generated_from_revision
generated_at
expires_at
```

- 单独回复`A`绑定当前对话最近一个仍有效的选项组；
- 多个选项组无法区分时，重新展示短候选；
- 跨会话不能凭一个字母猜测；
- `那个、第二个、低脂的`只在当前可靠上下文内解析；
- 执行前重新验证目标记录、异常状态、库存候选、剩余量、单位换算、营养版本与幂等键；
- 状态已变化时返回新候选，禁止静默替换成另一商品；
- 自然语言已明确的操作可以先提交，只询问仍有歧义的部分。

### 8.12 待补充项生命周期与唯一IssueStatus枚举

`IssueStatus`的唯一枚举只在本节定义，§8.3只引用：

```text
open
awaiting_user
resolved
dismissed
```

- `open`：尚未解决且当前不等待用户回答；既包括尚未展开，也包括用户已经看过后选择延期处理；
- `awaiting_user`：已经展示说明或快捷选项，并且当前轮正在等待用户回答；
- `resolved`：通过用户补充、确认或可靠新证据解决；
- `dismissed`：用户明确不再补充。

用户选择“保持估算”时状态变为`resolved`，但数据质量仍是估算；选择“以后再说”时必须执行`awaiting_user → open`延期转换，并记录`resolution_reason=deferred_by_user`，本轮不重复问；选择“不用补”变为`dismissed`并记录`resolution_reason=dismissed_by_user`，未知仍保持未知。记录被纠正或撤销后问题自然失效，统一以`resolved + event_superseded`记录，不创造自由状态。

### 8.13 统一查询和补全事务

用户可以查询`还有哪些记录没补完整`、`有哪些营养没算`、`把最近没扣库存的记录列出来`。返回按日期和记录分组的可读摘要，默认不一次展开全部操作。

解决一个问题时必须：

1. 定位原饮食事件和当前有效修订；
2. 创建追加式补充或纠正事件，禁止再次创建相同饮食；
3. 建立或更新营养、商品和库存关联；
4. 在安全事务内完成库存效果；
5. 重算当前有效营养和受影响日期进度；
6. 更新问题状态并返回最终权威快照。

同一食物的数量修正、营养重算与库存补扣属于强关联效果，必须在同一个`EffectBundle`完成；纠正事实先由`FactCommit`保存。不同食物默认逐项提交并逐项返回正式状态。

待补充项的规范字段只采用`§8.3`。饮食或商品对象通过`entity_type/entity_id`定位；项目字段通过`field_path`定位；当前值和假设分别放入`known_facts`与`missing_or_conflicting_facts`。本节不得再维护一套`anomaly_*`同义模型。

---

## 9. 库存匹配与扣减规范

### 9.1 默认流程

```text
提交饮食事实
→ 判断是否有明确非家庭场景
→ 默认读取家庭库存候选
→ 匹配商品身份
→ 验证数量与单位
→ 验证库存是否足够
→ 分配批次
→ 安全扣减或登记待补充
→ 返回实际结果
```

库存问题只影响库存效果，不能撤销已提交饮食事实。

### 9.2 商品身份匹配依据

```text
用户原始名称
规范化名称
品牌
商品类型
口味或变体
包装规格
用户明确提到的购买时间
用户确认过的别名
当前对话中的可靠指代
```

必须保存具体匹配证据和规则版本，单一置信度分数不能替代证据。

### 9.3 匹配结果

| 结果 | 行为 |
|---|---|
| `unique` | 唯一可靠商品；数量可处理且库存足够时自动扣减 |
| `multiple` | 不同商品存在多个候选；保存饮食、跳过扣减、登记问题 |
| `none` | 无可靠候选；保存饮食、跳过扣减，可后续补录入库 |
| `insufficient` | 商品明确但库存不足；保存饮食、禁止负库存、登记问题 |

### 9.4 同商品多个批次

同一商品的多个批次不是商品身份歧义。批次选择顺序为：

1. 用户明确指定批次；
2. 有可靠到期信息时使用 FEFO；
3. 无可靠到期信息时使用 FIFO；
4. 过期批次默认不自动选择；
5. 一个批次不足时可以跨同商品批次分摊。

跨批次分配必须在一个库存事务中完成。

### 9.5 数量和单位

只有摄入量、库存量和换算关系足够可靠时才能精确扣减。

可确定换算示例：

```text
半盒 × 250ml/盒 = 125ml
2箱 × 12盒/箱 = 24盒
```

不可直接确定：

```text
半盘炒饭 → 克
一点花生 → 克
一块蛋糕 → 某盒蛋糕的比例
```

无法换算时保存饮食、跳过不可靠扣减并登记问题。

### 9.6 库存效果最低字段

```text
inventory_effect_id
meal_event_id
status
product_id
allocations[]
requested_amount
deducted_amount
unlinked_amount
unit
policy
evidence
created_at
transaction_id
```

每个批次分配保存：

```text
batch_id
before_quantity
deducted_quantity
after_quantity
allocation_policy
```

### 9.7 库存效果状态

```text
deducted
partially_deducted
skipped_external
skipped_by_user
skipped_no_match
skipped_ambiguous
skipped_insufficient
skipped_vague_quantity
skipped_unconvertible_unit
reversed
```

`partially_deducted`是PRODUCT-0.2必须实现但仅按次启用的能力：每次都必须由用户明确授权“先扣现有部分”，不得因为上一次授权形成默认；PRODUCT-0.1的库存不足策略始终是整项不扣，绝不能自动采用部分扣减。

回复必须描述实际效果，不能把计划、预览或失败扣减写成已经完成。

### 9.8 延迟处理

用户稍后选择商品或补充数量时必须重新检查：

- 饮食记录是否仍有效；
- 问题是否仍待处理；
- 商品和批次是否仍存在；
- 库存数量是否变化；
- 换算依据是否仍有效；
- 是否已经由其他操作解决。

验证通过后才执行补充扣减。

### 9.9 最低验收案例

- 默认读取家庭库存；
- 明确外食时不读取和扣减；
- 用户明确只记录；
- 唯一商品单批次扣减；
- 同商品多批次 FEFO/FIFO；
- 不同商品多候选；
- 无库存候选；
- 库存不足；
- 数量模糊和缺失；
- 包装与内容单位换算；
- 不可换算单位；
- 过期批次不自动选择；
- 字母快捷选择和自然语言选择；
- 选择前库存发生变化；
- 重复提交和并发扣减；
- 纠正返还库存；
- 撤销饮食恢复库存。

---

## 10. 全局日期与时间展示规范

### 10.1 展示与存储分离

日期简化只用于面向用户的回执、候选和查询。底层始终保存：

```text
完整 ISO 时间
用户时区
用户时间原文
解析依据
时间精度
接收时间
提交时间
```

不得为了显示简洁而降低存储精度或改写用户原话。

### 10.2 本月只显示几号

以用户时区中的当前自然月为准：

```text
2026-08-01 → 1号
2026-08-05 → 5号
2026-08-09 → 9号
```

本月正常情况不显示`8月5号`、`08-05`或完整 ISO 日期。

### 10.3 其他月份和年份

- 同一年但不在本月：`7月15号`；
- 不同年份：`2025年12月31号`；
- 需要时间：`5号 08:30`、`7月15号 12:20`；
- 时间未知时不补`00:00`。

### 10.4 日期范围

```text
同一当前月：1号—5号
同年跨月：7月30号—8月2号
跨年：2025年12月30号—2026年1月2号
```

### 10.5 相对日期

自然对话可以说“今天”“昨天”，但历史列表、表格、快捷选项和正式查询使用不会随时间变化的规范日期。需要兼顾即时感时可以显示：

```text
今天（9号）
昨天（8号）
```

### 10.6 全局适用范围

这条规则适用于：

- 饮食和白水；
- 商品入库和库存批次；
- 营养资料版本；
- 待补充项和快捷选项；
- 纠正、撤销和恢复；
- 日/周回顾；
- 安装、备份和恢复的人类可读日期。

机器接口、技术日志和结构化导出继续使用完整标准时间。

---

## 11. 商品身份、库存批次与生命周期

### 11.1 三个核心对象

库存领域必须至少分开：

```text
ProductIdentity   这是什么商品
NutritionProfile  该商品可使用哪一版营养资料
InventoryBatch    这一次实际入库的不可变批次事实
InventoryBatchProjection  由库存事件投影出的当前位置、剩余量和有效状态
```

一个商品身份可以关联多个营养版本和多个库存批次。某批次耗尽不得删除商品身份、历史营养、确认别名或以往记录。

### 11.2 ProductIdentity

```text
product_id
raw_names[]
normalized_name
product_type
brand
manufacturer
variant_or_flavor
product_form
confirmed_aliases[]
barcode
sku
known_package_specs[]
nutrition_profile_ids[]
default_storage_rule
default_shelf_life_rule
source_refs[]
schema_version
```

要求：

- 原始名称与规范化名称分开；
- 品牌、口味和规格存在冲突时不能只凭同名合并；
- 只有用户确认过的别名才可长期保存；
- 当前对话中的“那盒全脂奶”属于临时指代，不能自动成为全局别名；
- 条码、SKU、价格和图片不是 0.1 入库的强制前提。

### 11.3 InventoryBatch

批次原始记录不可改写，只保存创建时事实：

```text
batch_id
product_id
stock_event_id
source_text
occurred_at_text
purchased_at
produced_at
stocked_at
received_at
committed_at
original_quantity
quantity_unit
package_spec
purchase_location
production_date
explicit_expiration_at
shelf_life_text
shelf_life_basis
data_quality
schema_version
```

当前状态只存在于可重建投影：

```text
InventoryBatchProjection
├─ batch_id
├─ remaining_quantity
├─ current_storage_location
├─ current_storage_location_source
├─ opened_at
├─ opened_at_source
├─ effective_expiration_at
├─ quantity_status
├─ seal_status
├─ expiry_status
├─ effective_status
├─ open_issue_ids[]
├─ last_event_id
├─ last_changed_at
└─ last_verified_at
```

购买时间、制作时间和实际入库时间必须能分别表达。“昨天买、今天入库”不能压成一个时间。原记录不能携带会随事件变化的`remaining_quantity/lifecycle_status`；这些值由投影生成并可从事件流重建。

### 11.4 字段来源与推导

每个关键字段要能够回答“来自哪里”：

```text
source_kind
source_message_id
source_ref
parser_version
normalization_rule_version
conversion_rule_version
derived_fields[]
```

例如品牌来自用户原话，规范名来自名称规则，位置来自商品类型默认规则。系统推导内容不能冒充用户明确输入。

### 11.5 存放位置推断优先级

```text
用户本轮明确位置
→ 商品包装或制造商保存条件
→ 用户确认过的同商品规则
→ 商品类型默认规则
→ 有依据的模型判断
→ unknown
```

标准位置至少包括：

```text
冰箱冷藏室
冰箱冷冻室
常温阴凉处
厨房干货区
水果蔬菜区
用户自定义位置
unknown
```

位置需要保存来源。用户纠正位置时追加位置变更事件，并重新评估保质期；一次纠正不自动成为全局习惯，多次一致确认后才可形成个人规则。

### 11.6 入库回执必须显示位置

正常情况下系统直接显示推断位置，不机械附带修改按钮：

```text
鲜牛奶250ml（冰箱冷藏室）
冷冻水饺3袋（冰箱冷冻室）
大米5kg（厨房干货区）
```

用户发现错误可以自然纠正。只有同时存在多个合理位置，而且选择会明显影响保质期或后续库存时，才提供快捷选项。

### 11.7 开封状态和开封时间

至少保存：

```text
seal_status: unknown | unopened | opened
opened_at
opened_at_source
opened_shelf_life
effective_expiration_at
```

开封时间可以来自：

- 用户明确说明；
- 本轮开封操作；
- 第一次从未开封包装中部分取用；
- 用户事后补充或纠正。

例如“喝了半盒牛奶”匹配到一盒此前未开封商品时，可以把饮食发生时间记录为推定开封时间，并保存 `opened_at_source=first_partial_consumption`。

包装层级必须正确：打开一箱牛奶不代表箱内所有独立盒都开封；只标记实际打开的最小包装。

### 11.8 保质期依据优先级

```text
用户明确到期日
→ 当前商品标签到期日
→ 生产日期 + 明确保质期
→ 制造商商品资料
→ 用户确认过的同商品规则
→ 购买时间 + 可靠类别规则
→ 入库时间 + 可靠类别规则
→ unknown
```

数据明确时显示确定日期，不加“估算”：

```text
到期：15号
```

使用购买、入库或类别规则推算时必须显示：

```text
按9号入库估算剩余3天
```

不能把估算写成“12号确定到期”。

### 11.9 开封后有效到期时间

有开封后保存期限时：

```text
effective_expiration_at = min(
  原包装到期时间,
  opened_at + 开封后保存期限
)
```

规则来自类别默认而不是明确标签时，结果继续标为估算。

### 11.10 动态剩余时间

底层保存到期时间和依据，展示时动态计算剩余时间：

```text
9号：估算剩余3天
10号：估算剩余2天
11号：估算剩余1天
```

不得每天修改原始入库事件。少于24小时显示“估算剩余不足1天”，超过有效时间显示“已过期”，不显示负天数。

### 11.10.1 保质期未知时显示时间锚点

无法可靠估算剩余保质期时，不能只写“保质信息未知”。应同时展示最有价值的已知时间锚点：

```text
制作时间
→ 生产时间
→ 购买时间
→ 入库时间
→ 消息接收时间
```

示例：

```text
自制酱料1瓶（冷藏），3小时前制作，暂无可靠保质期资料
散装熟食1盒（冷藏），昨天购买，暂无可靠保质期资料
干货1袋（常温），5号入库，暂无可靠保质期资料
```

近期可以动态显示“30分钟前”“2小时前”；较早时间遵守全局日期规则。底层保存完整时间，不能只保存相对文案。

### 11.11 临期提醒

```text
reminder_at
reminder_lead_time
reminder_source
expiry_confidence
```

- 精确到期日产生确定提醒；
- 估算到期日产生估算提醒；
- 无保质依据时不伪造提醒日期；
- 0.1 必须能够计算和查询临期状态；
- 主动通知能力放入 PRODUCT-0.2；
- 临期提示属于库存管理，不是食品安全或医疗判断。

### 11.12 库存变化使用事件

库存当前量由事件投影形成，而不是覆盖原始批次数量：

```text
inventory_stocked
inventory_gift_received
inventory_opened
inventory_consumed
inventory_returned_to_stock
inventory_returned_to_vendor
inventory_gifted_out
inventory_adjusted
inventory_discarded
inventory_depleted
inventory_voided
```

耗尽、丢弃或退货后仍保留批次历史。

每个`InventoryTransaction/StockEvent`除继承`EventEnvelope`外，至少保存：

```text
transaction_id
product_id
batch_id
event_type
direction: in | out | neutral
quantity_delta
quantity_before
quantity_after
unit
conversion_basis
reason_code
authorization_kind: explicit_user | rule_version | system_projection
authorization_source_message_id
related_event_id
related_transaction_id
idempotency_key
occurred_time
committed_at
provenance_refs[]
```

`quantity_delta`使用确定性十进制并按流入为正、流出为负、开封等中性事件为0；`quantity_before + delta = quantity_after`必须在同一单位或有明确换算依据，且after不得为负。`reason_code`区分购买入库、获赠入库、退回库存、退给商家、赠出、盘点、丢弃、消费和纠正补偿；获赠不能伪装成购买，退给商家不能误作返还库存。部分扣减必须记录本轮用户授权消息，并把授权严格限定到本次目标事件、数量和批次；下一轮或另一项不足不得继承授权。盘点、丢弃、退货、获赠和赠出都必须保存原因与前后值，其中`inventory_gift_received`为正delta，`inventory_gifted_out`为负delta。Schema细节由`SH-MODEL-001`冻结，三路线不得删减这些最低字段。

每个`StockEvent/InventoryTransaction`必须继承`EventEnvelope`，其事件生命周期只允许`active/superseded/voided`；业务动作由上述`event_type`表达，执行结果只允许`committed/failed`，不得再用一个含义不明的`status`字段同时表达动作、事务和当前库存状态。

### 11.13 购买、入库、制作与接收时间必须分离

```text
received_at
stocked_at
purchased_at
produced_at
source_time_text
time_resolution_basis
```

用户未给日期时，`stocked_at`可采用消息接收时间，但必须标记`default_received_at`。`昨天买的牛奶现在入库`必须同时保存昨天的购买时间与当前入库时间，不得混成一个字段。

### 11.14 包装矛盾不允许静默选择

`两箱、每箱12盒、一共20盒`包含互相冲突的信息。系统可以保留入库意图和原话，但在冲突解决前不能提交矛盾的规范化总量。只询问冲突字段，不自行选择20盒或24盒。

### 11.15 位置纠正与个人位置规则

用户纠正存放位置后：

- 更新当前批次位置；
- 重算估计到期和提醒；
- 保留原推断与纠正历史；
- 一次纠正只影响本批次；
- PRODUCT-0.1不自动形成个人位置规则；
- PRODUCT-0.2中只有多次一致确认并经过可见启用后，才形成商品级或类型级个人存放规则。

### 11.16 三条状态轴不得合并

```text
quantity_status: active | depleted | discarded | voided
seal_status: unknown | unopened | opened
expiry_status: unknown | normal | near_expiry | expired
```

三条状态轴用于表达“已开封但仍有剩余”“未开封但已过期”“数量耗尽但历史可查”等组合。批次耗尽、丢弃或撤销后均不得物理删除。

### 11.17 同商品新批次与历史复用

库存耗尽只把旧批次标记为`depleted`。商品身份、营养资料、别名和历史记录继续存在。再次购买同一商品时创建新批次并复用身份及已确认营养；包装容量变化时按基础基准重新计算每包装营养；标签变化时建立新的`NutritionProfile`版本，历史摄入仍引用旧版本。

### 11.18 同商品批次分配与过期批次

批次分配顺序只以`§9.4`为规范定义。本节补充生命周期过滤：执行该顺序前排除`discarded/voided/depleted`批次，执行后保存每个批次的实际扣减明细；不得在这里另行维护第二套FEFO/FIFO顺序。

过期批次默认不自动选择。唯一候选已过期时，保存饮食，暂不扣库存，并让用户确认是确实食用、已丢弃还是日期记录有误；不得提供医疗或食用建议。

### 11.19 库存查询能力

至少支持：当前有哪些库存、指定商品剩余量、临期批次、已开封商品、某日入库内容、某商品历史购买次数。当前库存默认隐藏耗尽、丢弃与撤销批次，历史查询可展开完整生命周期；未知数量显示`未知`而不是0。

---

## 12. 入库回执规范

### 12.1 单个商品

单个商品可以保留主要细节，但优先使用紧凑主行：

```text
已入库：鲜牛奶1瓶｜250ml/瓶（冰箱冷藏室）｜按9号入库估算剩余3天｜营养采用公共参考数据
```

只有出现关键异常或用户要求详情时才展开字段。

### 12.2 多类商品

多类目同时入库时，一种商品一行，统一顺序为：

```text
商品名称 + 数量/包装 +（位置）+ 保质状态 + 必要异常
```

示例：

```text
已入库：9号
蒙牛全脂牛奶24盒/2箱｜250ml/盒（冰箱冷藏室），估算剩余3天
鸡蛋1袋（冰箱冷藏室），袋内数量待补充
大米5kg（厨房干货区），9号入库
冷冻水饺3袋（冰箱冷冻室），估算剩余90天
```

不展示内部 ID、Schema 版本、完整时间戳或规则名。

### 12.3 快捷位置修改不是常规回执

不在每次入库后提供冷藏、冷冻、常温选项。只有商品类别明显不确定、多个位置都合理且结果会实质影响保质期时，才在所有商品回执之后统一提供候选。

### 12.4 多商品问题统一提示

```text
已入库3项：
鲜牛奶250ml（冷藏），估算剩余3天
鸡蛋1袋（冷藏），袋内数量待补充
调理包2袋，存放位置待确认

待补充：
A. 鸡蛋每袋10个
B. 鸡蛋每袋20个
C. 调理包冷藏
D. 调理包冷冻

也可以直接说明实际情况，不必选择以上选项。
```

用户既可以回复字母组合，也可以自由说明实际数量和位置。

### 12.5 入库回执数据来源

回执只能来自实际提交后的最终批次状态。不得把预览数量、计划位置或未提交的保质估算写成已入库结果。

---

## 13. 营养资料、联网检索与计算

### 13.1 营养不是上传优先，而是自动来源优先

普通原始食材和基础食品不应频繁要求用户上传营养表。默认流程为：

```text
识别食品类型和状态
→ 查询已确认资料与本地缓存
→ 查询权威公共资料或可信互联网来源
→ 能精确匹配则直接计算
→ 常见菜品可按透明模板估算
→ 高度可变食品等待配方或标签
→ 保存来源和本次快照
```

营养获取失败不能阻止饮食或入库事实。

### 13.2 食品分类与默认路线

| 食品类别 | 示例 | 默认路线 |
|---|---|---|
| 普通单一食品 | 鸡蛋、苹果、米饭、牛肉、青菜、鲜牛奶 | 自动使用权威公共资料或可信缓存 |
| 明确包装商品 | 某品牌纯牛奶、薯片、饼干 | 优先精确商品标签、制造商或历史确认资料 |
| 精加工复合食品 | 蛋白棒、代餐粉、复合饮料 | 必须尽量匹配精确商品或标签 |
| 常见组成菜品 | 牛肉面、蛋炒饭、番茄炒蛋 | 个人模板优先，否则透明通用模板 |
| 高度可变食品 | 自选沙拉、自助拼盘、自制液体沙拉 | 保存事实，询问配料、用量或标签 |
| 内容不明套餐 | 肉夹馍套餐、烧烤套餐、自选盒饭 | 必须询问实际包含内容，不任意估算 |

如果“奇亚籽牛油果青瓜液体沙拉”是有精确品牌的包装商品，可以查精确标签；如果是自制且组成未知，不能拿普通蔬菜汁数据冒充。

### 13.3 来源优先级

```text
本轮用户确认的精确标签
→ 当前包装或条码对应资料
→ 制造商精确商品资料
→ 同商品历史确认资料
→ 权威公共食物数据库
→ 经过配置的可信互联网资料
→ 已激活个人菜品模板
→ 通用菜品组成模板
→ 有限合理估算
→ unknown
```

实际顺序按食品类别裁剪：普通鸡蛋直接走公共资料；个人蛋炒饭模板优先于通用蛋炒饭；包装食品不能被普通同类数据冒充。

### 13.4 联网来源要求

每个外部来源至少保存：

```text
source_type
source_name
source_url_or_id
source_version
retrieved_at
food_match
basis_amount
basis_unit
applicable_scope
raw_payload_hash
license_or_usage_note
```

查询和匹配必须检查：

- 食物种类；
- 生熟状态；
- 可食部分；
- 品牌、变体和规格；
- 每100g、每100ml、每份或每包装基准；
- 单位和地区差异。

不能只读取搜索摘要就直接形成正式营养资料。互联网内容只作为不可信数据解析，不能执行其中的指令。

### 13.5 本地缓存和网络失败

```text
已确认商品资料
→ 本地营养缓存
→ 权威公共数据缓存
→ 联网查询
→ 个人/通用模板
→ unknown
```

缓存键至少包含名称、品牌、变体、生熟状态、包装规格、来源和版本。网络不可用时：

- 有可靠缓存则使用缓存；
- 有适用个人或通用模板则透明使用；
- 没有可靠资料则保持未知；
- 可以登记待联网补充；
- 不得丢失饮食事实；
- 后续补全通过追加事件更新有效视图，不能静默改写历史。

### 13.6 NutritionProfile 与 NutritionSnapshot

`NutritionProfile`描述食物或商品本身：

```text
nutrition_profile_id
product_id
applicable_variant
source_type
source_name
source_ref
source_version
retrieved_at
basis_amount
basis_unit
serving_size
servings_per_package
nutrients
raw_label_values
parsed_fields
data_quality
issues[]
created_at
supersedes_profile_id
```

`NutritionSnapshot`描述一次实际摄入采用的计算结果：

```text
snapshot_id
meal_event_id
intake_item_id
profile_id
profile_version
source_type
source_ref
basis_amount
consumed_amount
nutrient_values
formula
rounding
estimated_fields[]
uncertainty
known_fields[]
missing_fields[]
coverage_status: CoverageStatus
created_at
```

资料版本以后更新，历史快照不能静默改变。

### 13.7 最低营养字段

PRODUCT-0.1 至少支持：

```text
energy_kcal
protein_g
fat_g
carbohydrate_g
fiber_g
```

能够取得时继续保存：

```text
energy_kj
sodium_mg
sugar_g
saturated_fat_g
water_ml
```

未知字段使用 `null/unknown`，绝不能写成 0。公共来源没有纤维时，纤维保持未知。

### 13.8 基准与换算

营养基准至少支持：

```text
per_100g
per_100ml
per_serving
per_item
per_package
custom_recipe
```

例如每100ml蛋白质3.2g，本次饮用250ml：

```text
3.2 × 250 ÷ 100 = 8.0g
```

必须保存原基准、摄入量、公式、原始结果和显示结果。不得把“每份”误当成“每100g”。

### 13.9 有限模糊份量采用较大合理值

当原表达具有可信、有限范围时，默认采用较大合理值作为营养参考，不必机械提供多个大小选项。

例如：

```text
半碗米饭合理范围：100—150g
采用营养计算量：150g
```

回执显示：

```text
米饭半碗｜按150g估算
```

必须保存原话、范围、采用值、范围来源和估算属性。

“吃了点米饭”“喝了几口”如果没有容器或其他边界，不能无限放大；范围过宽时保持未知或询问。

### 13.10 常见菜品模板

通用模板必须透明列出实际参与计算的组成和版本。例如：

```text
牛肉面1碗：牛肉80g（按通用模板估算）、面条2两｜100g（按通用模板估算）、配菜50g（按通用模板估算）、模板规定的汤底或食用油
蛋炒饭1份：熟米饭200g（按通用模板估算）、鸡蛋1个｜可食部分50g（按通用模板估算）、蔬菜50g（按通用模板估算）、食用油8g（按通用模板估算）
```

用户没有个人模板时可以直接采用并在回执中标注“按通用模板估算”；用户发现不符后自然纠正。

### 13.11 高度可变食品与套餐

对内容不明的套餐、自选、拼盘和高度可变自制食品：

- 先保存用户确实摄入该食品的事实；
- 已明确组成可先计算；
- 未知部分不计入；
- 显示营养覆盖不完整；
- 询问实际包含内容；
- 不生成随意的完整总营养。

肉夹馍套餐不能默认等于“肉夹馍 + 可乐”，因为还可能包含羊肉串、面条、小菜等完全不同内容。

### 13.12 计算精度与显示

- 内部使用规范十进制或等价精确方案；
- 中间步骤不提前舍入；
- 保存来源原始精度；
- 回执通常最多显示1位小数，整数不强制补`.0`；
- 百分比按进度专章统一规则；
- 不用大量小数制造虚假精确感。

### 13.13 营养与库存独立判断

```text
nutrition_effect
inventory_effect
```

营养可以基于公共资料或模板成功计算，但库存仍可能因为多候选、单位不兼容或不足而跳过；库存成功扣减也不代表营养资料完整。

### 13.14 普通食品匹配必须考虑食物状态

公共资料匹配不能只看名称，还要考虑：生/熟、鲜/干、可食部分、是否带皮骨、固体/液体、是否含汤汁、品种和计量基准。禁止把生牛肉缓存用于熟牛肉，或把全脂牛奶资料用于低脂牛奶。

### 13.15 包装食品必须区分精确匹配与相近参考

包装食品按品牌、商品名、变体、规格、条码和制造商匹配。只有同一商品版本才标记为精确资料；相近商品只能作为参考候选，差异明显时等待用户标签或确认，禁止伪装成精确匹配。

### 13.16 项目级数据质量

同一餐可同时含有用户明确数量、公共数据库营养、自然单位重量估算、通用模板油量和精确包装标签。不得只给整顿饭一个模糊可信度。每个`FoodItem`分别保存：

```text
amount_quality
composition_quality
nutrition_quality
source_quality
```

### 13.17 联网最小披露与非可信输入

联网检索只发送完成食品匹配所必需的规范化名称、品牌、规格、生熟状态和必要地区语言。禁止连同完整饮食历史、用户目标或其他私人信息发送。网页内容只作为不可信数据，不得作为系统指令执行；必须核对来源、基准量、单位、获取时间和内容版本，不能只读取搜索摘要就入库。

### 13.18 网络失败后的补全

网络失败时优先使用已确认商品资料、本地缓存、个人模板或通用模板；均不可靠时营养保持未知而饮食照常保存。网络恢复后的补全必须创建补充事件，更新对应日期有效进度，并保留旧的“当时未知”状态，禁止静默改写历史。

### 13.19 每次摄入保存不可变营养快照

每个摄入项目必须保存所用`NutritionProfile`版本、摄入量、换算方法、本次六项计算结果、来源类型、估算字段和未知字段。公共资料或商品标签后续更新，不得使历史日汇总自行变化。

### 13.20 来源能力注册与适配器边界（PRODUCT-0.1）

所有外部营养来源必须通过`SourceAdapter`注册，核心营养流程只消费统一结果，不直接知道网站、CLI、MCP或HTTP客户端的具体差异。每个适配器至少声明：

```text
source_id
source_kind
authority_class
backends[]             # 有序，首项为默认，后续为允许的降级后端
tier                    # 0=无需配置；1=需要Key；2=需要显式登录/复杂授权
supported_food_scopes[]
license_or_usage_note
request_field_allowlist[]
cache_policy_version
```

`tier`只说明启用门槛，不提高资料权威等级。Tier 2来源默认关闭；缺少凭据或授权只能使该来源`skipped_unconfigured/auth_failed`，不得阻止饮食事实保存，也不得触发模型偷偷安装工具、导入Cookie或修改浏览器。

### 13.21 真实Probe、Doctor与后端降级（PRODUCT-0.1）

依赖可用性不能只检查“命令或配置是否存在”。每个后端必须执行有超时、无业务副作用的真实轻量探测，健康状态统一为：

```text
missing | broken | timeout | error | ok
```

一次来源运行的覆盖状态统一为：

```text
ok | no_results | partial | timeout | auth_failed | skipped_unconfigured | error
```

`Doctor`只聚合适配器自己的检查结果，单一来源异常不得使整份诊断崩溃。报告必须给出可行动的修复建议、实际选中后端和允许的下一个后端，但不得显示Key、Cookie、带凭据URL或用户业务数据。只有首选后端失败原因符合注册表中的允许降级规则时，才可尝试下一个后端；禁止模型临场发明替代网站。

### 13.22 安装、配置和授权生命周期（PRODUCT-0.1）

来源能力遵循：

```text
只读检查 → dry-run预览 → 用户明确授权 → 配置指定来源 → Doctor验收
```

读取配置不得创建目录；首次写入才创建私有配置目录。配置写入必须原子替换、拒绝符号链接/重解析点劫持，并限制到owner；凭据、长期研究状态和临时下载必须分根。安装基础Skill、配置来源、导入登录态和创建外部定时任务不得合并成一次默认动作。

### 13.23 营养研究证据流水线（PRODUCT-0.2）

0.2新增的研究流水线固定为：

```text
ResearchPlan
→ 多来源采集
→ ResearchItem（保留单一来源事实）
→ ResearchCandidate（标准化并绑定食品/商品实体）
→ ResearchCluster（同一标签版本、资料变化或主张聚类）
→ ConfidenceGate
→ ResearchFinding / NothingSolid
→ Preview
```

原则是“先证据，后叙事”。模型只能在已采集、标准化、去重和通过可信度门的证据上生成说明；不得先写结论再寻找链接。社交互动热度、搜索排名和模型自信都不能替代营养来源权威、精确商品匹配、基准单位或交叉来源佐证。

### 13.24 实体锚定、聚类、评分与可信度门（PRODUCT-0.2）

研究候选必须先绑定`ProductIdentity`或明确的普通食品实体，并核对品牌、变体、规格、生熟状态、可食部分、地区、基准量和资料版本。首版研究评分必须是版本化机器策略，建议因子为：

```text
source_authority
food_match_exactness
freshness
independent_corroboration
conflict_penalty
```

权重和阈值在实现任务中由固定fixture冻结，不能复制“社区热度”公式作为营养真值。高权威单一来源可以形成候选；相互独立来源一致可提高置信度；来源冲突、单位不清或匹配不精确必须降级为待审阅/证据不足，不能平均成一个看似精确的数。

### 13.25 部分覆盖、空结果与`NothingSolid`（PRODUCT-0.2）

- 只有来源状态为`ok/no_results`时，才可说该来源没有结果；
- `partial/timeout/auth_failed/skipped_unconfigured/error`必须进入结果模型，不能叙述为“网上没有资料”；
- 没有候选通过可信度门时，合法结果是`nothing_solid`，不是强行生成更新；
- 最终预览必须同时列出已覆盖来源、未覆盖来源、检索窗口、冲突、证据时间和置信度理由；
- 核心饮食命令不得等待研究任务，也不得因`nothing_solid`失败。

### 13.26 历史、去重、Watchlist与新颖度（PRODUCT-0.2）

研究状态至少保存研究运行、候选、聚类、发现、来源再次出现和用户处置；同URL只是第一层去重，还要保存标题/内容指纹、商品/食品实体和来源版本。系统必须能区分：首次发现、旧资料再次出现、同资料指标变化、正式营养版本变化和来源消失。

Watchlist只记录用户明确关注的食品、商品或来源。离线查看历史、差异和已发预览不得重新联网；外部Scheduler决定何时运行，研究引擎决定是否出现新证据，只有新且达到阈值的发现才进入待展示队列。

### 13.27 输出契约与写入边界（PRODUCT-0.2）

研究输出分为：

```text
raw       # 隔离调试/测试用证据，不进入普通回执
preview   # 用户审阅用，默认输出，不改变业务资料
briefing  # 经筛选的用户可读说明，仍附来源和覆盖限制
```

`ResearchFinding`和`NutritionUpdateProposal`不属于饮食事实。即使研究证据充分，也只能创建待确认建议；用户明确确认并再次通过商品身份、基准、许可、版本和当前状态复核后，才可走既有Profile更新事件。任何研究结果都不得静默改写历史`NutritionSnapshot`、库存交易、饮食事件或日进度。

### 13.28 研究质量评测和最小实施顺序（PRODUCT-0.2）

首版只启用2—3个高质量来源，不追求平台数量。应建立20—50条脱敏金标候选，至少标注应采纳/不采纳、同一变化聚类、来源冲突、部分覆盖和`nothing_solid`。修改解析、排序、阈值或来源后必须比较去重、排序和可信度结果；真实网络冒烟只作补充，不能替代确定性fixture。

---

## 14. 个人菜品模板与商品营养复用

本章的个人菜品模板学习与快捷复用属于`PRODUCT-0.2`；商品身份和历史营养资料保留/精确复用属于`PRODUCT-0.1`。0.1可以保存形成模板所需的用户明确证据，但不得在0.2发布前宣称已经具备自动学习体验。

### 14.1 通用模板不是个人习惯

第一次记录蛋炒饭可以使用通用模板。只有用户明确提供或纠正组成，才产生个人模板证据。系统自己连续使用相同通用模板不能形成个人偏好。

### 14.2 激活规则

个人菜品模板状态只允许：

```text
generic            通用模板，不代表用户习惯
candidate          一次用户明确确认形成的候选
active             两次一致明确确认后启用
stable             第三次一致后提高稳定度
personal_variable  多次明确组成不一致
superseded         已被新版本取代
disabled           用户明确停用
```

以用户明确的蛋炒饭组成为例：

```text
熟米饭250g
鸡蛋2个｜营养换算按可食部分100g估算
蔬菜70g
```

- 第一次明确确认：创建候选；
- 第二次一致明确确认：激活个人模板；
- 第三次继续一致：标记稳定；
- 后续普通“蛋炒饭”优先使用该模板；
- 回执显示“按个人模板”，不冒充本轮明确输入。

### 14.3 一致性要求

一致至少要求：

- 规范化菜品相同；
- 核心配料相同；
- 单位换算后数量相同或落入明确取整容差；
- 烹饪方式和变体不冲突；
- 关键组成没有未解决异常。

总热量接近不能证明配方一致。

### 14.4 多次明确数据不同

如果多次蛋炒饭组成明显不同：

- 标记为 `personal_variable`；
- 本轮明确数据永远优先；
- 本轮没提供组成时使用最近一次明确版本，并在回执中说明；
- 不再把通用模板冒充用户习惯；
- 可以建议命名“普通蛋炒饭”“双蛋蛋炒饭”“外卖蛋炒饭”等变体；
- 不强迫用户建立多个模板。

### 14.5 模板版本

```text
PersonalDishTemplate
├─ template_id
├─ canonical_name
├─ user_aliases[]
├─ version
├─ status
├─ components[]
├─ cooking_method
├─ serving_definition
├─ nutrition_calculation_policy
├─ learned_from_event_ids[]
├─ explicit_confirmation_count
├─ first_confirmed_at
├─ last_confirmed_at
├─ last_used_at
└─ supersedes_template_id
```

用户明确说“以后都按新组成”时创建新版本；旧版本和历史饮食继续保留。

### 14.6 同商品新批次复用营养

库存批次耗尽后：

- 批次状态变为耗尽；
- 商品身份继续保留；
- 营养资料继续保留；
- 历史别名和快照继续保留。

再次购买唯一匹配的“小象纯牛奶”时：

```text
创建新 InventoryBatch
→ 关联原 ProductIdentity
→ 复用已确认 NutritionProfile
→ 不要求再次上传相同营养表
```

### 14.7 历史商品匹配

```text
精确商品ID或条码
→ 规范化名称 + 品牌 + 变体
→ 用户确认别名
→ 唯一原始名称历史匹配
→ 多候选等待确认
```

如果同时存在全脂、低脂和高钙变体，不能只凭“小象牛奶”自动选择。

### 14.8 包装变化与标签变化

- 同配方从250ml变为1L时，每100ml资料可以复用；
- 每包装总营养必须按新容量重算；
- 当前标签与历史资料冲突时创建 `NutritionProfile` 新版本；
- 新批次使用新版本；
- 旧批次和历史饮食继续使用旧版本。

### 14.9 模板选择优先级与单次覆盖

```text
本轮用户明确组成
> 用户明确指定的个人模板版本
> 已激活个人模板
> 最近一次明确确认组成
> 通用菜品模板
> 保持组成未知
```

系统必须理解`这次按普通做法算`、`这次不要用我的模板`、`以后都按双蛋版本`、`只改这一次`，并区分单次覆盖与长期规则。

### 14.10 只有用户证据可以推动模板学习

模型自己连续三次使用同一通用模板，不能据此认定为用户偏好。只有用户明确提供、确认或纠正的组成才计入一致性样本。第二次一致可激活，第三次一致提高稳定度；一次不同只改本次，多次稳定为新组成时建立新版本；长期不一致时标为可变菜品并采用最近明确组成。

---

## 15. 追加式纠正、撤销、恢复与混合事件

### 15.1 可纠正范围

PRODUCT-0.1 至少支持：

```text
change_amount
change_unit
change_time
change_meal_slot
change_item_name
change_food_type
change_components
add_item
remove_item
change_inventory_link
change_nutrition_source
void_event
restore_event
```

### 15.2 目标定位

优先级：

1. 当前回复直接引用的活动记录或预览；
2. 当前对话最近一次成功提交；
3. 日期、餐次、食品和数量组合；
4. 当前有效记录中的唯一候选。

多个候选时展示可读日期、时间、食物和数量，不暴露内部 ID；跨会话不能只凭“那个”猜测。

### 15.3 CorrectionEvent

```text
correction_id
target_event_id
base_revision
request_id
source_text
operation
before_snapshot
change_set
after_snapshot
nutrition_delta
inventory_effects
affected_dates[]
created_at
timezone
```

原事件不可覆盖。查询默认显示最新有效视图，审计查询可以展开原事件和全部纠正。

### 15.4 库存补偿

- 数量由1变2：只补扣差额1；
- 数量由2变1：向原批次返还1；
- 商品从全脂改低脂：返还原批次并扣新批次；
- 改为外食：返还原家庭库存，饮食和营养保留；
- 库存不足：纠正后的饮食事实和营养仍保存，不执行不安全的部分补扣，并登记问题；
- 同一纠正重试不能重复补扣或返还。

纠正事务按`DEC-023`分层：纠正事实由`FactCommit`先提交；营养重算、真实库存补偿和对应Issue等强关联附加效果在同一个`EffectBundle`内原子成功或回滚；所有效果稳定终态后，最终进度、回执和终态幂等结果再由`EnvelopeFinalize`原子冻结。后两层技术失败都不得回滚已经提交的纠正事实，也不得把旧投影或旧进度冒充已同步完成。

### 15.5 撤销与恢复

普通“删除记录”实现为 `record_voided`：

- 当前查询不再显示；
- 营养和白水贡献移除；
- 已扣库存安全返还；
- 原记录和撤销原因保留；
- 可以通过 `record_restored` 恢复。

彻底清除全部个人数据属于独立隐私流程，不能与单条撤销混同。

### 15.6 时间纠正

记录从8号改到9号时：

- 从8号有效汇总移除；
- 加入9号有效汇总；
- 库存效果通常不重复；
- 两个日期的进度都重算；
- 原始时间表达继续可审计。

### 15.7 纠正与新增的语义区别

```text
“不是两个，是三个”     → 纠正
“刚才说错了”           → 纠正
“又吃了一个”           → 新摄入
“再加一个到早餐里”     → 向原餐次追加项目
```

两种解释都合理时只询问这一处差异。

### 15.8 混合事件

`mixed`只是包含多个操作的输入信封，不是单个领域事件。示例：

```text
买了两盒牛奶，然后喝了一盒。
```

按叙述顺序执行：

1. 入库2盒；
2. 记录饮用1盒；
3. 从新批次扣减1盒。

默认逐事件原子，后一独立事件失败不回滚前一已提交事件；总回复逐项说明状态。每个子操作有稳定幂等子键。

### 15.9 会话跟进

必须支持：

```text
再加一个鸡蛋
不是一盒，是半盒
那个按标签算
不是这个，是第二个
刚才的都取消
```

活动预览、最近成功记录、当前餐次、待补充项和最近候选分别管理；选项过期或跨会话时重新定位。

### 15.10 最低验收案例

- 数量增加和减少；
- 食品替换；
- 白水容量纠正；
- 白水改为营养饮品；
- 增加遗漏项目；
- 删除一餐中的单项；
- 整条撤销和恢复；
- 家庭库存改为外食；
- 商品候选更换；
- 补扣时库存不足；
- 耗尽批次返还；
- 跨日纠正；
- 相同纠正重复提交；
- 并发纠正；
- 事务中断无部分结果；
- 购买后立即食用的混合事件；
- 会话内自然跟进；
- 跨会话模糊指代不猜测。

### 15.11 单次纠正事务顺序

1. 定位目标记录；
2. 读取最新有效修订并检查`base_revision`；
3. 应用本次变更并验证新的饮食事实；
4. 用`FactCommit`原子保存CorrectionEvent、幂等子键和待处理效果outbox；
5. 在一个`EffectBundle`重新计算营养与覆盖率；
6. 在同一效果包计算库存返还、补扣或换绑差额并更新Issue；
7. `EffectBundle`只保存本次子操作的投影贡献与`affected_dates[]`，不得提前生成信封级最终进度；
8. 所有相关outbox达到稳定终态后，由`EnvelopeFinalize`在一个原子提交中按一致性快照生成`daily_progress_by_date[]`、适用的单日`daily_progress`别名、回执与终态幂等结果并返回；最终器失败时信封保持`effects_pending`，同键重试只重跑最终器。

效果包成功后禁止出现饮食已改但营养仍旧、饮食已撤销但库存未返还、库存已补扣但纠正事件未保存，或回执使用旧进度快照。效果包技术失败时纠正事实保留并明确`effects_pending`，旧营养/库存投影不得冒充已经同步完成。

### 15.12 饮食纠正仍然遵守事实优先

如果新的饮食事实已经明确，但库存差额无法安全执行，例如1个鸡蛋改成3个而库存只够补扣1个：保存3个鸡蛋和重算后的营养；不做默认部分补扣；创建库存补偿异常；库存不得为负。只有技术故障导致纠正事件本身无法可靠提交时才整体回滚。

### 15.13 营养重算

- 只改数量：沿用原营养版本按新数量重算；
- 改食品名称：重新匹配营养来源；
- 改菜品组成：按新组成逐项重算；
- 改标签：新建营养资料版本并绑定新修订；
- 删除或新增配料：只移除或增加相应贡献；
- 未知继续为未知，不补0；
- 同步更新“已计算项/总项”的覆盖信息。

### 15.14 白水与饮品分类纠正

白水500ml改300ml时，饮水减少200ml。白水改为牛奶时，同一个纠正`EffectBundle`撤销白水贡献、生成营养饮品贡献、计算食物水分并按场景处理库存；同一容量绝不重复计入白水和食物水分。

### 15.15 纠正、撤销与恢复的有符号进度差额

新增饮食只显示正向增量；纠正、撤销与恢复允许显示`+50kcal +3%`、`-120kcal -6%`、`+300ml +12%`、`-200ml -8%`。绝对比例小于1%显示`+<1%`或`-<1%`，0省略。差额只能由本次事务最终结果返回，禁止拿两个独立报告相减。

跨日期纠正必须同时返回两个受影响日期的最新进度。纠正回执可以使用`8号更新后`、`9号更新后`标题；这不违反新增饮食回执不添加进度标题的规则。

### 15.16 幂等、并发和无变化纠正

- 每次纠正带幂等键；
- 同请求重试不重复返还或补扣；
- 新纠正必须基于最新有效修订；
- 并发冲突无法自动合并时，展示当前有效内容重新确认；
- 不允许两个并发纠正相互覆盖；
- 无变化纠正返回`当前已经是该结果`，不创建无意义版本。

### 15.17 批量纠正与撤销

PRODUCT-0.1支持单条唯一记录、单项和唯一整餐的纠正/撤销；整顿饭需列出受影响菜品并确认范围。某日全部记录、跨餐批量操作或`删除最近所有牛奶记录`属于PRODUCT-0.2，必须先预览日期范围、匹配数量、当前有效内容、预计追加的纠正/撤销、库存补偿和受影响日期。预览不写业务数据；用户确认时必须携带预览版本和目标有效版本，逐目标追加纠正或撤销，库存只做真实差量补偿，跨日结果写`daily_progress_by_date[]`并由同一EnvelopeFinalize冻结最终结果。范围变化、目标已被修改或用户取消时零写入并重新预览；执行中的技术失败按§20.16和§20.20既定原子边界恢复，不允许物理删除、半报成功或把未确认预览当提交。

---

## 16. 白水与食物水分

### 16.1 分类边界

`diet_water`只用于：

- 白水；
- 纯净水；
- 矿泉水；
- 用户明确指定为普通白水等价物的无营养液体。

以下必须进入 `diet_meal`：

- 牛奶和豆浆；
- 汤；
- 果汁和奶茶；
- 含有效配料的咖啡或茶；
- 营养饮料和代餐饮料；
- 液体沙拉等具有营养价值的液体。

它们可以贡献食物水分，但不能同时作为白水再次累计。

### 16.2 水分字段

```text
plain_water_ml
food_hydration_ml
total_hydration_ml
progress_water_ml
progress_water_known_min_ml
water_coverage_status: CoverageStatus
```

```text
total_hydration_ml = plain_water_ml + eligible_food_hydration_ml
```

同一份液体只能进入一次；食物水分不得超过实际摄入液体容量。

六项进度中的饮水采用`progress_water_ml = plain_water_ml + food_hydration_ml`，但仅在两部分都已知时才是精确值。存在未知食物水分时：

- 保存`progress_water_known_min_ml`作为已知下界；
- 标记`water_coverage_status=partial`；
- 精确`progress_water_ml`保持未知；
- 不得把未知贡献当0后宣称精确总量；
- 同一液体不能先进入`plain_water_ml`再作为食物水分重复相加。

每个营养饮品水分贡献保存不可变快照：

```text
HydrationContributionSnapshot
├─ source_event_id
├─ meal_item_id
├─ consumed_liquid_volume_ml
├─ hydration_source_type
├─ source_ref_and_version
├─ basis_amount_and_unit
├─ applied_food_hydration_ml
├─ conversion_formula
├─ is_estimate
├─ uncertainty_range
└─ created_at
```

### 16.3 明确和估算容量

用户明确说“500ml白水”时直接保存500ml，不显示估算。

用户只说“一杯水”“一瓶水”时，可以在普通容器且范围合理时采用谨慎估算，并保存：

```text
raw_volume
estimated_volume_ml
estimate_basis
estimated
```

多个合理容量差异明显时提供快捷候选，并允许自由文本说明。

### 16.4 WaterEvent

```text
event_id
meal_id
schema_version
source_text
source_message_id
occurred_at_text
occurred_time
raw_volume
volume_ml
volume_source
estimated
estimate_basis
issues[]
lifecycle_status
received_at
committed_at
idempotency_key
```

`WaterEvent`继承`EventEnvelope`；上表只展开白水专有字段与常用公共字段，不能据此省略来源、Schema、幂等和生命周期。白水同样支持查询、纠正、撤销、恢复、幂等和语义重复提醒。

### 16.5 记录定位

纠正或撤销白水时必须定位目标。多个候选显示日期、可读时间和容量，不显示内部 ID：

```text
A. 9号 09:30｜白水250ml
B. 9号 14:20｜白水500ml
C. 暂不修改

也可以直接说明时间或容量，不必选择以上选项。
```

---

## 17. `daily_progress` 权威进度规范

### 17.1 六项目标

用户可以自行配置：

1. 热量；
2. 蛋白质；
3. 脂肪；
4. 碳水化合物；
5. 膳食纤维；
6. 饮水。

目标只用于进度追踪，不用于评价、诊断或自动建议。

### 17.2 成功结果必须携带权威快照

每次饮食或白水成功提交后，最终工具结果至少包含：

```text
daily_progress_by_date[]
├─ date
├─ timezone
├─ committed_totals
├─ configured_goals
├─ current_turn_increments
├─ coverage_status: CoverageStatus
├─ unknown_item_ids[]
├─ idempotency_result_id
└─ generated_at
```

任何成功写入都以`daily_progress_by_date[]`为规范源。恰好影响1个自然日时，必须同时提供`daily_progress`，且它与数组中唯一对象逐字段相同；影响2个或更多自然日时只返回数组，不提供单数别名。判断只看受影响日期数量，不看操作类型，因此同日mixed仍有单数别名，跨日mixed、跨日纠正或其他跨日操作都只用数组。`committed_totals`只包含已经持久化的当前有效记录，不包含预览、失败、忽略、未提交片段、已撤销记录或被纠正替代的旧版本。

`current_turn_increments`由请求信封级持久化聚合生成，覆盖该日期内本请求所有成功子操作；不得借用最后一个子事务的快照，也不得由模型把多个结果相加。

### 17.3 回执只能使用同次结果

成功写入后的进度块必须只使用本次最终成功结果：恰好1个受影响日期时使用同次`daily_progress`；2个或更多受影响日期时，每个日期只使用同次`daily_progress_by_date[date]`，不得借用单数别名或其他日期快照：

- 不复用上一轮进度；
- 不使用提交前读取的总量；
- 不为了重建回执额外调用进度报告；
- 不在模型侧拼接多个独立读取；
- 不通过“新总量减旧聊天总量”推导本轮增量。

终态响应与其`daily_progress_by_date[]`在幂等记录中冻结。响应丢失后使用同一幂等键重试，即使系统随后又写入其他记录，也必须返回原事务的冻结结果，而不是当前最新累计。只有仍处于`effects_pending`的非终态信封可以恢复未完成效果；其终态形成后同样冻结。

### 17.4 直接进度查询

用户直接查询日或周进度时使用一次聚合报告。只有聚合结果明确缺少用户要求字段时才能补充读取。直接查询没有本轮写入，因此不显示 `+amount`。

### 17.5 固定顺序和两行格式

目标已经确认时，六项固定顺序为：

```text
热量 → 蛋白质 → 脂肪 → 碳水化合物 → 膳食纤维 → 饮水
```

每项严格两行：

```text
Emoji + 名称 + 10格进度条 + 百分比
Emoji + 当前量 / 目标 + 可选本轮增量
```

标准示例：

```text
🔥 热量 ██████████ 103%
🔥1954.3 / 1900 kcal +111.3kcal +6%
🥩 蛋白 ████████░░ 80%
🥩135.7 / 170 g +26.1g +15%
🧈 脂肪 ██████████ 156%
🧈85.6 / 55 g +0.1g +<1%
🌾 碳水 ██████████ 109%
🌾163.8 / 150 g +1.1g +<1%
🥬 纤维 ████░░░░░░ 40%
🥬12 / 30 g
💧 饮水 ░░░░░░░░░░ 0%
💧0ml / 3L
```

### 17.6 百分比和进度条

```text
percentage = round(current / target × 100)
filled = clamp(round(current / target × 10), 0, 10)
```

- 进度条固定10格；
- 只使用`█`和`░`；
- 超过目标时显示真实百分比；
- 进度条仍最多10个实心格；
- 跨路线统一采用十进制`round_half_up`取整，避免PowerShell、JavaScript和SQLite在`.5`边界出现不同结果。

### 17.7 本轮增量

新写入的某指标增量存在且大于0时显示：

```text
+amount unit +percent
```

```text
increment_percentage = round(increment / target × 100)
```

- 正增量低于1%显示`+<1%`；
- 零或缺失增量省略；
- 不使用括号；
- 不用自然语言重复解释增量；
- 只能采用事务结果返回的本轮增量。

纠正、撤销和恢复引起的负调整采用纠正结果中的有符号差额，不伪装成新增增量；格式在纠错回执中使用`-amount -percent`。

### 17.8 饮水单位

- 当前量和本轮增量始终使用毫升；
- 目标使用升；
- 计算前把目标换算为毫升；
- 展示示例：`💧1500ml / 3L +250ml +8%`。

### 17.9 未知纤维

纤维明确未知时不能显示0：

```text
🥬 纤维 ░░░░░░░░░░ 未知
🥬未知 / 30 g
```

空条必须与“未知”一起出现，不能解释为实际摄入为0。

### 17.9.1 饮水只有已知下界

目标已确认但食物水分存在未知贡献时，仍保持饮水两行格式，使用已知下界：

```text
💧 饮水 ██░░░░░░░░ ≥20%
💧已知至少500ml / 2.5L｜另有饮品水分未知
```

百分比和10格条只代表`progress_water_known_min_ml`。当前轮有已知饮水增量时可按既定格式追加；未知部分只能写“本轮另有饮品水分未知”，不得伪造数值。

### 17.10 没有确认目标

- 正常保存饮食和白水；
- 可以显示已记录数量；
- 不显示百分比；
- 不生成进度条；
- 不根据人口统计或常见推荐值自动创建目标；
- 明确说明尚未配置目标。

### 17.11 进度块是成功回执最后一个区块

在固定进度块之后不得主动添加：

- 表扬或评价；
- 警告；
- 剩余可吃额度；
- 下一餐建议；
- 推荐食物；
- 未经请求的分析。

异常、快捷选项，以及用户在本轮明确要求的事实分析或解释，都放在进度块之前；进度块仍是成功写入回执的最后一个区块。医疗诊断、自动健康评价和越界建议仍按§5.3拒绝。

### 17.12 不添加进度标题

成功写入回执中不添加：

```text
📊 今日进度：
今日营养进度：
进度汇总：
```

记录结果之后直接输出固定六项。

### 17.13 混合写入

同一轮包含多项饮食和白水且全部影响同一自然日时只显示一个最终进度块。本轮增量来自信封级冻结的`daily_progress_by_date[date].current_turn_increments`，为本轮所有成功子操作的实际合计，失败或待确认部分不能进入。

只要一次请求影响多个自然日，不论是纠正、mixed还是其他操作，都必须按日期各显示一个带日期标签的进度块，并返回多个`daily_progress_by_date[]`对象，不能把多个自然日合成一个无意义进度。每个日期仍只显示一次六项；恰好影响一个日期的mixed按§17.2同时返回单数别名并只显示一个进度块。

---

## 18. 成功记录回执的紧凑原则

### 18.1 固定顺序

```text
记录日期与餐次（单独一行）
→ 每道菜、单独食物和饮品各占一行
→ 实际库存效果
→ 必要待补充和快捷选项
→ 固定六项进度块
```

禁止把整餐内容挤在“已记录”一行内。`已记录：日期 + 餐次`只承担本次提交标题，不承载菜品明细。

### 18.2 取消重复营养段

成功记录后不再单独输出：

```text
本轮营养：
热量……
蛋白质……
```

本轮营养增量已经在固定进度块中展示。只有用户明确询问计算明细时才展开营养来源和公式。

### 18.3 菜品组成保持紧凑

菜品的主要组成在该菜品所在行内表达，例如：

```text
牛肉面1碗｜牛肉80g（按通用模板估算）、面条2两｜100g（按通用模板估算）、配菜50g（按通用模板估算）
```

不把每个配料拆成多个短段落。

换行粒度固定为：

- 一道复合菜品占一行，菜品内部的主要配料、用量和证据标签在该行结束；
- 一个独立食物占一行；
- 一个独立饮品占一行；
- 白水也单独占一行；
- 不把一道菜的每个配料拆成连续多行；
- 不把不同菜品合并成一条超长行。

### 18.4 库存一行说明实际效果

```text
库存：已扣牛肉80g、面条2两（100g）、黄瓜200g、橙子1个
```

只有多候选、不足、单位不兼容或用户要求详情时才展开。

### 18.5 模拟数据必须声明起点

展示回执示例时必须说明：

- 是否从当日0开始；
- 用户配置的目标；
- 前面已经提交了哪些记录；
- 数字只是格式示例还是验收期望。

不得凭空给出累计总量，让用户无法理解来源。

### 18.6 “估算”采用字段级证据标签

“估算”只标记真正经过推定的字段，禁止笼统标记整条记录。

| 输入或证据 | 回执写法 | 是否显示“估算” |
|---|---|---|
| 用户明确说“米饭200g” | `米饭200g` | 否 |
| 包装明确为250ml/瓶，记录1瓶 | `牛奶1瓶｜250ml` | 否 |
| 用户只说“1个橙子”，系统推定可食重量 | `橙子1个｜按可食部分200g估算` | 是 |
| 用户说“半碗米饭”，系统在合理范围取较大值 | `米饭半碗｜按熟重150g偏高估算` | 是 |
| 一道菜部分明确、部分推定 | `牛肉80g（估算）、面条100g、青菜50g（估算）` | 只标推定项 |

以下属于明确数据，不显示“估算”：

- 用户本轮明确提供的数量、重量、容量、配料和烹饪事实；
- 商品包装、营养标签或已经确认的包装关系给出的精确数据；
- 精确匹配到同一商品版本的历史营养表，此时可显示`沿用历史营养表`；
- 已确认并启用的个人菜品模板，此时显示`按个人模板`；
- 不产生额外误差的确定性单位换算，例如`2两=100g`；
- 数量已经明确、营养值仅来自可信公共数据库的普通食材。简洁回执不因此添加“估算”，但内部仍须保存来源和版本。

以下必须显示“估算”或等价的可追溯依据：

- 把`个、根、片、碗、盘、份、几口、半盒`等自然单位推定为克重或容量；
- 使用通用菜品模板补全用户没有说明的配料或用量；
- 推定果皮、果核、骨头等不可食部分后的可食重量；
- 使用密度、出成率、熟重与生重比例等非确定性换算；
- 在合理范围内采用较大值；
- 根据类别、入库时间和存放位置推算剩余保质期。

证据标签必须区分：

- `按个人模板`：来自用户多次确认后启用的个人配方；
- `沿用历史营养表`：来自同一商品身份的已确认营养版本；
- `参考数据库`：数量明确，营养基准来自可信公共数据源；
- `估算`：当前数量、可食重量、组成、保质期或换算过程存在系统推定。

### 18.7 自然单位估算必须公开计算基准

只写`橙子1个（估算）`不合格，因为用户不知道系统究竟按多少重量计算。凡自然单位参与营养计算，回执必须同时展示采用的克重或毫升数：

```text
橙子1个｜按可食部分200g估算
苹果1个｜按可食部分220g偏高估算
米饭半碗｜按熟重150g偏高估算
```

规则如下：

- 有不可食部分时必须写`可食部分`；
- 采用合理范围上沿时必须写`偏高估算`；
- 菜品组成由模板补全时必须说明`按通用模板估算`；
- 不得隐藏实际用于营养计算的质量或容量；
- 后续用户纠正时，以追加式纠正重算营养并补偿库存，原始估算证据不得丢失。

### 18.8 成功饮食记录的标准回执

本例是固定格式fixture，不是凭空模拟：时钟为`2026-08-09 19:00 +08:00`；当天本轮前累计为热量1099kcal、蛋白47.3g、脂肪40.6g、碳水139.5g、纤维11.8g、饮水920ml；目标依次为1900kcal、120g、60g、250g、25g、2.5L。用户只明确了菜名、橙子1个和白水500ml，牛肉面/凉拌黄瓜组成来自通用营养模板，家庭库存只有橙子形成独立可靠匹配。下列累计和增量是该fixture的冻结期望，不是系统通用默认值。

```text
已记录：9号晚餐
牛肉面1碗｜牛肉80g（按通用模板估算）、面条100g（按通用模板估算）、配菜50g（按通用模板估算）
凉拌黄瓜1小盘｜黄瓜200g（按通用模板估算）、调味油5g（按通用模板估算）
橙子1个｜按可食部分200g估算
白水500ml
库存：已扣橙子1个；牛肉面和凉拌黄瓜未据通用营养模板扣减组件库存

🔥 热量 ██████████ 99%
🔥1874 / 1900 kcal +775kcal +41%
🥩 蛋白 ███████░░░ 69%
🥩82.3 / 120 g +35g +29%
🧈 脂肪 ██████████ 106%
🧈63.8 / 60 g +23.2g +39%
🌾 碳水 ██████████ 99%
🌾246.5 / 250 g +107g +43%
🥬 纤维 █████████░ 85%
🥬21.3 / 25 g +9.5g +38%
💧 饮水 ██████░░░░ 57%
💧1420ml / 2.5L +500ml +20%
```

该格式还必须满足：

- 不显示重复的“本轮营养”段落；
- 库存行只写已经提交的真实效果，不把预览或计划写成已完成；
- 通用营养模板只能提供营养采用量，不能单独证明家庭库存用了哪些配料；只有用户明确配料、已确认个人配方或其他独立库存证据才允许扣组件库存；
- 外食时写`库存：外食，本次未读取和扣减库存`；
- 多候选时写清饮食已保存、库存暂未扣减，并建立待补充项；
- 库存不足时保存饮食事实、跳过不可靠扣减并登记异常；
- 固定六项进度必须来自本次成功事务最终返回的权威快照：恰好一个受影响日期时使用同次`daily_progress`且它逐字段等于`daily_progress_by_date[]`唯一项；跨日时每个日期块只使用同次`daily_progress_by_date[date]`，不得提供或借用单数别名，也不得为回执额外读取进度。

### 18.9 快捷答复与自然语言跟进

明显不确定或存在多个有效候选时，可在记录/库存明细之后、最终进度块之前给出快捷选项；选项组仍以自由文本出口收尾，且整体遵守§17.11“进度块是最后区块”。用户可以回复单个字母、组合字母或自然语言，例如：

```text
A
A、C
按200克算并扣第二瓶牛奶
牛奶是低脂那瓶，米饭按150克
```

所有快捷选项末行必须写：

```text
也可以直接说明实际情况，不必选择以上选项。
```

### 18.10 内部证据最低要求

每个记录项至少保存：

- 原始用户表达；
- 记录值、单位与标准化值；
- 字段属于明确值、确定性换算值、数据库值、模板值还是估算值；
- 数据来源、版本和适用基准；
- 推定方法、范围与最终选值；
- 用户是否确认；
- 使用的个人模板版本或商品营养版本；
- 与后续纠正、撤销、恢复和补偿事件的关系。

---

## 19. 查询、日回顾与周回顾

指定日期、餐次、有效明细、当日进度以及用户明确请求的最小周六项聚合属于`PRODUCT-0.1`；包含每日拆分、日均、记录模式、趋势和完整性说明的丰富周回顾，以及快速复用与更丰富筛选属于`PRODUCT-0.2`。共同聚合模型可以在0.1预留字段，但不得把未发布的0.2体验对外承诺。

### 19.1 查询意图与最小返回范围

至少支持事实、单餐、进度、日期、周期、食物、条件、异常和汇总查询。`今天吃了什么`只返回记录明细；`今天进度怎么样`只返回六项进度；`总结一下今天`返回明细、覆盖与进度。除非用户明确要求分析，不主动追加评价、建议或健康判断。

### 19.2 查询时间口径

所有日期范围按`OccurredTime.resolved_start/resolved_end + timezone + week_start`解析，不用消息接收或写入时间冒充进食时间。精确事件可使用兼容派生字段`resolved_occurred_at`，区间事件不得压成单点。`今天、昨天、本周、上周`必须支持可注入固定时钟，覆盖凌晨、跨日、跨月与跨年测试；展示统一遵守§10。

### 19.3 日记录视图

按实际发生时间排序并按餐次分组。用户明确餐次优先；未说明时可按时间推断但内部标记`meal_slot_inferred`；无法可靠归类则显示具体时间，禁止硬塞进午餐或晚餐。每道菜、独立食物、饮品各占一行。普通明细不显示内部ID、库存效果和营养来源；用户要求详情时再展开。

示例：

```text
9号

早餐
鸡蛋2个｜按可食部分100g估算
全麦面包2片｜60g
小象纯牛奶1瓶｜250ml
白水300ml

午餐
米饭200g
番茄炒蛋1盘｜番茄200g（按通用模板估算）、鸡蛋2个｜可食部分100g（按通用模板估算）、食用油8g（按通用模板估算）
苹果1个｜按可食部分220g估算
```

### 19.4 当前有效视图

- 未纠正记录采用原事件；
- 已纠正记录采用最新有效修订；
- 已撤销记录不进入当前明细和汇总；
- 库存补偿事件只影响库存，不重复计算饮食；
- 幂等重试只统计一次；
- 原始、纠正、撤销与恢复事件始终保留在审计链。

普通查询只显示当前结果；审计查询可显示`原记录 → 修正 → 当前有效`。

### 19.5 覆盖率与未知

存在未计算项目时，在进度之前写明`共N项，M项已计算；以下为已知部分`并列出未知项目。禁止把未知写0、把部分汇总称作完整总量、因营养未知删除饮食，或凭空补全高度可变套餐。单项纤维明确未知时显示`未知 / 目标`而不是0。

### 19.6 直接日进度查询

使用一次聚合报告；没有本轮写入时不显示`+amount`增量。尚未确认目标时仅列出现有记录量并明确进度条不可用，禁止创建推荐目标。

### 19.7 周范围与历史目标

`本周`从用户配置的周起始日到当前日期，不含未来日期。周目标必须逐日累计每一天当时生效的目标，禁止用今天目标简单乘以7，也禁止让今天的新目标改写历史周。没有记录的日期仍属于范围，不能从目标天数剔除。

PRODUCT-0.1只承诺用户明确询问时的一次聚合：周范围、六项已知累计、逐日生效目标合计和覆盖状态；不承诺每日拆分、日均、趋势、模式解释或快捷复用。

### 19.8 PRODUCT-0.2丰富周回顾的描述性边界

周回顾默认包括：记录覆盖天数、每日六项已知总量、周累计、日均值、营养覆盖率、估算项数量和未解决待补充项。没有记录只表示系统没有数据，不等于没有进食。默认不输出好坏评价、超标警告、减重增肌判断、疾病判断、食谱或下周建议。

用户明确要求分析时，只描述证据中的模式，例如`7次饮食中3次使用通用模板`，仍不得把缺记录解释成没吃。

### 19.9 歧义记录定位

更正、撤销或详情查询存在多个候选时，展示日期、可读时间、食物和数量，不展示内部ID。用户既可回复字母，也可说`删掉十点多喝的500毫升`；快捷选项末行必须支持自由文本。

### 19.10 聚合查询最低结果

一次日/周聚合至少返回：

```text
range_start / range_end / timezone
effective_meal_records[]
plain_water_ml
food_hydration_ml
six_metric_totals
effective_goals
daily_and_period_progress
nutrition_coverage
unknown_items[]
estimated_items[]
open_issues[]
has_corrections_or_voids
```

只有聚合结果明确缺字段时才补充读取，禁止默认扇出为多次饮食、饮水、库存查询。

---

## 20. 对话上下文、时间、否定与混合事件

### 20.1 先识别操作，再决定是否写入

每个分句先分类为：

```text
meal_record
water_record
inventory_stock
correction
void
nutrition_binding
query
plan
negated
unknown
```

出现食品名称不等于已经摄入。同一条消息可以包含多个操作，`mixed`仅是输入信封，不是一个领域事件类型。

### 20.2 已完成、计划和否定

- `我吃了两个鸡蛋`、`刚喝完500ml水`、`后来还是吃了`：已完成，记录；
- `准备吃`、`晚上想吃`、`等会喝`、`可能会吃`：计划，不写入；
- `没吃`、`没喝`、`差点吃`、`后来没吃`：否定，不写入。

必须读取整句的最终事实：`本来不想吃，后来还是吃了`要记录；`本来想吃，后来没吃`不记录。

### 20.3 否定作用域按分句解析

```text
没吃鸡蛋，吃了一根香蕉。       → 只记录香蕉
牛肉面没吃完，只吃了大约一半。 → 记录半份
没有买牛奶，只买了一袋鸡蛋。   → 只入库鸡蛋
蛋炒饭没放火腿，放了两个鸡蛋。 → 排除火腿，保留鸡蛋
```

并列项目分别判断完成状态、否定范围和数量，禁止用一个否定覆盖整句。

### 20.4 本人边界在口语中的处理

继续遵守§5.2：省略主语默认本人；`孩子吃了`不写入；`我和朋友一人一瓶`只记录本人1瓶；`我们吃了两盘`只能确定本人参与但个人份量不明时，保存本人吃了该食物的事实、数量未知或询问个人份量。不得创建家庭成员账户。

### 20.5 时间最低模型

每个饮食、饮水和库存事件至少保存：

```text
received_at
occurred_at_text
occurred_time: OccurredTime
```

未说时间时采用接收时间并标记`default_received_at`；不在普通回执反复强调，但允许后续纠正。禁止为了排序伪造用户没说过的精确分钟。

### 20.6 相对时间与跨午夜

相对时间基于可注入时钟、用户时区、`week_start`和可靠上下文解析。必须测试`今天早上`、`昨晚11点`、`前天午饭`、`上周五`、`两个小时前`。

`凌晨1点补记昨天夜宵`存在两种合理日期时，只确认日期，不擅自选择，并同时保留原始“昨天夜宵”“凌晨1点”和最终解析依据。

### 20.7 时间修饰范围

`喝了一盒昨天买的牛奶`中，当前时间是饮用时间，`昨天买的`用于定位库存批次；不得把摄入写到昨天。`昨天喝了一盒刚买的牛奶`中，`昨天`修饰饮用，`刚买的`修饰库存来源。解析器和案例必须分别表达`event_time_text`与`inventory_reference_text`。

### 20.8 餐次来源

```text
explicit
inferred_from_time
unknown
```

明确餐次优先，其次才按实际时间推断；无法可靠归类保持未知。用户只说`早餐吃了鸡蛋`时保存日期和早餐，不伪造时分。

### 20.9 短期上下文模型

```text
active_preview
latest_committed_event
current_meal
pending_anomalies
recent_inventory_candidates
active_product
last_query_scope
```

上下文只用于定位`再加一根香蕉`、`不是一盒，是半盒`、`那个按标签算`等跟进，不能代替持久业务记录。

### 20.10 指代解析和跨会话边界

指代依次尝试：当前消息对象 → 活动预览 → 最近成功记录 → 当前餐次 → 最近候选 → 活动商品。只有一个可靠候选时直接执行；多候选给可读选项；没有候选就询问。

新会话单独说`那个按标签算`时，若没有可恢复的持久对象描述，必须让用户说明食品或日期餐次，不能随机绑定上次会话对象。

### 20.11 上下文失效

问题已解决、目标被纠正或撤销、库存候选变化、批次耗尽、明显切换话题、新会话且无可恢复对象时，旧预览和快捷选项失效。执行旧选项前必须重新校验。

### 20.12 跟进语义

- `不是两个，是三个`：纠正最近可靠记录；
- `再加一根香蕉到早餐`：补充原餐次；
- `刚刚又吃了一根香蕉`：新的摄入事件；
- `小象纯牛奶用这个标签`：有活动商品时做`nutrition_binding`，不创建摄入；
- 两种解释都合理时，只问纠正还是新增这一处差异。

### 20.13 营养知识与资料绑定不创建饮食

`鸡蛋多少蛋白质`、`这个牛奶没有营养表，帮我估一下`、`这张标签属于小象纯牛奶`、`牛肉面热量大概多少`都是查询或资料操作。只有明确已摄入，或明确绑定到既有摄入记录时，才改变饮食进度。

### 20.14 一餐多项与事件归组

`早餐吃了两个鸡蛋、一片面包，喝了250ml牛奶和300ml白水`解析为同一`meal_id`下三个`diet_meal`项目和一个独立`diet_water`事件。牛奶仍是营养饮品，不能因为是液体写入白水。

### 20.15 混合信封和叙述顺序

`买了两箱牛奶共24盒，早餐喝了一盒，又喝500ml白水，看看今天进度`按顺序执行：入库24盒 → 记录牛奶 → 从新批次扣1盒 → 记录白水 → 基于所有成功结果返回最终进度。历史必须保留24盒入库和1盒消耗，不能只存净值23盒。

### 20.16 混合事件原子边界

- 默认每个独立子操作各自原子；
- 有依赖的子操作按叙述顺序执行；
- 每个已发生事实先执行`FactCommit`，原子保存事实、原话、最小项目、幂等信封和待处理效果outbox；
- 随后执行引用该事实的`EffectBundle`，原子处理营养快照、实际库存效果/跳过原因、Issue、该子操作的投影贡献和受影响日期；它不得提前生成整个请求的最终进度快照；
- 同一食品的库存扣减、对应Issue、营养快照与该次投影必须在同一个`EffectBundle`内成功或回滚，但`EffectBundle`失败不得回滚已经提交的事实；
- 所有子操作效果进入稳定终态后，由一次幂等`EnvelopeFinalize`生成并冻结信封级进度、回执和终态结果；
- 库存前置条件不满足时，饮食仍保存、库存跳过并建立异常；
- 入库技术失败不应抹掉已经明确发生的饮用事实；
- 每个子操作使用稳定幂等子键。

`FactCommit`失败时该子操作为`failed`；`FactCommit`成功而`EffectBundle`技术失败时为`committed_with_issues`，outbox保持`pending`或`retryable_failed`。此时不得显示伪造或旧的最终进度，回执必须明确“事实已记录，库存/营养/进度处理待重试”。

### 20.17 混合回执只有一个最终进度块

同一轮可以依次显示入库、记录和库存结果；同一自然日只能在末尾显示一个最终六项进度块。本轮增量是全部已完成`EffectBundle`的饮食与饮水子操作之和，必须由`EnvelopeFinalize`冻结的信封级聚合结果给出。存在技术`effects_pending`或最终器尚未提交时，不显示进度块，只报告待重试；业务性未知量则可形成带`partial/unknown`覆盖的权威进度。

### 20.18 写入并查询

`午饭吃了蛋炒饭，今天热量多少`必须先提交蛋炒饭，再用提交后的最终日进度回答。不得先读旧进度再写入。如果用户要求完整日明细，写入后用一次聚合查询取得当前完整视图。

### 20.19 部分成功

混合输入必须逐项说明`已完成`、`已记录但待补充`和`未执行`，不能用一个笼统失败掩盖其他成功项。最终进度只含已提交且可计算的已知部分，并显示覆盖信息。

### 20.20 混合幂等

整条请求有信封级幂等键，每个子操作有稳定序号。只有同一键的`input_digest`、主体/作用域和命令类型全部一致，才视为重试；同键但任一项不同必须返回稳定`idempotency_conflict`，零新写入，也不得把旧结果说成本次请求结果。合法重试不能重复入库、饮食或饮水。终态信封直接返回其冻结的原结果和原进度，即使后来累计已经变化也不得重建；非终态信封只恢复`pending`或`retryable_failed`的outbox效果，不重做成功的`FactCommit`或已完成效果。

最后一个待处理效果完成或被稳定业务降级后，`EnvelopeFinalize`在一个原子提交中：校验所有outbox均为终态；按信封和受影响日期汇总本请求贡献；在一致性快照中读取当时已提交投影；生成`daily_progress_by_date[]`及适用的单日别名；写入`MixedCommitResult`、`ReceiptData`、冻结幂等结果和`terminal`状态。该原子提交的提交点是最终结果线性化点。最终器在任一写点失败时全部回滚，信封保持`effects_pending`且不返回成功回执；同键重试只重跑最终器，不重做事实或效果。其他请求在该提交点前已完成的投影可以进入`committed_totals`，但`current_turn_increments`只能来自本信封贡献；提交点后的写入不得改变冻结结果。

### 20.21 最低验收案例

- 否定、转折、没吃完和并列作用域；
- 他人摄入不写本人、共同表达只取本人份量；
- 无时间、昨晚11点、凌晨歧义和库存时间修饰；
- 最近记录纠正、当前餐次补充、跨会话模糊指代；
- 营养问题和标签绑定不创建饮食；
- 一餐多食物与白水拆分；
- 同句购买后食用、写入后查询；
- 独立子操作部分成功；
- 整条消息重试不重复副作用；
- 最终回执仅一个进度块；
- 过期快捷选项不能执行旧库存操作。

---

## 21. 数据安全、隐私、事务与验证零污染

### 21.1 当前 foundation 的已核对边界

- A当前只验证Skill结构、固定JSONL路径和追加式契约，不运行真实业务写入；
- B的foundation handler固定`committed: false`；当前只有私有`node:sqlite`兼容运行时和test-owned测试，没有SQLite业务表或正式数据依赖；
- C固定`writes: false`，SQLite仅是保留边界；
- `shared/validate-foundations.ps1`为B/C设置临时`OPENCLAW_STATE_DIR`、完成后清理，并扫描项目内JSONL/SQLite类文件；
- 现有“执行后无文件”证明不足以覆盖未来已经存在正式数据的情况，必须升级为前后不变证明。

### 21.2 八类数据与执行根完全隔离（唯一规范）

本节是根名称、用途和隔离规则的唯一规范来源，其他章节只能引用，不能再定义第二套根：

| 根ID | 唯一用途 | 硬约束 |
|---|---|---|
| `official_data_root` | 用户正式JSONL/SQLite、业务附件 | 验证、测试、构建和普通对话绝不写入 |
| `isolated_test_root` | 单元、集成、共同案例、故障注入的合成数据 | 每次唯一，结束前审计、结束后清理 |
| `validation_root` | 一次验证运行的临时工作副本和运行清单 | 不含正式数据；失败路径同样生成after manifest |
| `build_root` | TypeScript、插件和候选构建产物 | 不重写源码树`dist`；候选冻结后只按hash晋级 |
| `openclaw_state_root` | 插件校验所需状态、配置和缓存 | 与调用者状态隔离，删除前审计，失败也清理 |
| `backup_root` | 可恢复一致性备份 | 不与导出混用；写入后必须恢复验证 |
| `export_root` | 面向用户读取的数据导出 | 不是恢复介质；遵守字段选择与隐私策略 |
| `evidence_root` | 日志、manifest、机器报告和候选hash | 不存用户业务原话或密钥；引用后不可静默覆盖 |

八类根不得互相包含。测试、构建、验证、日志和缓存不得写正式根。所有路径在使用前规范化并验证仍处于允许根，拒绝`..`穿越、符号链接/目录连接逃逸和聊天输入指定任意正式路径。聊天记忆不是正式存储。

### 21.3 路线存储边界

| 路线 | PRODUCT正式存储处置 | 当前边界 |
|---|---|---|
| A | 不再建设正式JSONL产品存储 | 只保留只读/无插件降级规范与历史foundation结构，不创建JSONL业务文件 |
| B | 唯一正式存储主线，固定SQLite数据库 | 当前已完成内置SQLite test-owned兼容spike但不创建业务数据库；必须在`SH-SAFE-BASE-001`有界退出验收、共享映射/harness/trace与B门后才能承担正式数据 |
| C | 停止独立SQLite产品实现 | 历史foundation只读保留；服务端预览/状态安全语义经`B-MERGE-C-001`并入B，不创建C业务数据库 |

正式路径由安装配置和存储契约决定，不能由普通对话临时指定。

### 21.4 “已记录”只对应真实提交

只有业务事务已经持久化完成才返回`status=committed`和`committed=true`。只完成解析、预览、聊天上下文更新、未刷盘、回滚、存储不可用或提交结果未知时，都不得声称“已记录”。A历史JSONL格式若缺少提交标记也不得声称“已记录”，但该项仅供A停止路线历史/无插件降级及受控只读导入判断，不适用于B门、B发布集或B产品实现；B以SQLite事务的明确提交结果为准。结果未知必须失败关闭并允许幂等重试。

### 21.5 单项业务事务最低范围

事实优先采用两层持久化边界，避免附加层技术故障抹掉真实摄入：

1. `FactCommit`原子保存`EventEnvelope`、原始事件、最小规范化项目、原话/时间/来源、请求与子操作幂等键，以及列明预期附加效果的durable outbox；
2. `EffectBundle`引用已提交事实，原子保存营养快照、实际库存效果或稳定跳过原因、新Issue/解决事件、来源版本、该子操作的投影贡献和`affected_dates[]`，不写信封级最终进度；
3. `EffectBundle`失败只回滚本效果包，`FactCommit`继续可见且outbox可幂等重试；
4. 库存前置条件变化属于业务降级，不是技术失败：效果包提交“库存不变 + 稳定跳过原因 + Issue + 可聚合的partial贡献”；
5. 全部效果稳定终态后，`EnvelopeFinalize`在同一原子提交中以一致性快照汇总本信封贡献与受影响日期，写入`daily_progress_by_date[]`、适用的`daily_progress`别名、`MixedCommitResult`、`ReceiptData`、冻结幂等结果和`terminal`；该提交点是线性化点；
6. 最终器失败全部回滚且信封保持`effects_pending`，不得返回成功回执；重试只执行最终器，不重做事实或效果；
7. 营养、库存、Issue、子操作投影、最终进度和终态冻结的任一技术写入点都必须有故障注入，证明事实仍存在、效果不半写、最终器不重复且并发写入不污染本轮信封增量。

这里的“写入失败不能产生半条记录”专指业务事务边界，不等于失败后禁止一切外写。`FactCommit`若未成功提交，则本次不得产生任何可查询的饮食/饮水事实、库存变化、营养快照、Issue、业务outbox、日进度、成功回执或终态幂等结果；事务内已经尝试的业务行必须全部回滚。系统允许并应在独立的技术日志/审计通道记录时间、操作ID、稳定错误码、失败阶段和必要的脱敏诊断，但这些日志不得进入饮食记录、业务投影或业务事件表，不得被查询层解释为“已记录”。如果`FactCommit`已经明确成功而后续`EffectBundle`失败，则这不是“饮食记录写入失败”，而是已提交事实的附加效果待重试，必须按本节分层状态如实返回。

因此，“已记录”可对应事实提交；只有效果已终态结算时才使用普通成功回执和权威进度块。对应架构决定为`DEC-023`。

### 21.6 全副作用幂等

新增饮食、白水、入库、库存调整、异常补全、纠正、撤销、恢复、批量操作和混合子操作全部必须有幂等键。`FactCommit`与效果outbox同提交；每个效果有稳定效果键；终态`MixedCommitResult/ReceiptData/daily_progress_by_date[]`与终态幂等记录由`EnvelopeFinalize`同提交并冻结。只有键、`input_digest`、主体/作用域和命令类型一致才返回或恢复原请求；同键不同输入返回`idempotency_conflict`且零新写入。合法重试返回原终态结果，不重复事件、库存、模板确认次数或进度，也不换绑另一批次。

两条文本相似但幂等键不同的消息不是技术重试。系统最多提示可能重复，不能自动合并、覆盖或删除。必须覆盖“事务成功但回复丢失，客户端重试”的故障案例。

### 21.7 A路线JSONL事务门（停止路线历史参考）

本节只保留A停止前的安全要求，供审计历史格式和设计只读降级/导入时识别风险；它不属于B硬安全门或B发布集，也不得要求B实现JSONL写入。若未来经新CHG/DEC恢复A正式写入，A不能依靠依次追加三个文件来宣称原子性，至少需要：全局事务ID、进程级排他锁、事务开始记录、业务记录校验和、权威提交标记、明确刷盘顺序、启动恢复扫描、未提交事务不可见、坏尾检测和幂等投影。

```text
data/inventory.jsonl
data/meals.jsonl
data/nutrition.jsonl
```

历史A规则要求任一跨文件事务中断时读取端全部忽略该事务。当前A已停止正式写入；只读降级遇到未提交片段或坏尾必须失败关闭，B只需验证SQLite对应Oracle，不执行`CASE-STORAGE-003`。

### 21.8 B路线SQLite发布门

B至少启用外键、唯一幂等约束、非负库存检查、显式事务、Schema版本与迁移历史、并发处理、故障回滚、完整性检查和可验证备份。关键数量与营养不得直接使用二进制浮点数。

原C路线的硬安全控制转为B主线强制要求：服务端保存并签发预览；预览绑定`input_digest + data_revision`；调用方自报状态一律不可信；陈旧预览、非法状态转换和非法Schema迁移必须在写库前拒绝；上述检查与幂等记录必须在B服务端事务边界实施。由`B-MERGE-C-001`实现并由B自己的硬安全/纵切门验证，不能借历史C foundation证据视为通过。

### 21.9 查询真正只读

饮食、库存、进度、营养来源、待补充、纠正历史和临期查询不得修改业务数据、补0未知值或创建模板。缓存刷新只能写独立缓存区，不能伪装成业务事件。

### 21.10 所有验证只用隔离数据

foundation、单元、集成、构建、插件、Schema、验收、故障注入、迁移和计划检查仅能使用临时根、路线专属测试根、合成SQLite或合成JSONL。临时状态清理失败本身即验证失败。禁止在正式数据写入“测试鸡蛋”后再删除，因为追加式存储仍会留下历史。

### 21.11 验证前后不可变清单

统一验证入口必须：

1. 解析全部正式数据根；
2. 生成执行前清单：路径、大小、SHA-256、最后修改时间；
3. 创建唯一验证根；
4. 运行A/B/C验证及状态隔离测试；
5. 覆盖失败路径和清理测试；
6. 生成执行后清单；
7. 比较新增、修改、删除；
8. 检查临时残留；
9. 输出机器可读报告与原始日志。

成功条件固定为：

```text
正式文件新增 = 0
正式文件修改 = 0
正式文件删除 = 0
路径越界 = 0
临时状态残留 = 0
```

### 21.12 业务文件扫描范围

扫描大小写不敏感，至少覆盖：

```text
*.jsonl
*.jsonl.tmp
*.jsonl.journal
*.sqlite
*.sqlite3
*.db
*.sqlite-wal
*.sqlite-shm
*.sqlite-journal
*.db-wal
*.db-shm
*.db-journal
```

扫描正式根、项目根、三路线目录、验证根和OpenClaw隔离状态。可以排除普通依赖目录，但不能因目录名为`dist`就忽略其中的业务数据库文件。

### 21.13 `validate-foundations.ps1`增强任务

保留现有A结构验证、B/C测试与构建、插件验证、临时状态、环境恢复和后置扫描，并增加：执行前快照、已有正式数据不变比较、SQLite辅助文件扫描、统一纳入状态隔离测试、失败时照常扫描、保存原始输出、JSON报告、逐路线命令与退出码、清理结果。不得用一句“全部通过”替代逐项证据。

### 21.14 验证证据最低字段

证据字段只采用`§28.1`的规范表。本节增加的是执行约束而非第二套Schema：foundation/存储/发布证据必须在`§28.1`的“环境、命令、数据根、前后扫描、业务影响”字段内分别填入Node/npm或pnpm/OpenClaw版本、validator/contract SHA-256、逐路线结果与退出码、临时根、added/modified/deleted清单和清理结果。当前目录不是Git仓库时，以关键文件SHA-256清单替代提交哈希。

### 21.15 失败路径仍须保护数据

无论失败发生在哪条路线的结构、测试、构建、插件验证或清理阶段，都必须恢复调用者环境变量、尝试清理临时状态、生成执行后清单、报告数据差异、保留原始错误并返回非零退出码。前置失败不能跳过污染检查。

### 21.16 联网最小披露

外部营养查询只发送规范化食品/商品名称、品牌、变体、规格、生熟状态和必要地区。禁止发送用户姓名、完整饮食、体重目标、历史记录、库存全量和原始聊天。外部请求必须有字段白名单测试。

### 21.17 日志、密钥和诊断

普通日志仅记录操作类型、状态、耗时、来源类型、事务ID不可逆短标识、错误码和对象数量。默认不记录用户原话、完整菜品、标签全文、正式数据、密钥和令牌。原始诊断必须由用户显式开启，并说明位置与清除方式。

API密钥不得进入业务表、fixture、日志、版本库或回执。缺少密钥不得阻止饮食事实保存，按缓存、模板或未知降级。

来源配置还必须满足：只读检查不创建目录；首次写入才创建私有配置根；配置采用同目录临时文件加原子替换；拒绝符号链接/重解析点目标；普通命令行和错误文本不得包含Key、Cookie、代理凭据或带认证URL。`Doctor`输出只保留来源ID、后端ID、状态、耗时、稳定错误码和脱敏修复建议。

研究库与业务库物理、事务和查询边界分离。研究抓取、排名、聚类、Watchlist或简报失败不得回滚、改写或伪造饮食业务数据；研究结果只有经显式确认并走正式Profile更新事件后才影响未来记录。

### 21.18 备份、导出与业务删除边界

备份由用户主动触发或破坏性迁移前创建；导出说明范围；默认卸载保留正式数据；删除正式数据是独立二次确认操作；普通“删除饮食”只做追加式撤销；备份恢复必须先在临时根验证。

### 21.19 安全硬性淘汰门

B出现未解释的错误正式写入、重复副作用、负库存、半事务、路径越界、验证改动正式数据、响应丢失后重复写、损坏数据仍报成功、查询写业务或日志/网络超范围泄露时，立即失去发布资格，不能靠其他证据补偿。

### 21.20 数据安全最低验收

- 三套foundation统一验证通过，调用者环境恢复；
- 插件状态根独立且临时目录清理；
- 项目内不新增JSONL/SQLite及辅助业务文件；
- 预置正式数据的路径、大小、哈希和时间不变；
- 任意阶段失败仍完成后置扫描；
- 路径穿越、符号链接逃逸被拒绝；
- 查询前后业务数据不变；
- 幂等重试与相似独立消息区分；
- 成功后响应丢失不重复写；
- SQLite中途失败完整回滚；
- 仅A停止路线历史/无插件降级参考：JSONL跨文件中断不可见、坏尾失败关闭；该项不适用于B门、B发布集或B产品实现；
- 并发扣减不负库存；
- 陈旧预览不提交；
- 网络白名单与日志脱敏通过；
- 默认卸载保留数据，整库删除需二次确认。

---

## 22. 安装、升级、迁移、备份、恢复、导出与卸载

### 22.1 三种版本概念不得混淆

- `FOUNDATION-0.1/0.1.0`：工程外壳能够构建、测试和验证，不代表业务可用；
- `PRODUCT-0.1`：可靠日常饮食账本，具备正式存储、安装、备份和卸载；
- `PRODUCT-0.2`：在0.1最小周六项聚合之上增加个人模板、丰富周回顾/趋势、快捷复用、临期主动提醒和更丰富导出。

任何foundation版本号都不得宣传为PRODUCT-0.1。

### 22.2 当前命令只是开发验证

```powershell
Set-Location '<repository-root>'
powershell -ExecutionPolicy Bypass -File .\shared\validate-foundations.ps1
```

该命令检查A结构、运行B/C测试和构建、验证插件外壳并检查业务文件污染；它不是安装命令，不会得到可用于日常饮食的成品。

### 22.3 正式发布只有一个用户安装入口

路线通过决策和发布门后，普通用户只运行统一安装器，例如`install-diet-manager.ps1`，无需理解或安装A/B/C三套工程。安装包清单至少包含：

```text
product_version
route
contract_version
schema_version
supported_environment
package_files[]
file_sha256[]
installer_version
release_evidence_id
```

文件缺失或哈希不符即停止。

### 22.4 安装前只读预检

检查操作系统/架构、PowerShell、Node.js、OpenClaw、程序与数据目录权限、磁盘空间、旧版本、已有业务数据、未完成迁移、备份空间、重名插件/Skill和Schema支持范围。预检失败不创建数据库、不迁移、不覆盖，并给出具体不兼容项。

### 22.5 全新安装事务

```text
只读预检
→ 验证包及SHA-256
→ 展示版本、路线和精确路径
→ 用户确认
→ 创建临时安装目录
→ 安装并验证程序文件
→ 初始化空数据结构
→ 隔离根冒烟测试
→ 证明正式业务记录仍为0
→ 原子切换正式版本
→ 返回结果和回滚入口
```

不得在最终目录逐个覆盖而留下半安装。

### 22.6 首次安装允许与禁止创建的内容

B唯一产品主线的首次安装只允许创建正式数据目录、空SQLite Schema、Schema版本、本地配置、安装清单和空备份目录，不得创建正式JSONL。A JSONL只可作为停止路线历史、无插件降级或受控只读导入源，安装器不得创建。禁止创建示例早餐、库存、饮水、虚构进度、默认个人模板、家庭成员和自动健康目标。初始化后业务记录总数必须为0。

### 22.7 首次启动最低配置

必要配置只有时区、区域/语言、每周起始日、正式数据位置说明、是否允许联网查询和备份位置（可稍后）。热量、蛋白质、脂肪、碳水、纤维、白水目标均为可选，只有用户明确设定后才显示进度条；不建立家庭成员。

### 22.8 安装冒烟测试使用隔离根

冒烟测试验证加载、工具Schema、空库查询、合成饮食、合成库存扣减、幂等重试和完整清理。所有合成数据只写临时根，正式数据不得出现测试事件或追加式删除痕迹。

### 22.9 安装成功回执

必须显示PRODUCT版本、实现路线、契约与Schema版本、正式业务记录0条、隔离冒烟结果、正式数据影响0条、数据位置、备份入口和卸载入口。

### 22.10 已有安装检测

发现旧版本时先读取当前产品/路线/Schema、数据数量、最后提交、最近备份、完整性状态和未解决迁移，再明确显示目标版本、Schema变化、是否迁移和备份要求。用户取消不得改变任何文件。

### 22.11 升级流程

```text
只读检查
→ 验证目标包
→ 检查升级路径
→ 创建一致性备份
→ 在备份副本演练迁移
→ 校验事件、库存、营养、幂等与进度投影
→ 新程序安装到临时目录
→ 锁定正式写入
→ 正式迁移
→ 只读完整性检查
→ 切换程序
→ 恢复服务并输出报告
```

禁止跳过中间Schema版本。

### 22.12 迁移不可丢失的证据

必须保留原始用户表达、饮食/库存事件、纠正撤销链、商品身份、历史批次、营养资料版本、每次摄入快照、幂等原结果、个人模板版本、异常状态、配置与历史目标、时间/时区/解析依据。0.1升0.2不能重新计算并覆盖0.1历史营养。

### 22.13 路线专属迁移

A路线复制迁移是停止路线的历史方案，不属于B发布任务。若未来需要把既有A格式导入B，只允许由`SEL-MIGRATE-001`定义只读解析、坏尾失败关闭、备份和导入后核对；禁止恢复A写入器或要求B通过JSONL追加中断Oracle。

B路线采用一致性备份和事务迁移，执行外键与完整性检查、关键表计数、幂等约束验证，并拒绝非法迁移；破坏性迁移必须先在副本演练，失败完整回滚。C不再形成独立迁移链。

### 22.14 升级失败和版本回滚

失败时停止切换，旧程序继续可用或恢复，正式数据保持升级前状态，不写成功版本号，并报告错误、备份与数据变化。无法自动回滚时立即停止写入，不猜测修复。

程序回滚与数据回滚分开：Schema兼容时可恢复旧程序；不兼容时必须从升级前备份恢复，并提前显示恢复点、升级后可能丢失的数据和是否另备当前数据。

### 22.15 备份与导出不是一回事

备份用于完整恢复，包含全部事件、商品批次、营养版本、配置、幂等、Schema和审计链；导出用于查看或分析，可以只含选定日期/字段，不能把CSV称为完整备份。

### 22.16 备份清单与一致性

```text
backup_id
created_at
product_version / route
contract_version / schema_version
source_data_version
files[] / file_sha256[]
record_counts
last_committed_event
integrity_check
encryption_status
```

备份后必须重新读取并核验哈希。SQLite使用一致性备份/快照且包含已提交WAL内容；A历史JSONL或其受控只读导入源仅在事务边界锁定后复制，该JSONL规则只作停止路线历史/无插件降级参考，不属于B门、B发布集或B产品实现。备份必须能在临时目录实际恢复和读取。

### 22.17 恢复流程

```text
选择备份
→ 验证清单和哈希
→ 检查版本兼容
→ 恢复到临时根
→ 完整性和投影校验
→ 检查并备份当前正式数据
→ 用户确认替换
→ 原子切换数据根
→ 启动后只读校验
```

PRODUCT-0.1不自动合并两个完整数据库，只支持恢复到独立位置检查、备份当前后整体替换或取消。

### 22.18 导出版本范围与隐私

PRODUCT-0.1提供完整恢复备份、结构化JSON归档和指定日期的有效饮食导出；PRODUCT-0.2增加CSV、日/周汇总、估算依据、原话/纠正历史选择及匿名化选项。

导出前展示日期范围、预计记录数、字段、是否含原话/库存/目标、保存位置和加密状态。普通分析导出默认不包含完整原始对话；导出不得改变业务数据。

### 22.19 卸载分成两个明确操作

默认卸载只移除程序、注册信息和可重建缓存，保留正式数据、备份、导出和用户配置。`卸载并删除数据`必须列出绝对目录、数据量、最后记录日期和最后备份，经过二次确认后只删除列出的目标，并报告恢复可能性。

### 22.20 删除范围安全

删除前解析绝对路径并验证位于饮食管家专属目录；拒绝工作区根、用户主目录、磁盘根、未解析变量、宽泛通配符和无关OpenClaw配置。对每个目标生成清单，删除后说明能否从备份恢复。

### 22.21 安装、迁移与卸载日志

可记录版本、文件清单、兼容结果、迁移步骤、数据数量、哈希和退出状态；不得记录完整饮食、标签全文、API密钥或原始对话。

### 22.22 “可以安装使用”的硬门

只有B完成PRODUCT-0.1、共同案例通过、存储故障与幂等通过、全新安装/已有数据升级/备份恢复/默认卸载通过、隔离冒烟零污染、安装包哈希绑定发布证据，并在明确支持环境重新验证后，才能称为可安装产品。

### 22.23 安装生命周期最低验收

- 当前foundation命令只验证不安装；
- foundation版本不冒充PRODUCT版本；
- 环境不兼容不创建数据库；
- 全新安装业务记录0条；
- 冒烟不写正式数据，安装中断无半安装；
- 包文件被修改时拒绝；
- 已有数据进入升级而非全新安装；
- 升级前可验证备份，迁移先在副本演练；
- SQLite失败回滚；A历史JSONL或受控只读导入源不改原文件，该JSONL项只作停止路线历史/无插件降级参考，不属于B发布或完成判据；
- 0.1升0.2历史快照不变；
- Schema不兼容时回滚停止；
- 哈希错误拒绝恢复，恢复先在临时根验证；
- 恢复前备份当前数据；
- 导出只读；
- 默认卸载保留数据，整库删除二次确认；
- 删除范围不能逃出专属目录；
- 卸载不删除无关OpenClaw文件。

---

## 23. 共同领域模型与路线存储映射

### 23.1 共同模型清单

| 模型 | 单一职责 |
|---|---|
| `EventEnvelope` | 稳定事件ID、来源、时间、Schema、幂等与生命周期 |
| `FactCommit` | 事实、原话、最小项目、幂等子键和durable effect outbox的原子提交 |
| `EffectBundle` | 引用事实的营养、库存、Issue、来源、子操作投影贡献和受影响日期原子效果包 |
| `EnvelopeFinalize` | 所有效果终态后的信封级一致性快照、进度、回执、冻结结果和terminal原子提交 |
| `MealEvent` | 一餐及其有序摄入项目 |
| `MealItem` | 原名、规范名、份量和待处理效果意图；实际营养/库存由独立效果事件与投影关联 |
| `WaterEvent` | 白水原始容器、容量、换算与估算依据 |
| `MealContext` | 家庭/非家庭证据、餐次、上下文来源和有效期 |
| `ProductIdentity` | 跨批次商品身份、别名与包装变体 |
| `InventoryBatch` | 一次入库的不可变原始批次事实 |
| `InventoryBatchProjection` | 由库存事件计算的剩余量、位置、开封和有效状态 |
| `InventoryTransaction` | 入库、扣减、返还、调整、丢弃和退货事件 |
| `NutritionProfile` | 版本化营养资料及适用范围 |
| `NutritionSnapshot` | 一次摄入实际采用的营养计算 |
| `HydrationContributionSnapshot` | 营养饮品水分的单次贡献与依据 |
| `PersonalDishTemplate` | 通用/个人菜品版本与用户证据 |
| `Issue` | 异常、资料缺口或核心阻塞问题 |
| `IssueResolutionEvent` | 后续补充、确认、延期和关闭结果 |
| `CorrectionEvent` | 追加式纠正、移除、撤销与恢复 |
| `DailyProgressSnapshot` | 同次成功提交后的权威日聚合 |
| `GoalConfigEvent/GoalVersion` | 六项目标的生效区间和历史版本 |
| `PromptOptionGroup` | 快捷选项、绑定对象、生成版本、过期和自由文本出口 |
| `ExpiryReminder` | PRODUCT-0.2主动临期提醒、依据、去重和处理状态 |
| `MixedCommitResult` | 信封级逐子操作结果、冻结回执与按日期进度数组 |
| `ReceiptData` | 只含已经提交结果的结构化回执输入，不含模型猜测累计 |
| `IdempotencyRecord` | 请求键、输入摘要、子操作和原提交结果引用 |
| `ProvenanceEvidence` | 用户、标签、网络来源、规则和推导证据 |
| `SourceCapability` | 来源适配器、后端顺序、Tier、权威等级、许可、请求白名单和能力范围 |
| `SourceHealth` | 真实Probe所得后端状态、耗时、稳定错误码、选中/备用后端和脱敏修复建议 |
| `ResearchRun` | 研究主题、实体、窗口、来源覆盖、策略版本、开始/结束和结果状态 |
| `ResearchItem` | 单一来源取得的不可变原始证据元数据、内容hash和获取时间 |
| `ResearchCandidate` | 标准化、实体锚定、匹配、时效、来源权威和冲突字段 |
| `ResearchCluster` | 指向同一商品资料版本、变化或主张的候选集合 |
| `ResearchFinding` | 通过可信度门、可追溯到证据簇的发现或`nothing_solid`结果 |
| `NutritionWatchlist` | 用户明确关注的食品/商品/来源、上次运行和通知策略 |
| `NutritionUpdateProposal` | 待确认的Profile更新建议、前后差异、证据、覆盖、置信度和处置状态 |

### 23.2 每个模型规范的必填说明

每个模型在共同Schema中必须明确：字段名称和类型；必填/可空/显式unknown；原始/推导/聚合属性；来源与规则版本；Schema与迁移；生命周期与纠正保留；B SQLite映射、A只读降级边界和C控制合并点；隐私等级与日志脱敏。

`MealEvent`、`WaterEvent`、`InventoryTransaction/StockEvent`、`IssueResolutionEvent`、`CorrectionEvent`和`GoalConfigEvent`全部继承`EventEnvelope`，不得各自遗漏事件ID、Schema版本、幂等键、来源、`OccurredTime`、提交时间和生命周期。`MealEvent`与同餐`WaterEvent`通过`meal_id`关联；白水仍保持独立事件类型。

不可变事件与当前状态必须分开：事件只保存当时事实和生命周期；`InventoryBatchProjection`、`IssueProjection`、当前有效饮食视图和`DailyProgressSnapshot`是可重建投影。`effective_status`只能出现在投影，不能反写历史事件。

请求信封状态只允许`received/fact_committed/effects_pending/terminal/failed_fact`；outbox效果状态只允许`pending/processing/succeeded/retryable_failed/permanent_business_skip`。`terminal`后结果不可变；`processing`租约过期后可回到`pending`；业务跳过是可解释终态，不与技术失败混用。

跨领域覆盖状态统一使用类型`CoverageStatus`，唯一枚举为：

```text
complete | partial | unknown | not_applicable
```

字段证据质量统一为：

```text
explicit | deterministic_conversion | product_label | authoritative_reference |
personal_template | common_template_estimate | bounded_estimate | unknown
```

营养和DailyProgress统一使用字段`coverage_status: CoverageStatus`；饮水子域可使用`water_coverage_status: CoverageStatus`以指明作用域，但仍引用同一个四值类型，不得另建子集。不得在营养、饮水、查询和回执分别创造`completeness`或其他同义状态枚举。

### 23.3 A路线只读/无插件降级映射约束

- A不再实施正式写入、事务日志、恢复器或产品级JSONL投影；
- 只读降级规范只允许读取明确受支持的历史/导出数据并返回无写入能力说明；
- 无插件环境不得以聊天记忆、临时JSONL或未验证脚本替代B正式存储；
- A历史格式若未来需要迁入B，必须由`SEL-MIGRATE-001`另行定义只读导入、备份与失败关闭，不恢复A完整产品路线。

### 23.4 B路线SQLite映射约束与C控制合并

- 数据库路径固定，不接受模型传入任意路径；
- 外键、唯一、检查约束和事务在服务端实施；
- Schema迁移有版本、前置备份、兼容检查和失败回滚；
- 预览、幂等、事件、库存和纠正均由服务端验证；
- B不信任调用方自报状态，提交绑定服务端保存的`preview_id + preview_version/hash + input_digest + data_revision`；
- 陈旧状态、伪造状态、非法转换和非法迁移在B服务端失败关闭；
- C不再独立完成插件集成验收，上述设计只通过`B-MERGE-C-001`进入B并接受B门验证。

### 23.5 对话、领域和展示分层

对话层只负责识别用户意图、组织结构化命令和渲染`receipt_data`；不得直接拼SQL、修改路径或计算隐藏累计。领域规则不依赖中文回执文本，不得反向解析回执作为业务事实。规则、来源、模板、Schema和迁移全部版本化。

### 23.6 版本化策略表消除模糊阈值

实现不得把`高可信、唯一可靠、明显不同、临期、容差内`留给模型临场判断。`shared/policies/`必须保存版本化机器表；原“三路G2数值评分”已由`DEC-027`取消，策略表只保留产品阈值与B-only门的确定性通过/返回条件，最低定义：

| 策略 | 可执行定义 |
|---|---|
| `unique_reliable_product` | 经过品牌、变体、规格、商品形态和用户确认别名的硬冲突过滤后恰好剩1个候选；不得只看相似度最高 |
| `quick_option_candidate` | 候选数量2—4、每项可形成完整安全动作、目标和revision仍有效；否则不展示字母项 |
| `reliable_expiry` | 显式到期日，或版本化规则同时具备品类、位置、开封状态和必需时间锚点；缺任一关键输入即unknown |
| `near_expiry` | `0 < remaining_duration <= policy.near_expiry_threshold`；阈值按品类/位置/开封状态给出具体小时或天，表中无值即unknown，不采用全局猜测 |
| `template_consistent` | 主要组件集合相同，且每个主要组件规范量差异不超过`max(10g, 10%)`；调味品是否忽略由模板类别明确配置 |
| `template_materially_different` | 主要组件增删，或任一主要组件差异超过上述容差；进入新版本/`personal_variable`判断 |
| `rounding` | 内部十进制不提前舍入；显示量最多1位小数；百分比和10格条使用`round_half_up` |

这些初始阈值必须由固定fixture验证。任何阈值变化都是L3行为变更，需要新`CHG-*`、策略版本、案例和回归；不得静默更新历史快照。

### 23.7 来源能力与研究状态映射

- PRODUCT-0.1的`SourceCapability/SourceHealth`属于确定性执行能力，不属于用户饮食事实；可保存在私有配置/诊断状态，但不得进入普通业务查询；
- PRODUCT-0.2的研究状态使用独立SQLite库或严格独立Schema/事务边界，不能与饮食事实共用未区分的表、outbox或幂等键；
- `ResearchItem`保存来源事实，`Candidate/Cluster/Finding`保存可重建判断，`NutritionUpdateProposal`保存待确认差异；任何一层都不能冒充`NutritionProfile`或`NutritionSnapshot`；
- 研究库可删除/重建而不损坏饮食业务库；已被用户确认并正式采用的Profile仍由业务事件和不可变快照负责；
- A只读降级不得自建研究写入，B承担正式来源/研究实现，C不恢复独立路线；
- 研究结果公共形状、字段和状态必须先由Schema/契约任务冻结，之后才能建立迁移或推送能力。

---

## 24. 需求、案例、任务、证据追踪与验收 Oracle

### 24.1 强制追踪链

```text
用户决定/产品规则
→ REQ-*稳定需求
→ CASE-*共同验收案例
→ CASE断言路径→产生该断言的SH/B/SEL组件任务，或已取消A/C历史任务
→ CASE完整Oracle→阶段门/集成/发布责任任务
→ 精确交付物
→ 实际验证命令
→ EV-*新鲜证据
→ DEC-*路线或发布决定
```

任一环缺失都不得标记完成：没有案例的需求不可验证；没有Oracle的案例只是示例；没有断言产生者的Oracle无法实施；没有同一集成候选完整执行的案例不能形成产品级通过结论；没有交付物路径的任务只是愿望；没有新鲜证据的成果仍未验证；没有任务/案例关联的日志是孤立证据。

路线已经由`DEC-027`固定为B，且CONTRACT-v2已冻结为`contract_id=diet-manager/contract-v2`、`contract_version=2`、`contract_sha256=632B2BBF8D0E6C655F4C0A47958828A86C67B3240065984CCC78A808E6F7072E`。`X-GATE-002`现在只允许把§29.6的确定性相对路径模板解析为B的绝对路径清单，并复核B硬安全、纵切/故障证据与配置键；map在本门验收前仍不存在，任何`SEL-*`任务不得转为`进行中`。

### 24.2 需求阶段映射

| 阶段 | 需求族 |
|---|---|
| PRODUCT-0.1 | §30中处置为`0.1 必须`的具体行 |
| PRODUCT-0.2 | §30中处置为`0.2 必须`的具体行 |
| 后续或排除 | §30中处置为`后续候选/明确排除/已取代`的具体行 |
| 跨版本硬约束 | 安全、隐私、非功能和运维需求虽从0.1开始，但后续版本不得退化 |

机器追踪表必须为每条具体需求保存`stage`、`status`、`source`、`contract_version`和规范章节，不能只靠前缀推断。

### 24.3 案例机器结构

每个案例至少具有：

```json
{
  "id": "CASE-MEAL-003",
  "requirement_ids": ["REQ-MEAL-002", "REQ-NUTR-004"],
  "stage": "PRODUCT-0.1",
  "source_text": "午饭吃了半碗米饭",
  "setup": {
    "clock": "2026-08-09T12:00:00+08:00",
    "timezone": "Asia/Shanghai",
    "locale": "zh-CN",
    "week_start": "monday",
    "prior_context": [],
    "inventory_fixture": "inventory-rice-unique-unconvertible",
    "nutrition_fixture": "common-foods",
    "goals_fixture": null
  },
  "oracle": {
    "command_status": "committed",
    "inventory_read": true,
    "inventory_effect_status": "skipped_vague_quantity",
    "nutrition_source_type": "authoritative_public_database",
    "estimated_fields": ["items[0].nutrition_applied_quantity"],
    "issue_codes": ["quantity_estimated", "inventory_unit_unconvertible"]
  },
  "forbidden": [
    "negative_inventory",
    "invented_user_fact",
    "internal_id_in_receipt",
    "extra_progress_query"
  ]
}
```

示例字段值用于说明Schema；正式Oracle以经过冻结的共同代码和fixture为准。

### 24.4 Oracle 必填维度

| 维度 | 必填断言 |
|---|---|
| 固定环境 | 时钟、时区、locale、week_start、来源fixture版本 |
| 上下文 | 先前消息、活动预览/最近事件、库存、营养、模板、目标 |
| 命令 | 意图、结果状态、稳定原因码、事件数量和类型 |
| 时间 | 原文、解析区间、依据、最终显示 |
| 数量 | 用户描述量、营养采用量、库存请求量、实际扣减量及证据 |
| 库存 | 是否读取、候选、批次分配、前后值、跳过原因、非负 |
| 营养 | 来源/版本、基准、公式、已知/未知、覆盖、字段级估算 |
| 问题 | code、类型、生命周期、允许解决、是否展示快捷项 |
| 提交 | 事务、幂等、重试、最终视图、禁止副作用 |
| 回执 | 黄金文本/结构、日期、逐菜换行、证据标签、自由文本末行 |
| 进度 | 提交后累计、本轮增量、目标、未知纤维、水分和固定格式 |
| 外部请求 | 次数、白名单字段、缓存、禁止泄露字段 |

### 24.4.1 断言级责任与整案通过

`C[]`是任务引用的精确案例集合，不等于每个前置组件都必须在下游尚未存在时通过整案。任务启动brief必须把每个`C[]`案例解析到稳定Oracle路径：

```yaml
case_assertion_paths:
  CASE-MEAL-001:
    - /oracle/parsing/items
    - /oracle/fact_commit/meal_event
  CASE-STORAGE-001:
    - /oracle/idempotency/same_key_same_input
full_case_set: none
```

唯一语义如下：

1. 路径采用共同案例Schema中的稳定JSON Pointer风格；禁止写“解析相关”“库存部分”“其余全部”等自由文本范围。
2. 组件任务只可认领其交付物直接产生、或其已完成上游依赖已经提供的断言。不得在实现`EnvelopeFinalize`之前认领最终进度和冻结回执，不得在回执渲染器之前认领黄金文本。
3. 同一案例可以由多个组件分别产生不同断言；每条必填Oracle路径至少有一个明确产生任务。组件断言全部通过只证明该组件完成，不等于案例整案通过。
4. `case_assertion_paths.<CASE-ID>: ["*"]`只用于任务在C[]中明确逐案承担完整Oracle的情形；阶段批量整案责任统一使用`full_case_set`。两者都必须在同一个集成候选、同一次可追溯运行中执行，不得把不同版本、不同路线或不同历史EV的局部通过拼成整案通过。
5. `full_case_set`只允许`none`、精确CASE-ID数组或下列机器可解析选择器。此处是选择器定义的唯一注册表；G1/G2使用精确ID，发布集只从§30需求行的“阶段”和“最低案例”列做去重并按CASE-ID升序冻结，禁止人工省略：

```text
G1_COMMON_B_ONLY=[CASE-STORAGE-001,CASE-STORAGE-002,CASE-STORAGE-004,CASE-STORAGE-005,CASE-STORAGE-006,CASE-STORAGE-007,CASE-EFFECT-003,CASE-FOUNDATION-002,CASE-INVENTORY-007,CASE-PURCHASE-001,CASE-QUERY-003,CASE-INVENTORY-001,CASE-INVENTORY-004]
G2_VERTICAL_SLICE_B_ONLY=[CASE-MIXED-001,CASE-CORR-001,CASE-QUERY-001,CASE-EFFECT-001,CASE-MEAL-006,CASE-NUTR-008,CASE-MEAL-003,CASE-MEAL-004,CASE-INVENTORY-003,CASE-INVENTORY-004,CASE-NUTR-001,CASE-NUTR-002,CASE-NUTR-005,CASE-STORAGE-001,CASE-RECEIPT-001,CASE-RECEIPT-003,CASE-PROGRESS-010]
B_RELEASE_EXCLUDED=[CASE-STORAGE-003]
RELEASE_0_1_MUST=DIFFERENCE(SELECT_CASES(§30.stage IN ["0.1 必须","明确排除"]),B_RELEASE_EXCLUDED)
RELEASE_0_2_MUST=DIFFERENCE(UNION(RELEASE_0_1_MUST,SELECT_CASES(§30.stage="0.2 必须")),B_RELEASE_EXCLUDED)
```

当前注册表展开计数必须为`G1=13`、`G2=17`、`RELEASE_0_1_MUST=124`、`RELEASE_0_2_MUST=152`；G1/G2只是保留原安全/纵切案例覆盖的B-only门名，不再表示三路线评分。`CASE-STORAGE-003`是A JSONL追加中断专属历史案例，只供停止路线审计和只读降级风险参考，必须从B的G1、可靠性任务和累计发布集排除；B以SQLite故障/回滚案例承担相应安全要求。`RELEASE_0_1_MUST`仍必须显式包含明确排除的`CASE-SCOPE-001`，用于证明“无家庭成员模型、无医疗健康助手”的拒绝边界；`RELEASE_0_2_MUST`是累计发布集，不是只跑0.2新增案例。任何REQ/CASE阶段映射变更都必须使校验器重新展开、更新计数并生成`CHG-*`，不得手改计数掩盖差异。
6. 固定整案责任为：`X-GATE-001=G1_COMMON_B_ONLY`；`B-SLICE-001`和`X-GATE-002=G2_VERTICAL_SLICE_B_ONLY`；`SEL-RELEASE-001=RELEASE_0_1_MUST`；`SEL-RELEASE-002=RELEASE_0_2_MUST`。A/C相应M3/M4整案责任已取消；B门证据必须来自B当前实现，G3证据必须来自同一不可变发布候选。
7. `SH-TRACE-001`生成并校验`CASE断言路径→产生任务`、`CASE→整案责任任务`两张派生矩阵；拒绝未定义路径、下游依赖倒挂、缺失产生者、阶段无整案责任、把局部EV冒充整案EV和候选hash不一致。
8. 已完成遗留任务没有brief时，仅可由结构化EV明确证明的完整案例按`*`迁移；无法证明的路径不得反推为已完成。

### 24.5 故障案例附加断言

```text
failure_injection_point
pre_state_hash
expected_error_code
should_rollback
post_state_hash
state_after_restart
same_key_retry_result
official_business_data_diff
```

### 24.6 ID、共用 Oracle 与维护

- 案例ID为`CASE-<领域>-<三位数>`；
- 已发布ID不改变含义，规则变化新增案例或升级契约；
- B对全部产品行为使用共同Oracle；A只读降级复用零写入Oracle，C安全语义在B适配器中复用同一Oracle；
- 示例数字必须来自fixture，禁止在实现里硬编码；
- 每条`0.1 必须`和`0.2 必须`需求至少有正常和边界/失败案例；`明确排除`按§0.9只需拒绝边界案例；
- 每条安全规则必须有故障注入案例。
- 上述“案例通过”在需求发布层始终指同一集成候选执行完整Oracle；组件任务可以按§24.4.1完成自己负责的断言路径，但不得据此宣称产品案例已通过。

---

## 25. 共同验收案例目录

> 本节是最低案例集。实际输入、固定环境、种子、完整Oracle与黄金回执由`shared/acceptance-cases/`维护；B不得复制或改写业务期望，A只读降级与C历史foundation也不得建立另一套业务期望。

### 25.1 饮食、时间和上下文

| 案例 | 场景 | 关键Oracle |
|---|---|---|
| `CASE-MEAL-001` | 早餐鸡蛋、面包、250ml牛奶 | 一餐多项；牛奶归meal；一次提交返回权威进度 |
| `CASE-MEAL-002` | 吃了昨天买的牛奶 | “昨天买的”定位库存批次，不是摄入日期 |
| `CASE-MEAL-003` | 半碗米饭，fixture给出100–150g可靠范围 | 饮食提交；营养用150g偏高估算；库存不借用该克重 |
| `CASE-MEAL-004` | 公司吃一个苹果 | 明确外食；不读取、不扣家庭库存 |
| `CASE-MEAL-005` | 苹果只记录别扣库存 | 用户显式跳过库存 |
| `CASE-MEAL-006` | 米饭200g、鸡胸150g | 明确克重不标估算；营养来源可追溯 |
| `CASE-MEAL-007` | 一碗牛肉面 | 透明版本化通用模板；逐组件证据 |
| `CASE-MEAL-008` | 一个肉夹馍套餐 | 保存事实；组成和营养未知；询问关键组成 |
| `CASE-MEAL-009` | 没吃鸡蛋，吃了香蕉 | 否定作用域正确，只记录香蕉 |
| `CASE-MEAL-010` | 本来不想吃，后来还是吃了 | 最终完成事实优先，食物上下文必须明确 |
| `CASE-MEAL-011` | 孩子吃两个鸡蛋 | 不写本人、不建成员 |
| `CASE-MEAL-012` | 昨晚11点吃面 | 固定时钟/时区跨日解析 |
| `CASE-MEAL-013` | 凌晨1点补记昨天夜宵 | 日期歧义时只确认日期，不静默归接收日 |
| `CASE-MEAL-014` | 明确饮食但未说时间 | `default_received_at`并保存来源 |
| `CASE-MEAL-015` | 明天准备吃鸡蛋 | 计划，零业务写入 |
| `CASE-MEAL-016` | 最后没吃 | 结合上下文判定未发生，零饮食写入 |
| `CASE-MEAL-017` | 吃了两个鸡蛋（省略主语） | 默认本人；不创建subject实体 |
| `CASE-MEAL-018` | 我和朋友一人一瓶牛奶 | 只记录本人1瓶；朋友部分不写 |
| `CASE-MEAL-019` | 我们吃了两盘炒饭 | 保存本人参与摄入事实；本人份量unknown，不把2盘全记本人 |
| `CASE-MEAL-020` | 上一条隔日“公司吃”已过期；本条“苹果记不清是在公司还是回家后吃的” | 旧上下文不沿用，当前规范化`scene=unknown`；仍令`inventory_read=true`并尝试家庭库存，唯一且可换算则扣，否则按稳定原因跳过，不能因unknown永久跳过库存 |
| `CASE-MEAL-021` | 只吃一个明确食物 | 仍使用有序`items[]`和`meal_id`；不退化成另一套单项Schema |
| `CASE-SCOPE-001` | 根据今天记录给我诊断或减重建议 | 可返回事实数据；明确不提供医疗诊断/自动健康建议，不新增健康层数据 |

### 25.2 入库、商品与库存

| 案例 | 场景 | 关键Oracle |
|---|---|---|
| `CASE-PURCHASE-001` | 两箱×12盒×250ml牛奶 | 同时保存2箱、24盒、6000ml和公式 |
| `CASE-PURCHASE-002` | 一袋鸡蛋 | 1袋明确；袋内枚数未知，不谎称外层数量缺失 |
| `CASE-PURCHASE-003` | 鲜牛奶未说位置 | 按优先级推断并在回执展示位置和来源 |
| `CASE-PURCHASE-004` | 无到期日但有版本化规则 | 用入库锚点估算，天数来自fixture |
| `CASE-PURCHASE-005` | 保质规则不可靠 | 不编天数，显示最有价值时间锚点 |
| `CASE-PURCHASE-006` | 一次多类商品 | 一商品一行，共同问题集中提示 |
| `CASE-PURCHASE-007` | 同名同规格再次购买 | 新批次；复用身份和适用营养版本 |
| `CASE-PURCHASE-008` | 同名多品牌/口味/规格 | 不误复用，给少量候选及自由文本 |
| `CASE-PURCHASE-009` | 首次部分使用未开封单件 | 证据可靠时推断开封并保存来源 |
| `CASE-PURCHASE-010` | 用户纠正位置 | 追加事件；旧值可审计；重算保质 |
| `CASE-INVENTORY-001` | 唯一商品、单批、精确数量 | 扣减前后值和单位可核对 |
| `CASE-INVENTORY-002` | 同商品多批次 | 指定批次优先，否则FEFO/FIFO，可跨批分配 |
| `CASE-INVENTORY-003` | 不同商品多个候选 | 饮食提交；跳过扣减；不擅自选择 |
| `CASE-INVENTORY-004` | 库存不足 | 饮食提交；库存不负；默认整项不扣 |
| `CASE-INVENTORY-005` | 营养可估但库存单位不可换 | 营养可算；库存不扣并建问题 |
| `CASE-INVENTORY-006` | 稍后选择时库存已变化 | 旧选项失效；执行前重新校验 |
| `CASE-INVENTORY-007` | 两并发请求争最后库存 | 至多一笔扣减；另一笔保存饮食并报不足 |
| `CASE-INVENTORY-008` | 纠正返还库存 | 返还原批次；同键重试不重复返还 |
| `CASE-INVENTORY-009` | 过期和未过期批次并存 | 不自动选过期；不丢饮食事实 |

### 25.3 营养、模板与历史复用

| 案例 | 场景 | 关键Oracle |
|---|---|---|
| `CASE-NUTR-001` | 精确包装标签绑定商品 | 标签优先；保存原值、解析、版本、适用商品 |
| `CASE-NUTR-002` | 鸡蛋/米饭/苹果无标签 | 自动用权威公共库/缓存，不要求上传 |
| `CASE-NUTR-003` | 普通蛋炒饭 | 透明通用模板；整菜和组件不重复 |
| `CASE-NUTR-004` | 奇亚籽牛油果青瓜液体沙拉 | 不套普通果蔬汁；先保存事实，等标签/配方 |
| `CASE-NUTR-005` | 同商品标签v1→v2 | 新记录用v2，历史快照保留v1 |
| `CASE-NUTR-006` | 只有部分营养字段 | 未知不补0；`coverage_status=partial`，只称已知部分 |
| `CASE-NUTR-008` | 橙子1个 | 展示可食克重，估算只落推定字段 |
| `CASE-NUTR-009` | 明确200g米饭+公共库 | 数量不标估算；详情标参考数据库 |
| `CASE-NUTR-010` | 冻结的营养来源注册表执行结构与追踪验证 | JSON可解析；16/16语义追踪、36/36指针、来源状态和请求白名单全部符合冻结Oracle |
| `CASE-NUTR-011` | 来源缓存优先级和HTTP 503失败映射 | 低层缓存不抢高层来源；普通503=`source_unavailable`，明确全局限流503=`rate_limited` |
| `CASE-SOURCE-001` | 首选营养后端存在但实际启动损坏 | Probe返回`broken`而非可用；按注册规则降级到通过真实Probe的备用后端；保存选中后端和诊断，不泄露凭据 |
| `CASE-SOURCE-002` | Tier 1缺Key、Tier 2未授权且网络不可用 | 状态分别为`skipped_unconfigured/auth_failed`；Doctor给可行动建议；饮食事实照常提交，营养走可靠缓存/模板或`unknown` |
| `CASE-SOURCE-003` | 只读Doctor与dry-run | 不创建配置/业务/研究目录，不安装依赖、不写浏览器、不创建调度；输出计划和脱敏状态 |
| `CASE-TEMPLATE-001` | 首次明确纠正蛋炒饭 | 形成candidate，不立刻永久默认 |
| `CASE-TEMPLATE-002` | 第二次相同明确组成 | 激活active个人模板 |
| `CASE-TEMPLATE-003` | 第三次相同 | stable并保留证据事件 |
| `CASE-TEMPLATE-004` | 多次明确组成不同 | personal_variable；采用最近明确版本并披露 |
| `CASE-TEMPLATE-005` | 系统三次使用通用估算 | 不得形成个人模板 |

### 25.4 白水、进度、回执与查询

| 案例 | 场景 | 关键Oracle |
|---|---|---|
| `CASE-WATER-001` | 500ml白水 | WaterEvent，明确量不标估算 |
| `CASE-WATER-002` | 一杯水 | 谨慎容量换算并展示依据 |
| `CASE-WATER-003` | 250ml牛奶 | 归meal；白水增量0；食物水分≤250ml |
| `CASE-WATER-004` | 汤/豆浆/咖啡/茶 | 归meal，不写白水事件 |
| `CASE-WATER-005` | 营养饮品同时有营养和水分 | 液体只存一次，水分只贡献一次 |
| `CASE-WATER-006` | 500ml白水+水分未知饮品 | 已知下界500ml；精确总量unknown；`water_coverage_status=partial` |
| `CASE-PROGRESS-001` | 六项目标已配置 | 固定顺序、每项两行、10格条 |
| `CASE-PROGRESS-002` | 超目标 | 真实百分比，条最多10格 |
| `CASE-PROGRESS-003` | 正增量低于1% | `+<1%` |
| `CASE-PROGRESS-004` | 纤维当前未知 | 显示未知，不显示0 |
| `CASE-PROGRESS-005` | 未配置目标 | 只显示量和目标缺失，不造条 |
| `CASE-PROGRESS-006` | 成功写入 | 仅用同次最终daily_progress |
| `CASE-PROGRESS-007` | 直接日/周进度查询 | 一次聚合，无本轮增量 |
| `CASE-PROGRESS-008` | 白水500+食物水分200 | 饮水700ml且无重复贡献 |
| `CASE-PROGRESS-009` | 目标2.5L，已知500+未知饮品 | 显示`≥20%`和已知下界说明 |
| `CASE-PROGRESS-010` | 同一请求多子操作同日提交 | 信封级冻结合计增量，不借最后子事务快照 |
| `CASE-PROGRESS-011` | 跨日纠正 | `daily_progress_by_date[]`含两个日期，各自有符号差额 |
| `CASE-PROGRESS-012` | 周范围跨越目标版本且含unknown项 | 一次六项聚合；按各日历史目标解释，披露partial/unknown，不套当前目标改写历史 |
| `CASE-RECEIPT-001` | 一餐多菜 | 标题单行；每菜/食物/饮品一行；菜内同一行 |
| `CASE-RECEIPT-002` | 明确和推定字段混合 | 只标推定字段 |
| `CASE-RECEIPT-003` | 提供快捷项 | 2–4项、自由文本末行、组合/自然语言可用 |
| `CASE-RECEIPT-004` | 正常成功记录 | 无本轮营养重复段；进度后无评价建议 |
| `CASE-RECEIPT-005` | 用户明确要求分析已记录结果 | 只增加用户所请求的事实解释并放在固定进度块之前；进度块仍收尾，且不越界给医疗或自动健康建议 |
| `CASE-RECEIPT-006` | 单道菜或单个食物 | 标题和唯一项目各自一行；不合并成长行，也不输出空项目占位 |
| `CASE-QUERY-001` | 今天吃了什么 | 用户自然日、当前有效视图、可读时间 |
| `CASE-QUERY-002` | 昨天大概热量/蛋白 | 一次聚合；覆盖和未知明确 |
| `CASE-QUERY-003` | 查牛奶库存 | 当前有效批次与总量一致，不暴露ID |
| `CASE-QUERY-004` | 多条水中定位纠错 | 可读时间/容量候选，不显示ID |
| `CASE-QUERY-005` | 空结果 | 明确无记录，不生成示例数据 |
| `CASE-DATE-001` | 本月5日 | `5号` |
| `CASE-DATE-002` | 同年上月15日 | `7月15号`一类月日格式 |
| `CASE-DATE-003` | 跨年 | 完整年月日 |

### 25.5 纠错、问题、存储、混合与隐私

| 案例 | 场景 | 关键Oracle |
|---|---|---|
| `CASE-CORR-001` | 2个鸡蛋改3个 | 追加纠错、库存差量、营养重算、旧事件可审计 |
| `CASE-CORR-002` | 今天改昨天 | 两个自然日进度均更新 |
| `CASE-CORR-003` | 删除一餐中的牛奶 | 只移除单项，不冲销整餐 |
| `CASE-CORR-004` | 刚才那顿取消 | void、库存返还、移出当前聚合 |
| `CASE-CORR-005` | 多条都像目标 | 不猜；可读候选，不暴露ID |
| `CASE-CORR-006` | 0.2批量纠正范围含歧义且用户取消预览 | 只返回可读影响范围；确认前零纠正、零库存补偿、零进度变化 |
| `CASE-CORR-007` | 0.2明确选择两天内三条牛奶记录并确认批量撤销 | 预览列全范围；确认后逐目标追加撤销、只返还真实扣减、生成两日`daily_progress_by_date[]`并冻结结果；同键重试不重复补偿 |
| `CASE-ISSUE-001` | 同时有量和库存问题 | 统一提示、可组合处理、不连续骚扰 |
| `CASE-ISSUE-002` | 自由文本回答快捷问题 | 与字母选择同义执行 |
| `CASE-ISSUE-003` | 以后再说 | 状态保持open，本轮不再提示，记录有效 |
| `CASE-STORAGE-001` | 同幂等键重试 | 事件、库存、模板计数和原结果不变 |
| `CASE-STORAGE-002` | SQLite扣减阶段故障 | 完整回滚，无半事务 |
| `CASE-STORAGE-003` | A JSONL追加中断（停止路线历史/只读降级参考） | 半事务不可见；坏尾失败关闭；不进入B门或B发布集，不要求B实现JSONL |
| `CASE-STORAGE-004` | 路径注入 | 拒绝任意正式路径，无越界写入 |
| `CASE-STORAGE-005` | 迁移失败 | 旧数据可恢复，Schema不误升 |
| `CASE-STORAGE-006` | 首次响应丢失、随后发生其他写入、原键重试 | 返回首次冻结结果和原进度，不返回当前新累计 |
| `CASE-STORAGE-007` | 同一幂等键改用不同食物、主体/作用域或命令 | 返回`idempotency_conflict`；零新写入，不把旧结果冒充本次结果 |
| `CASE-EFFECT-001` | FactCommit后营养写入故障 | 饮食事实保留；EffectBundle回滚；outbox待重试；无伪进度 |
| `CASE-EFFECT-002` | FactCommit后库存/Issue/进度各点故障 | 每点事实保留、效果不半写、同键重试只补未完成效果 |
| `CASE-EFFECT-003` | 全部子效果成功后EnvelopeFinalize任一写点故障，并有另一请求并发提交 | 最终器全回滚、信封保持effects_stable且无成功回执；同键重试只最终化一次，本轮增量只含本信封，冻结累计以最终提交点为准 |
| `CASE-MIXED-001` | 买一箱牛奶又喝一盒 | 先入库后匹配本轮饮食 |
| `CASE-MIXED-002` | 第二子操作失败 | 第一项保留，逐项报告 |
| `CASE-MIXED-003` | 同轮三项饮食加白水 | 一个同日最终进度块；每项正式枚举状态；合计增量持久化 |
| `CASE-PRIV-001` | 查公共营养 | 外部请求仅白名单字段 |
| `CASE-PRIV-002` | 技术错误 | 日志/回复不泄露原话、库存、密钥、SQL、路径 |
| `CASE-FOUNDATION-001` | 运行三路线foundation并验证Skill层与状态恢复 | 三条路线都含可加载Skill层；A为纯Skill；B/C插件只作为Skill后端；三foundation与调用者状态恢复；不得借此证明业务实现 |
| `CASE-FOUNDATION-002` | 前后业务数据比较 | 正式JSONL/SQLite及辅助文件零新增/改/删 |
| `CASE-POLICY-001` | 候选、临期、模板、取整和B-only门条件恰在阈值上下 | 固定策略版本对低于、等于、高于边界给出确定结果；不恢复三路评分 |
| `CASE-POLICY-002` | 策略版本缺失、hash不符或fixture未覆盖 | 失败关闭并要求受控策略更新，不让模型临场补阈值 |

### 25.6 PRODUCT-0.2复用、回顾与生命周期

| 案例 | 场景 | 关键Oracle |
|---|---|---|
| `CASE-REVIEW-001` | 早餐和昨天一样 | 从有效快照创建新事件，重新计算时间/库存/进度 |
| `CASE-REVIEW-002` | 再来一份 | 仅可靠上下文复用，跨会话/多候选澄清 |
| `CASE-REVIEW-003` | 批量补录三顿一项不完整 | 按序逐项状态，可识别事实提交 |
| `CASE-REVIEW-004` | 周趋势含未知营养 | 一次聚合；覆盖明确；无健康评价 |
| `CASE-REVIEW-005` | 常吃食物/常用餐 | 只按有效记录，旧纠正版本不重复计数 |
| `CASE-LIFE-001` | 牛奶坏了丢弃 | 追加丢弃，批次不可用，不写饮食 |
| `CASE-LIFE-002` | 盘点少2个鸡蛋 | 追加盘点调整，保存前后与原因 |
| `CASE-LIFE-003` | 批次进入临期窗口 | 当前有效位置/开封/规则生成一次提醒 |
| `CASE-LIFE-004` | 延期或忽略提醒 | 保存提醒状态，避免重复打扰 |
| `CASE-LIFE-005` | 退货或获赠 | 对应追加库存事件，投影一致 |
| `CASE-LIFE-006` | 用户首次纠正鲜奶存放位置 | 只追加纠正当前批次；不静默形成商品级或类型级默认位置 |
| `CASE-LIFE-007` | 多次一致位置确认后用户可见启用个人规则 | 创建版本化个人位置规则；新批次披露规则来源，用户仍可按批次纠正或停用 |
| `CASE-LIFE-008` | 库存不足时用户明确“本次先扣现有部分” | 只在本事件与指定批次产生`partially_deducted`，保存授权消息；下一轮不足仍须重新授权 |
| `CASE-LIFE-009` | 用户把库存牛奶赠出2盒 | 追加`inventory_gifted_out`负delta，保存原因、授权及前后值；不写饮食、不误作退货 |

### 25.7 PRODUCT-0.2营养资料研究与变更监测

| 案例 | 场景 | 关键Oracle |
|---|---|---|
| `CASE-RESEARCH-001` | 两个权威来源报告同一商品标签版本变化 | 绑定精确商品/规格/基准，聚成一个finding，保留两份Item；生成preview建议，不修改Profile或历史Snapshot |
| `CASE-RESEARCH-002` | 一个来源超时、一个未配置、一个返回无结果 | 结果为`partial`并逐源披露；不得说“网上没有资料”，不得强制生成结论或阻塞饮食记录 |
| `CASE-RESEARCH-003` | 多条转载描述同一资料变化 | URL、标题/内容指纹和实体聚类去重为一个发现；重复运行只新增sighting，不重复通知 |
| `CASE-RESEARCH-004` | 候选只有低质量热度或来源相互冲突 | 低于可信度门返回`nothing_solid`或待审阅冲突；不得用热度平均成营养值 |
| `CASE-RESEARCH-005` | 用户确认关注商品出现可靠新标签 | 先创建`NutritionUpdateProposal`；确认前业务库全表不变；确认后只影响未来Profile版本，历史Snapshot不变 |
| `CASE-RESEARCH-006` | 离线查询研究历史和Watchlist | 不发网络请求；返回首次/再次出现/变化/消失状态和上次覆盖信息 |

### 25.8 导出与PRODUCT-0.1安装运维

| 案例 | 场景 | 关键Oracle |
|---|---|---|
| `CASE-EXPORT-001` | 导出7月饮食营养 | 仅选择范围；含版本/时区/unknown语义 |
| `CASE-EXPORT-003` | 用归档恢复/迁移 | 版本可识别，unknown不变0，快照可复核 |
| `CASE-EXPORT-004` | 把最小用户导出当作整库恢复介质 | 明确拒绝；导出可读但不可冒充一致性备份，恢复只能使用已验证备份 |
| `CASE-OPS-001` | 全新安装0.1 | 预检、Schema、隔离冒烟、加载通过；正式数据0 |
| `CASE-OPS-002` | 兼容数据升级 | 先备份；历史、快照、幂等一致 |
| `CASE-OPS-003` | 迁移中途故障 | 新版不生效；旧数据可读；版本不误升 |
| `CASE-OPS-004` | 恢复到已有目录 | 先备当前、临时验证，未确认不覆盖 |
| `CASE-OPS-005` | 卸载保留数据 | 程序移除，业务和备份保留 |
| `CASE-OPS-006` | 卸载并删数据 | 二次确认、精确范围、最后备份和恢复说明 |
| `CASE-OPS-007` | 未验证依赖环境安装 | 预检停止，不创建/迁移正式库 |
| `CASE-OPS-008` | 请求整库清除但尚未确认 | 只预览范围和备份，确认前零删除 |
| `CASE-OPS-009` | 已冻结候选通过全部门后晋级 | 逐文件hash与候选manifest完全相同，原字节晋级，不在发布步骤重建 |
| `CASE-OPS-010` | 候选冻结后任一文件或依赖字节改变 | 原候选立即失效，重新生成hash并重跑受影响门，禁止沿用旧EV |

---

## 26. B唯一主线、安全绑定门与里程碑

### 26.1 路线收敛后的当前矩阵

| 路线 | 当前状态 | 允许范围 | 禁止范围 |
|---|---|---|---|
| A：纯Skill + JSONL | `有限对照` | 只读/无插件降级规范、历史foundation结构和未来受控只读导入说明 | 新建正式JSONL写入、事务、完整纵切或独立发布 |
| B：轻量插件 + SQLite | `实验中`，唯一产品主线 | 在共同底座后实施完整PRODUCT-0.1/0.2，并吸收C的服务端安全控制 | 在`SH-SAFE-BASE-001`、B硬安全、纵切/故障和路径绑定门前宣称可用或启动下游任务 |
| C：强约束插件 + 状态机 + SQLite | `停止` | 历史foundation只读保留；设计语义仅通过`B-MERGE-C-001`迁入B | 独立存储、完整纵切、并行产品实现或独立发布 |

2026-08-10以前的三路线比较是历史规划背景；`DEC-027`已直接收敛为B唯一主线。该裁决是用户范围决定，不是G1/G2评分结果，不得补写评分、门通过或EV。

### 26.2 B-only门共同规则

- G0仍只是历史foundation基底核验，不选择路线、不激活产品任务；
- `X-GATE-001`改为B-only硬安全门，只审查B在共同存储风险案例、C安全控制合并和正式根零差异上的证据；
- `X-GATE-002`改为B-only纵切就绪与路径绑定门：仓库只提交`shared/selected-route-map.template.json`相对路径合同；只在B纵切/故障证据、固定契约版本/hash、精确路径和有界机器预检全部通过后，在本机原子生成被忽略的`shared/selected-route-map.json`；
- 两个门都不再进行A/B/C加权评分，也不产生“B胜出”的伪EV；未来实际门结论仍须引用新鲜EV；
- 用户选B不能绕过`SH-SAFE-BASE-001`、共同契约/Schema/Oracle、B存储硬安全、B纵切/故障或G3；
- A/C取消任务保留历史行和`DEC-027`引用，不删除；有价值C设计只能由`B-MERGE-C-001`迁入B；
- contract hash未知时禁止提前创建或伪填selected-route map。

### 26.3 G0：foundation基底与数据卫生核验点（非决策门）

证明A/B/C目录、测试、构建、插件外壳和OpenClaw状态隔离可用，且验证不创建JSONL/SQLite业务数据。G0通过只表示可以继续工程实验，不表示业务完成或产品可安装。

当前项目的G0事实必须由本版重新运行的验证证据确认；旧0.2的证据可作历史参考，不能替代最后修改后的新鲜验证。

当前派生结果：`EV-20260809-009`已证明A结构、B/C各7/7、构建、插件校验和状态隔离通过，空业务数据基线下JSONL/SQLite及辅助文件新增为0、临时残留为0。因此foundation基底重验完成；验证器对预置正式数据和失败路径的加固仍由`SH-SAFE-BASE-001`承担，并作为G1进入条件，不得被本次空基线证据关闭。

### 26.4 G1：B存储硬安全绑定门

进入条件：共同事件/数量/商品/营养/问题/纠正Schema、安全Oracle和前后数据快照验证器冻结。

B执行共同最小存储风险切片：

1. 建商品和库存批次；
2. 查询投影；
3. 提交饮食并扣减；
4. 同幂等键重试；
5. 准备后提交前故障；
6. 持久化后回复前故障；
7. 库存不足；
8. 路径越界；
9. 重启后投影；
10. 再跑foundation并证明正式数据不变；
11. 服务端preview绑定`input_digest + data_revision`；
12. 拒绝陈旧/伪造调用方状态、非法状态转换与非法迁移。

B专项必须证明SQLite驱动、迁移、约束、回滚、响应丢失，以及`B-MERGE-C-001`迁入的服务端权威状态与幂等控制。硬性指标全部为0：错误正式写入、重复副作用、负库存、半事务、路径越界、未处理损坏、foundation污染、陈旧预览提交、非法状态/迁移写入。`X-GATE-001`只能输出`pass_b_safety`或`return_to_b_storage`，并伴随新鲜EV和精确后继任务处置；当前两者均未形成。

### 26.5 G2：B最小纵向切片与就绪绑定门

仅在`X-GATE-001=pass_b_safety`后，B执行纵向切片：购买 → 商品/批次/营养 → 库存查询 → 在家饮食 → 默认库存关联 → 营养 → 原子提交和最终进度 → 追加纠正 → 库存补偿/营养重算/跨日进度 → 有效视图查询。

切片必须含明确量、自然单位、合理上界估算、外食、多候选、不足、公共资料、包装标签、历史复用、幂等、故障、逐菜回执、快捷答复和最终进度。

G2不再评分或选路，只验证B是否满足固定共同Oracle、C控制合并、故障矩阵和下游路径绑定。`X-GATE-002`只能输出`bind_b_ready`或`return_to_b_slice`。仓库中的相对模板不代表门通过；只有冻结身份、全部有界预检、被忽略的本地绝对map和两轮独立复核共同通过并形成关闭EV时，才可判定`bind_b_ready`并解锁`SEL-*`。当前仍不得把用户裁决或候选map写成门已关闭。

### 26.6 G3：不可变发布候选门

`G3-0.1`和`G3-0.2`分别执行。进入门前先生成不可变候选包和逐文件SHA-256，再在精确候选字节上完成全新安装、升级、故障、恢复、追踪和零污染E2E。

门输出：

```text
release
conditional_release
return_to_milestone
no_release
```

只有`release`解锁晋级。`conditional_release`不能绕过安全门；条件关闭后必须重打候选、重跑E2E和G3。`return_to_milestone`登记重开任务、失效证据和新审查条件。`no_release`取消对应版本后续任务。

发布动作只能把G3审查过的相同字节从候选区原样晋级；不得在门后重建、编辑文档或替换依赖。任一字节变化使旧E2E和门结论失效。

### 26.7 M0–M9 里程碑

里程碑状态从任务与证据派生，不另行维护。

| 里程碑 | 目标 | 进入条件 | 退出条件 | 门 |
|---|---|---|---|---|
| M0 | 三foundation和零业务数据基底 | 项目存在 | 新鲜G0证据齐全 | G0 |
| M1 | 完整会话迁移与无冲突共同契约 | G0 | 0.3批准；业务/日期回执/营养/问题纠正契约冻结 | — |
| M2 | 模型、Schema、案例、追踪和数据快照设施 | M1 | 需求—案例—任务—证据可机器校验 | — |
| M3 | B存储安全、C控制合并与硬门 | M2 | B风险矩阵完成且`X-GATE-001=pass_b_safety` | G1-B-only |
| M4 | B最小纵向切片、故障与路径绑定 | G1-B-only | B切片/故障证据完整，CONTRACT hash冻结并由`X-GATE-002`生成B-only map | G2-B-only |
| M5 | B完成0.1饮食/白水/库存/营养 | G2-B-only | 核心写入、来源和批次案例通过 | — |
| M6 | 0.1问题、纠正、回执、进度和查询 | M5 | 有效视图、补偿、黄金回执和聚合通过 | — |
| M7 | 0.1安装、迁移、备份、恢复与发布 | M6 | 不可变候选通过G3并原样晋级 | G3-0.1 |
| M8 | 0.2模板、复用、周回顾、生命周期、营养研究/Watchlist和导出 | 0.1发布 | 0.2案例通过、研究默认只预览且0.1无退化 | — |
| M9 | 0.2升级和发布 | M8 | 不可变0.2候选通过G3并晋级 | G3-0.2 |

### 26.8 强制执行顺序

```text
M1安全底座与共同契约
→ M2模型/案例/追踪/隔离设施
→ B-MERGE-C-001与B存储实现
→ X-GATE-001（B-only硬安全）
→ B纵向切片与故障验证
→ X-GATE-002（B-only就绪/contract hash/selected-route-map绑定）
→ B的M5–M6
→ M7候选/E2E/G3-0.1/原样晋级
→ M8
→ M9候选/E2E/G3-0.2/原样晋级
```

禁止跳过共同契约由B自行解释，也禁止“先写功能以后补Schema和测试”。A/C取消不影响`SH-SAFE-BASE-001`当前RED矩阵及其既有48个顶层RED断言范围；本次路线裁决不修改、不宣称通过这些测试。

---

## 27. 决策、开放问题、风险、技术债与失效规则

### 27.1 决策记录纪律

决定使用顺序型`DEC-NNN`，状态只允许`生效/被替代/撤销`。每条决定记录日期、问题、选项、最终决定、理由、影响的REQ/CASE/TASK、替代关系和复核条件。路线门决定额外保存`gate_id`、`route`、`outcome`、`evidence_ids`及G3候选清单哈希；不得在实际证据前预创建“通过”决定。

其他治理对象状态同样固定：

```text
Q: open | decided | closed | cancelled
RISK: open | mitigated | accepted | closed
DEBT: open | accepted | closed
CHG: 提议 | 批准 | 实施中 | 已验证 | 拒绝 | 撤销
```

Q/RISK/DEBT每行必须有owner、精确责任任务、下一动作和关闭证据；不得用斜线、波浪线或省略前缀的方式压缩多个ID。

### 27.2 当前生效决定

`DEC-003`—`DEC-026`日期为2026-08-09且继续`生效`；`DEC-002`已于2026-08-13按用户对DOC-0.4的明确批准更新；`DEC-001`已于2026-08-10被`DEC-027`替代并保留历史行；`DEC-027`日期为2026-08-10、状态为`生效`。若再变更，继续保留原行并另建新ID。

| ID | 问题与考虑选项 | 最终决定 | 影响ID | 替代关系 | 复核条件 |
|---|---|---|---|---|---|
| `DEC-001` | 三路全做、先指定主线、先做短风险门 | **被替代**：曾采用G1/G2早期比较选路 | `X-GATE-001`、`X-GATE-002` | 曾替代“三路做到M8再选”；现由`DEC-027`替代 | 只作历史，不得恢复为现行三路全量比较 |
| `DEC-002` | 多文档分管状态或单一总控 | `DOC-0.4`是唯一现行权威计划；任务/证据/决定各有唯一台账。`DOC-0.3`保留为只读、冻结SHA-256的历史基线，不再与0.4竞争当前SSOT | `DOC-003-001`、`DOC-004-001`、`DOC-004-002`、`SH-TRACE-001` | 替代多份人工状态快照；更新原“DOC-0.3为权威计划”的已过时表述 | 文档拆分时仍须保持机器SSOT；0.4结构变更后须重生成并独立复核追踪镜像 |
| `DEC-003` | 家庭成员模型、只记本人、健康助手 | 只记录本人；不建成员；不建健康/医疗助手 | `REQ-SCOPE-001`、`REQ-SCOPE-002`、`CASE-SCOPE-001` | 替代家庭成员与0.3健康层建议 | 用户明确扩大产品边界时 |
| `DEC-004` | 明确在家才读库存或默认读库存 | 无非家庭证据时默认读取并尝试家庭库存 | `REQ-PANTRY-001`、`CASE-MEAL-004`、`CASE-MEAL-005` | 替代“明确说在家才扣” | 错扣率证据显示规则不可接受时 |
| `DEC-005` | 库存/营养问题阻塞整条或事实先行 | 已发生事实优先，附加问题降级为Issue | `REQ-CORE-001`、`CASE-INVENTORY-003`、`CASE-INVENTORY-004` | 替代整条preview/failed | 事实本身无法确定时不适用 |
| `DEC-006` | 全部未知、每次询问、有限范围估算 | 常见食物用可信资料；有界模糊量取合理上界；估算落字段 | `REQ-NUTR-001`、`REQ-NUTR-004`、`REQ-NUTR-005`、`CASE-MEAL-003` | 替代“模糊量永不换算” | 来源或有界范围政策升级时 |
| `DEC-007` | 全部拆配料、全部询问、分级模板 | 常见有边界菜用透明模板；未知套餐先存事实再问 | `REQ-MEAL-003`、`CASE-MEAL-007`、`CASE-MEAL-008` | 替代两个极端 | 模板误差越过策略阈值时 |
| `DEC-008` | 一次学习、永不学习、证据阈值 | 个人配方一次候选、两次激活、三次稳定；系统估算不学习 | `REQ-TEMPLATE-001`、`CASE-TEMPLATE-001`、`CASE-TEMPLATE-005` | 替代静默学习 | PRODUCT-0.2用户测试后 |
| `DEC-009` | 批次耗尽即删资料或保留身份 | 保留商品身份/营养；精确同商品新批次复用 | `REQ-PRODUCT-004`、`CASE-PURCHASE-007` | 替代随批次删除资料 | 商品变体误匹配证据出现时 |
| `DEC-010` | 物理改写、禁止纠错、追加纠错 | 追加式纠正/撤销/恢复并同步库存、营养、进度 | `REQ-CORR-001`、`REQ-CORR-002`、`REQ-CORR-003`、`CASE-CORR-001`、`CASE-CORR-004` | 替代禁止历史纠错 | Schema大版本迁移时 |
| `DEC-011` | 多批均视为歧义或按批次政策分配 | 同商品指定→FEFO→FIFO；不同商品不自动选 | `REQ-PANTRY-003`、`CASE-INVENTORY-002`、`CASE-INVENTORY-003` | 替代同商品多批即multiple | 版本化批次政策变化时 |
| `DEC-012` | 每次询问位置或分层推断 | 分层推断并披露；明显不确定才给选项 | `REQ-PRODUCT-003`、`CASE-PURCHASE-003` | 替代每次给改位置快捷项 | 位置策略版本变化时 |
| `DEC-013` | 总显示完整日期或按当前月简写 | 本月几号、同年跨月月日、跨年完整 | `REQ-TIME-002`、`CASE-DATE-001`、`CASE-DATE-003` | 替代所有日期带月份 | locale策略变化时 |
| `DEC-014` | 只收字母或字母+自然语言 | 快捷项可组合/自然语言，末行允许自由文本 | `REQ-QUICK-001`、`CASE-RECEIPT-003` | 替代封闭字母选择 | PromptOptionGroup协议变化时 |
| `DEC-015` | 所有液体归水或水/营养饮品分开 | 白水独立；营养饮品归饮食；水分只计一次 | `REQ-WATER-001`、`REQ-WATER-002`、`CASE-WATER-003`、`CASE-WATER-005` | 替代饮水延后和液体混记 | 水分模型大版本变化时 |
| `DEC-016` | 整餐一行或逐菜紧凑 | 标题一行、每菜/单项一行、菜内组件同一行；无本轮营养段 | `REQ-RECEIPT-001`、`REQ-RECEIPT-002`、`CASE-RECEIPT-001`、`CASE-RECEIPT-004` | 替代整餐超长行 | 用户明确改变回执格式时 |
| `DEC-017` | 混合输入全回滚或逐子操作 | 按叙述顺序逐子操作；后项失败不回滚前项 | `REQ-MIXED-001`、`CASE-MIXED-001`、`CASE-MIXED-002` | 替代整条全原子 | 强关联事务定义变化时 |
| `DEC-018` | foundation版本等同产品或严格分层 | foundation仅是基底，当前不可正式安装；最终首先交付给用户和智能体的是可加载、可执行的饮食管家 Skill：A为纯 Skill，B/C为 Skill 加确定性插件后端，插件、存储和领域服务只作 Skill 的执行后端，不构成独立产品 | `REQ-SCOPE-003`、`CASE-FOUNDATION-001` | 替代版本混称 | 用户撤回或改变 Skill-first 边界时重开；G3-0.1 release后仅改变产品状态 |
| `DEC-019` | 多套问题自由状态或统一四状态 | Issue生命周期统一为open/awaiting_user/resolved/dismissed | `REQ-ISSUE-001`、`CASE-ISSUE-003` | 替代offered/deferred/invalidated | Issue契约升级时 |
| `DEC-020` | 用压缩摘要或逐轮读取写0.3 | 从初始完整44轮逐段读取、逐段写入；后续批准与纠正继续追加，当前50/50已登记 | `DOC-003-001`、`DOC-004-001`、`DOC-004-002`、附录A | 替代摘要式重写 | 新会话要求并入时继续追加，不改写初始44轮历史 |
| `DEC-021` | 全部周能力0.2或分层 | 0.1只提供用户请求的最小周六项聚合；丰富周回顾/趋势和模板复用为0.2 | `REQ-QUERY-003`、`REQ-REVIEW-001` | 澄清旧“周回顾全部0.2” | 0.1范围变更时 |
| `DEC-022` | 未知饮品水分记0、猜值或下界 | 只显示已知下界和partial | `REQ-PROGRESS-003`、`CASE-WATER-006` | 替代未知当0 | 水分来源能提供可靠范围时 |
| `DEC-023` | 事实、子效果和最终回执单事务或分层提交 | `FactCommit`→逐子操作`EffectBundle`→信封级`EnvelopeFinalize`；任一效果/最终器技术失败不回滚事实 | `REQ-SAFE-002`、`CASE-EFFECT-001`、`CASE-EFFECT-002`、`CASE-EFFECT-003`、`SH-MODEL-003` | 修正“事实+营养+库存+进度全原子”和“每个效果包提前写最终快照” | 故障注入不能证明安全时 |
| `DEC-024` | 重试返回当前视图、原终态或同键冲突 | 同键同摘要返回冻结终态；同键不同输入=`idempotency_conflict`；非终态只恢复pending效果/最终器；跨日用`daily_progress_by_date[]` | `REQ-PROGRESS-004`、`REQ-SAFE-003`、`CASE-STORAGE-006`、`CASE-STORAGE-007` | 替代“重试从当前状态重建”与静默复用旧结果 | 幂等协议大版本变化时 |
| `DEC-025` | 模糊词由模型判断或机器策略 | 候选、临期、模板容差、取整和B-only门条件全部版本化并用fixture验证；原三路G2评分语义由`DEC-027`取消 | `REQ-NFR-003`、`SH-DECISION-001` | 替代高可信/明显不同等自由判断 | 策略阈值变更时必须L3变更 |
| `DEC-026` | 先做路线存储或先加固验证器 | `SH-SAFE-BASE-001`先于M3任何真实存储风险试验 | `SH-SAFE-BASE-001`、`B-STOR-001`、`B-MERGE-C-001` | 修正旧SH-SAFE任务过晚；A/C任务处置由`DEC-027`更新 | 加固交付物已由新鲜证据关闭时 |
| `DEC-027` | 继续三路比较、先做A/C再选、或用户直接集中开发B | 用户直接裁决B为唯一PRODUCT-0.1/0.2主线；A只保留只读/无插件降级规范；C停止独立完整开发，其服务端preview/状态安全控制经`B-MERGE-C-001`并入B；`X-GATE-001`与`X-GATE-002`改为B-only安全/就绪绑定门 | `CHG-20260810-001`、`A-STOR-001`、`A-STOR-002`、`A-SLICE-001`、`A-FAULT-001`、`B-MERGE-C-001`、`B-STOR-001`、`B-STOR-002`、`B-SLICE-001`、`B-FAULT-001`、`C-STOR-001`、`C-STOR-002`、`C-SLICE-001`、`C-FAULT-001`、`X-GATE-001`、`X-GATE-002` | 替代`DEC-001`的现行选路语义；不替代`DEC-026`的安全前置 | 用户明确撤回B集中开发；或B硬安全失败时只能回到受控范围决定，不能自动复活A/C完整路线 |
| `DEC-028` | 安全底座继续追求全部平台细节或按业务风险收口 | 保留防止业务数据污染、路径越界、命令注入、秘密泄漏、伪提交和清理逃逸的安全门；Windows `conhost.exe`精确进程总数等不改变业务正确性的单平台细节转为deferred debt，不再追加规避实现；`SH-SAFE-BASE-001`仍须完成一次有界退出验收后才能解锁其后继依赖 | `CHG-20260811-002`、`SH-SAFE-BASE-001`、`B-STOR-001` | 细化`DEC-026`的完成边界，不取消安全前置 | 新证据表明deferred项会导致业务数据错误、路径逃逸、命令逃逸或部署不可用时重开 |
| `DEC-029` | 原样引入两个外部Skill、只吸收架构或完全不吸收 | 采用“吸收架构、不复制产品”：0.1纳入营养来源适配器/真实Probe/Doctor/显式授权；0.2纳入证据研究、去重、部分覆盖、Watchlist和资料更新预览；核心饮食记录不依赖研究，研究不得直接写业务 | `CHG-20260813-001`、`REQ-SOURCE-001`、`REQ-RESEARCH-001`、`REQ-RESEARCH-002`、`SEL-NUTR-001`、`SEL-RESEARCH-001`、`SEL-NUTR-WATCH-001` | 新增决定；不替代B唯一主线、FactCommit分层或历史快照规则 | 用户扩大为通用互联网Skill，或来源/研究证据显示当前边界无法满足营养资料质量时重开 |

### 27.3 问题台账

| ID | 范围/问题 | 状态/所有者 | 精确阻断任务 | 下一动作 | 关闭证据 |
|---|---|---|---|---|---|
| `Q-001` | 0.1/0.2首批公共营养库、可信网站、许可证和离线缓存策略 | `closed` / Codex | `SH-CONTRACT-003` | 无；实现客户端由`SEL-NUTR-001`负责 | `EV-20260809-007` |
| `Q-002` | B在目标OpenClaw/Node环境可稳定打包的SQLite驱动 | `closed` / B实现者 | 无 | 无；后续Node/OpenClaw大版本变化按reopen条件重验 | `EV-20260812-027`：Node 24.15/24.16、内置`node:sqlite`、21项测试、TypeScript与OpenClaw 2026.7.1 build/validate通过 |
| `Q-003` | 哪条路线承担完整0.1，是否保留有限对照 | `closed` / 用户 | 无 | 无；不得再等待G2评分选路 | `DEC-027`：B唯一主线，A只读降级，C停止并合并控制 |
| `Q-004` | 纠正导致需要补扣但库存不足的最简回执 | `closed` / 产品规则负责人 | 无 | 无；实现与黄金案例继续消费冻结结构 | `EV-20260811-015`：结构化不足结果、实际扣减/未扣减事实与用户可见文案边界已冻结 |
| `Q-005` | 首批保质/开封规则覆盖品类和数据来源 | `closed` / 产品规则负责人 | 无 | 无；后续实现只消费v1策略，阈值变化走L3重开 | `EV-20260811-016`：五类到期来源、四类开封来源、八个提醒窗口和unknown降级已冻结；不构成食品安全结论 |
| `Q-006` | 0.2导出格式、字段范围、原话/历史与匿名化组合 | `open` / 0.2隐私与导出产品负责人 | `SEL-EXPORT-001` | 0.1发布后做隐私需求评审 | 导出契约、案例与隐私复核EV |
| `Q-007` | PRODUCT-0.1明确支持环境与最低版本 | `open` / 发布负责人 | `SEL-INSTALL-001` | 基于Node≥24.15/OpenClaw矩阵确定范围 | 安装兼容矩阵+不可变候选E2E |

开放问题不得写成已经决定的产品事实。只阻断其`适用范围`和`阻断点`，例如Q-006不得阻断G3-0.1。

### 27.4 当前风险

| ID | 风险/等级/门 | 状态/所有者 | 责任任务 | 缓解动作 | 关闭证据 |
|---|---|---|---|---|---|
| `RISK-001` | 50轮会话因压缩、路线裁决或资料吸收遗漏；高/DOC批准 | `closed` / Codex | 无 | 50/50落点；前48轮保留0.3历史，第49轮资料吸收与第50轮DOC-0.4现行入口批准均已复核 | `EV-20260813-033`；`EV-20260809-010`覆盖初始44轮，`EV-20260809-011`覆盖第45轮 |
| `RISK-002` | 共同契约旧语义或未完成复核；高/M1 | `closed` / 契约负责人 | 无 | 三份v2契约均已机器验证并完成独立语义复核；实现阶段继续锁定其hash | `EV-20260811-013`、`EV-20260811-014`、`EV-20260811-015` |
| `RISK-003` | A跨JSONL原子/幂等/恢复不可证；严重/原G1-A | `closed` / 计划负责人 | 无 | `DEC-027`取消A正式写入，保留只读/无插件降级 | A的M3/M4写入任务已取消；若恢复正式写入必须新CHG/DEC并重开安全门 |
| `RISK-004` | B驱动或打包不兼容；高/G1-B-only | `closed` / B实现者 | 无 | 保持Node引擎、内置驱动与OpenClaw门；宿主/驱动/包边界变化即重开 | `EV-20260812-027`：本地与独立环境均完成测试、TypeScript和OpenClaw插件构建/校验 |
| `RISK-005` | 营养采用量误作库存扣量；高/G2/G3 | `open` / 领域负责人 | `SH-MODEL-001`、`SEL-PANTRY-001` | 四量分离+模板不作库存证据 | 对应Oracle与E2E EV |
| `RISK-006` | 外部营养来源变化/不可追溯；高/G2/G3 | `open` / 营养负责人 | `SH-CONTRACT-003`、`SEL-NUTR-001` | 来源版本、缓存、不可变快照 | 来源故障与历史快照EV |
| `RISK-007` | 系统估算误学个人模板；高/G3-0.2 | `open` / 0.2负责人 | `SEL-TEMPLATE-001` | 只计用户证据、版本状态机 | 模板CASE EV |
| `RISK-008` | 回执旧进度/额外查询推增量；高/G2/G3 | `open` / 进度负责人 | `SEL-PROGRESS-001` | 冻结信封级进度与黄金回执 | `CASE-STORAGE-006`、`CASE-PROGRESS-010` EV |
| `RISK-009` | 三路继续全量导致重复投入；高/路线范围 | `closed` / 计划负责人 | 无 | 精确取消A/C M3/M4并新增B合并任务 | `DEC-027`与§31任务状态；不需要伪造G2 EV |
| `RISK-010` | 状态复制或案例变后未重验；高/M2/G3 | `closed` / 治理负责人 | `SH-TRACE-001` | `CHG-20260813-002`改变计划/生成器后立即重开；四镜像已确定性重生成并重新独立复核 | `EV-20260813-035`证明74 REQ/153 CASE/63 TASK/70 governance/35 EV当前闭环；后续计划、生成器或案例变化再次重开 |
| `RISK-011` | 验证污染正式数据；严重/所有门 | `closed` / 验证负责人 | `SH-SAFE-BASE-001` | 前后manifest、失败照扫、sidecar和临时根 | `EV-20260811-012`：有界退出验收后业务候选0、fixture残留0；相关根/manifest focused通过 |
| `RISK-012` | 日志/外部请求泄露；高/G2/G3 | `open` / 安全复核者 | `SH-CASE-003`、`SEL-RELIABILITY-001` | 白名单与脱敏故障案例 | 隐私测试EV |
| `RISK-013` | 同名变体错复用营养；高/G2/G3 | `open` / 商品负责人 | `SH-MODEL-001`、`SEL-NUTR-001` | 硬冲突过滤和版本隔离 | 变体CASE EV |
| `RISK-014` | 纠正未补偿库存/进度；严重/G2/G3 | `open` / 纠正负责人 | `SEL-CORR-001` | EffectBundle补偿、跨日与幂等 | CORR+EFFECT CASE EV |
| `RISK-015` | 0.3重复枚举/状态；高/DOC批准 | `closed` / Codex | `DOC-003-001`、`SH-TRACE-001` | SSOT复核并让机器持续检查 | `EV-20260809-010`缓解历史重复；`EV-20260812-026`关闭机器镜像与一致性缺口 |
| `RISK-016` | 附加层或最终器技术失败回滚事实/冻结伪进度；严重/G2/G3 | `open` / 存储负责人 | `SH-MODEL-003`、`SEL-FACTEFFECT-001` | FactCommit+EffectBundle+EnvelopeFinalize+durable outbox | `CASE-EFFECT-001`、`CASE-EFFECT-002`、`CASE-EFFECT-003`故障EV |
| `RISK-017` | 把“用户选B”误写成B已安全、已就绪或map可生成；严重/G1-B-only/G2-B-only | `open` / 计划负责人 | `X-GATE-001`、`X-GATE-002` | 门只绑定B且保持未开始；contract hash未知时拒绝map | B硬安全与纵切新鲜EV、冻结contract hash和B-only map |
| `RISK-018` | 将搜索热度、转载或单一弱来源误作营养真值；高/G3-0.2 | `open` / 研究负责人 | `SEL-RESEARCH-001` | 实体锚定、来源权威、交叉佐证、冲突降级与金标回归 | 研究CASE与独立来源质量EV |
| `RISK-019` | 来源凭据、Cookie、代理或带认证URL进入日志/配置错误位置；严重/G3 | `open` / 安全复核者 | `SEL-NUTR-001`、`SEL-RELIABILITY-001` | Tier显式授权、私有原子配置、Probe/Doctor脱敏、安全失败案例 | SOURCE/PRIV CASE与正式候选隐私EV |
| `RISK-020` | 研究任务阻塞核心记录或未经确认改写Profile/历史快照；严重/G3-0.2 | `open` / 产品与存储负责人 | `SEL-RESEARCH-001`、`SEL-NUTR-WATCH-001` | 独立状态根、preview默认、业务全表零写、确认后追加版本 | RESEARCH故障/确认/E2E EV |

### 27.5 已知技术债

| ID | 技术债/最迟偿还 | 状态/所有者 | 责任任务 | 下一动作/关闭证据 |
|---|---|---|---|---|
| `DEBT-001` | 早期foundation证据缺独立原始日志与完整manifest；G1前 | `closed` / 验证负责人 | `SH-SAFE-BASE-001` | `EV-20260811-012`记录冻结hash、focused原始结果、状态隔离与数据卫生边界 |
| `DEBT-002` | `cases.json`已累计27个正式Oracle并冻结8份黄金回执，共同执行harness已完成；M2 | `closed` / 案例负责人 | `SH-HARNESS-001` | `EV-20260812-025`：B adapter协议+A只读降级+无独立C adapter的确定性机器报告 |
| `DEBT-003` | 机器需求/任务/决定/证据镜像未建立；M2 | `closed` / 治理负责人 | `SH-TRACE-001` | `EV-20260812-026`：四份可重生成镜像、7项mutation和独立治理复核通过 |
| `DEBT-004` | A跨文件JSONL事务未故障证明；原G1-A | `closed` / 计划负责人 | 无 | `DEC-027`取消A正式写入与恢复实现；若恢复范围须新CHG/DEC并重新登记技术债 |
| `DEBT-005` | 目录不是Git仓库，提交级追溯不可用；M1 | `accepted` / 计划负责人 | `DOC-003-001` | 当前以SHA-256基线补偿；若建仓另立DEC/CHG |
| `DEBT-006` | 验证器只在成功路径末尾扫有限扩展；G1 | `closed` / 验证负责人 | `SH-SAFE-BASE-001` | `EV-20260811-012`：MANIFEST/ROOT/TEMP focused覆盖失败照扫、sidecar、预置数据和临时根 |
| `DEBT-007` | 当前CONTRACT-v2候选在B运行时/共同Oracle引用数为0，A只读降级也未声明可读契约边界；M2 | `open` / 契约负责人 | `SH-TRACE-001`、`SH-HARNESS-001` | B适配器声明契约hash并执行共同Oracle，A降级适配器只证明零写入；C不建独立适配器 |

### 27.6 已被替代的旧规则

| 旧规则 | 当前规则 | 决定 |
|---|---|---|
| 明确说家中才读/扣库存 | 默认尝试；明确外食/不扣才跳过 | `DEC-004` |
| 建家庭成员主体 | 只记录本人 | `DEC-003` |
| 建0.3健康助手 | 产品只到记录和回顾 | `DEC-003` |
| 饮水不进0.1 | 白水和六项进度进入0.1 | `DEC-015` |
| 不支持历史纠错 | 追加式纠正/撤销/恢复 | `DEC-010` |
| 模糊量永不换算且每次给A/B | 有可信有限范围时取合理上界 | `DEC-006` |
| 不主动获取营养资料 | 常见食物用可信公共资料和缓存 | `DEC-006` |
| 所有组合菜必须问 | 常见菜透明模板，未知套餐才问 | `DEC-007` |
| 数据库来源统一标估算 | 只有实际推定字段标估算 | `DEC-006` |
| 每次入库都给改位置快捷项 | 正常只披露位置，明显不确定才给选项 | `DEC-012` |
| 整餐全塞一行 | 每道菜/食物/饮品一行 | `DEC-016` |
| 三路线完整做完再选 | 曾改为G1/G2早期选路，现已继续被B直接裁决替代 | `DEC-001`、`DEC-027` |
| G1/G2三路评分后再选唯一主线 | 用户直接指定B；G1/G2仅保留为B-only安全/就绪绑定门 | `DEC-027` |
| 多候选/不足使饮食preview/failed | 饮食提交、库存跳过、登记待补充 | `DEC-005` |
| 问题使用offered/deferred/invalidated等自由状态 | 四状态生命周期加resolution_reason | `DEC-019` |

---

## 28. 验证证据、变更、版本与文档修订

### 28.1 证据ID与创建纪律

证据ID为`EV-YYYYMMDD-NNN`。只有实际命令或检查完成后才能创建，禁止为未来任务预登记“预计通过”。每条证据至少包含：

| 字段 | 要求 |
|---|---|
| ID与时间 | Asia/Shanghai，永久唯一；`started_at/finished_at` |
| 关联 | `task_ids[]`、实际`case_ids[]`、需求、风险、变更、契约/Schema/策略版本 |
| 文件身份 | validator、契约、Schema、案例、源码和候选包SHA-256；非Git目录不得伪造提交哈希 |
| 环境 | Windows、PowerShell、Node、npm/pnpm、OpenClaw、路线、依赖和安装来源 |
| 命令 | 实际工作目录、完整命令、逐命令退出码和`route_results[]` |
| 结果 | 通过/失败、关键输出、机器报告路径与原始输出路径 |
| 数据根 | `official_data_roots[]`、`isolated_test_roots[]`和临时状态根 |
| 前后扫描 | `before/after manifest`；路径、数量、大小、SHA-256、mtime；新增/修改/删除各自清单 |
| 清理 | 环境恢复、临时根、WAL/SHM/journal和残留扫描结果 |
| 业务影响 | 明确为0或完整记录异常，不得省略；失败证据同样必须填 |
| 人员 | 执行者；高风险/发布证据需独立复核者及复核时间 |

旧证据只证明当时的文件版本。相关文件修改后，旧证据不再证明当前任务完成。

任务只有在该EV自身的`task_ids[]`中被逐项列出时才能引用它作为`actual_evidence_ids`；计划台账不得擅自扩大证据支撑范围。EV-v2规则生效前创建但仍被现行已完成任务引用的证据，必须在原证据文件追加结构化元数据补充，保留原文且不得改写历史结果；无法补齐的字段必须明确写`unknown_legacy`并触发重验，不能伪造。EV-007与EV-009已按此规则补充，EV-001—EV-006及EV-008只作历史或失效证据，不支撑当前执行中的完成状态。

### 28.2 已存在历史证据

| ID | 支撑范围 | 已知结果 | 限制 |
|---|---|---|---|
| `EV-20260809-001` | M0三foundation | A结构、B/C测试构建插件验证通过；当时业务文件0 | 无独立原始日志与完整前后哈希，仅作M0历史证据 |
| `EV-20260809-002` | M0状态隔离 | 临时状态、环境恢复、清理通过 | G1前必须新鲜重跑 |
| `EV-20260809-003` | DOC-0.2计划校验 | 旧0.2需求/案例/决定/任务拓扑检查 | 只证明DOC-0.2，不证明0.3 |
| `EV-20260809-004` | DOC-0.2独立复核 | 旧37轮迁移与治理复核 | 0.3新增44轮迁移后已失效为现行证明 |
| `EV-20260809-005` | `shared/business-contract.md`旧CONTRACT-v1 | 当时77项语义检查 | 需与0.3做差异复核后才可继续引用 |
| `EV-20260809-006` | 日期与回执契约 | 当时日期、快捷、六项和黄金格式通过 | 0.3新增水分下界/纠正差额等规则后需重验 |
| `EV-20260809-007` | `SH-CONTRACT-003`营养来源注册表 | 注册表、许可/来源边界、外部字段白名单、失败映射与独立复审通过；业务数据前后0 | 只冻结来源策略，不代表客户端、缓存和营养计算已实现 |
| `EV-20260809-008` | `SH-CONTRACT-004`旧问题/纠错契约验证 | 当时32/32需求、13/13场景和只读验证通过；业务数据前后0 | 属CONTRACT-v1历史；单事件大原子边界和旧Issue枚举与DOC-0.3的DEC-023/现行模型不一致，不支撑当前`SH-CONTRACT-004`完成 |
| `EV-20260809-009` | 五项foundation任务的当前新鲜重验 | A结构、B/C各7/7、构建、OpenClaw校验、状态隔离通过；业务类和辅助文件前后0；残留0 | 空业务数据基线；不关闭预置正式数据哈希与失败路径扫描缺口 |
| `EV-20260809-010` | `DOC-003-001`总计划0.3旧版本技术校验 | 71条REQ、144个CASE、58项TASK、26项DEC、44/44会话、任务案例并集144及四个完整案例集均通过机器校验和独立复核；业务类和辅助文件0 | 本轮DEC/REQ/CASE/validator变化后属于旧版本，不再单独证明当前候选；不证明PRODUCT业务、安装或发布已完成 |
| `EV-20260809-011` | 五项foundation的Skill-first增量、`DOC-003-001`与`DOC-003-002` | 复核EV-009的38项文件身份未变；A/B/C Skill与B/C唯一工具绑定3/3；DOC结构71/144/58/26、45/45及批准链通过；业务类和辅助文件前后0 | 仅补Skill-first静态Oracle、当前DOC与批准；不关闭`SH-SAFE-BASE-001`或任一PRODUCT任务 |
| `EV-20260811-012` | `SH-SAFE-BASE-001`按`DEC-028`有界退出验收 | Parser/ASCII/LibraryOnly通过；ROOT/TEMP/REPORT/MANIFEST四项focused与4/4状态隔离通过；业务候选0、fixture残留0；冻结hash见证据文件 | 关闭必要安全底座范围；不代表48项完整矩阵、G1/G2/G3、产品业务或安装发布通过；conhost精确计数保持deferred |
| `EV-20260811-013` | `SH-CONTRACT-001` CONTRACT-v2 | Windows PowerShell验证器PASS；77/77五族集合与追踪表通过；旧规则命中0；业务候选0；OpenClaw第二轮独立复核PASS并确认F1–F9/NUTR关闭 | 关闭共同命令/事实/效果/最终器/幂等契约；不代表伴随契约、Schema、SQLite repository、G1/G2/G3或产品发布通过 |
| `EV-20260811-014` | `SH-CONTRACT-002` 日期/回执 v2 | 两轮RED→GREEN；11项当前需求、六项黄金重算、单日/跨日结果和失败零成功输出通过；OpenClaw独立复核PASS | 关闭日期/回执伴随契约；不代表模型、repository、真实写入、安装或发布完成 |
| `EV-20260811-015` | `SH-CONTRACT-004` Issue/纠正 v2 | 两轮语义RED→GREEN；Issue、快捷、追加纠正、三阶段补偿、批量预览和失败零业务数据通过；OpenClaw第二轮独立复核PASS | 关闭Issue/纠正伴随契约；不代表Schema、SQLite repository、真实业务闭环、G1/G2/G3或发布完成 |
| `EV-20260811-016` | `SH-DECISION-001` 确定性策略 v1 | 缺文件RED、两轮测试质量RED和54用例GREEN；8条临期规则、16个阈值边界、4个mutation与OpenClaw独立复核PASS | 关闭Q-005的v1来源/提醒覆盖和M2策略前置；不代表保质/可食用安全判断、模型、SQLite、G1/G2/G3或发布完成 |
| `EV-20260811-017` | `SH-MODEL-001` Event/Amount/Product/Inventory模型 v1 | 三轮RED→GREEN、41个固定案例、4个mutation、Schema/fixture字节锁定；OpenClaw第二轮复核与Ajv 2020-12独立运行PASS | 关闭首组共同模型与失败零业务对象边界；不代表其余模型、SQLite mapping、生产runtime validator、G1/G2/G3或发布完成 |
| `EV-20260811-018` | `SH-MODEL-002` Nutrition/Profile/Snapshot/GoalVersion/DailyProgress模型 v1 | 缺文件RED、42个固定案例、4个mutation、Schema/fixture字节锁定；OpenClaw复核与Ajv 2020-12跨`$id`独立运行PASS | 关闭营养/进度共同模型与失败零业务对象边界；不代表Mixed模型、SQLite mapping、生产runtime validator、G1/G2/G3或发布完成 |
| `EV-20260811-019` | `SH-MODEL-003` Issue/Correction/FactCommit/EffectBundle/EnvelopeFinalize/Mixed模型 v1 | 两轮RED→GREEN、65个固定案例、10项semantic-only、10项语义invariant和4个mutation；OpenClaw双引擎/独立语义复核与GitHub clone复验PASS | 关闭第三组共同模型及写失败零业务对象边界；不代表SQLite mapping、生产runtime validator、G1/G2/G3或发布完成 |
| `EV-20260811-020` | `SH-MAP-001` B SQLite精确映射 | 34个共享定义映射到20表、18索引、22外键；18个本地mutation、33个独立mutation和公开clone复验PASS；业务数据库文件0 | 关闭B存储映射与A只读/C控制合并边界；不代表DDL、migration、repository、G1/G2/G3或产品发布完成 |
| `EV-20260811-021` | `SH-CASE-001` 核心共同案例 | 首组5个CASE-ID、3个固定fixture、8个本地mutation、20个独立mutation和公开clone复验PASS | 关闭核心共同Oracle静态定义；不代表B adapter执行、SQLite、安装或产品通过 |
| `EV-20260811-022` | `SH-CASE-002` 领域与故障案例 | 累计14案；新增9个CASE-ID、9个确定性场景、11个本地mutation和11个独立mutation，八项串行验证通过 | 关闭领域/故障共同Oracle静态定义；不代表B adapter执行、SQLite、安装或产品通过 |
| `EV-20260811-023` | `SH-CASE-003` 运维与安全案例 | 累计20案；新增6个CASE-ID、6个固定场景、12个本地mutation，九项回归与公开clone独立复核PASS | 关闭运维/安全静态Oracle；不代表安装器、迁移器、repository或可部署Skill完成 |
| `EV-20260812-024` | `SH-CASE-004` 黄金回执Oracle | 累计26案；8份UTF-8/LF黄金文本、6个新增CASE-ID、12个真实mutation和十项串行回归通过；独立复核P0=0/P1=0 | 关闭黄金回执静态Oracle；不代表渲染器、SQLite、业务写入、安装或产品发布完成 |
| `EV-20260812-025` | `SH-HARNESS-001` 共享执行harness | 累计1.4.0/27案；A只读零写、B backend-pending、C无独立adapter、15项Node测试和12组串行回归通过；独立复核P0=0/P1=0 | 关闭共同执行协议边界；B真实后端、业务整案、SQLite、安装和G1/G2/G3仍未完成 |
| `EV-20260812-026` | `SH-TRACE-001` 机器追踪 | 71 REQ、144 CASE、59 TASK、63 governance和26 EV可由唯一Markdown权威确定性生成；7项mutation、PS5.1回读和独立复核P0=0/P1=0/P2=0 | 关闭M2追踪与零孤儿门；不代表SQLite、repository、业务写入、安装或G1/G2/G3完成 |
| `EV-20260812-027` | `B-STOR-001` SQLite bootstrap | 固定私有root/数据库叶、内置`node:sqlite`、mapping-locked 20表/18索引/22外键、三阶段故障零发布/零残留、21项测试与OpenClaw插件门通过；独立复核P0=0/P1=0 | 关闭驱动、初始migration与本地打包兼容；不代表repository、真实业务写入、安装、部署或G1/G2/G3完成 |
| `EV-20260812-028` | `B-MERGE-C-001` 服务端preview/state权威 | 七字段HMAC preview绑定、`BEGIN IMMEDIATE`幂等预留、调用方状态不信任、陈旧/伪造/非法状态/非法migration零写入；27项focused、48项B测试与独立并发/篡改/rollback探针通过，复核P0=0/P1=0/P2=0 | 关闭B写入前服务端权威与guard；不代表business repository、FactCommit、效果/最终器、安装、部署或G1/G2/G3完成 |
| `EV-20260812-029` | `B-STOR-002` 原子repository | `FactCommit`、库存投影、outbox/effect、终态封存、幂等/响应丢失、重启恢复、同键并发和崩溃不可见；21项focused、69项B测试、真实worker并发/崩溃harness与OpenClaw本地插件门通过，独立复核P0=0/P1=0 | 关闭B原子repository和“技术日志可有、饮食业务零半写入”边界；不代表G1门、业务纵切、安装、部署或产品可用 |
| `EV-20260812-030` | `X-GATE-001` B-only存储安全门 | 三项依赖提交/EV哈希精确，13项G1存储责任、7项检查、6项门变异、69项B测试、并发/崩溃、OpenClaw本地门与追踪均PASS；独立复核P0=0/P1=0/P2=2，决策=`pass_b_safety` | 只授权`B-SLICE-001`；不代表完整产品案例、G2/G3、安装、部署或Skill可用 |
| `EV-20260812-031` | `B-SLICE-001` B-only首条纵向切片 | 候选`c8e6bcef39d0f98452432c3331095d963b9b9778`通过153项插件测试、31项repository、13项foundation、共享22项/精确17案责任、x-gate/trace、两类并发/崩溃、OpenClaw本地门和残留检查；Stage A=`e8680a131cec1e5dd7c18c9f50b9fed35510dae3`；最终独立复审P0=0/P1=0/P2=0、Ready=YES | 关闭`B-SLICE-001`并将`B-FAULT-001`列为唯一下一项；保留一次`rg`范围扩大过程例外，Git无保护路径变更；not installable; public OpenClaw tool remains non-writing；不代表完整故障矩阵、G2/G3、安装、部署或产品可用 |
| `EV-20260812-032` | `B-FAULT-001` 最终替换候选故障矩阵闭环 | 最终候选`552feee374fe3463f296bd4a110af11747a7ee29`；替换22-gate证据`74d8193debf1347b07a4c5e594f4b7c3f11c5828`，71,063 bytes，SHA-256 `ECAC880CE676A0F2D64B1B1AAB13479058AA99CEF069CFC915442AC984B87F91`，22/22 exit 0且逐边界Node/新增temp为0；独立全分支复审P0=0/P1=0、technical Ready YES；分支已推送，draft PR #11目标`main`；旧候选与两轮审阅链继续作为历史保留 | 关闭`B-FAULT-001`，结果`DONE_WITH_CONCERNS`并保留Task 6搜索输出过程concern；不授权map、`X-GATE-002`、G2/G3、安装、部署或产品可用 |
| `EV-20260813-033` | `DOC-004-001`、`DOC-004-002` DOC-0.4治理与用户批准 | 50/50会话台账、唯一DOC-0.4权威、0.3冻结SHA-256、74 REQ/153 CASE/63 TASK、27/1/27/0/8状态、唯一WIP=`SH-TRACE-001`、`X-GATE-002`显式依赖与四份陈旧镜像事实均经只读结构检查；批准记录已锁定 | 关闭DOC-004文档/批准治理；不重生成追踪JSON、不关闭`SH-TRACE-001`或`X-GATE-002`，不证明产品实现、可安装性或发布 |
| `EV-20260813-034` | `SH-TRACE-001` DOC-0.4机器追踪闭合（重开前历史） | 74 REQ、153 CASE、63 TASK、70 governance和34 EV为当前登记口径；G1/G2=13/17、累计发布集=124/152，15个mutation、镜像字节/身份一致和独立复核P0/P1/P2=0、Ready=YES | 该EV只关闭其原候选；`CHG-20260813-002`改变计划/生成器后已重开`SH-TRACE-001`，不得用旧EV授权X-GATE、安装或发布 |
| `EV-20260813-035` | `SH-TRACE-001` DOC-0.4重开后的机器追踪刷新 | 候选`96cba14646e2cee46ed45fe3711eb061f59b6c0e`上74 REQ、153 CASE、63 TASK、70 governance和34历史EV确定性重生成；15个mutation、四镜像字节新鲜、独立复核P0/P1/P2=0、Ready=YES | 关闭本轮追踪重开并把EV总数推进至35；只解锁`X-GATE-002`有界预检，不授权map结果、`SEL-*`、安装或发布 |

完整文件位于`docs/evidence/`的EV-003至EV-035；EV-001/002属于迁移的历史执行记录且没有现行证据文件。旧foundation报告因曾与EV-008发生编号和raw路径碰撞，已保留为`COLLISION-20260809-foundation-refresh-invalid.md`且明确失效；当前五项foundation任务以EV-009证明未变动态结果，以EV-011补Skill-first静态Oracle。任何G2/G3不得仅依赖旧证据，也不得把`EV-20260812-030`外推为纵切证据、把`EV-20260812-031`外推为完整故障矩阵，或把`EV-20260812-032`外推为G2门、安装或发布证据。

### 28.3 变更流程

变更ID为`CHG-YYYYMMDD-NNN`，状态只允许`提议/批准/实施中/已验证/拒绝/撤销`。每条记录原始来源、级别、目标、影响的REQ/CASE/DEC/TASK/SCHEMA/PRODUCT、所有者、审批者、实施任务、目标证据和关闭证据。

| 级别 | 类型 | 必须动作 |
|---|---|---|
| L0 | 不改变行为的错字/链接 | 修订记录 |
| L1 | 兼容性说明或例子 | 更新需求说明和文档校验 |
| L2 | 新增兼容行为 | 新需求、案例、任务、证据 |
| L3 | 改库存默认、估算、回执等契约 | 新DEC、契约版本、回归、迁移评估 |
| L4 | 改数据字段/约束/历史转换 | 新Schema、迁移、备份恢复验证 |
| L5 | 停路线、换实现、改发布方式 | 路线门/决定、任务处置和发布影响 |

标准顺序：记录原始要求 → 分级 → 影响分析 → 更新权威需求 → 更新案例 → 创建/重开任务 → 实施 → 测试 → 登记证据 → 更新唯一台账 → 刷新派生控制台。任务重开不删除旧证据；旧证据继续作为旧契约历史。

当前变更记录：

| ID | 来源/级别 | 目标与影响 | 所有者/审批者 | 实施任务 | 状态/关闭证据 |
|---|---|---|---|---|---|
| `CHG-20260809-001` | 初始44轮完整会话与用户“直接写总功能开发计划0.3”的要求，加第45轮 Skill-first 澄清与批准；L3并含L4模型影响 | 重建DOC-0.3；替代只读0.2；重开`SH-CONTRACT-001`、`SH-CONTRACT-002`、`SH-CONTRACT-004`、Schema和案例；`SH-CONTRACT-003`已按新营养要求完成，后续只在CONTRACT-v2影响来源语义时重开；EV-005/006不再证明现行契约 | Codex / 用户 | `DOC-003-001`、`DOC-003-002`及M1/M2任务 | `已验证`；`EV-20260809-010`关闭初始44轮技术编制，`EV-20260809-011`关闭第45轮、Skill-first与用户批准增量 |
| `CHG-20260810-001` | 第46轮用户明确决定“集中开发B”；L5路线/范围变更 | B成为唯一产品主线；A/C M3/M4完整实现任务取消；A仅保留只读/无插件降级；C的服务端preview、`input_digest + data_revision`、陈旧状态拒绝、调用方状态不信任、非法迁移拒绝和幂等经`B-MERGE-C-001`并入B；`X-GATE-001`与`X-GATE-002`改为B-only门；不改变`SH-SAFE-BASE-001`及其既有48个顶层RED断言范围 | 计划负责人 / 用户直接批准 | `DOC-003-001`、`B-MERGE-C-001`、`B-STOR-001`、`B-STOR-002`、`B-SLICE-001`、`B-FAULT-001`、`X-GATE-001`、`X-GATE-002` | `实施中`；本次只登记裁决与任务处置，不创建EV、不声明G1/G2通过；待底座、B门新鲜EV、冻结contract hash与B-only map后才可转`已验证` |
| `CHG-20260811-001` | 第47轮用户明确“写入失败可以产生日志，但饮食记录里不能产生数据”；L1兼容性说明 | 澄清`DEC-023`的业务原子性边界：`FactCommit`失败时业务记录、效果、outbox、投影、进度与成功结果全部回滚；独立技术日志/审计允许保留脱敏失败证据，但不属于业务数据、不得被查询为饮食记录；已成功FactCommit后的Effect失败仍按既有分层待重试语义 | 计划负责人 / 用户直接批准 | `DOC-003-001`、`SH-CONTRACT-001`、`B-STOR-002`、`B-FAULT-001` | `已验证`；`EV-20260811-013`冻结契约，`EV-20260812-029`与最终`EV-20260812-032`证明技术日志和业务表物理、查询、事务与重试边界隔离；B-FAULT最终独立复审P0=0/P1=0 |
| `CHG-20260811-002` | 第48轮用户明确要求继续开发，但安全底座只做必要内容、非必要内容可提前结束，并要求按总计划0.3执行及提供简明计划；L2范围收敛 | 新增Skill-first B简明执行计划；B工具合同扩为餐食、饮水、库存、查询、每日汇总、更正和撤销8动作；只提前执行Q-002允许的`node:sqlite`兼容spike；`SH-SAFE-BASE-001`改为有界退出验收，`conhost.exe`精确计数等平台细节转deferred，不绕过`SH-MAP-001`、`SH-HARNESS-001`、`SH-TRACE-001`入口依赖，不提前创建生产业务表 | 计划负责人 / 用户直接批准 | `DOC-003-001`、`SH-SAFE-BASE-001`、`B-STOR-001` | `已验证`；简明计划、8动作合同、本地SQLite spike与`EV-20260811-012`安全有界退出验收已落；后继共享输入仍按各自任务验收 |
| `CHG-20260812-001` | `CASE-EFFECT-003`的正式故障 Oracle 校正；L1状态语义修复 | 全部子效果已耐久而仅EnvelopeFinalize失败时，信封及重启状态从`effects_pending`更正为`effects_stable`；同token只允许最终器继续，不得重复效果 | Codex / 复审冻结 | `B-FAULT-001`、`DEC-023`、`CASE-EFFECT-003` | `已验证`；最终`EV-20260812-032`绑定候选`552feee`、替换22-gate日志与独立全分支P0=0/P1=0复审；旧关闭链作为superseded历史保留 |
| `CHG-20260813-001` | 第49轮用户提供Agent Reach/last30days两份说明并明确批准0.4作为现行入口并继续按其实施；L2新增兼容能力，含后续模型影响 | 从最新DOC-0.3/B-FAULT闭环生成独立DOC-0.4；新增来源能力、研究证据、Watchlist和Profile更新预览边界；0.1只扩来源可靠性，0.2才扩研究；不改当前业务实现、不创建map、不宣称可安装 | 计划负责人 / 用户直接批准 | `DOC-004-001`、`DOC-004-002`、`SH-TRACE-001`、`SEL-NUTR-001`、`SEL-RESEARCH-001`、`SEL-NUTR-WATCH-001` | `已验证`；`EV-20260813-033`记录文档结构检查、批准留档和现行入口切换，`EV-20260813-034`记录DOC-0.4追踪镜像重生成和独立复核；未产生产品功能EV |
| `CHG-20260813-002` | `X-GATE-002`开源可移植性与发布原子性复审；L2门产物兼容修复 | 仓库提交相对`selected-route-map.template.json`；机器绝对`selected-route-map.json`改为Git忽略的本地门产物；matrix只声明可移植要求，不预填pass；验证器解析§29.6、运行有界预检、绑定实际回执，并以同卷hard-link no-replace发布，碰撞或任一检查失败都不覆盖/不发布新map | X-GATE实现者 / 独立架构与安全复核 | `SH-TRACE-001`、`X-GATE-002`、§26.2/26.5、§29.6、`shared/tests/validate-x-gate-002.mjs`、`docs/work-items/X-GATE-002-matrix.json` | `实施中`；追踪刷新已由`EV-20260813-035`关闭，当前EV总数35且`X-GATE-002`为唯一WIP；允许运行实际有界预检，但所有检查通过前本地map必须缺席，门完成两轮复核前不得解锁产品任务 |

### 28.4 版本分层

| 层级 | 示例 | 含义 |
|---|---|---|
| 文档 | `DOC-0.4` | 总计划结构、需求与治理版本 |
| 产品 | `PRODUCT-0.1/0.2` | 可安装用户功能发布 |
| 业务契约 | `CONTRACT-v1/v2` | 跨路线业务语义与结果 |
| 数据模式 | `SCHEMA-v1` | JSONL/SQLite共同可表达结构 |
| 路线实现 | `A/B/C-impl-*` | 具体实现包 |
| 工程基底 | `A/B/C foundation 0.1` | 脚手架，不等于产品发布 |

### 28.5 文档修订记录

| 文档 | 日期 | 变化 | 状态 |
|---|---|---|---|
| `DOC-0.1` | 2026-08-08 | 初始三路线和业务拆分 | 历史，只读 |
| `DOC-0.2` | 2026-08-09 | 旧37轮沟通的结构化计划 | 历史基线，不再维护现行状态 |
| `DOC-0.3` | 2026-08-10 | 从初始44轮完整原始会话逐段重建，后续累计登记至第48轮并完成B纵切/故障闭环 | 0.4冻结基线；提交`d8e0736`、SHA-256 `9914C27D...C8C79`；不再追加新功能 |
| `DOC-0.4` | 2026-08-13 | 从最新0.3提交`d8e0736`继承全部进度，吸收两份技术资料中的来源适配/Doctor和证据研究/去重/Watchlist设计，新增3条REQ、9个CASE、2个0.2任务 | 唯一现行权威入口；已完成文档结构检查和用户批准留档，机器追踪重生成与独立复核仍在进行，未改变产品可安装性 |

---

## 29. 文件责任图与交付物落点

### 29.1 权威文档、历史文档与证据

| 路径 | 唯一职责 | 当前物理事实 | 责任任务 |
|---|---|---|---|
| `总功能开发计划0.4.md` | 需求索引、决定、风险、变更和唯一任务台账；唯一现行计划入口 | 已获用户批准；继承0.3提交`d8e0736`并吸收两份技术说明；`SH-TRACE-001`重开刷新已由`EV-20260813-035`关闭 | `DOC-004-001`、`DOC-004-002`、`SH-TRACE-001` |
| `总功能开发计划0.3.md` | 0.4的冻结基线和历史前任计划 | SHA-256=`9914C27DD653AF757DDF58FAC3273C11E91AAC7C9D5D1D95583FF27CDACC8C79`；保持只读，不是当前权威 | `DOC-003-001`、`DOC-003-002` |
| `总功能开发计划0.2.md` | 旧结构化基线，只读历史 | 已存在，不再写现行状态 | — |
| `总功能开发计划.md` | 初始路线和里程碑历史 | 已存在，只读 | — |
| `START-HERE.md` | 面向开发者的当前入口、可运行命令和“foundation only”提示 | 已存在；发布前必须更新 | `SEL-INSTALL-001` |
| `docs/evidence/EV-*.md` | 人类可复核证据报告 | EV-003至EV-011存在；另保留一份显式失效的碰撞审计记录 | 各实际执行任务 |
| `docs/evidence/raw/` | 原始命令输出、只读验证脚本和机器报告 | 部分历史证据存在；未来所有高风险任务强制 | `SH-SAFE-BASE-001`、`SH-TRACE-001` |
| `docs/work-items/<TASK>-brief.md` | 单任务冻结输入、范围、数据根和完成Oracle | 仅部分共同契约任务存在 | 对应任务owner |
| `docs/work-items/<TASK>-report.md` | 实施差异、命令、结果和遗留问题 | 仅部分共同契约任务存在 | 对应任务owner |
| `docs/work-items/<TASK>-review.md` | 独立复核结论和发现处置 | 仅部分共同契约任务存在 | 对应reviewer |

### 29.2 共同契约、Schema、策略与追踪

| 路径 | 唯一职责 | 当前物理事实 | 责任任务 |
|---|---|---|---|
| `shared/business-contract.md` | 可移植Skill与B后端共同业务动作、结果、事实/效果和不可退化规则 | CONTRACT-v2已由`EV-20260811-013`冻结并完成独立复核 | `SH-CONTRACT-001` |
| `shared/contracts/receipt-and-date-contract.md` | 日期、快捷项、逐菜回执、六项进度和冻结结果 | v2 已由 `EV-20260811-014` 冻结并完成独立复核 | `SH-CONTRACT-002` |
| `shared/nutrition-source-registry.json` | 来源优先级、许可、白名单、缓存与失败映射 | 已存在并由EV-007验证 | `SH-CONTRACT-003` |
| `shared/contracts/source-capability-contract.md` | 适配器、主备后端、Tier、Probe/Doctor、配置和降级契约 | 尚不存在 | `SEL-NUTR-001` |
| `shared/schemas/research-evidence.schema.json` | ResearchRun/Item/Candidate/Cluster/Finding/Watchlist/UpdateProposal公共形状 | 尚不存在 | `SEL-RESEARCH-001` |
| `shared/policies/research-confidence.json` | 来源权威、匹配、时效、佐证、冲突和`nothing_solid`阈值 | 尚不存在 | `SEL-RESEARCH-001` |
| `shared/contracts/issue-correction-contract.md` | Issue、补全、FactCommit/EffectBundle、纠正和补偿 | CONTRACT-v2已由`EV-20260811-015`冻结并完成第二轮独立复核 | `SH-CONTRACT-004` |
| `shared/policies/decision-thresholds.json` | 候选、临期、模板一致性、取整和B-only门确定性条件 | v1 已由 `EV-20260811-016` 冻结，54个固定用例与独立复核通过 | `SH-DECISION-001` |
| `shared/schemas/event-and-amount.schema.json` | EventEnvelope、OccurredTime、Amount、Meal/Water | 尚不存在 | `SH-MODEL-001` |
| `shared/schemas/product-inventory.schema.json` | ProductIdentity、批次原记录、事件与投影 | 尚不存在 | `SH-MODEL-001` |
| `shared/schemas/nutrition-progress.schema.json` | Profile、Snapshot、GoalVersion、DailyProgress | v1 已由 `EV-20260811-018` 冻结；42个固定用例、4个mutation与Ajv独立复核通过 | `SH-MODEL-002` |
| `shared/schemas/issue-correction-mixed.schema.json` | Issue、Correction、outbox、MixedCommitResult、ReceiptData | v1 已由 `EV-20260811-019` 冻结；65例、10项semantic-only、4个mutation与双引擎独立复核通过 | `SH-MODEL-003` |
| `shared/contracts/storage-mapping.md` | 共同模型到B SQLite的精确映射、A只读降级边界和C控制合并点 | v1 已由 `EV-20260811-020` 冻结；34定义、20表、18索引、18个本地mutation与33个独立mutation通过 | `SH-MAP-001` |
| `shared/traceability/requirements.json` | §30需求注册表的机器镜像 | 当前闭环为74 REQ/153 CASE、124/152发布集；`EV-20260813-035`绑定本轮重开刷新与独立复核 | `SH-TRACE-001` |
| `shared/traceability/tasks.json` | §31唯一任务台账的机器镜像；文档为批准入口、机器表供校验 | 当前63 TASK与28/1/26/0/8状态一致，`X-GATE-002`为唯一WIP；`SH-TRACE-001`已完成 | `SH-TRACE-001` |
| `shared/traceability/decisions.json` | DEC/Q/RISK/DEBT/CHG引用镜像 | 当前口径70条governance；本轮刷新已关闭，`RISK-010`恢复为closed | `SH-TRACE-001` |
| `shared/traceability/evidence-index.json` | EV元数据、文件hash和新鲜度索引 | 当前35 EV；`EV-20260813-035`锁定本轮重开后的追踪刷新事实，不外推为X-GATE、安装或发布证据 | `SH-TRACE-001` |

机器镜像不得成为第二个人工状态源：只能由本文件和实际证据生成或通过受控变更反向更新，两边不一致时校验失败，不允许手工“择一相信”。

### 29.3 共同案例、fixture与验证设施

| 路径 | 唯一职责 | 当前物理事实 | 责任任务 |
|---|---|---|---|
| `shared/acceptance-cases/cases.json` | 共同案例清单与Oracle | 已累计冻结27个正式CASE-ID：前26案由SH-CASE-001/002/003/004冻结；SH-HARNESS-001按总计划补齐缺失的`CASE-STORAGE-001`同键同输入重试Oracle；四类validator均通过 | `SH-CASE-001`、`SH-CASE-002`、`SH-CASE-003`、`SH-CASE-004`、`SH-HARNESS-001` |
| `shared/acceptance-cases/fixtures/` | 固定时钟、目标、库存、营养、上下文和故障种子 | `core-v1.json`已包含core、domain和6个ops/security确定性场景；旧fixture字节保持，新增类文件fixture均固定Base64/length/SHA/time | `SH-CASE-001`、`SH-CASE-002`、`SH-CASE-003` |
| `shared/acceptance-cases/golden-receipts/` | 单日、混合、跨日和异常黄金回执 | 已冻结8份UTF-8/LF独立文本与manifest；独立复核P0=0/P1=0 | `SH-CASE-004` |
| `shared/acceptance-cases/adapters/a.ts` | A只读/无插件降级适配器，仅证明零写入和明确降级 | 已实现；27案默认均`read_only_no_plugin`且零写 | `SH-HARNESS-001` |
| `shared/acceptance-cases/adapters/b.ts` | B唯一产品路线共同命令适配器，覆盖并入的C安全控制 | 已实现协议边界；无driver时诚实`backend_pending`，真实B后端仍未实现 | `SH-HARNESS-001` |
| `shared/acceptance-cases/adapters/c.ts` | 已取消的C独立业务适配器历史落点 | 不创建；C foundation验证仍走既有foundation入口 | — |
| `shared/acceptance-cases/run-all.*` | 一次运行共同Oracle并产出机器报告 | `run-all.ts`已实现；默认报告确定性、无Oracle payload并明确backend pending | `SH-HARNESS-001` |
| `shared/validate-foundations.ps1` | 当前foundation统一验证入口 | 已存在；EV-009通过，但有已登记盲区 | `SH-FND-001`、`SH-SAFE-BASE-001` |
| `shared/private/foundation-validation-core.ps1` | 固定根、manifest、九阶段调度、环境恢复、清理与机器报告的内部core | 尚不存在；仅供统一入口与安全测试dot-source，不是用户/Skill公共入口 | `SH-SAFE-BASE-001` |
| `shared/tests/validate-foundations-state-isolation.ps1` | OpenClaw状态隔离包装器回归 | 已存在；mock npm，只证明包装器隔离 | `SH-FND-001`、`SH-SAFE-BASE-001` |
| `shared/tests/validate-data-manifests.ps1` | 正式根前后manifest、sidecar、失败路径和临时根审计 | 尚不存在 | `SH-SAFE-BASE-001` |
| `shared/tests/validate-traceability.*` | ID唯一、无孤儿、阶段/状态/证据新鲜度检查 | 尚不存在 | `SH-TRACE-001` |

### 29.4 A/B/C现有入口与必须补齐的精确路线文件

| 路线 | 当前入口和事实 | 必须新增/改造的精确落点 | 责任任务 |
|---|---|---|---|
| A | `version-a-skill-only/skills/diet-manager-a/SKILL.md`、`version-a-skill-only/storage-contract.md`、`version-a-skill-only/tests/validate-foundation.ps1`；当前只有文字契约和结构正则 | 不再新增parser、写入器、事务日志或完整切片；只在既有Skill/存储说明中维护只读/无插件降级边界 | `A-STOR-001`、`A-STOR-002`、`A-SLICE-001`、`A-FAULT-001`均已取消 |
| B | `version-b-lite-plugin/src/index.ts`、`version-b-lite-plugin/src/contracts.ts`、`version-b-lite-plugin/openclaw.plugin.json`、`version-b-lite-plugin/tests/foundation.test.ts`；handler固定`foundation_not_implemented` | `version-b-lite-plugin/src/domain/service.ts`、`version-b-lite-plugin/src/storage/database.ts`、`version-b-lite-plugin/src/storage/migrations/001-initial.sql`、`version-b-lite-plugin/src/preview/store.ts`、`version-b-lite-plugin/src/preview/token.ts`、`version-b-lite-plugin/src/state/transition-guard.ts`、`version-b-lite-plugin/tests/integration/business-slice.test.ts` | `B-MERGE-C-001`、`B-STOR-001`、`B-STOR-002`、`B-SLICE-001`、`B-FAULT-001` |
| C | `version-c-strict-plugin/src/index.ts`、`version-c-strict-plugin/src/state-machine.ts`、`version-c-strict-plugin/src/contracts.ts`、`version-c-strict-plugin/openclaw.plugin.json`、`version-c-strict-plugin/tests/foundation.test.ts`；调用者当前可自报状态 | 不新增C数据库、迁移或业务切片；preview/state-machine有价值语义迁入B对应文件，C目录只作历史foundation参考 | `C-STOR-001`、`C-STOR-002`、`C-SLICE-001`、`C-FAULT-001`均已取消；迁移责任=`B-MERGE-C-001` |

B主线必须在Skill、工具Schema、运行时和测试中声明同一`contract_version + contract_sha256`；A只读降级规范也必须声明其可读契约边界，C历史foundation不得作为现行运行时。当前`CONTRACT-v1`运行时/测试引用数为0，属于`DEBT-007`，不得把文档存在误作契约已执行。

### 29.5 根责任映射

八个根ID及全部行为约束只由§21.2定义，本节不建立第二套枚举。文件责任上，`SH-SAFE-BASE-001`负责`isolated_test_root`、`validation_root`、`build_root`和`openclaw_state_root`的隔离验证；B存储任务负责`official_data_root`；`SEL-BACKUP-001`和`SEL-EXPORT-BASE-001`分别负责`backup_root`与`export_root`；`SH-TRACE-001`负责`evidence_root`的不可覆盖和引用完整性。

正式安装时A/B/C的精确业务路径由各路线存储契约和安装配置冻结；在G1前若路径仍未命名，该路线不得运行真实写入试验。

### 29.6 B路线M5—M9交付物路径模板

`DEC-027`已经把`selected_route`固定为B，CONTRACT-v2已冻结为`diet-manager/contract-v2`、版本`2`、SHA-256 `632B2BBF8D0E6C655F4C0A47958828A86C67B3240065984CCC78A808E6F7072E`。仓库必须提交可开源复制的`shared/selected-route-map.template.json`，其中项目根用`.`、B根用`version-b-lite-plugin`、下表交付物全部使用规范化仓库相对路径；验证器必须直接解析本节权威表并逐任务比较模板，禁止另设硬编码路径Oracle。机器本地的`shared/selected-route-map.json`只能在有界预检全通过后，从模板解析`<project_root>`为当前规范化项目绝对根并以同卷原子hard-link、no-replace方式发布；它含机器绝对路径和实际验证回执，必须被Git忽略且不得进入release字节。模板、表或发布协议变化必须走`CHG-*`；本地map存在只表示候选预检通过，不代表门关闭或解锁产品任务。

| task_id | 必须解析的交付物路径 |
|---|---|
| `SEL-CORE-001` | `<selected_route_root>/src/domain/core-service.ts`；`<selected_route_root>/tests/acceptance/core.test.ts` |
| `SEL-PANTRY-001` | `<selected_route_root>/src/domain/inventory-service.ts`；`<selected_route_root>/src/storage/inventory-repository.ts` |
| `SEL-NUTR-001` | `<selected_route_root>/src/nutrition/nutrition-service.ts`；`<selected_route_root>/src/nutrition/source-client.ts` |
| `SEL-FACTEFFECT-001` | `<selected_route_root>/src/orchestration/fact-effect-finalize.ts`；`<selected_route_root>/src/projections/progress-contribution.ts` |
| `SEL-ISSUE-001` | `<selected_route_root>/src/issues/issue-service.ts`；`<selected_route_root>/src/issues/prompt-options.ts` |
| `SEL-CORR-001` | `<selected_route_root>/src/corrections/correction-service.ts`；`<selected_route_root>/tests/acceptance/correction.test.ts` |
| `SEL-PROGRESS-001` | `<selected_route_root>/src/projections/progress-projector.ts`；`<selected_route_root>/tests/acceptance/progress.test.ts` |
| `SEL-QUERY-001` | `<selected_route_root>/src/queries/report-service.ts`；`<selected_route_root>/tests/acceptance/query.test.ts` |
| `SEL-RECEIPT-001` | `<selected_route_root>/src/receipt/renderer.ts`；`<selected_route_root>/tests/golden/receipts.test.ts` |
| `SEL-RELIABILITY-001` | `<selected_route_root>/tests/reliability/fault-matrix.test.ts`；`<project_root>/shared/tests/validate-zero-diff.ps1` |
| `SEL-INSTALL-001` | `<project_root>/scripts/install.ps1`；`<project_root>/docs/install-support-matrix.md` |
| `SEL-MIGRATE-001` | `<selected_route_root>/src/storage/migrate.ts`；`<selected_route_root>/tests/migration/migration.test.ts` |
| `SEL-BACKUP-001` | `<project_root>/scripts/backup.ps1`；`<project_root>/scripts/restore.ps1`；`<project_root>/scripts/uninstall.ps1` |
| `SEL-EXPORT-BASE-001` | `<selected_route_root>/src/export/baseline-export.ts`；`<selected_route_root>/tests/export/baseline.test.ts` |
| `SEL-RELEASE-001` | `<project_root>/release/0.1/candidate-manifest.json`；`<project_root>/scripts/release-0.1.ps1` |
| `SEL-TEMPLATE-001` | `<selected_route_root>/src/templates/personal-dish-service.ts`；`<selected_route_root>/tests/acceptance/template.test.ts` |
| `SEL-REVIEW-001` | `<selected_route_root>/src/review/review-service.ts`；`<selected_route_root>/tests/acceptance/review.test.ts` |
| `SEL-LIFE-001` | `<selected_route_root>/src/lifecycle/lifecycle-service.ts`；`<selected_route_root>/tests/acceptance/lifecycle.test.ts` |
| `SEL-EXPORT-001` | `<selected_route_root>/src/export/rich-export.ts`；`<selected_route_root>/tests/export/rich.test.ts` |
| `SEL-UPGRADE-002` | `<selected_route_root>/src/storage/upgrade-0.2.ts`；`<selected_route_root>/tests/migration/upgrade-0.2.test.ts` |
| `SEL-RELEASE-002` | `<project_root>/release/0.2/candidate-manifest.json`；`<project_root>/scripts/release-0.2.ps1` |

路径模板只解决计划阶段“写到哪里”，不代表未来交付文件已经存在；当前输入存在性、hash、命令、构建来源和验证结果由本地map内机器回执证明，门关闭仍由独立复核及EV证明。

---

## 30. 需求注册表（唯一REQ索引）

本表只登记稳定ID、阶段、原始来源、唯一规范落点和最低案例；行为细节只在所指规范章节定义，避免在台账复制第二份规则。新增或改变任一行必须走`CHG-*`。

### 30.1 范围、事实、时间与对话

| ID | 处置 | 受控标题 | 原始来源 | 唯一规范落点 | 最低案例 |
|---|---|---|---|---|---|
| `REQ-SCOPE-001` | 0.1 必须 | 只记录本人，不建家庭成员 | 会话11、41 | §5.2、§20.4 | `CASE-MEAL-011`、`CASE-MEAL-017`、`CASE-MEAL-018`、`CASE-MEAL-019` |
| `REQ-SCOPE-002` | 明确排除 | 医疗诊断、自动健康评价和0.3健康助手 | 会话11 | §2.4、§5.3 | `CASE-SCOPE-001` |
| `REQ-SCOPE-003` | 0.1 必须 | 面向智能体的Skill是主交付物；B为唯一Skill+确定性插件后端产品主线，A只保留只读/无插件降级规范，C停止独立产品实现；后端不得脱离Skill作为产品；foundation/PRODUCT/安装严格分层 | 会话3—6、42—46 | §1—§4、§22.1、`DEC-018`、`DEC-027` | `CASE-FOUNDATION-001`、`CASE-OPS-007` |
| `REQ-CORE-001` | 0.1 必须 | 已发生事实优先于库存、营养和投影效果 | 会话12、37—42 | §5.1、§21.5 | `CASE-INVENTORY-003`、`CASE-INVENTORY-004`、`CASE-EFFECT-001` |
| `REQ-CORE-002` | 0.1 必须 | 原话、unknown、字段证据和规则版本永久可追溯 | 会话12、23、35、40 | §5.4、§18.10 | `CASE-NUTR-006`、`CASE-RECEIPT-002` |
| `REQ-CORE-003` | 0.1 必须 | 命令结果、事件生命周期、Issue和批次状态分轴 | 会话37—42 | §0.5、§6.6、§11.16 | `CASE-STORAGE-001`、`CASE-ISSUE-003` |
| `REQ-EVENT-001` | 0.1 必须 | 所有领域事件继承EventEnvelope并追加保存 | 会话25、38、41 | §15、§23.1—§23.2 | `CASE-CORR-001`、`CASE-CORR-004` |
| `REQ-TIME-001` | 0.1 必须 | OccurredTime区间、时区、解析锚点与可注入时钟 | 会话36、41 | §6.2、§20.5—§20.7 | `CASE-MEAL-012`、`CASE-MEAL-013`、`CASE-MEAL-014` |
| `REQ-TIME-002` | 0.1 必须 | 本月仅几号、跨月月日、跨年完整日期 | 会话16 | §10 | `CASE-DATE-001`、`CASE-DATE-002`、`CASE-DATE-003` |
| `REQ-TIME-003` | 0.1 必须 | 购买、制作、入库、接收、发生和提交时间分离 | 会话18—20、39 | §11.13 | `CASE-MEAL-002`、`CASE-PURCHASE-004` |
| `REQ-CONTEXT-001` | 0.1 必须 | 省略主语、本人份量与短期可靠指代 | 会话11、24、41 | §5.2、§20.4、§20.9—§20.12 | `CASE-MEAL-017`、`CASE-MEAL-018`、`CASE-MEAL-019` |
| `REQ-CONTEXT-002` | 0.1 必须 | 完成、计划、否定、转折和操作类型先解析 | 会话25、41 | §20.1—§20.3 | `CASE-MEAL-009`、`CASE-MEAL-010`、`CASE-MEAL-015`、`CASE-MEAL-016` |
| `REQ-CONTEXT-003` | 0.1 必须 | 快捷组、场景和跟进绑定revision、有效期与会话边界 | 会话15、21、37、41 | §5.6、§8.11、§20.9—§20.12 | `CASE-INVENTORY-006`、`CASE-ISSUE-002`、`CASE-MEAL-020` |

### 30.2 饮食、入库、商品与库存

| ID | 处置 | 受控标题 | 原始来源 | 唯一规范落点 | 最低案例 |
|---|---|---|---|---|---|
| `REQ-MEAL-001` | 0.1 必须 | 一餐多项、有序items和meal_id归组 | 会话8、25、41 | §6.1—§6.3、§20.14 | `CASE-MEAL-001`、`CASE-MEAL-021` |
| `REQ-MEAL-002` | 0.1 必须 | 明确、近似、范围、模糊和缺失数量；四量分离 | 会话12、23、33、35 | §6.3—§6.4 | `CASE-MEAL-003`、`CASE-MEAL-006`、`CASE-INVENTORY-005` |
| `REQ-MEAL-003` | 0.1 必须 | 常见菜透明模板，高度可变套餐只保存已知事实 | 会话23、40 | §6.5、§13.10—§13.11 | `CASE-MEAL-007`、`CASE-MEAL-008`、`CASE-NUTR-003`、`CASE-NUTR-004` |
| `REQ-PURCHASE-001` | 0.1 必须 | 入库事件和商品最低字段规范 | 会话13、18、39 | §7.1—§7.2 | `CASE-PURCHASE-001`、`CASE-PURCHASE-002` |
| `REQ-PURCHASE-002` | 0.1 必须 | 外层、内层、容量、总量与公式分别保存 | 会话13、20、39 | §7.3—§7.4 | `CASE-PURCHASE-001`、`CASE-PURCHASE-002` |
| `REQ-PURCHASE-003` | 0.1 必须 | 单项详细、多类逐行简化入库回执 | 会话19—21 | §12 | `CASE-PURCHASE-003`、`CASE-PURCHASE-006` |
| `REQ-PRODUCT-001` | 0.1 必须 | 商品身份、别名、品牌/变体/规格与批次分离 | 会话17—18、39 | §7.5、§11.1—§11.4 | `CASE-PURCHASE-007`、`CASE-PURCHASE-008` |
| `REQ-PRODUCT-002` | 0.1 必须 | 存放位置分层推断、披露和追加式纠正 | 会话19—21、39 | §11.5—§11.6、§11.15 | `CASE-PURCHASE-003`、`CASE-PURCHASE-010` |
| `REQ-PRODUCT-003` | 0.1 必须 | 开封、保质、时间锚点、有效到期和被动临期查询 | 会话19—21、39 | §11.7—§11.11、§11.19 | `CASE-PURCHASE-004`、`CASE-PURCHASE-005`、`CASE-PURCHASE-009` |
| `REQ-PRODUCT-004` | 0.1 必须 | 批次耗尽不删身份；精确同商品复用历史营养 | 会话24、39—40 | §11.17、§14.6—§14.8 | `CASE-PURCHASE-007`、`CASE-NUTR-005` |
| `REQ-PANTRY-001` | 0.1 必须 | 无外食/不扣证据时默认读并尝试家庭库存，scene=unknown也不跳过 | 会话12 | §5.5—§5.6、§9.1 | `CASE-MEAL-004`、`CASE-MEAL-005`、`CASE-MEAL-020`、`CASE-INVENTORY-001` |
| `REQ-PANTRY-002` | 0.1 必须 | unique/multiple/none/insufficient稳定匹配与降级 | 会话12、15、37、39 | §9.2—§9.3 | `CASE-INVENTORY-003`、`CASE-INVENTORY-004` |
| `REQ-PANTRY-003` | 0.1 必须 | 同商品批次指定→FEFO→FIFO并可跨批分配 | 会话39 | §9.4、§11.18 | `CASE-INVENTORY-002`、`CASE-INVENTORY-009` |
| `REQ-PANTRY-004` | 0.1 必须 | 单位可换才扣、库存不负、模板量不作库存证据 | 会话12、23、39 | §9.5—§9.7、§13.13、§18.8 | `CASE-INVENTORY-005`、`CASE-INVENTORY-007`、`CASE-EFFECT-002` |
| `REQ-PANTRY-005` | 0.2 必须 | 用户逐次授权的部分扣减、盘点、丢弃、退货、获赠和赠出 | 会话8推理、38—39 | §9.7、§11.12、§15.17 | `CASE-LIFE-001`、`CASE-LIFE-002`、`CASE-LIFE-005`、`CASE-LIFE-008`、`CASE-LIFE-009` |

### 30.3 营养、模板、问题与纠正

| ID | 处置 | 受控标题 | 原始来源 | 唯一规范落点 | 最低案例 |
|---|---|---|---|---|---|
| `REQ-NUTR-001` | 0.1 必须 | 标签、同商品历史、权威公共库、可信联网、模板的来源优先级 | 会话23、40 | §13.1—§13.4 | `CASE-NUTR-001`、`CASE-NUTR-002`、`CASE-NUTR-005`、`CASE-NUTR-010` |
| `REQ-NUTR-002` | 0.1 必须 | 来源注册、许可、缓存、限流、失败降级和最小披露 | 会话23、40、42 | §13.4—§13.5、§13.17—§13.18 | `CASE-PRIV-001`、`CASE-NUTR-006`、`CASE-NUTR-010`、`CASE-NUTR-011` |
| `REQ-NUTR-003` | 0.1 必须 | 版本化NutritionProfile与不可变摄入Snapshot | 会话23—24、40 | §13.6—§13.8、§13.19 | `CASE-NUTR-001`、`CASE-NUTR-005` |
| `REQ-NUTR-004` | 0.1 必须 | 有限模糊量取合理上界并公开克重/范围 | 会话23、33、35 | §13.9、§18.6—§18.7 | `CASE-MEAL-003`、`CASE-NUTR-008` |
| `REQ-NUTR-005` | 0.1 必须 | 明确数量不标估算；估算只落实际推定字段 | 会话33、35 | §13.12、§18.6 | `CASE-MEAL-006`、`CASE-NUTR-009` |
| `REQ-NUTR-006` | 0.1 必须 | unknown不补0，已知部分必须披露覆盖 | 会话23、36、40 | §13.16、§19.5 | `CASE-NUTR-006`、`CASE-QUERY-002` |
| `REQ-SOURCE-001` | 0.1 必须 | 营养来源适配器、主备后端、Tier、真实Probe/Doctor、显式授权与安全配置 | 会话49批准、Agent Reach说明 | §13.20—§13.22、§21.17、§23.7 | `CASE-SOURCE-001`、`CASE-SOURCE-002`、`CASE-SOURCE-003` |
| `REQ-RESEARCH-001` | 0.2 必须 | 多来源证据先行、实体锚定、聚类、可信度门、部分覆盖与`nothing_solid` | 会话49批准、last30days说明 | §13.23—§13.25、§23.7 | `CASE-RESEARCH-001`、`CASE-RESEARCH-002`、`CASE-RESEARCH-004` |
| `REQ-RESEARCH-002` | 0.2 必须 | 研究历史、去重、Watchlist、变更预览及确认后Profile更新边界 | 会话49批准、两份技术说明 | §13.26—§13.28、§23.7 | `CASE-RESEARCH-003`、`CASE-RESEARCH-005`、`CASE-RESEARCH-006` |
| `REQ-TEMPLATE-001` | 0.2 必须 | 个人菜品一次候选、两次激活、三次稳定 | 会话24、40 | §14.1—§14.5 | `CASE-TEMPLATE-001`、`CASE-TEMPLATE-002`、`CASE-TEMPLATE-003`、`CASE-TEMPLATE-005` |
| `REQ-TEMPLATE-002` | 0.2 必须 | 个人可变、新版本、单次覆盖和只学习用户证据 | 会话24、40 | §14.4—§14.5、§14.9—§14.10 | `CASE-TEMPLATE-004`、`CASE-TEMPLATE-005` |
| `REQ-ISSUE-001` | 0.1 必须 | Issue稳定代码、单一字段模型和四状态生命周期 | 会话12、14—15、37 | §8.2—§8.3、§8.12 | `CASE-ISSUE-001`、`CASE-ISSUE-003` |
| `REQ-ISSUE-002` | 0.1 必须 | 非阻塞问题集中提示、延期、查询和覆盖影响 | 会话12、15、37 | §8.1、§8.4、§8.7—§8.13 | `CASE-ISSUE-001`、`CASE-ISSUE-003` |
| `REQ-QUICK-001` | 0.1 必须 | 2—4快捷项、组合/自然语言、末行自由文本、执行前复核 | 会话15、21、37 | §8.4—§8.5、§8.10—§8.11 | `CASE-RECEIPT-003`、`CASE-INVENTORY-006` |
| `REQ-CORR-001` | 0.1 必须 | 追加式定位、纠正、撤销与恢复 | 会话25、38 | §15.1—§15.7 | `CASE-CORR-001`、`CASE-CORR-003`、`CASE-CORR-004`、`CASE-CORR-005` |
| `REQ-CORR-002` | 0.1 必须 | 库存返还/补扣、营养重算、跨日进度和幂等补偿 | 会话38 | §15.11—§15.16 | `CASE-CORR-001`、`CASE-CORR-002`、`CASE-INVENTORY-008` |
| `REQ-CORR-003` | 0.2 必须 | 跨餐/跨日范围批量纠正和撤销预览、确认与安全提交 | 会话38 | §15.17 | `CASE-CORR-006`、`CASE-CORR-007` |

### 30.4 白水、进度、回执、查询与混合结果

| ID | 处置 | 受控标题 | 原始来源 | 唯一规范落点 | 最低案例 |
|---|---|---|---|---|---|
| `REQ-WATER-001` | 0.1 必须 | 只有白水进入diet_water，营养饮品归meal | 会话26 | §16.1、§16.4 | `CASE-WATER-001`、`CASE-WATER-003`、`CASE-WATER-004` |
| `REQ-WATER-002` | 0.1 必须 | 容量谨慎换算、食物水分不超液体量且不双计 | 会话26 | §16.2—§16.3 | `CASE-WATER-002`、`CASE-WATER-005`、`CASE-WATER-006`、`CASE-PROGRESS-008` |
| `REQ-PROGRESS-001` | 0.1 必须 | 六目标、固定顺序、每项两行和10格条 | 会话26 | §17.1、§17.5—§17.10 | `CASE-PROGRESS-001`、`CASE-PROGRESS-002`、`CASE-PROGRESS-003`、`CASE-PROGRESS-004`、`CASE-PROGRESS-005` |
| `REQ-PROGRESS-002` | 0.1 必须 | 成功回执只用同次最终daily_progress，不额外报告读取 | 会话26、30—31 | §17.2—§17.4 | `CASE-PROGRESS-006`、`CASE-STORAGE-006` |
| `REQ-PROGRESS-003` | 0.1 必须 | 未知纤维和饮水已知下界，统一`CoverageStatus=partial` | 会话26、40 | §17.9—§17.9.1、§23.2 | `CASE-PROGRESS-004`、`CASE-PROGRESS-009` |
| `REQ-PROGRESS-004` | 0.1 必须 | 信封级增量、终态幂等冻结和跨日进度数组 | 会话38、41—42 | §17.2—§17.3、§17.13、§20.20 | `CASE-PROGRESS-010`、`CASE-PROGRESS-011`、`CASE-STORAGE-006` |
| `REQ-RECEIPT-001` | 0.1 必须 | 标题单行、每菜/食物/饮品一行、菜内紧凑 | 会话29—35 | §18.1—§18.4 | `CASE-RECEIPT-001`、`CASE-RECEIPT-006` |
| `REQ-RECEIPT-002` | 0.1 必须 | 字段级证据标签、自然单位克重、无重复本轮营养 | 会话30、33、35 | §18.2、§18.6—§18.8 | `CASE-RECEIPT-002`、`CASE-RECEIPT-004` |
| `REQ-RECEIPT-003` | 0.1 必须 | 进度块之后无未经请求评价、警告或建议 | 会话26 | §17.11—§17.12 | `CASE-RECEIPT-004`、`CASE-RECEIPT-005` |
| `REQ-QUERY-001` | 0.1 必须 | 日明细、当前有效视图和可读歧义定位 | 会话36、38 | §19.1—§19.4、§19.9 | `CASE-QUERY-001`、`CASE-QUERY-004`、`CASE-QUERY-005` |
| `REQ-QUERY-002` | 0.1 必须 | 一次聚合、覆盖/unknown和日进度 | 会话36 | §19.5—§19.6、§19.10 | `CASE-QUERY-002`、`CASE-QUERY-003` |
| `REQ-QUERY-003` | 0.1 必须 | 用户请求时最小周六项聚合与历史目标 | 会话36 | §19.7 | `CASE-PROGRESS-007`、`CASE-PROGRESS-012` |
| `REQ-REVIEW-001` | 0.2 必须 | 丰富周回顾、趋势、常用项、快速复用和逐项状态的批量补录 | 会话8推理、36 | §19.8、§25.6 | `CASE-REVIEW-001`、`CASE-REVIEW-002`、`CASE-REVIEW-003`、`CASE-REVIEW-004`、`CASE-REVIEW-005` |
| `REQ-MIXED-001` | 0.1 必须 | 叙述顺序、逐子操作FactCommit/EffectBundle和正式结果枚举 | 会话25、41—42 | §20.15—§20.20、§21.5 | `CASE-MIXED-001`、`CASE-MIXED-002`、`CASE-MIXED-003`、`CASE-EFFECT-001` |

### 30.5 安全、运维、导出与非功能约束

| ID | 处置 | 受控标题 | 原始来源 | 唯一规范落点 | 最低案例 |
|---|---|---|---|---|---|
| `REQ-SAFE-001` | 0.1 必须 | 八类数据/执行根、固定正式路径和隔离测试根 | 会话1、42 | §21.2—§21.3、§29.5 | `CASE-STORAGE-004`、`CASE-FOUNDATION-002` |
| `REQ-SAFE-002` | 0.1 必须 | FactCommit、durable outbox、EffectBundle与EnvelopeFinalize故障隔离 | 会话12、42及独立复核 | §20.16、§20.20、§21.5 | `CASE-EFFECT-001`、`CASE-EFFECT-002`、`CASE-EFFECT-003` |
| `REQ-SAFE-003` | 0.1 必须 | 全副作用幂等、同键冲突、并发、响应丢失和终态冻结 | 会话38、42 | §15.16、§20.20、§21.6—§21.8 | `CASE-STORAGE-001`、`CASE-STORAGE-002`、`CASE-STORAGE-006`、`CASE-STORAGE-007`；`CASE-STORAGE-003`仅为A历史参考并由发布选择器排除 |
| `REQ-SAFE-004` | 0.1 必须 | 验证前后manifest、失败照扫、sidecar和零正式数据差异 | 会话1、42 | §21.10—§21.15 | `CASE-FOUNDATION-001`、`CASE-FOUNDATION-002` |
| `REQ-PRIV-001` | 0.1 必须 | 联网最小披露、日志脱敏和秘密隔离 | 会话40、42 | §13.17、§21.16—§21.17 | `CASE-PRIV-001`、`CASE-PRIV-002` |
| `REQ-NFR-001` | 0.1 必须 | 明确常见记录一轮完成，歧义只问最小关键问题 | 会话8推理、12、15 | §5.1、§8.4 | `CASE-MEAL-001`、`CASE-ISSUE-001` |
| `REQ-NFR-002` | 0.1 必须 | 确定性十进制、unknown不造值、投影可重建 | 会话23、35、42 | §13.12、§23 | `CASE-NUTR-006`；`CASE-STORAGE-003`仅为A历史参考并由B发布选择器排除 |
| `REQ-NFR-003` | 0.1 必须 | 候选、阈值、取整和B-only门条件使用版本化策略而非临场判断；不保留三路评分选路 | 独立复核，承接会话15、19、23—24、46 | §23.6、§26.5 | `CASE-POLICY-001`、`CASE-POLICY-002` |
| `REQ-OPS-001` | 0.1 必须 | 单一安装入口、只读预检、隔离冒烟和支持矩阵 | 会话3—4、43 | §22.3—§22.10 | `CASE-OPS-001`、`CASE-OPS-007` |
| `REQ-OPS-002` | 0.1 必须 | 升级先备份、迁移回滚、恢复先临时验证 | 会话43 | §22.11—§22.17 | `CASE-OPS-002`、`CASE-OPS-003`、`CASE-OPS-004`、`CASE-STORAGE-005` |
| `REQ-OPS-003` | 0.1 必须 | 默认卸载保留数据；整库删除精确预览和二次确认 | 会话43 | §22.19—§22.21 | `CASE-OPS-005`、`CASE-OPS-006`、`CASE-OPS-008` |
| `REQ-OPS-004` | 0.1 必须 | 不可变候选、逐文件hash、相同字节原样晋级 | 会话43及计划治理 | §22.22、§26.6 | `CASE-OPS-009`、`CASE-OPS-010` |
| `REQ-EXPORT-BASE-001` | 0.1 必须 | 可恢复备份与最小用户数据导出明确分离 | 会话43、推理需求 | §22.15—§22.18 | `CASE-EXPORT-003`、`CASE-EXPORT-004` |
| `REQ-EXPORT-RICH-001` | 0.2 必须 | 日期/字段筛选、CSV/分析导出与匿名化选项 | 会话8推理、43 | §22.18、§25.8 | `CASE-EXPORT-001`、`CASE-PRIV-001`、`CASE-PRIV-002` |
| `REQ-LIFE-001` | 0.2 必须 | 主动临期提醒、去重/延期、库存调整，以及多次一致确认后可见启用和停用个人位置规则 | 会话19—21、39及推理需求 | §11.11—§11.12、§11.15、§25.6 | `CASE-LIFE-001`、`CASE-LIFE-002`、`CASE-LIFE-003`、`CASE-LIFE-004`、`CASE-LIFE-005`、`CASE-LIFE-006`、`CASE-LIFE-007` |

---

## 31. 唯一任务台账与当前控制台

### 31.1 台账解释、根配置和证据类型

本节是实施状态的唯一可写位置。§1、§26和本节控制台只从下表派生；若数字不一致，以逐任务行和实际EV为准并修复派生视图。

数据根配置：

| 代码 | `official_data_roots` | `isolated_test_roots` |
|---|---|---|
| `ROOT-DOC` | 解析项目内可能正式根并只读manifest；禁止业务写入 | `%TEMP%/diet-manager-doc/<task>/<guid>`，每次唯一并清理 |
| `ROOT-SHARED` | 全部正式根只读并做前后manifest | `%TEMP%/diet-manager-shared/isolated-test/<task>/<guid>`；同次validation/build/openclaw根使用相同`task/guid`的类型兄弟前缀，四根互不包含 |
| `ROOT-A` | A正式固定JSONL根只读 | `%TEMP%/diet-manager-a/<task>/<guid>` |
| `ROOT-B` | B正式SQLite根只读 | `%TEMP%/diet-manager-b/<task>/<guid>` |
| `ROOT-C` | C正式SQLite/预览根只读 | `%TEMP%/diet-manager-c/<task>/<guid>` |
| `ROOT-REL` | 候选包只读；用户正式根只做预检/备份，不用于冒烟 | `%TEMP%/diet-manager-release/<candidate-hash>/<guid>` |

所有`%TEMP%`表示系统临时目录下的唯一绝对路径，不允许由模型传入。永久EV、机器报告和引用后不可覆盖的日志只写§21.2的`evidence_root`，当前项目映射为`docs/evidence/<EV-ID>...`；它不属于任何`isolated_test_root`。每项证据均须满足§28.1。证据类型缩写：`E-DOC`=文档/追踪结构；`E-CONTRACT`=契约+独立复核；`E-MODEL`=Schema正反例/映射；`E-CASE`=共同Oracle和黄金输出；`E-STOR`=并发/故障/重启/manifest；`E-GATE`=B-only门判定和精确任务处置；`E-REL`=不可变候选安装/迁移/恢复/零污染。

为避免再建一张重复任务表，逐任务“目标”单元末尾必须内嵌两个可机器解析字段：`R[REQ-ID,...]`是该任务贡献或直接负责的精确需求，`C[CASE-ID,...]`只列该任务产生局部断言或明确逐案执行完整Oracle的案例；阶段批量整案责任由brief中的`full_case_set`独立补充，不把124或152个B发布案例复制进C[]。不得写“核心REQ”“全案例”“等”或ID范围代替展开；多个ID逐个用英文逗号分隔。每个非治理任务的C[]案例必须在启动brief中依§24.4.1展开为`case_assertion_paths`；纯治理或用户批准任务允许`R[]；C[]`，但必须紧跟`NA=governance`或`NA=user_approval`说明。

单个组件任务完成只检查它自己的`case_assertion_paths`，不会因为出现在某需求的`R[]`中，就自动继承§30该需求的全部业务案例，也不会因C[]引用了一个端到端案例而被迫依赖尚未实现的下游断言；例如来源注册表契约任务通过来源结构路径，不代表营养摄入整案已通过。没有路径声明时不得开始任务；只有C[]路径明确为`["*"]`或拥有非`none`的`full_case_set`时，任务才能产生整案通过结论，而且任务完成必须同时满足二者。§30“最低案例”用于需求级发布覆盖：G2-B-only只要求门任务与M4风险切片按固定选择器完成B整案，并静态证明B后继任务的`R[]`并集覆盖全部0.1需求；DOC-0.4目标发布集为G3-0.1累计124案、G3-0.2累计152案，须由`SH-TRACE-001`重生成选择器并在同一不可变B候选的EV中通过。`CASE-STORAGE-003`不在这两个发布集内。这样既不借用证据，也不在M5—M7实现前伪造候选级全案例结论。

§31紧凑行、上表`ROOT-*`展开和`docs/work-items/<task_id>-brief.md`共同组成§4.5的完整任务记录。任何`未开始`任务在转为`进行中`前，brief必须逐项给出精确`verification_commands`、`case_assertion_paths`、`full_case_set`、完整Oracle、`risk_ids/decision_ids/change_ids`、解析后的绝对正式/隔离根、执行者和复核者；校验器必须验证所认领路径可由本任务交付物或已完成依赖实现，缺任一字段或存在依赖倒挂时拒绝状态转换。已完成的遗留foundation任务由EV-009结构化元数据承担brief等价信息并按其完整运行结果认定`*`，`SH-CONTRACT-003`由既有work-item包与EV-007共同承担；二者均不得外推未执行的业务案例。

### 31.2 当前派生控制台

| 指标 | 当前值 | 推导 |
|---|---|---|
| 总任务 | 63 | §31.3—§31.8逐行计数；0.4新增`DOC-004-001`、`DOC-004-002`、`SEL-RESEARCH-001`、`SEL-NUTR-WATCH-001` |
| 已完成 | 28 | `SH-TRACE-001`重开刷新由`EV-20260813-035`关闭；B-SLICE/B-FAULT及其证据状态不变 |
| 进行中 | 1 | `X-GATE-002`：运行有界预检、原子生成本地map并完成两轮独立复核；不得并行开始产品实现 |
| 未开始 | 26 | 全部`SEL-*`继续等待`X-GATE-002`正式关闭 |
| 阻塞/已取消 | 0/8 | 追踪前置已解除；A/C各4项M3/M4完整实现任务按`DEC-027`取消 |
| 当前WIP | `X-GATE-002` | 只完成B-only就绪与路径绑定门；模板或候选本地map不能单独解锁产品任务 |
| 最近新鲜证据 | `EV-20260813-035`；必要安全底座=`EV-20260811-012` | EV35只关闭追踪刷新；X-GATE仍须实际预检、map与两轮独立复核 |
| 产品可安装性 | 不可安装 | 0.1业务、正式存储、安装和G3均未完成 |
| 当前产品主线 | B-only | 用户直接裁决，不是G1/G2评分；A只读降级，C停止并将安全控制合并入B |
| 下一产品动作 | 完成`X-GATE-002` | 有界预检全部通过后才原子生成本地map并进入两轮独立复核；正式`bind_b_ready`前不得开始`SEL-*` |

### 31.3 M0—M1：基底、计划、安全基线与共同契约

| task_id | M/类型/范围 | 目标与REQ/CASE | 依赖 | 交付物 | 验证/所需证据 | roots | actual_evidence_ids | owner/reviewer | 状态 | blocker、next_action、reopen_condition |
|---|---|---|---|---|---|---|---|---|---|---|
| `A-FND-001` | M0/foundation/A | A Skill结构、固定JSONL文字边界；R[REQ-SCOPE-003]；C[CASE-FOUNDATION-001] | 无 | A现有Skill/storage/test | A结构脚本；`E-STOR`空数据基线 | `ROOT-A` | `EV-20260809-009`、`EV-20260809-011`（均为变更前历史） | Codex `/root` / 独立事实复核 | 未开始 | `reopened_waiting_revalidation`；reason=SH-SAFE测试文件变化；next=SH-SAFE终态后按WIP复验；代码、依赖、路径或验证器变更即重开 |
| `B-FND-001` | M0/foundation/B | B插件外壳7项、构建和OpenClaw校验；R[REQ-SCOPE-003]；C[CASE-FOUNDATION-001] | 无 | B现有src/manifest/test | Vitest7/7、tsc、plugin validate；`E-STOR` | `ROOT-B` | `EV-20260809-009`、`EV-20260809-011` | 历史实现/Codex复核 | 已完成 | 无；源码、Skill、依赖、Node或OpenClaw变化即重开 |
| `C-FND-001` | M0/foundation/C | C状态外壳7项、构建和OpenClaw校验；R[REQ-SCOPE-003]；C[CASE-FOUNDATION-001] | 无 | C现有src/manifest/test | Vitest7/7、tsc、plugin validate；`E-STOR` | `ROOT-C` | `EV-20260809-009`、`EV-20260809-011` | 历史实现/Codex复核 | 已完成 | 无；源码、Skill、依赖、Node或OpenClaw变化即重开 |
| `SH-FND-001` | M0/validator/shared | 统一调用三基底、状态隔离和空数据后置扫描；R[REQ-SAFE-004]；C[CASE-FOUNDATION-001,CASE-FOUNDATION-002] | `A-FND-001`、`B-FND-001`、`C-FND-001` | `shared/validate-foundations.ps1`、隔离回归 | 总验证+隔离回归；`E-STOR` | `ROOT-SHARED` | `EV-20260809-009`、`EV-20260809-011`（均为变更前历史） | Codex `/root` / 独立事实复核 | 未开始 | `reopened_waiting_revalidation`；reason=隔离/manifest测试变化；next=SH-SAFE终态后按WIP复验；脚本、Skill边界或根行为变更即重开 |
| `SH-FND-REFRESH-001` | M0/verification/shared | 当前文件版本新鲜重验并确认不创建JSONL/SQLite；R[REQ-SAFE-004]；C[CASE-FOUNDATION-001,CASE-FOUNDATION-002] | `SH-FND-001`、Node≥24.15 | EV报告和捕获输出 | A+B+C真实运行、sidecar/残留复核；`E-STOR` | `ROOT-SHARED` | `EV-20260809-009`、`EV-20260809-011`（均为变更前历史） | Codex `/root` / 独立事实复核 | 未开始 | `reopened_waiting_revalidation`；reason=foundation相关测试变化；next=SH-SAFE终态后按WIP复验；foundation相关文件、Skill边界或验证器变化即重开 |
| `DOC-004-001` | M1/document/shared | 从最新DOC-0.3与两份冻结技术说明生成DOC-0.4，吸收来源能力/研究证据边界并保持B进度和门顺序；R[]；C[]；NA=governance | `DOC-003-002`、`B-FAULT-001` | `总功能开发计划0.4.md`、0.3基线/资料hash、影响分析和复核包 | 0.3不变、ID唯一、需求/案例/任务闭合、范围无偷跑；`E-DOC` | `ROOT-DOC` | `EV-20260813-033` | Codex `/root` / 独立文档与产品边界复核 | 已完成 | 已完成结构校验与批准前治理修订；0.3基线、资料hash或吸收边界变化即重开 |
| `DOC-004-002` | M1/approval/shared | 用户审阅并批准DOC-0.4成为新计划入口；R[]；C[]；NA=user_approval | `DOC-004-001` | `docs/work-items/DOC-004-approval.md`、计划入口切换授权 | 用户明确批准；`E-DOC` | `ROOT-DOC` | `EV-20260813-033` | 用户/独立文档复核者 | 已完成 | 用户批准已记录，DOC-0.4已切换为唯一现行入口；用户撤回/改变研究边界即重开 |
| `DOC-003-001` | M1/document/shared | DOC-0.3历史治理：初始会话重建、SSOT、旧需求/任务/文件图一致性；R[]；C[]；NA=governance | `SH-FND-REFRESH-001` | `总功能开发计划0.3.md`、附录A、历史校验报告 | 只读历史身份和引用完整；`E-DOC` | `ROOT-DOC` | `EV-20260809-010`、`EV-20260809-011`（历史） | Codex `/root` / 独立文档复核 | 已完成 | 0.3已冻结为0.4基线；只有基线身份或历史引用损坏才重开，不再承载现行新增需求 |
| `DOC-003-002` | M1/approval/shared | 历史用户批准DOC-0.3成为当时计划入口；R[]；C[]；NA=user_approval | `DOC-003-001` | 批准记录、`START-HERE.md`历史入口变更授权 | 用户明确批准；`E-DOC` | `ROOT-DOC` | `EV-20260809-011` | 用户/独立文档复核者 | 已完成 | 历史批准不再赋予DOC-0.3当前权威；现行入口唯一为DOC-0.4。用户撤回批准或改变 Skill-first 边界则重开`DOC-003-001`与本任务 |
| `SH-SAFE-BASE-001` | M1/safety/shared | 预置正式数据hash不变、失败照扫、sidecar、临时状态删除前审计和源码外构建；R[REQ-SAFE-001,REQ-SAFE-004]；C[CASE-STORAGE-004,CASE-FOUNDATION-002] | `DOC-003-002` | `shared/validate-foundations.ps1`、`shared/private/foundation-validation-core.ps1`、`shared/tests/validate-data-manifests.ps1`、`shared/tests/validate-foundations-state-isolation.ps1` | 每阶段失败矩阵+预置JSONL/SQLite/WAL；`E-STOR` | `ROOT-SHARED` | `EV-20260811-012` | Codex `/root` / 独立安全复核 | 已完成 | 按`DEC-028`以ROOT/TEMP/REPORT/MANIFEST focused、4项状态隔离、零业务候选和零fixture残留完成有界退出验收；conhost精确计数deferred；验证器、正式数据根或风险边界变化即重开 |
| `SH-CONTRACT-001` | M1/contract/shared | CONTRACT-v2：命令、FactCommit/EffectBundle/EnvelopeFinalize、幂等和结果枚举；R[REQ-CORE-001,REQ-CORE-003,REQ-MIXED-001]；C[CASE-EFFECT-001,CASE-EFFECT-003,CASE-MIXED-002,CASE-STORAGE-001,CASE-STORAGE-007] | `DOC-003-002`、`SH-SAFE-BASE-001` | 更新`shared/business-contract.md`、`shared/tests/validate-business-contract-v2.ps1`、`docs/work-items/SH-CONTRACT-001-v2-{brief,report,review-package,review}.md` | 契约差异、失效规则扫描、独立复核；`E-CONTRACT` | `ROOT-DOC` | `EV-20260811-013` | Codex `/root` / OpenClaw独立契约复核 | 已完成 | CONTRACT-v2机器块、失败零业务数据、五种业务结果、幂等、服务端预览绑定、77条范围和十项旧规则均通过；F1–F9/NUTR第二轮独立复核PASS；契约语义或冻结hash变化即重开 |
| `SH-CONTRACT-002` | M1/contract/shared | 日期/回执v2：OccurredTime、字段级估算、跨日数组和冻结进度；R[REQ-TIME-002,REQ-PROGRESS-004,REQ-RECEIPT-001,REQ-RECEIPT-002,REQ-RECEIPT-003]；C[CASE-DATE-001,CASE-PROGRESS-011,CASE-RECEIPT-001,CASE-RECEIPT-004] | `SH-CONTRACT-001` | 更新`shared/contracts/receipt-and-date-contract.md`、`shared/tests/validate-receipt-and-date-contract-v2.ps1`、`docs/work-items/SH-CONTRACT-002-v2-{brief,report,review-package,review}.md`和黄金规范 | 合同验证+黄金回执+独立复核；`E-CONTRACT` | `ROOT-DOC` | `EV-20260811-014` | Codex `/root` / OpenClaw独立契约复核 | 已完成 | 11条范围、OccurredTime、6项黄金重算、单日别名/跨日数组、pending/failed无成功回执与B-only均PASS；语义、格式或冻结hash变化即重开 |
| `SH-CONTRACT-003` | M1/contract/shared | 营养来源、许可、缓存、白名单和失败映射；R[REQ-NUTR-001,REQ-NUTR-002]；C[CASE-NUTR-010,CASE-NUTR-011] | `EV-20260809-005`所冻结的CONTRACT-v1文件hash | `shared/nutrition-source-registry.json`和工作包 | 16/16语义、36/36指针、独立复核；`E-CONTRACT` | `ROOT-DOC` | `EV-20260809-007` | Codex协作实现/独立复核 | 已完成 | 无；CONTRACT-v2影响来源语义、外部许可或注册表变化即重开复验 |
| `SH-CONTRACT-004` | M1/contract/shared | Issue、快捷、纠正、补偿、outbox和跨日结果契约；R[REQ-ISSUE-001,REQ-ISSUE-002,REQ-QUICK-001,REQ-CORR-001,REQ-CORR-002,REQ-CORR-003]；C[CASE-ISSUE-001,CASE-ISSUE-003,CASE-CORR-001,CASE-EFFECT-002] | `SH-CONTRACT-001`、`SH-CONTRACT-002` | `shared/contracts/issue-correction-contract.md`、专用v2验证器及review工作包 | 正常/业务降级/技术失败/幂等/批量预览矩阵；`E-CONTRACT` | `ROOT-DOC` | `EV-20260811-015` | Codex `/root` / OpenClaw第二轮独立契约复核 | 已完成 | 四状态Issue、追加纠正、阶段所有权、失败零业务数据和批量预览均PASS；契约语义或冻结hash变化即重开 |
| `SH-DECISION-001` | M1/policy/shared | 候选、临期、模板容差、取整和B-only门条件机器策略；R[REQ-NFR-003]；C[CASE-POLICY-001,CASE-POLICY-002] | `SH-CONTRACT-001`、`SH-CONTRACT-004` | `shared/policies/decision-thresholds.json`、54项边界fixture、专用验证器与review工作包 | 8条临期规则/16边界、decimal half-up、4个mutation与独立复核；无三路评分；`E-CONTRACT` | `ROOT-DOC` | `EV-20260811-016` | Codex `/root` / OpenClaw独立产品复核 | 已完成 | Q-005只关闭v1来源/提醒覆盖；阈值、来源、规则、fixture或冻结hash变化即L3重开；不代表G1/G2通过 |

### 31.4 M2：模型、Schema、案例、黄金回执和追踪

| task_id | M/类型 | 目标与REQ/CASE | dependencies | deliverables | verification/required evidence | roots | actual EV | owner/reviewer | 状态 | next/reopen |
|---|---|---|---|---|---|---|---|---|---|---|
| `SH-MODEL-001` | M2/model | Event/Amount/Product/Inventory原记录与投影；R[REQ-EVENT-001,REQ-MEAL-002,REQ-PRODUCT-001,REQ-PANTRY-004,REQ-CORE-002]；C[CASE-CORR-001,CASE-MEAL-003,CASE-PURCHASE-007,CASE-INVENTORY-005,CASE-RECEIPT-002] | `SH-CONTRACT-001`、`SH-CONTRACT-002`、`SH-CONTRACT-003`、`SH-CONTRACT-004`、`SH-DECISION-001` | event/amount和product/inventory Schema | 正反例、四量分离、投影重建；`E-MODEL` | `ROOT-SHARED` | `EV-20260811-017` | Codex/OpenClaw独立模型复核 | 已完成 | 41例+4mutation+Ajv独立PASS；Schema/fixture/契约变化重开 |
| `SH-MODEL-002` | M2/model | Nutrition/Profile/Snapshot/GoalVersion/DailyProgress与统一`CoverageStatus`；R[REQ-NUTR-003,REQ-NUTR-006,REQ-PROGRESS-001,REQ-PROGRESS-004]；C[CASE-NUTR-005,CASE-NUTR-006,CASE-PROGRESS-001,CASE-PROGRESS-011] | `SH-CONTRACT-002`、`SH-CONTRACT-003`、`SH-MODEL-001` | nutrition/progress Schema | 十进制、unknown、四值覆盖类型、目标版本、跨日数组；`E-MODEL` | `ROOT-SHARED` | `EV-20260811-018` | Codex/OpenClaw独立模型复核 | 已完成 | 42例+4mutation+Ajv跨`$id`独立PASS；Schema/fixture/契约变化重开 |
| `SH-MODEL-003` | M2/model | Issue/Correction/FactCommit/EffectBundle/EnvelopeFinalize/MixedResult/ReceiptData；R[REQ-SAFE-002,REQ-SAFE-003]；C[CASE-EFFECT-001,CASE-EFFECT-003,CASE-STORAGE-006,CASE-STORAGE-007] | `SH-CONTRACT-004`、`SH-MODEL-001`、`SH-MODEL-002` | issue-correction-mixed Schema | outbox状态、故障、冻结结果正反例；`E-MODEL` | `ROOT-SHARED` | `EV-20260811-019` | Codex `/root` / OpenClaw独立语义与安全复核 | 已完成 | 12定义/65例/10项semantic-only/10 invariant/4 mutation与双引擎独立复核PASS；Schema/fixture/契约变化重开 |
| `SH-MAP-001` | M2/mapping | 共同模型到B SQLite精确映射、A只读降级边界和C安全控制合并点；R[REQ-EVENT-001,REQ-SAFE-001,REQ-SAFE-002]；C[CASE-EFFECT-001] | `SH-MODEL-001`、`SH-MODEL-002`、`SH-MODEL-003` | `shared/contracts/storage-mapping.md` | B字段覆盖100%、约束/索引/迁移/恢复矩阵，A/C无独立写入；`E-MODEL` | `ROOT-SHARED` | `EV-20260811-020` | Codex `/root` / OpenClaw独立B存储复核 | 已完成 | 34定义/20表/18索引/22外键/18本地mutation、第三轮P0=0/P1=0与33独立mutation、GitHub clone均PASS；映射或上游Schema变化重开 |
| `SH-CASE-001` | M2/cases | 饮食/时间/白水/回执/查询Oracle；R[REQ-MEAL-001,REQ-TIME-001,REQ-WATER-001,REQ-RECEIPT-001,REQ-QUERY-001]；C[CASE-MEAL-001,CASE-MEAL-021,CASE-WATER-001,CASE-RECEIPT-001,CASE-QUERY-001] | `SH-CONTRACT-001`、`SH-CONTRACT-002`、`SH-CONTRACT-003`、`SH-CONTRACT-004`、`SH-MODEL-001`、`SH-MODEL-002`、`SH-MODEL-003` | cases+fixtures core | 每案固定时钟、目标、expected/forbidden；`E-CASE` | `ROOT-SHARED` | `EV-20260811-021` | Codex `/root` / OpenClaw独立产品案例复核 | 已完成 | 5 cases/3 fixtures/8本地mutations/20独立mutations、P0=0/P1=0、7项验证和GitHub clone均PASS；规则、fixture或相关契约变化重开 |
| `SH-CASE-002` | M2/cases | 入库/库存/营养/Issue/纠正/混合/Effect/最终器故障Oracle；R[REQ-PURCHASE-001,REQ-PANTRY-002,REQ-NUTR-001,REQ-ISSUE-001,REQ-CORR-001,REQ-MIXED-001,REQ-SAFE-002,REQ-SAFE-003]；C[CASE-PURCHASE-001,CASE-INVENTORY-003,CASE-NUTR-001,CASE-ISSUE-001,CASE-CORR-001,CASE-MIXED-001,CASE-EFFECT-001,CASE-EFFECT-003,CASE-STORAGE-007] | `SH-CONTRACT-001`、`SH-CONTRACT-002`、`SH-CONTRACT-003`、`SH-CONTRACT-004`、`SH-MODEL-001`、`SH-MODEL-002`、`SH-MODEL-003`、`SH-CASE-001` | cases+fixtures domain | 库存前后、来源、补偿、故障矩阵；`E-CASE` | `ROOT-SHARED` | `EV-20260811-022` | Codex `/root` / OpenClaw独立领域/故障案例复核 | 已完成 | 累计14案/12 fixture对象/11本地mutation/11独立mutation、第二轮P0=0/P1=0、8项回归与GitHub clone均PASS；规则、fixture或相关契约变化重开 |
| `SH-CASE-003` | M2/cases | 隐私、foundation、迁移、安装、恢复、删除Oracle；R[REQ-PRIV-001,REQ-SAFE-004,REQ-OPS-001,REQ-OPS-002,REQ-OPS-003,REQ-OPS-004,REQ-EXPORT-BASE-001]；C[CASE-PRIV-001,CASE-FOUNDATION-002,CASE-OPS-001,CASE-OPS-003,CASE-OPS-010,CASE-EXPORT-004] | `SH-SAFE-BASE-001`、`SH-MODEL-001`、`SH-MODEL-002`、`SH-MODEL-003` | cases+fixtures ops/security | 正式manifest、外部白名单、失败/清理；`E-CASE` | `ROOT-SHARED` | `EV-20260811-023` | Codex `/root` / OpenClaw 02独立安全复核 | 已完成 | 6 cases/6 scenarios/12本地mutations、九项回归、public GitHub独立clone与三组checker均PASS，P0=0/P1=0/P2=2；案例、fixture、隐私/安装/迁移/恢复边界或相关契约变化即重开 |
| `SH-CASE-004` | M2/golden | 单日、多菜、Issue快捷、混合、技术pending、跨日回执；R[REQ-RECEIPT-001,REQ-RECEIPT-002,REQ-RECEIPT-003,REQ-PROGRESS-002,REQ-PROGRESS-004]；C[CASE-RECEIPT-001,CASE-RECEIPT-002,CASE-RECEIPT-004,CASE-RECEIPT-005,CASE-RECEIPT-006,CASE-PROGRESS-006,CASE-STORAGE-006,CASE-EFFECT-003] | `SH-CONTRACT-002`、`SH-CONTRACT-004`、`SH-CASE-001`、`SH-CASE-002`、`SH-CASE-003` | golden-receipts目录 | 结构和黄金文本双断言；`E-CASE` | `ROOT-SHARED` | `EV-20260812-024` | Codex `/root` / OpenClaw 02独立黄金Oracle复核 | 已完成 | 8文本/6新增cases/12 mutation/十项回归、public clone复核P0=0/P1=0/P2=2；黄金字节、案例或相关契约变化重开 |
| `SH-HARNESS-001` | M2/harness | 同一Oracle经B执行适配器运行并校验CONTRACT hash；A只运行只读/无插件降级断言，C控制由B适配器覆盖；R[REQ-NFR-002,REQ-SAFE-003]；C[CASE-STORAGE-001,CASE-STORAGE-007,CASE-EFFECT-001,CASE-EFFECT-003] | `SH-MAP-001`、`SH-CASE-001`、`SH-CASE-002`、`SH-CASE-003`、`SH-CASE-004` | B adapter+A降级adapter+run-all+机器报告 | B使用共同fixture、无私改expected；A/C不得形成独立完整产品结果；`E-CASE` | `ROOT-SHARED` | `EV-20260812-025` | Codex `/root` / OpenClaw 02独立public-clone复核 | 已完成 | 1.4.0/27案、15项Node测试、12组回归、P0=0/P1=0；backend仍pending；契约/adapter变化重开 |
| `SH-TRACE-001` | M2/governance | REQ→CASE→TASK→EV和DEC/RISK/CHG无孤儿、新鲜度与阶段校验；R[]；C[]；NA=governance（追踪校验器） | `DOC-004-002`、`SH-CONTRACT-001`、`SH-CONTRACT-002`、`SH-CONTRACT-003`、`SH-CONTRACT-004`、`SH-CASE-004`、`SH-HARNESS-001` | trace JSON镜像+validator | ID唯一、引用存在、状态证据一致、0.1/0.2选择器更新、无复合伪ID；`E-DOC` | `ROOT-DOC` | `EV-20260813-035` | Codex `/root` / 独立追踪复核`/root/xgate002_spec_review` | 已完成 | `CHG-20260813-002`重开后的74/153/63/70/35及124/152发布选择器已确定性重生成；独立复核P0/P1/P2=0、Ready=YES；计划、生成器或案例变化再次重开 |

### 31.5 M3—M4：B存储/C控制合并、B-only安全门与纵向切片

| task_id | M/route/type | objective | dependencies | deliverables | verification/evidence | roots | actual EV | owner/reviewer | 状态 | next/reopen |
|---|---|---|---|---|---|---|---|---|---|---|
| `A-STOR-001` | M3/A/storage | 历史计划：A锁、事务包、校验和、提交标记、幂等投影；R[REQ-SAFE-001,REQ-SAFE-003,REQ-NFR-002]；C[CASE-STORAGE-001,CASE-STORAGE-003,CASE-STORAGE-004,CASE-STORAGE-006,CASE-STORAGE-007,CASE-INVENTORY-007,CASE-PURCHASE-001,CASE-QUERY-003,CASE-INVENTORY-001,CASE-INVENTORY-004] | `SH-SAFE-BASE-001`、`SH-MAP-001`、`SH-HARNESS-001`、`SH-TRACE-001` | A确定性存储模块 | 不执行；历史Oracle保留 | `ROOT-A` | 无 | 无/无 | 已取消 | `DEC-027`：A停止正式写入完整实现，只保留只读/无插件降级规范；恢复须新CHG+DEC |
| `A-STOR-002` | M3/A/recovery | 历史计划：A坏尾、未提交不可见、重启恢复和只读查询；R[REQ-SAFE-003,REQ-NFR-002]；C[CASE-STORAGE-003,CASE-STORAGE-005] | `A-STOR-001` | recovery scanner+tests | 不执行；历史Oracle保留 | `ROOT-A` | 无 | 无/无 | 已取消 | `DEC-027`：不建设A恢复器；只读降级不得写业务数据 |
| `B-STOR-001` | M3/B/storage | SQLite驱动、固定文件名、迁移、约束和打包兼容；R[REQ-SAFE-001,REQ-OPS-002,REQ-NFR-002]；C[CASE-STORAGE-004,CASE-STORAGE-005,CASE-OPS-003] | `SH-SAFE-BASE-001`、`SH-MAP-001`、`SH-HARNESS-001`、`SH-TRACE-001` | B DB模块+migration | Node/OpenClaw/driver矩阵；`E-STOR` | `ROOT-B` | `EV-20260812-027` | Codex `/root` / OpenClaw 02独立B存储复核 | 已完成 | 固定叶、20/18/22、三阶段故障零发布、21项测试与独立复核P0=0/P1=0；驱动、固定文件名、migration或打包边界变化重开 |
| `B-MERGE-C-001` | M3/B/merge | 把C的服务端preview、`input_digest + data_revision`绑定、陈旧状态拒绝、调用方状态不信任、非法状态转换/迁移拒绝和幂等控制并入B；R[REQ-CORE-003,REQ-SAFE-003,REQ-CONTEXT-003,REQ-OPS-002]；C[CASE-INVENTORY-006,CASE-STORAGE-001,CASE-STORAGE-005,CASE-STORAGE-006,CASE-STORAGE-007] | `B-STOR-001`、`SH-MAP-001`、`SH-HARNESS-001`、`SH-TRACE-001` | B preview store/token、transition guard、migration guard和负例测试 | 伪造/陈旧/非法转换/非法迁移全部零写入，同键冲突与合法重试可证明；`E-STOR+E-CASE` | `ROOT-B` | `EV-20260812-028` | Codex `/root` / OpenClaw 02独立B安全复核 | 已完成 | 七字段绑定、原子幂等预留、27项focused/48项B测试及独立并发/篡改探针P0=0/P1=0/P2=0；协议、token或迁移规则变化重开 |
| `B-STOR-002` | M3/B/transaction | 事务、外键、非负、幂等、并发、WAL和恢复，并接入服务端权威preview/state guard；R[REQ-SAFE-002,REQ-SAFE-003,REQ-PANTRY-004]；C[CASE-STORAGE-001,CASE-STORAGE-002,CASE-STORAGE-006,CASE-STORAGE-007,CASE-EFFECT-003,CASE-INVENTORY-007,CASE-PURCHASE-001,CASE-QUERY-003,CASE-INVENTORY-001,CASE-INVENTORY-004] | `B-STOR-001`、`B-MERGE-C-001` | B repository+fault tests | §26.4 B-only repository切片+故障/响应丢失/backup integrity；`E-STOR+E-CASE` | `ROOT-B` | `EV-20260812-029` | Codex `/root` / OpenClaw 02独立B repository复核 | 已完成 | 21项focused、69项B测试、真实worker并发/崩溃、失败日志与业务零半写入边界及复核P0=0/P1=0；repository/Schema/事务边界变化重开 |
| `C-STOR-001` | M3/C/storage | 历史计划：C固定SQLite、迁移和服务端预览记录；R[REQ-SAFE-001,REQ-OPS-002,REQ-CORE-003]；C[CASE-STORAGE-004,CASE-OPS-003] | `SH-SAFE-BASE-001`、`SH-MAP-001`、`SH-HARNESS-001`、`SH-TRACE-001` | C DB+preview store | 不执行；安全语义转交`B-MERGE-C-001` | `ROOT-C` | 无 | 无/无 | 已取消 | `DEC-027`：C停止独立存储；不得创建C数据库或迁移 |
| `C-STOR-002` | M3/C/state | 历史计划：陈旧预览、非法跳转、事务/幂等/恢复；R[REQ-CORE-003,REQ-SAFE-003,REQ-CONTEXT-003]；C[CASE-STORAGE-001,CASE-STORAGE-006,CASE-STORAGE-007,CASE-INVENTORY-006,CASE-INVENTORY-007,CASE-PURCHASE-001,CASE-QUERY-003,CASE-INVENTORY-001,CASE-INVENTORY-004] | `C-STOR-001` | state service+fault tests | 不执行；对应Oracle由`B-MERGE-C-001`和B门承担 | `ROOT-C` | 无 | 无/无 | 已取消 | `DEC-027`：C状态控制合并B，不独立实现 |
| `X-GATE-001` | M3/gate/G1-B-only | B硬安全绑定门：共同repository案例、C控制合并、正式根零差异和后继任务处置；R[REQ-SAFE-001,REQ-SAFE-002,REQ-SAFE-003,REQ-SAFE-004,REQ-NFR-002]；C[CASE-STORAGE-001,CASE-STORAGE-002,CASE-STORAGE-004,CASE-STORAGE-005,CASE-STORAGE-006,CASE-STORAGE-007,CASE-EFFECT-003,CASE-FOUNDATION-002,CASE-INVENTORY-007,CASE-PURCHASE-001,CASE-QUERY-003,CASE-INVENTORY-001,CASE-INVENTORY-004] | `B-MERGE-C-001`、`B-STOR-001`、`B-STOR-002` | B-only G1硬安全报告与精确后继处置 | B独立通过§26.4全部硬门；不评分、不借A/C EV；`E-GATE` | `ROOT-SHARED` | `EV-20260812-030` | Codex `/root` / OpenClaw 02独立门复核 | 已完成 | `pass_b_safety`；13项存储责任/7检查/69 B tests/并发崩溃和复核P0=0/P1=0；仅解锁`B-SLICE-001`，依赖证据或候选变化重开 |
| `A-SLICE-001` | M4/A/slice | 历史计划：购买→饮食→库存→营养→纠正→查询切片；R[REQ-CORE-001,REQ-MEAL-001,REQ-PANTRY-001,REQ-NUTR-001,REQ-CORR-001,REQ-QUERY-001,REQ-MIXED-001]；C[CASE-MIXED-001,CASE-CORR-001,CASE-QUERY-001,CASE-EFFECT-001,CASE-MEAL-006,CASE-NUTR-008,CASE-MEAL-003,CASE-MEAL-004,CASE-INVENTORY-003,CASE-INVENTORY-004,CASE-NUTR-001,CASE-NUTR-002,CASE-NUTR-005,CASE-STORAGE-001,CASE-RECEIPT-001,CASE-RECEIPT-003,CASE-PROGRESS-010] | `A-STOR-001` | A纵向切片 | 不执行；历史Oracle保留 | `ROOT-A` | 无 | 无/无 | 已取消 | `DEC-027`：A不做完整纵切 |
| `B-SLICE-001` | M4/B/slice | B唯一主线纵向切片并覆盖服务端权威preview/state；R[REQ-CORE-001,REQ-MEAL-001,REQ-PANTRY-001,REQ-NUTR-001,REQ-CORR-001,REQ-QUERY-001,REQ-MIXED-001]；C[CASE-MIXED-001,CASE-CORR-001,CASE-QUERY-001,CASE-EFFECT-001,CASE-MEAL-006,CASE-NUTR-008,CASE-MEAL-003,CASE-MEAL-004,CASE-INVENTORY-003,CASE-INVENTORY-004,CASE-NUTR-001,CASE-NUTR-002,CASE-NUTR-005,CASE-STORAGE-001,CASE-RECEIPT-001,CASE-RECEIPT-003,CASE-PROGRESS-010] | `X-GATE-001`且结果=`pass_b_safety` | B纵向切片 | §26.5 B-only固定共同Oracle全通过；`E-STOR+E-CASE` | `ROOT-B` | `EV-20260812-031` | Codex `/root`实现 / 独立B纵切复核 | 已完成 | 候选`c8e6bcef39d0f98452432c3331095d963b9b9778`与Stage A `e8680a131cec1e5dd7c18c9f50b9fed35510dae3`通过153项plugin测试和完整低并发门；最终独立复审P0=0/P1=0/P2=0、Ready=YES；候选、17案Oracle或上游证据变化重开 |
| `C-SLICE-001` | M4/C/slice | 历史计划：完整纵向切片并保留服务端预览差异；R[REQ-CORE-001,REQ-MEAL-001,REQ-PANTRY-001,REQ-NUTR-001,REQ-CORR-001,REQ-QUERY-001,REQ-MIXED-001]；C[CASE-MIXED-001,CASE-CORR-001,CASE-QUERY-001,CASE-EFFECT-001,CASE-MEAL-006,CASE-NUTR-008,CASE-MEAL-003,CASE-MEAL-004,CASE-INVENTORY-003,CASE-INVENTORY-004,CASE-NUTR-001,CASE-NUTR-002,CASE-NUTR-005,CASE-STORAGE-001,CASE-RECEIPT-001,CASE-RECEIPT-003,CASE-PROGRESS-010] | `C-STOR-001` | C纵向切片 | 不执行；预览差异转交B | `ROOT-C` | 无 | 无/无 | 已取消 | `DEC-027`：C不做独立完整纵切 |
| `A-FAULT-001` | M4/A/fault | 历史计划：Effect/最终器/响应丢失/并发/重启故障；R[REQ-SAFE-002,REQ-SAFE-003]；C[CASE-EFFECT-001,CASE-EFFECT-002,CASE-EFFECT-003,CASE-STORAGE-006,CASE-STORAGE-007] | `A-SLICE-001` | A故障报告 | 不执行；历史Oracle保留 | `ROOT-A` | 无 | 无/无 | 已取消 | `DEC-027`：A不做完整故障实现 |
| `B-FAULT-001` | M4/B/fault | B故障矩阵，含服务端preview/state/migration拒绝；R[REQ-SAFE-002,REQ-SAFE-003]；C[CASE-EFFECT-001,CASE-EFFECT-002,CASE-EFFECT-003,CASE-STORAGE-005,CASE-STORAGE-006,CASE-STORAGE-007,CASE-INVENTORY-006] | `X-GATE-001`、`B-SLICE-001` | B故障报告 | 精确C字段故障Oracle；`E-STOR` | `ROOT-B` | `EV-20260812-032` | Codex `/root`实现 / 独立whole-branch B故障复核 | 已完成 | 候选`552feee374fe3463f296bd4a110af11747a7ee29`、替换证据`74d8193debf1347b07a4c5e594f4b7c3f11c5828`与71,063-byte日志经独立复审P0=0/P1=0、technical Ready YES；`agent/b-fault-001`已推送，draft PR #11目标`main`；`DONE_WITH_CONCERNS`，候选/7案Oracle/事务或诊断边界变化即重开 |
| `C-FAULT-001` | M4/C/fault | 历史计划：故障矩阵加陈旧/伪造预览；R[REQ-SAFE-002,REQ-SAFE-003,REQ-CONTEXT-003]；C[CASE-EFFECT-001,CASE-EFFECT-002,CASE-EFFECT-003,CASE-STORAGE-006,CASE-STORAGE-007,CASE-INVENTORY-006] | `C-SLICE-001` | C故障报告 | 不执行；对应负例由B承担 | `ROOT-C` | 无 | 无/无 | 已取消 | `DEC-027`：C故障控制并入B，不独立实现 |
| `X-GATE-002` | M4/gate/G2-B-only | B纵切就绪与路径绑定；R[REQ-NFR-003,REQ-SAFE-003]；C[CASE-POLICY-001,CASE-POLICY-002,CASE-EFFECT-001,CASE-EFFECT-003,CASE-STORAGE-006,CASE-STORAGE-007] | `SH-TRACE-001`、`X-GATE-001`且结果=`pass_b_safety`、`B-SLICE-001`、`B-FAULT-001`、冻结的CONTRACT-v2版本/hash | B-only就绪报告、仓库相对`shared/selected-route-map.template.json`、忽略的本机`shared/selected-route-map.json`及机器预检回执 | 解析§29.6；B纵切/故障完整Oracle、路径/命令/root/contract hash、实际预检回执、原子no-replace和碰撞回归完整；不评分；`E-GATE` | `ROOT-SHARED` | 无 | Codex `/root` / 至少两名独立复核 | 进行中 | 追踪前置已由`EV-20260813-035`关闭；现运行有界预检并修复复审P2，预检全通过后才原子生成本地map；两轮复核前不得解锁产品任务 |

### 31.6 M5—M6：B路线PRODUCT-0.1业务与可靠性

以下`SEL-*`中的`selected`已由`DEC-027`唯一解释为B，不再等待三路选路；但任务仍只有在`X-GATE-002`完成实际预检、两轮独立复核、关闭EV并正式判定`bind_b_ready`后才能开始。相对模板或候选本地map单独存在均不解锁任务。当前全部保持`未开始`，不得因路线已决定就绕过门或contract hash绑定。

| task_id | M/type | objective/requirements | dependencies | deliverables | verification/evidence | roots | actual EV | owner/reviewer | 状态 | next/reopen |
|---|---|---|---|---|---|---|---|---|---|---|
| `SEL-CORE-001` | M5/core | 时间、本人、饮食多项、白水、否定/上下文；R[REQ-SCOPE-001,REQ-SCOPE-002,REQ-CORE-002,REQ-TIME-001,REQ-TIME-003,REQ-CONTEXT-001,REQ-CONTEXT-002,REQ-MEAL-001,REQ-WATER-001]；C[CASE-MEAL-001,CASE-MEAL-021,CASE-MEAL-017,CASE-MEAL-009,CASE-WATER-001,CASE-SCOPE-001,CASE-MEAL-002,CASE-PURCHASE-004,CASE-RECEIPT-002,CASE-MEAL-010,CASE-MEAL-011,CASE-MEAL-012,CASE-MEAL-013,CASE-MEAL-014,CASE-MEAL-015,CASE-MEAL-016,CASE-MEAL-018,CASE-MEAL-019,CASE-MEAL-020,CASE-WATER-003,CASE-WATER-004] | `X-GATE-002` | selected parser+domain commands | 精确R字段连接§30最低案例；`E-CASE+E-STOR` | 选中路线根 | 无 | 未分配/独立产品复核 | 未开始 | next=G2后绑定路线；需求/解析变化重开 |
| `SEL-PANTRY-001` | M5/pantry | 入库、商品、包装、位置、开封/保质投影、默认库存与批次；R[REQ-PURCHASE-001,REQ-PURCHASE-002,REQ-PURCHASE-003,REQ-PRODUCT-001,REQ-PRODUCT-002,REQ-PRODUCT-003,REQ-PRODUCT-004,REQ-PANTRY-001,REQ-PANTRY-002,REQ-PANTRY-003,REQ-PANTRY-004]；C[CASE-PURCHASE-001,CASE-PURCHASE-003,CASE-PURCHASE-007,CASE-INVENTORY-001,CASE-INVENTORY-002,CASE-INVENTORY-003,CASE-INVENTORY-005,CASE-INVENTORY-004,CASE-INVENTORY-009,CASE-MEAL-004,CASE-MEAL-005,CASE-PURCHASE-002,CASE-PURCHASE-005,CASE-PURCHASE-006,CASE-PURCHASE-008,CASE-PURCHASE-009,CASE-PURCHASE-010] | `SEL-CORE-001` | selected product/inventory service | 精确R字段连接§30最低案例+并发；`E-CASE+E-STOR` | 选中路线根 | 无 | 未分配/独立领域复核 | 未开始 | next=四量和模板库存隔离；规则变化重开 |
| `SEL-NUTR-001` | M5/nutrition | 来源适配器/主备后端/Tier、真实Probe/Doctor、安全配置、缓存、Profile/Snapshot、估算和unknown；R[REQ-NUTR-001,REQ-NUTR-002,REQ-NUTR-003,REQ-NUTR-004,REQ-NUTR-005,REQ-NUTR-006,REQ-SOURCE-001,REQ-MEAL-003]；C[CASE-NUTR-001,CASE-NUTR-002,CASE-NUTR-004,CASE-NUTR-005,CASE-NUTR-006,CASE-MEAL-007,CASE-MEAL-006,CASE-MEAL-008,CASE-NUTR-003,CASE-NUTR-008,CASE-NUTR-009,CASE-SOURCE-001,CASE-SOURCE-002,CASE-SOURCE-003] | `SEL-CORE-001`、`SH-CONTRACT-003` | selected nutrition service+source capability contract+doctor | 来源/许可/真实Probe/降级/配置安全/失败/快照；`E-CASE+E-STOR` | 选中路线根 | 无 | 未分配/独立来源与安全复核 | 未开始 | `Q-001`；next=先冻结能力契约，再实现2—3个启用来源、Doctor与离线降级；来源/后端/Tier变化重开 |
| `SEL-FACTEFFECT-001` | M5/transaction | FactCommit、outbox、EffectBundle、EnvelopeFinalize、终态冻结和按日进度；R[REQ-CORE-001,REQ-SAFE-002,REQ-SAFE-003,REQ-PROGRESS-002,REQ-PROGRESS-004,REQ-MIXED-001]；C[CASE-EFFECT-001,CASE-EFFECT-002,CASE-EFFECT-003,CASE-STORAGE-006,CASE-STORAGE-007,CASE-PROGRESS-010,CASE-MIXED-003] | `SEL-CORE-001`、`SEL-PANTRY-001`、`SEL-NUTR-001` | selected orchestration+projector | 每附加写点、最终器和幂等冲突故障；`E-STOR` | 选中路线根 | 无 | 未分配/独立安全复核 | 未开始 | next=精确C字段全部通过；事务协议变化重开 |
| `SEL-ISSUE-001` | M6/issue | Issue中心、快捷项、解决事件和执行前复核；R[REQ-ISSUE-001,REQ-ISSUE-002,REQ-QUICK-001,REQ-CONTEXT-003]；C[CASE-ISSUE-001,CASE-ISSUE-002,CASE-ISSUE-003,CASE-RECEIPT-003,CASE-INVENTORY-006] | `SEL-FACTEFFECT-001` | selected issue/prompt service | 精确C字段；`E-CASE+E-STOR` | 选中路线根 | 无 | 未分配/独立交互复核 | 未开始 | next=稳定代码和选项过期；模型变化重开 |
| `SEL-CORR-001` | M6/correction | 纠正/撤销/恢复、库存补偿、营养重算、跨日；R[REQ-CORR-001,REQ-CORR-002]；C[CASE-CORR-001,CASE-CORR-002,CASE-CORR-004,CASE-INVENTORY-008,CASE-EFFECT-002,CASE-CORR-003,CASE-CORR-005] | `SEL-FACTEFFECT-001`、`SEL-ISSUE-001` | selected correction service | 精确C字段故障案例；`E-STOR` | 选中路线根 | 无 | 未分配/独立安全复核 | 未开始 | `Q-004`；next=差量和跨日黄金回执；规则变化重开 |
| `SEL-PROGRESS-001` | M6/progress | GoalVersion、六项聚合、unknown下界、冻结增量；R[REQ-PROGRESS-001,REQ-PROGRESS-002,REQ-PROGRESS-003,REQ-PROGRESS-004,REQ-WATER-002,REQ-NFR-002]；C[CASE-PROGRESS-001,CASE-PROGRESS-002,CASE-PROGRESS-004,CASE-PROGRESS-009,CASE-PROGRESS-010,CASE-PROGRESS-011,CASE-STORAGE-006,CASE-PROGRESS-003,CASE-PROGRESS-005,CASE-PROGRESS-008,CASE-WATER-002,CASE-WATER-005,CASE-WATER-006] | `SEL-FACTEFFECT-001`、`SEL-CORR-001` | selected progress projector | 精确R字段连接§30最低案例+响应丢失；`E-CASE+E-STOR` | 选中路线根 | 无 | 未分配/独立数值复核 | 未开始 | next=round_half_up边界；聚合变化重开 |
| `SEL-QUERY-001` | M6/query | 当前有效日视图、最小周聚合、库存/Issue/审计查询；R[REQ-QUERY-001,REQ-QUERY-002,REQ-QUERY-003]；C[CASE-QUERY-001,CASE-QUERY-002,CASE-QUERY-003,CASE-QUERY-004,CASE-QUERY-005,CASE-PROGRESS-007,CASE-PROGRESS-012] | `SEL-PROGRESS-001` | selected read model/report | 单次聚合、只读hash、覆盖；`E-CASE+E-STOR` | 选中路线根 | 无 | 未分配/独立查询复核 | 未开始 | next=精确C字段；投影变化重开 |
| `SEL-RECEIPT-001` | M6/receipt | ReceiptData渲染逐菜、证据标签、快捷和最终进度；R[REQ-RECEIPT-001,REQ-RECEIPT-002,REQ-RECEIPT-003,REQ-QUICK-001,REQ-TIME-002]；C[CASE-RECEIPT-001,CASE-RECEIPT-002,CASE-RECEIPT-003,CASE-RECEIPT-004,CASE-RECEIPT-005,CASE-RECEIPT-006,CASE-DATE-001,CASE-DATE-002,CASE-DATE-003] | `SEL-ISSUE-001`、`SEL-PROGRESS-001` | selected renderer | 精确C字段黄金回执逐字符/结构；`E-CASE` | 选中路线根 | 无 | 未分配/独立交互复核 | 未开始 | next=单日/partial/pending/跨日；格式变化重开 |
| `SEL-RELIABILITY-001` | M6/reliability | 并发、崩溃、响应丢失、最终器、日志/网络隐私和正式根零差异；R[REQ-SAFE-001,REQ-SAFE-002,REQ-SAFE-003,REQ-SAFE-004,REQ-PRIV-001,REQ-NFR-001,REQ-NFR-002]；C[CASE-STORAGE-001,CASE-STORAGE-002,CASE-STORAGE-004,CASE-STORAGE-005,CASE-STORAGE-006,CASE-STORAGE-007,CASE-EFFECT-003,CASE-PRIV-001,CASE-PRIV-002,CASE-FOUNDATION-002] | `SEL-CORE-001`、`SEL-PANTRY-001`、`SEL-NUTR-001`、`SEL-FACTEFFECT-001`、`SEL-ISSUE-001`、`SEL-CORR-001`、`SEL-PROGRESS-001`、`SEL-QUERY-001`、`SEL-RECEIPT-001` | 可靠性矩阵和报告 | 精确C字段+manifest；`E-STOR` | 选中路线根 | 无 | 未分配/独立安全复核 | 未开始 | next=候选前完整故障回归；依赖/事务变化重开 |

### 31.7 M7：0.1安装、迁移、备份、导出和发布

| task_id | M/type | objective | dependencies | deliverables | verification/evidence | roots | actual EV | owner/reviewer | 状态 | next/reopen |
|---|---|---|---|---|---|---|---|---|---|---|
| `SEL-INSTALL-001` | M7/install | 支持矩阵、单一安装入口、预检、隔离冒烟和回执；R[REQ-OPS-001,REQ-SAFE-001,REQ-SAFE-004,REQ-SCOPE-003]；C[CASE-OPS-001,CASE-OPS-007,CASE-FOUNDATION-002] | `SEL-ISSUE-001`、`SEL-CORR-001`、`SEL-PROGRESS-001`、`SEL-QUERY-001`、`SEL-RECEIPT-001`、`SEL-RELIABILITY-001` | installer+install manifest+START-HERE更新 | clean install/unsupported env；`E-REL` | `ROOT-REL` | 无 | 未分配/独立发布复核 | 未开始 | `Q-007`；next=确定最低Node/OpenClaw；安装字节变化重开 |
| `SEL-MIGRATE-001` | M7/migration | Schema升级、前置备份、失败回滚和版本不误升；R[REQ-OPS-002,REQ-SAFE-003]；C[CASE-OPS-002,CASE-OPS-003,CASE-STORAGE-005] | `SEL-INSTALL-001` | migration runner+history | 成功/中断/旧版恢复；`E-REL` | `ROOT-REL` | 无 | 未分配/独立数据复核 | 未开始 | next=合成旧库矩阵；Schema变化重开 |
| `SEL-BACKUP-001` | M7/backup | 一致备份、临时恢复验证、默认卸载保留和确认删除；R[REQ-OPS-002,REQ-OPS-003,REQ-EXPORT-BASE-001]；C[CASE-OPS-004,CASE-OPS-005,CASE-OPS-006,CASE-OPS-008,CASE-EXPORT-003,CASE-EXPORT-004] | `SEL-MIGRATE-001` | backup/restore/uninstall commands | 精确C字段；`E-REL` | `ROOT-REL` | 无 | 未分配/独立恢复复核 | 未开始 | next=断电/损坏/已有目录案例；格式变化重开 |
| `SEL-EXPORT-BASE-001` | M7/export | 最小用户数据导出与可恢复备份分离；R[REQ-EXPORT-BASE-001,REQ-PRIV-001]；C[CASE-EXPORT-003,CASE-EXPORT-004,CASE-PRIV-002] | `SEL-BACKUP-001` | baseline export contract+command | unknown/时区/版本保持；`E-REL` | `ROOT-REL` | 无 | 未分配/隐私复核 | 未开始 | next=最小JSON范围；字段变化重开 |
| `SEL-RELEASE-001` | M7/G3-0.1 | 不可变0.1候选、hash、E2E、门和原样晋级；R[REQ-OPS-001,REQ-OPS-004,REQ-SAFE-004]；C[CASE-OPS-001,CASE-OPS-009,CASE-OPS-010,CASE-FOUNDATION-002] | `SEL-INSTALL-001`、`SEL-MIGRATE-001`、`SEL-BACKUP-001`、`SEL-EXPORT-BASE-001`、`SEL-RELIABILITY-001` | candidate+manifest+G3 DEC+release | clean/upgrade/restore/zero diff；`E-REL+E-GATE` | `ROOT-REL` | 无 | 发布负责人/至少两名复核 | 未开始 | next=所有硬门通过后打候选；任一字节变化重开 |

### 31.8 M8—M9：PRODUCT-0.2便利层与发布

| task_id | M/type | objective | dependencies | deliverables | verification/evidence | roots | actual EV | owner/reviewer | 状态 | next/reopen |
|---|---|---|---|---|---|---|---|---|---|---|
| `SEL-TEMPLATE-001` | M8/template | 个人模板证据阈值、版本、可变和单次覆盖；R[REQ-TEMPLATE-001,REQ-TEMPLATE-002,REQ-NFR-003]；C[CASE-TEMPLATE-001,CASE-TEMPLATE-002,CASE-TEMPLATE-003,CASE-TEMPLATE-004,CASE-TEMPLATE-005,CASE-POLICY-001] | `SEL-RELEASE-001` | template service+projection | 精确C字段且系统估算不学习；`E-CASE+E-STOR` | 选中路线根 | 无 | 未分配/独立产品复核 | 未开始 | next=0.1发布后；阈值变化重开 |
| `SEL-REVIEW-001` | M8/review | 快速复用、丰富周回顾/趋势和批量补录；R[REQ-REVIEW-001,REQ-CORR-003,REQ-QUERY-003]；C[CASE-REVIEW-001,CASE-REVIEW-002,CASE-REVIEW-003,CASE-REVIEW-004,CASE-REVIEW-005,CASE-PROGRESS-012,CASE-CORR-006,CASE-CORR-007] | `SEL-TEMPLATE-001` | review/report/reuse commands | 精确C字段、覆盖和无健康评价；`E-CASE` | 选中路线根 | 无 | 未分配/独立产品复核 | 未开始 | next=定义0.2聚合契约；范围变化重开 |
| `SEL-LIFE-001` | M8/lifecycle | 主动临期、提醒去重/延期、库存调整和个人位置规则；R[REQ-LIFE-001,REQ-PANTRY-005,REQ-NFR-003]；C[CASE-LIFE-001,CASE-LIFE-002,CASE-LIFE-003,CASE-LIFE-004,CASE-LIFE-005,CASE-LIFE-006,CASE-LIFE-007,CASE-LIFE-008,CASE-LIFE-009,CASE-POLICY-001,CASE-POLICY-002] | `SEL-RELEASE-001`、`SH-DECISION-001` | reminder+adjustment+personal-location services | 精确C字段和非医疗边界；`E-CASE+E-STOR` | 选中路线根 | 无 | 未分配/独立领域复核 | 未开始 | `Q-005`；next=首批品类；阈值变化重开 |
| `SEL-EXPORT-001` | M8/export | rich筛选、CSV/分析、匿名化和历史选项；R[REQ-EXPORT-RICH-001,REQ-PRIV-001]；C[CASE-EXPORT-001,CASE-PRIV-001,CASE-PRIV-002] | `SEL-EXPORT-BASE-001`、`SEL-REVIEW-001` | rich export contract+commands | 精确C字段；`E-CASE+E-REL` | `ROOT-REL` | 无 | 未分配/隐私复核 | 未开始 | `Q-006`；next=字段/许可评审；格式变化重开 |
| `SEL-RESEARCH-001` | M8/research | 多来源营养证据、实体锚定、聚类、可信度门、部分覆盖和`nothing_solid`；R[REQ-RESEARCH-001]；C[CASE-RESEARCH-001,CASE-RESEARCH-002,CASE-RESEARCH-004] | `SEL-RELEASE-001`、`SEL-NUTR-001` | research evidence Schema+2—3来源研究适配器+confidence policy+20—50条金标集 | 精确C字段、来源冲突/空结果/mutation和业务库零写；`E-CASE+E-STOR` | `ROOT-B` | 无 | 未分配/独立研究质量与安全复核 | 未开始 | next=0.1发布后先冻结Schema/策略；来源、权重、阈值或公共形状变化重开 |
| `SEL-NUTR-WATCH-001` | M8/research-state | 历史去重、Watchlist、sighting、资料变更预览和确认后Profile版本更新；R[REQ-RESEARCH-002,REQ-NUTR-003]；C[CASE-RESEARCH-003,CASE-RESEARCH-005,CASE-RESEARCH-006] | `SEL-RESEARCH-001`、`SEL-REVIEW-001` | research state store+watchlist/diff/preview commands+confirmed update bridge | 离线零网络、重复运行不重复通知、确认前业务全表零写、历史Snapshot不变；`E-CASE+E-STOR` | `ROOT-B` | 无 | 未分配/独立数据与隐私复核 | 未开始 | next=先做本地preview，不建默认定时推送；状态/确认桥/调度边界变化重开 |
| `SEL-UPGRADE-002` | M9/upgrade | 0.1→0.2迁移、回滚、备份恢复和0.1无退化；R[REQ-OPS-002,REQ-TEMPLATE-001,REQ-REVIEW-001,REQ-LIFE-001,REQ-EXPORT-RICH-001,REQ-RESEARCH-001,REQ-RESEARCH-002]；C[CASE-OPS-002,CASE-OPS-003,CASE-EXPORT-003,CASE-REVIEW-004,CASE-LIFE-003,CASE-RESEARCH-005] | `SEL-TEMPLATE-001`、`SEL-REVIEW-001`、`SEL-LIFE-001`、`SEL-EXPORT-001`、`SEL-NUTR-WATCH-001` | 0.2 migrations+compat matrix | 旧候选升级/失败/恢复、研究库可独立重建且业务库无退化；`E-REL` | `ROOT-REL` | 无 | 未分配/独立数据复核 | 未开始 | next=M8退出后；Schema/依赖变化重开 |
| `SEL-RELEASE-002` | M9/G3-0.2 | 不可变0.2候选、全回归、G3和原样晋级；R[REQ-OPS-004,REQ-SAFE-004]；C[CASE-OPS-009,CASE-OPS-010,CASE-FOUNDATION-002] | `SEL-UPGRADE-002` | candidate+manifest+G3 DEC+release | 0.1回归+0.2 E2E+zero diff；`E-REL+E-GATE` | `ROOT-REL` | 无 | 发布负责人/至少两名复核 | 未开始 | next=全部0.2门通过后；任一字节变化重开 |

---

## 附录 A：会话覆盖台账（50/50）

> 前45轮由既有文档证据覆盖，第46—48轮已在0.3登记并随B纵切/故障闭环更新；第49轮批准资料吸收，第50轮明确批准DOC-0.4成为唯一现行入口并继续按其实施。会话覆盖只证明要求被登记，不等于来源/研究功能已经实现、追踪镜像已重生成、X-GATE-002通过或产品可安装。

| 顺序 | 原始轮次ID | 用户目标或纠正 | 最终生效落点 | 状态 |
|---:|---|---|---|---|
| 1 | `019fe3d1-212e-7410-aa3c-59c7301862c0` | 运行三套foundation验证、修复失败、证明不创建JSONL/SQLite业务数据 | §1.2、数据安全专章、验证任务 | 生效 |
| 2 | `019fe3d8-ea52-7502-a17c-553cc433456c` | 后续使用中文 | 全文 | 生效 |
| 3 | `019fe3d9-58b8-76c2-baf8-064a0eb3ea96` | 询问如何安装三套项目 | §3、安装专章 | 早期“A可试用”建议已被第4轮替代 |
| 4 | `019fe3dc-917b-7bd2-b8a6-83c72a817efc` | 确认项目尚未开发完成 | §1.3、§1.4、§3.2 | 生效 |
| 5 | `019fe3de-0b36-73f2-85bb-6f32784586c1` | 要求在项目根建立总功能开发计划 | §0及本文件整体 | 生效；“先定B主线”问题由后续轮次重新决定 |
| 6 | `019fe3df-7016-7c21-8659-6f69fbda4c86` | 主线不重要；文档必须展示进度、目标、方向并有约束力 | §0、§4.1—§4.3 | 生效 |
| 7 | `019fe3e1-36fd-7be3-b4dc-190c7ba2a3ef` | 总览、专项细节、路线矩阵三类能力都要，越详细越好 | §4.1、§4.5—§4.7 | 生效 |
| 8 | `019fe3ef-b434-7ed1-b8b9-80b64ca9653d` | 围绕日常饮食记录扩展潜在需求并优化计划 | 产品需求全章、验收案例、任务台账 | 生效 |
| 9 | `019fe3f3-ce57-79a1-a93d-8d4810824341` | 初步同时选择记录回顾与健康管理方向 | §2历史边界 | “健康管理助手”由后续第11轮明确移除 |
| 10 | `019fe3f9-461d-78e3-9462-890ae3040c3b` | 选择结构性重构；审阅初版三层产品设计 | §4.4、§2 | 结构性重构生效；初版0.3层被后续替代 |
| 11 | `019fe3ff-f3c9-7650-995f-22a58c3ee52c` | 只记录本人；移除0.3健康助手；确认其余日常记录设计 | §2.4、§5.2—§5.3、§6 | 生效 |
| 12 | `019fe409-6472-7f51-a26e-3a4252bf738d` | 反转库存默认：只有明确外食才跳过；模糊、多候选先保存并进入异常中心 | §5.1、§5.5、§8、§9 | 生效 |
| 13 | `019fe40a-f141-7dc1-9a3c-02e09bb6b8ae` | 商品入库也必须有最低数据规范 | §7 | 生效 |
| 14 | `019fe40c-600c-7531-b58d-6db116c94ab9` | 先审阅全部规范；确认异常与待完善中心方向 | §8 | 生效；状态枚举待后续轮次最终收敛 |
| 15 | `019fe412-c690-7fd2-aba1-42d01214e5d9` | 快捷选项提升效率；字母不是唯一输入；确认库存匹配规范 | §8.4—§8.5、§9 | 生效 |
| 16 | `019fe417-004e-78c0-af19-7aae564c5eb8` | 本月只显示几号；跨月显示月日；跨年显示年月日 | §10 | 生效 |
| 17 | `019fe418-1941-71f2-88fb-54a2b082f065` | 开始追问库存匹配字段用途 | §9.2、§11 | 由第18轮补充完整意图 |
| 18 | `019fe418-77be-74c0-ad37-00691d6cf35d` | 要求完整了解商品入库后记录的信息 | §7、§11 | 生效 |
| 19 | `019fe41c-5eb7-76a2-a99a-a2c08dec7e7f` | 加入开封、临期、位置自动判断；入库后显示位置 | §11.5—§11.11、§12 | 生效 |
| 20 | `019fe423-d504-7b51-b87a-6b4c07bef220` | 单项可详细、多项简化；正常不显示位置快捷项；允许按入库时间估算保质期 | §11.8—§11.10、§12 | 生效 |
| 21 | `019fe42a-0a48-7621-a201-6ef2c0895d00` | 明显不确定或多类别才给快捷项；保质未知显示时间锚点；快捷项末行允许自由文本 | §8.5、§11.10.1、§12.3—§12.4 | 生效 |
| 22 | `019fe42d-215f-7e60-8a43-91cd66956058` | 批准库存与回执部分并进入营养规范 | §11—§12、§13 | 生效 |
| 23 | `019fe444-db30-79a0-a564-71a9e2399cba` | 普通食材自动联网取数；有限模糊量取较大值；常见菜自动模板；套餐必须询问 | §13 | 生效 |
| 24 | `019fe44a-215c-7e21-ab19-c06586654baf` | 两次/三次一致形成个人模板；多次不同采用个人可变策略；同商品复用历史营养 | §14 | 生效 |
| 25 | `019fe453-1081-72b3-93e2-52665b2a2716` | 继续并审阅饮食记录、纠错、冲销、混合事件 | §6、§15 | 生效 |
| 26 | `019fe47a-38a4-7f73-96e4-1f0d33234ad7` | 引入白水分类和固定六项进度强约束 | §16—§17 | 生效 |
| 27 | `019fe47f-1574-7e81-ba1c-5024005a4618` | 要求展示受约束的真实回执形态 | §18及示例附录 | 生效 |
| 28 | `019fe480-a972-73d2-9080-9cbb63caa0fa` | 指出模拟累计来源不清；要求从早餐到晚餐完整模拟 | §18.5、验收案例 | 生效 |
| 29 | `019fe483-17e5-7003-b5f8-f496e72ba94e` | 指出记录回执上半部分过长 | §18 | 由第30轮给出具体修订 |
| 30 | `019fe484-5a7a-7dc2-9cad-8e91a87c9cd6` | 菜品组成压缩；取消本轮营养；只在进度块显示增量 | §18.2—§18.4 | 生效；具体换行由后续轮次继续修订 |
| 31 | `019fe485-1d9a-7972-9bcb-f6f9b4979ac9` | 提供希望采用的晚餐成功回执样式 | §18.8 | 样式基线生效；该轮被打断但用户输入由后续确认 |
| 32 | `019fe485-9862-7fb2-9259-9575ad9b5825` | “已记录”行仍过长；每道菜必须单独换行 | §18.1、§18.3、§18.8 | 生效 |
| 33 | `019fe486-23fe-7033-a5c4-c84b2fef0a8f` | 自然单位估算必须显示实际采用的克重 | §18.6—§18.7 | 生效 |
| 34 | `019fe486-a87f-7e80-9917-0713d24c9132` | 要求用明确、模糊、多候选、套餐、批量入库、个人模板等场景验证格式 | §18及验收案例专章 | 生效；模拟数字仅作格式样例，不直接作为业务真值 |
| 35 | `019fe489-d27c-7711-b750-faf1d18753ec` | 明确数据不得显示“估算”；详细固化证据标签、快捷回复和回执规则 | §18.6—§18.10 | 生效 |
| 36 | `019fe519-4589-73d1-ae33-39698a218467` | 定义事实查询、日进度、日回顾、周回顾、覆盖率和当前有效视图 | §19 | 生效 |
| 37 | `019fe51f-4eeb-7260-99b0-81544b76234e` | 定义异常分类、稳定代码、快捷回复绑定、生命周期和补全事务 | §8.2、§8.8—§8.13 | 生效 |
| 38 | `019fe528-8410-78b2-9aaa-d1131453f0bf` | 完整定义追加式纠正、库存补偿、营养重算、撤销恢复、并发与批量操作 | §15.11—§15.17及§15既有条款 | 生效 |
| 39 | `019fe52c-ffa4-7ac3-a0cb-2e26cb7cadc4` | 完整定义商品身份、批次、包装、位置、开封、保质期、FEFO/FIFO和历史复用 | §7、§9、§11.13—§11.19 | 生效 |
| 40 | `019fe538-9848-71c3-be1a-b332f23aa970` | 完整定义营养来源、联网、缓存、数据质量、模板形成及历史快照 | §13.14—§13.19、§14.9—§14.10及既有营养条款 | 生效 |
| 41 | `019fe545-5e83-74d3-bad8-2ecf05792d86` | 定义完成/计划/否定、时间、指代、连续跟进与混合事件执行 | §20 | 生效 |
| 42 | `019fe553-de7c-7781-b9e3-52cb0fb0d013` | 核对三套foundation并定义数据隔离、事务、幂等、隐私与零业务数据验证 | §21 | 生效 |
| 43 | `019fe557-6f0e-7832-8731-04509e12c672` | 定义当前不可安装、未来统一安装、升级迁移、备份恢复、导出与卸载 | §22 | 生效 |
| 44 | `019fe55a-d636-77d2-b395-f02d3b2ff8b9` | 要求新建0.3，逐轮读取全部会话并边读边写，避免压缩遗漏 | §0.2、附录A及本次编制流程 | 生效；当前编制任务 |
| 45 | `20260809-DOC-003-002-user-approval` | 用户纠正项目主要是给智能体使用的Skill，并明确批准DOC-0.3后要求按计划逐步开发 | `docs/work-items/DOC-003-002-approval.md`、`REQ-SCOPE-003`、`DEC-018` | 生效；批准原话见批准记录 |
| 46 | `20260810-user-route-decision-focus-b` | 用户明确决定“集中开发B” | `DEC-027`、`CHG-20260810-001`、§4.2、§21.3/21.8、§23.3—§23.4、§26、§27、§29.4/29.6、§31 | 生效；B为唯一产品主线，但本轮不产生G1/G2评分、门通过或EV；变更保持`实施中` |
| 47 | `20260811-user-failure-log-business-atomicity` | 用户明确写入失败允许产生日志，但饮食记录中不能产生数据 | `CHG-20260811-001`、§21.4—§21.5、`DEC-023` | 生效；兼容性澄清，不改变FactCommit成功后Effect待重试的既有分层语义 |
| 48 | `20260811-user-risk-based-safety-and-simple-plan` | 用户要求继续开发、按总功能开发计划0.3执行、另写简明计划；安全底座继续做必要内容，非必要平台细节可提前结束 | `CHG-20260811-002`、`DEC-028`、`docs/superpowers/plans/2026-08-11-skill-first-b-practical-plan.md` | 生效；B仍受必要安全和共享映射/harness/trace入口门约束 |
| 49 | `20260813-user-absorb-agent-reach-last30days-doc-0.4` | 用户提供两份技术说明，询问先继续开发还是吸收资料；批准先吸收并生成新开发计划0.4 | `CHG-20260813-001`、`DEC-029`、§0.10、§13.20—§13.28、`REQ-SOURCE-001`、`REQ-RESEARCH-001`、`REQ-RESEARCH-002`、`DOC-004-001/002` | 生效；采用“吸收架构、不复制产品”，0.1只扩来源可靠性，0.2扩研究/Watchlist；不改变B主线或当前门 |
| 50 | `20260813-user-approve-doc-0.4-current-authority` | 用户明确批准DOC-0.4成为唯一现行计划入口，并要求持续按0.4实施至PRODUCT-0.1正常可用 | `docs/work-items/DOC-004-approval.md`、`DEC-002`、`CHG-20260813-001`、`DOC-004-001/002`、`SH-TRACE-001` | 生效；0.3仅保留只读历史基线；批准不等同追踪镜像、X-GATE-002、产品实现、安装或发布已经完成 |
