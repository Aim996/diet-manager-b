# EV-20260813-036 — X-GATE-002 B-only 就绪与路径绑定门

## 身份与结论

- `evidence_id`: `EV-20260813-036`
- `evidence_type`: `E-GATE`
- `recorded_at`: `2026-08-13T11:19:00+08:00`
- `task_ids`: [`X-GATE-002`]
- `risk_ids`: [`RISK-017`]
- `change_ids`: [`CHG-20260813-002`]
- `reviewed_candidate`: `4646194fe8a7acc03157419f1789bbb6a9460ee6`
- `decision`: `bind_b_ready`
- `result`: `PASS — 只授权 SEL-CORE-001，不代表产品可用、可安装、可发布或远程 OpenClaw 已验收`

## 冻结输入与本地门产物

- CONTRACT：`diet-manager/contract-v2`，版本`2`，SHA-256 `632B2BBF8D0E6C655F4C0A47958828A86C67B3240065984CCC78A808E6F7072E`。
- 路线：`B`；配置键：`official_data_root`。
- 路径合同：21个任务、43条仓库相对路径；仓库只跟踪`shared/selected-route-map.template.json`。
- 本地门产物：`shared/selected-route-map.json`，普通非reparse文件、Git忽略、不属于开源发布字节。
- 复审时本地map：18,213 bytes，SHA-256 `5611CE14E5767E971B0CE8D0FA3501EE52A21B7FA50E6AFCC4DC7B5C94D090C4`；回执SHA-256 `130E20768C63EC8B55BF024DACFE6BC882FBA479ED81210CE640263E59132E07`，`base_commit`精确等于复审候选。
- 四项前置：`X-GATE-001 + EV-030`、`B-SLICE-001 + EV-031`、`B-FAULT-001 + EV-032`、`SH-TRACE-001`复审候选`96cba14646e2cee46ed45fe3711eb061f59b6c0e + EV-035`。
- 正式build沿用唯一一次候选`bdbc5e229af24bc78002e63951b0f9fbde51207f`；命令`npm run build`、执行数1、复审修复期间未重跑；两个dist文件字节数与SHA-256逐项冻结并由候选Git blob复验。

控制器闭环提交会改变当前HEAD，因此旧本地map会按设计失效。闭环后必须删除旧的可再生本地map，针对闭环HEAD重新执行同一有界预检并通过normal校验；该后置回执是环境绑定的本机状态，不写进本证据形成自引用。

## 实际有界预检

使用冻结Node.js `v24.15.0`，验证器串行执行8项检查，全部`exit_code=0`：

1. focused foundation contract/root/Skill：15/15；
2. TypeScript `--noEmit`；
3. source/dist parity与精确输入hash；
4. 隔离OpenClaw plugin build-check；
5. 隔离OpenClaw plugin validate；
6. X-GATE-001 regression/self-test；
7. trace normal；
8. trace self-test。

预检后：normal=`bind_b_ready`；self-test覆盖4项计划变异、1项发布碰撞、1项失败不发布、1项命令失败、5项前置变异、13项身份/回执变异和1项reparse清理变异；review regression 3/3通过。发布使用同卷hard-link no-replace，碰撞不覆盖，失败不发布。

## 两轮独立复核

- 规格复核 `/root/xgate002_spec_review`：P0=0、P1=0、P2=0、Ready=YES。
- 质量/安全复核 `/root/xgate002_quality_review`：原formal-build回执P2已关闭；P0=0、P1=0、P2=0、Ready=YES。

两名复核者都确认：map未跟踪且被Git忽略；回执HEAD、输入hash、8项命令结果和正式build逐字段绑定；无凭据、局域网endpoint或机器绝对路径进入tracked发布字节；该门最多只授权`SEL-CORE-001`。

## 非声明

本证据不表示PRODUCT-0.1或0.2已完成、可安装、可发布，也不表示远程OpenClaw 02—07已经安装或端到端验收。它不授权同时启动其余`SEL-*`任务，不包含Gateway密钥、局域网地址、饮食业务数据或用户隐私数据。
