# EV-20260812-028 — B-MERGE-C-001 Server Authority

## Identity

- evidence_id: `EV-20260812-028`
- task_id: `B-MERGE-C-001`
- date: `2026-08-12`
- reviewed implementation: `0184ac8eb53583db1e95a4c55fa146a0dfca58cf`
- frozen branch head before closure: `5a06000380c2fcfd548455e10204c3e53a2779c8`
- branch: `agent/b-merge-c-001-server-authority`
- draft PR: <https://github.com/Aim996/diet-manager-b/pull/7>
- result: `accepted_b_server_authority_ready_b_stor_002_next`

## Scope proved

This evidence proves the B-only server-authority layer that `B-STOR-002` must call before any dietary write:

- canonical, exact seven-field preview binding;
- versioned HMAC-SHA256 token with a private adapter-owned key;
- persisted server authority in the existing control tables, without a new migration;
- `BEGIN IMMEDIATE` first-create atomicity and exact same-key retry;
- stable conflict rejection for changed digest, subject, command, preview hash or data revision;
- read-only authorization against token, request, stored binding, current revision, envelope state and idempotency linkage;
- strict envelope/effect transition guards based on server-read state;
- current application/user/migration/schema identity guard before authority work;
- rejection of unregistered table/index/view/trigger changes;
- zero dietary/business rows for every success and failure in this milestone.

It does not prove a dietary repository, `FactCommit`, effect execution, final result replay, installation, deployment or product readiness.

## Local verification

```text
Focused server authority: 27/27 PASS
Complete B package: 4 files / 48 tests PASS
TypeScript --noEmit: PASS
TypeScript build: PASS
OpenClaw metadata build check: Plugin metadata is up to date.
OpenClaw plugin validation: Plugin diet-manager-b is valid.
Protected-path delta: 0
Public handler/contract/manifest/migration-v1/lockfile delta: 0
Test-owned residual: 0
Dietary/business rows: 0
```

The local commands used the verified Node `v24.15.0` runtime with SHA-256 `3331E1FFE19874215472217C5E94F5A0C6D8E18C4AC7111D3937AA0AD5E9B4A5`. No model call was used for implementation, tests, build or plugin validation.

## Independent review

OpenClaw 02 performed one bounded read-only public-clone review. It reproduced 27/27 focused tests, 48/48 complete package tests, both TypeScript gates and both OpenClaw plugin gates, then added concurrency, lock-timeout, tamper, schema-drift, rollback and dynamic-getter probes. The reviewer removed its clone and all review-owned temporary databases.

```text
B_MERGE_C_001_REVIEW|PASS|P0=0|P1=0|P2=0|commit=0184ac8eb53583db1e95a4c55fa146a0dfca58cf|cleanup=1
```

## Product and data boundary

- the public Skill/OpenClaw handler remains non-writing and returns `foundation_not_implemented`;
- preview success writes only two control rows and stores no raw chat, food name, quantity, token or secret;
- a write failure may later create a separate redacted technical log, but it cannot create a dietary record, half record or business row;
- the next permitted WIP is `B-STOR-002`, which must integrate this authority into one atomic business repository transaction;
- G1/G2/G3, installation, deployment and real dietary use remain unclaimed.
