# EV-20260811-018 — SH-MODEL-002 Nutrition and Progress v1

## Result

`SH-MODEL-002` is complete for its frozen scope: versioned nutrition profiles, immutable nutrition snapshots, user-confirmed goal versions, fixed six-metric daily progress and single/cross-day progress results.

This evidence does not claim a production runtime validator, SQLite mapping, repository, writer, OpenClaw/MCP business handler, installable Skill or product readiness.

## Frozen deliverables

| File | SHA-256 |
|---|---|
| `shared/schemas/nutrition-progress.schema.json` | `E8F0C95006529D5D6F9C388E657EA1F834C567D975577C5903B2B28D79C26DE8` |
| `shared/tests/fixtures/nutrition-progress-cases.json` | `0686975A30F8F74ECD8866F5F0D795F226E1AA374B9165ECB2CE44F12F498623` |
| `shared/tests/validate-nutrition-progress-schemas.ps1` | `1C0C63BD3A99BD8884B7C87F3B58E8FF44C9818BEAF0B241C2AD63E4B68556C7` |
| `docs/work-items/SH-MODEL-002-review-package.md` | `7D3E85887BCC319CF769324CCD0529FB40BB28F60DFEBEE341749352A5F40C94` |
| `docs/work-items/SH-MODEL-002-review.md` | `6D3032EE38F3C3D201E4EE320FC85E02228157185824254646EA9F58F969DC04` |

## TDD evidence

1. Missing-schema RED: `NUTRITION_PROGRESS_SCHEMA_FILE_MISSING`.
2. First implementation exposed and fixed a PowerShell 5.1 single/empty-array preservation bug without weakening model semantics.
3. The fixed 42-case corpus and four independent mutations reached:

```text
NUTRITION_PROGRESS_SCHEMAS|PASS|version=1.0.0|cases=42|definitions=10|mutations=4
```

The exact case IDs, order, outcomes, schema bytes and fixture bytes are test-owned literals. The mutations reject changed schema identity, a JSON-number quantity, unsupported coverage and missing fixture coverage.

## Business invariants proved

- the only shared coverage values are `complete`, `partial`, `unknown` and `not_applicable`; estimation remains field evidence;
- persisted quantities are canonical decimal strings or null, and unknown is not invented as zero;
- snapshots retain the exact nutrition profile ID/version used at commit time even after a newer profile supersedes it;
- known/missing/value partitions and complete/partial/unknown meaning are checked independently;
- goals are user-confirmed, versioned, have exactly six nullable metrics and receive no population defaults;
- progress uses the exact order energy, protein, fat, carbohydrate, fiber and water;
- totals/goals are nonnegative, signed corrections remain in current-turn increments, and direct queries have null increments;
- incomplete water is a nonnegative lower bound with partial coverage, not an exact total;
- a single date requires a deep-identical alias, while multiple dates forbid the alias and remain sorted/unique;
- failed FactCommit/finalization can produce only a separate redacted technical log and produces zero nutrition/progress or other dietary business objects.

## Independent review

OpenClaw returned `PASS`, P0=0, P1=0 and:

```text
Ready for SH-MODEL-002 completion: Yes
```

The reviewer used Ajv 8 plus `ajv-formats`, registered the Event/Amount and Nutrition/Progress resources by exact `$id`, asserted date/date-time formats and independently executed all 42 cases. All 14 expected-valid cases passed the real schema runtime; schema-expressible invalid cases failed at schema level; the 16 cross-field invalid cases failed in the independent semantic layer; total mismatches were zero.

Nonblocking follow-ups are recorded in `docs/work-items/SH-MODEL-002-review.md`: negative-zero canonicalization at the future runtime boundary, additional `not_applicable`/water/direct-query/extra-property boundaries, and explicit evidence-subset policy.

## Fresh gates

Source and independent clone both reproduced:

```text
CONTRACT_V2|PASS|id=diet-manager/contract-v2|statuses=5|protocol=3|legacy_guards=10
RECEIPT_DATE_V2|PASS|id=diet-manager/receipt-date-contract-v2|metrics=6|trace=11|legacy_guards=10
ISSUE_CORRECTION_V2|PASS|id=diet-manager/issue-correction-contract-v2|statuses=4|codes=23|operations=13|trace=6|legacy_guards=12
DECISION_THRESHOLDS|PASS|id=diet-manager/decision-thresholds/v1|version=1.0.0|cases=54|expiry_rules=8|gates=2|mutations=4
CORE_MODEL_SCHEMAS|PASS|version=1.0.0|cases=41|event_defs=6|inventory_defs=6|mutations=4
NUTRITION_PROGRESS_SCHEMAS|PASS|version=1.0.0|cases=42|definitions=10|mutations=4
```

Additional source gates:

```text
MODEL_PARSER_ERRORS=0
VALIDATOR_NONASCII=0
STRICT_JSON=PASS
PROTECTED_CHANGED=0
BUSINESS_CANDIDATES=0
```

## GitHub delivery

The frozen model candidate was committed and pushed to private `main` as:

```text
877170c4b2a515ecc2d56e7fa1b863ca92ce17b3
```

The independent clone at `E:\codx\skill\github\diet-manager-b` fast-forwarded to that exact commit, remained clean and reproduced all six validators. The documentation-only closure commit that contains this evidence is intentionally not self-referenced by hash.
