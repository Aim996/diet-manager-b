# EV-20260812-025 — SH-HARNESS-001 Shared Execution Harness

## Identity

- evidence_id: `EV-20260812-025`
- task_id: `SH-HARNESS-001`
- date: `2026-08-12`
- implementation review target: `6601db6320078c264dfa4c481cc66d82fe360f4b`
- branch: `agent/sh-harness-001-shared-runner`
- draft PR: <https://github.com/Aim996/diet-manager-b/pull/4>
- result: `accepted_harness_ready_backend_pending`

## Scope proved

This evidence proves one shared acceptance execution protocol:

- A is read-only/no-plugin/zero-write degradation only;
- B is the only executable product route;
- B without a backend driver reports pending, never product PASS;
- adapters cannot receive Oracle or forbidden authority;
- runner alone performs exact Oracle comparison;
- failed B execution cannot report a dietary business write;
- C has no independent adapter;
- default machine report is deterministic and safe to publish.

It does not prove SQLite, repository, business commands, installation or an OpenClaw/MCP production adapter.

## Plan prerequisite corrected

The manifest RED detected that required `CASE-STORAGE-001` was referenced by Plan 0.3 but absent from the 26-case catalog. The accepted correction:

- preserves the prior 26 case values and order;
- appends exactly `CASE-STORAGE-001`;
- reuses `domain-idempotency-conflict-v1` without changing fixture bytes;
- freezes same-key/same-input replay as exact-original-result with zero new meal, inventory, template and total business writes;
- adds one domain mutation against re-execution;
- sets the cumulative catalog to `1.4.0 / 27`.

## Verification evidence

### Node harness

```text
Node.js v24.14.0
15 tests
15 pass
0 fail
```

The implementation review target had 14/14 PASS. The fifteenth closure test directly addresses reviewer P2-1 by proving a Proxy is rejected before any trap invocation.

Default CLI:

```text
protocol_status=passed
product_status=backend_pending
case_count=27
a_degraded=27
a_business_writes=0
b_backend_pending=27
b_executed=0
b_compared=0
b_business_writes=0
c_independent_adapters=0
oracle_leak=false
machine_path_leak=false
mismatch_path_leak=false
```

### Windows PowerShell 5.1 serial regression

```text
CONTRACT_V2|PASS
RECEIPT_DATE_V2|PASS
ISSUE_CORRECTION_V2|PASS
DECISION_THRESHOLDS|PASS
CORE_MODEL_SCHEMAS|PASS
NUTRITION_PROGRESS_SCHEMAS|PASS
ISSUE_CORRECTION_MIXED_SCHEMAS|PASS
STORAGE_MAPPING|PASS
CORE_ACCEPTANCE_CASES|PASS|version=1.4.0|cases=5|fixtures=3|mutations=8
DOMAIN_ACCEPTANCE_CASES|PASS|version=1.4.0|cases=10|scenarios=9|mutations=12
OPS_SECURITY_ACCEPTANCE_CASES|PASS|version=1.4.0|cases=6|scenarios=6|mutations=12
GOLDEN_RECEIPTS|PASS|case_version=1.4.0|cases=8|assets=8
```

### Hygiene

```text
candidate changed files=17
protected path delta=0
business artifact delta=0
CR/NUL/BOM issues=0
secret-like added lines=0
machine-path added lines=0
git diff --check=PASS
```

No production business database or record was created during validation.

## Independent review

The reviewer used one model session and one no-credential isolated public clone. It recomputed the six hashes, compared the prior 26 raw case blocks, read the harness implementation, ran Node tests and the default CLI, confirmed PR metadata, then deleted the clone.

```text
SH_HARNESS_001_REVIEW|PASS|P0=0|P1=0|P2=2|sha=6601db6320078c264dfa4c481cc66d82fe360f4b|cleanup=1
```

Both P2 items are closed in the documentation/test-only closure delta; no production code changed after the reviewed candidate.

## Exit and next task

`SH-HARNESS-001` is complete at the protocol boundary. The next total-plan task is `SH-TRACE-001`. The B backend remains pending, so `B-STOR-001` and product implementation still cannot claim acceptance from this evidence alone.
