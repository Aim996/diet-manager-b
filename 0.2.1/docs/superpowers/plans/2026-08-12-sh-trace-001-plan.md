# SH-TRACE-001 Implementation Plan

**Goal:** Generate and validate the Plan 0.3 REQ→CASE→TASK→EV and DEC/RISK/CHG trace graph without creating a second manual state source.

**Architecture:** One dependency-free Node 24 module parses only formal Markdown registries, builds a normalized in-memory graph, validates all edges, and either writes four deterministic JSON mirrors or compares them byte-for-byte. Tests mutate temporary copies rather than changing authoritative files.

**Tech stack:** Node.js 24 built-ins, JSON, Markdown table parsing, SHA-256, Git.

## Constraints

- Work only on `agent/sh-trace-001-machine-mirror`.
- Never read, hash, edit, track or execute the five protected lease files.
- Do not add SQLite, business records, production adapters, OpenClaw/MCP calls or new safety-foundation behavior.
- Preserve `总功能开发计划0.3.md` as the sole approved state source.
- Routine verification is local and deterministic; use at most one final independent OpenClaw review.

## Task 1: Freeze governance inputs

- [x] Write this design, implementation plan and work-item brief.
- [x] Mark `SH-TRACE-001` as the only current WIP.
- [x] Add missing EV-020 through EV-025 rows to §28.2 and remove formal composite pseudo-IDs.
- [x] Freeze exact expected counts: 71 REQ, 144 CASE, 59 TASK, 28 DEC, 7 Q, 17 RISK, 7 DEBT, 4 CHG.

## Task 2: Build RED tests and parser

- [x] RED: absent mirrors fail.
- [x] RED: duplicate ID, orphan reference, combined pseudo-ID and unknown task dependency fail.
- [x] RED: selector count/order drift and task-case union drift fail.
- [x] RED: completed task without registered evidence and stale mirror bytes fail.
- [x] Implement formal-table parsing, ID extraction and normalized stage/status mapping.

## Task 3: Generate mirrors and derived matrices

- [x] Generate `requirements.json`, including the exact 144-case registry and four selectors.
- [x] Generate `tasks.json`, including requirements/cases/dependencies/evidence plus assertion/full-case matrices.
- [x] Generate `decisions.json` for all five governance families.
- [x] Generate `evidence-index.json` with exact path, length and SHA-256.
- [x] Require deterministic UTF-8/LF/two-space JSON and atomic replacement.

## Task 4: Verify and close

- [x] Run mutation tests and normal byte-for-byte validation.
- [x] Run focused Plan/JSON/UTF-8/secret/machine-path/protected-scope checks.
- [x] Write report, review package, independent review and closure evidence.
- [x] Update Plan 0.3 and development progress to completed; set `B-STOR-001` as the next approved task.
- [x] Commit, push and maintain one stacked draft PR with candidate-time local/remote/PR-head equality; post-review closure is documentation and regenerated identity only.
