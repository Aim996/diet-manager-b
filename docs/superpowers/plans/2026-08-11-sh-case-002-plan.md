# SH-CASE-002 Domain and Failure Acceptance Cases Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Extend the cumulative shared Oracle from five core cases to fourteen cases by adding nine deterministic domain and failure scenarios without creating a database or runtime adapter.

**Architecture:** `shared/acceptance-cases/cases.json` remains the only case Oracle and `fixtures/core-v1.json` remains the only fixture catalog. A new domain validator owns literal expectations for the nine-case suffix, while the existing core validator continues proving the original five cases did not drift.

**Tech Stack:** JSON, Markdown, Windows PowerShell 5.1, existing CONTRACT-v2 companion contracts and shared Schema files.

## Global Constraints

- Develop B only; A remains read-only degradation and C has no independent product implementation.
- Do not read, hash, edit, track or execute the five protected lease files.
- Do not create SQLite, DB, WAL, SHM, journal or JSONL business files.
- Do not execute Node, npm, Vitest or a real OpenClaw/MCP business adapter in this task.
- Preserve the original five case values and three original fixture objects.
- Expected values are test-owned literals; validators may compute hashes but may not copy candidate output into expectations.
- A failed FactCommit may produce only a separate redacted technical log and zero business artifacts.
- Every behavior change begins with a focused RED and ends with focused GREEN plus parser/data gates.

The exact nine-case suffix is:

```text
CASE-PURCHASE-001
CASE-INVENTORY-003
CASE-NUTR-001
CASE-ISSUE-001
CASE-CORR-001
CASE-MIXED-001
CASE-EFFECT-001
CASE-EFFECT-003
CASE-STORAGE-007
```

---

### Task 1: Create the independent domain validator and capture RED

**Files:**

- Create: `shared/tests/validate-domain-acceptance-cases.ps1`
- Read: `shared/acceptance-cases/cases.json`
- Read: `shared/acceptance-cases/fixtures/core-v1.json`

**Interfaces:**

- Consumes optional `-CasesPath` and `-FixturesPath` strings.
- Produces stable `DOMAIN_CASE_*` failure codes and final `DOMAIN_ACCEPTANCE_CASES|PASS|version=1.1.0|cases=9|scenarios=9|mutations=11`.

- [ ] **Step 1: Write ASCII-only validation helpers**

Implement strict equivalents of `Assert-DomainTrue`, `Assert-DomainEqual`, `Assert-DomainExactProperties`, `Assert-DomainExactStringArray`, `Read-DomainJson`, `Copy-DomainJson` and `Get-DomainCaseById`. `Assert-DomainEqual` must distinguish string, bool and integer JSON scalar types before comparing values.

- [ ] **Step 2: Freeze the independent ID lists**

Use literal arrays for the fourteen cumulative IDs, the nine domain IDs and the nine scenario IDs exactly as frozen in `SH-CASE-002-brief.md`. Require no duplicates and exact order.

- [ ] **Step 3: Add the missing-domain-case gate only**

Before implementing case-specific checks, require `version=1.1.0`, cumulative count 14 and the registered nine-case suffix. The existing catalog must fail with prefix:

```text
DOMAIN_CASE_SET_VERSION_INVALID
```

- [ ] **Step 4: Run and verify RED**

Run the new validator in Windows PowerShell 5.1. Confirm non-zero exit and exact prefix `DOMAIN_CASE_SET_VERSION_INVALID`; a parser error or file-missing error is not an acceptable RED.

- [ ] **Step 5: Run validator self-gates**

Require Parser errors=0, ASCII bytes only, CR=0 and NUL=0.

- [ ] **Step 6: Commit the RED validator**

```powershell
git add -- shared/tests/validate-domain-acceptance-cases.ps1
git commit -m "test: define SH-CASE-002 domain oracle"
```

### Task 2: Add the nine deterministic scenario fixtures

**Files:**

- Modify: `shared/acceptance-cases/fixtures/core-v1.json`
- Test: `shared/tests/validate-domain-acceptance-cases.ps1`

**Interfaces:**

- Consumes the existing environment, goals and query-view fixtures unchanged.
- Produces `version=1.1.0` and exact `domain_scenarios[]` objects referenced by the nine new cases.

- [ ] **Step 1: Extend the fixture-root gate**

Require exact root properties:

```text
fixture_catalog_id, version, environments, goals, query_views, domain_scenarios
```

Require original fixture Base64 snapshots to match the three `1.0.0` objects before accepting the suffix.

- [ ] **Step 2: Add the nine fixture scenarios**

Create exact scenario objects for purchase package quantities, two distinct inventory products, exact milk label/profile, consolidated amount/inventory Issues, two-to-three egg correction, ordered purchase/drink children, nutrition-effect failure, finalizer/concurrent failure and three idempotency conflicts.

For the two fault scenarios, include exact properties:

```text
failure_injection_point, pre_state_vector, pre_state_hash,
expected_error_code, should_rollback, post_state_vector,
post_state_hash, state_after_restart, same_key_retry_result,
official_business_data_diff
```

Hashes are uppercase SHA-256 of the exact UTF-8 state-vector strings and are checked independently by the validator.

- [ ] **Step 3: Prove fixtures alone are not enough**

Run the domain validator. Expected failure remains a missing/incorrect cumulative case-set error because the nine cases have not been added yet.

- [ ] **Step 4: Commit the fixture extension**

```powershell
git add -- shared/acceptance-cases/fixtures/core-v1.json shared/tests/validate-domain-acceptance-cases.ps1
git commit -m "test: add SH-CASE-002 domain fixtures"
```

### Task 3: Append the nine case Oracles and preserve the core subset

**Files:**

- Modify: `shared/acceptance-cases/cases.json`
- Modify: `shared/tests/validate-core-acceptance-cases.ps1`
- Modify: `shared/tests/validate-domain-acceptance-cases.ps1`

**Interfaces:**

- Consumes fixture IDs from Task 2.
- Produces cumulative catalog `1.1.0`, fourteen exact ordered cases, the original core validator result and the domain validator initial GREEN result.

- [ ] **Step 1: Add case-specific domain assertions before data changes**

Implement exact shape/value/reference checks for all nine Oracles from the brief. Require every setup to use `env-zh-cn-20260811` and its matching `domain_scenario_fixture`. Require all fault fields and the three idempotency conflict variants.

- [ ] **Step 2: Verify the stronger validator remains RED**

Run it against the five-case catalog and require failure at catalog version/ID count before any case-specific assertion can pass accidentally.

- [ ] **Step 3: Append the nine cases**

Advance catalog version to `1.1.0`, preserve the first five case JSON values, and append exact purchase, inventory, nutrition, Issue, correction, mixed, two failure and idempotency-conflict cases.

- [ ] **Step 4: Adapt the core validator without weakening it**

Replace its exact whole-catalog five-ID check with an exact fourteen-ID registered list plus an exact first-five core subset. Keep every existing core shape/value check and all eight mutations. Change only its final version field to `1.1.0`; keep `cases=5|fixtures=3|mutations=8` because it reports the subset it owns.

- [ ] **Step 5: Run initial GREEN**

Expected outputs:

```text
DOMAIN_ACCEPTANCE_CASES|PASS|version=1.1.0|cases=9|scenarios=9|mutations=0
CORE_ACCEPTANCE_CASES|PASS|version=1.1.0|cases=5|fixtures=3|mutations=8
```

- [ ] **Step 6: Commit the cumulative catalog**

```powershell
git add -- shared/acceptance-cases/cases.json shared/acceptance-cases/fixtures/core-v1.json shared/tests/validate-core-acceptance-cases.ps1 shared/tests/validate-domain-acceptance-cases.ps1
git commit -m "feat: add SH-CASE-002 domain cases"
```

### Task 4: Add eleven anti-weakening mutations

**Files:**

- Modify: `shared/tests/validate-domain-acceptance-cases.ps1`

**Interfaces:**

- Consumes a JSON clone of the cumulative case and fixture catalogs.
- Produces eleven named mutation PASS lines and final `mutations=11`.

- [ ] **Step 1: Implement exact mutation names**

```text
MUT-DOMAIN-DROP-REQUIRED-CASE
MUT-DOMAIN-COLLAPSE-PACKAGE-QUANTITIES
MUT-DOMAIN-AUTOSELECT-INVENTORY
MUT-DOMAIN-OVERRIDE-EXACT-LABEL
MUT-DOMAIN-SERIALIZE-ISSUES
MUT-DOMAIN-OVERWRITE-CORRECTION-TARGET
MUT-DOMAIN-REORDER-MIXED-CHILDREN
MUT-DOMAIN-ROLLBACK-FACT-ON-EFFECT-FAILURE
MUT-DOMAIN-EXPOSE-FINALIZER-SUCCESS
MUT-DOMAIN-REUSE-IDEMPOTENCY-RESULT
MUT-DOMAIN-ALLOW-FAILED-FACT-BUSINESS-WRITE
```

- [ ] **Step 2: Run each mutation through real validators**

Every mutation changes a plain JSON clone and must be rejected for its intended `DOMAIN_CASE_*` prefix. Do not assert only that the mutation function ran.

- [ ] **Step 3: Run final focused GREEN**

```text
DOMAIN_ACCEPTANCE_CASES|PASS|version=1.1.0|cases=9|scenarios=9|mutations=11
```

- [ ] **Step 4: Re-run parser and byte gates**

Require both validators Parser=0, ASCII=true, CR=0 and NUL=0; parse both JSON files strictly.

- [ ] **Step 5: Commit mutation hardening**

```powershell
git add -- shared/tests/validate-domain-acceptance-cases.ps1
git commit -m "test: harden SH-CASE-002 oracle"
```

### Task 5: Regress, review and close the task

**Files:**

- Create: `docs/work-items/SH-CASE-002-report.md`
- Create: `docs/work-items/SH-CASE-002-review-package.md`
- Create after PASS: `docs/work-items/SH-CASE-002-review.md`
- Create after PASS: `docs/evidence/EV-20260811-022-sh-case-002.md`
- Modify: `docs/开发进度.md`
- Modify: `总功能开发计划0.3.md`

**Interfaces:**

- Consumes frozen candidate hashes and independent reviewer results.
- Produces completion evidence only after P0=0/P1=0.

- [ ] **Step 1: Run all eight relevant validators**

Run the new domain validator, original core validator and six upstream contract/model validators serially in Windows PowerShell 5.1. Do not execute protected validators.

- [ ] **Step 2: Run repository hygiene gates**

Require strict JSON, exact 14/9 IDs, original-five semantic preservation, Parser/ASCII/CR/NUL gates, `git diff --check`, protected delta=0 and business candidate count=0.

- [ ] **Step 3: Obtain independent review**

The reviewer verifies package hashes, writes an independent checker, does not use candidate validator output as expected data, and independently mutates purchase quantities, inventory ambiguity, label priority, Issue consolidation, correction append-only behavior, mixed ordering, both failure layers and idempotency conflicts.

- [ ] **Step 4: Record P2 without expanding scope**

Any non-blocking adapter mapping, richer fault schedule or future runtime observation remains deferred to harness/B storage unless it proves the static Oracle unsound.

- [ ] **Step 5: Write evidence and progress after PASS**

Record completed, pending, new work, discovered problems, optimizations and later additions. Do not claim database, repository, runtime failure injection, installability or production OpenClaw integration.

- [ ] **Step 6: Push and reproduce in the independent clone**

Commit exact closure files, push private `main`, fast-forward `E:\codx\skill\github\diet-manager-b`, run all eight validators and require source/origin/clone equality plus clean status.
