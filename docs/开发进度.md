# 饮食管家 B：开发进度

> 更新时间：2026-08-12
> 权威计划：[总功能开发计划0.3.md](../总功能开发计划0.3.md)
> 当前路线：只开发 B；A 只保留只读降级规范，C 的必要服务端控制已经并入 B
> 当前 WIP：`B-SLICE-001` Tasks 1–9 已完成；下一步进入 Task 10 的候选冻结、证据包与最终独立审查

## 1. 这个项目现在是什么

这是一个给智能体使用的饮食记录 Skill。最终形态分为两层：

1. Skill/适配层理解用户意图并调用固定业务命令；OpenClaw、Docker、MCP 或其他智能体平台都只接薄适配器。
2. B 后端保存权威数据并负责事务、幂等、库存、营养、纠正、进度、查询和回执。

目前仍是开发版本，不能安装给真实用户，也不能写真实饮食数据。底层仓储、购买入库、餐食闭环、结构化回执、Issue 快捷提示、追加式纠正/撤销/恢复，以及有序“购买后食用”混合操作已经可执行；快捷项执行、饮水、Skill 指令层、完整中文文本渲染与安装发布仍未完成。

## 2. 当前数字

| 项目 | 当前值 | 含义 |
|---|---:|---|
| 总任务 | 59 | 总计划 0.3 的正式任务台账 |
| 已完成 | 23 | 39.0%；正式台账尚未把在制 `B-SLICE-001` 计为完成 |
| 进行中 | 1 | `B-SLICE-001` 已完成 Tasks 1–9；Task 9 已提交，下一步是 Task 10 |
| 未开始 | 27 | 后续纵向业务、可靠性、安装发布与 0.2 功能 |
| 已取消 | 8 | A/C 独立完整产品实现 |
| 共同案例目录 | 1.4.0 / 27 案 | 静态共同 Oracle，不代表 27 项产品功能已可用 |
| 最新证据 | `EV-20260812-030` | B-only 存储安全门通过 |
| 当前开发分支 | `agent/b-slice-001-vertical` | Tasks 1–9 已形成可追溯提交；Task 10 尚未开始 |

产品结论仍是：**不可安装、不可接入真实饮食数据、不可宣称 PRODUCT-0.1 可用。**

## 3. 已开发

### 3.1 路线与共同契约

- 已裁决只开发 B，不再并行维护 A/B/C 三套产品。
- 必要安全底座已按业务风险有界收口，不继续追求无关 Windows/平台细节的“完美安全”。
- 三份 v2 业务契约、确定性策略、共同模型、B SQLite 映射、共同案例、共享 harness 和机器追踪已完成。
- OpenClaw/MCP 被定位为未来薄适配器，不复制 repository 或业务规则。

### 3.2 B SQLite 基础（`B-STOR-001`）

- Node 24 内置 `node:sqlite`，无第三方 SQLite 驱动。
- 固定私有根与数据库文件名。
- migration 0001 建立 20 张表、18 个索引、22 个外键。
- 新库先在候选文件完整验证，再一次发布；失败无候选/WAL/SHM 残留。
- 未知库、漂移库、reparse/hard-link 等身份异常在返回可写连接前拒绝。
- 证据：`EV-20260812-027`。

### 3.3 B 服务端预览与状态权威（`B-MERGE-C-001`）

- 服务端创建并保存 preview，调用方不能自报状态。
- 签名 token 绑定 preview、输入 digest、主体、命令、预览内容和数据 revision。
- 同 key 完全相同请求返回原 preview；任一身份变化稳定冲突且零新写入。
- forged、stale、ghost、篡改、非法状态转换和 migration/schema 漂移都在业务写入前拒绝。
- 证据：`EV-20260812-028`。

### 3.4 B 原子 repository（`B-STOR-002`）

#### FactCommit

- 严格 prepared DTO；动态 getter、自定义原型、多余字段和非 canonical payload 在 SQL 前拒绝。
- `BEGIN IMMEDIATE` 内重新验证 migration、签名 preview 和服务器当前 repository revision。
- event、meal items、effect outbox、两次 envelope 状态转换和 idempotency checkpoint 一次提交。
- 每一个写点都有故障注入；提交前失败后完整表快照不变。
- 技术失败日志可以在回滚后单独写入，但只含阶段、稳定错误码、trace ID 和 input digest；不进入饮食表。
- 日志写入自身失败不会覆盖主错误，也不会产生业务数据。

#### EffectBundle 与库存

- 重启后可枚举 `pending/retryable_failed` 库存效果。
- 入库原子创建/校验 product、batch、inventory transaction 和唯一当前 projection。
- 扣减要求产品、批次、单位一致且数量足够，绝不写成负数。
- 库存不足时保留已提交事实，但不写 inventory transaction、不改变 projection。
- 响应丢失或并发重试从原始 transaction payload 重放，不用后来变化的当前投影伪造旧结果。

#### EnvelopeFinalize

- 只接受 effects 已稳定的预计算终态。
- finalization、envelope 终态和冻结 idempotency 结果一次提交。
- 任一写点失败只回滚最终器，不删除已提交事实/效果。
- 提交后响应丢失或后来有其他请求，仍返回原冻结结果。

#### 并发、崩溃与备份

- 真实 worker threads、独立 SQLite 连接和启动屏障，不是假并发。
- 相同事实并发：一份物理事实，两次同一权威结果。
- 同 authority 不同事实：一成功、一稳定冲突。
- 相同 effect 并发：一份 inventory mutation。
- 进程在未提交事务中退出后，重开数据库看到 0 条未提交 event。
- `quick_check`、外键检查、migration 身份和 `node:sqlite` backup 打开验证都通过。

#### 验证结果

| 验证 | 结果 |
|---|---|
| focused repository | 21/21 PASS |
| B 包完整 Vitest | 5 files / 69 tests PASS |
| TypeScript `--noEmit` / build | PASS |
| 并发与崩溃恢复 harness | PASS |
| OpenClaw 本地 plugin build/validate | PASS，无模型调用 |
| 临时数据库/日志残留 | 0 |
| 独立复核 | PASS，P0=0/P1=0，cleanup=1 |

证据：`EV-20260812-029`。

### 3.5 B 最小纵切 Tasks 1—3

- Task 1：冻结 B 领域命令协议、餐食/库存/营养输入和查询类型。
- Task 2：repository 支持有序信封操作、逐操作 FactCommit/EffectBundle 边界和冻结结果。
- Task 3：购买入库纵切完成；购买事实、商品、批次、入库 transaction、projection、营养标签版本和库存查询已接通。
- 对应已推送提交：`7283e3a`、`ddf3ed0`、`4503be6`、`1b9b3d0`。

### 3.6 B 最小纵切 Task 4

- 一次 `record_meal` 保存一条不可变餐食事实与有序项目；四种数量角色分开保存，估算只标记被推断字段。
- 家庭餐才查询库存；匹配唯一且足量时原子扣减。多候选或不足时保留餐食，跳过扣减、建立 Issue，库存永不为负。
- 外食明确跳过家庭库存读取和扣减，但仍保存营养采用来源与不可变快照。
- 公共资料和商品标签都形成版本化营养档案；历史餐食快照继续引用提交时版本。中文食物名称用规范摘要派生内部 ID，不把中文名称误当 ASCII 幂等键。
- 每个营养来源冻结 `per_100g/per_100ml/per_serving/per_item/per_package/custom_recipe` 基准、基准数量和单位；采用整数 `round_half_up` 按实际营养摄入量换算，并同时保存原始营养、公式和换算值。
- `EffectBundle` 在一个 `BEGIN IMMEDIATE` 中写营养、库存/跳过、Issue 和本操作进度贡献；技术失败时整包回滚，已提交餐食事实与 pending outbox 保留。未知营养/库存作用量保持 `null`，不会静默变成 0。
- `EnvelopeFinalize` 在同一最终事务中核对本餐贡献、读取前一日累计快照、冻结新的同日累计进度；第二餐回执不再只显示第二餐，也不会重复累计旧快照。
- `query_meals` 使用上海自然日 `[start,end)`、只返回 active 当前视图且不暴露内部 ID；`query_daily_summary` 读取最新权威累计快照的六项营养，未知值保持 `null`。
- 无效自然日（例如 2 月 31 日）被稳定拒绝，不被 JavaScript 日期自动纠正为另一天。
- pending EffectBundle checkpoint 现在严格核对标准四键、authority、operation sequence、完整 effect ID/kind/state 集合和 CAS 影响行数；篡改会在任何派生业务写入前失败。
- finalized 重放会经 repository 复核 finalization/idempotency 两份终态并深冻结返回对象；同 token 重放零新业务行。
- 兼容 Task 3 已存在的旧六字段营养 intent 与无 basis 旧档案；新快照仍冻结当前 basis，营养版本建立 `supersedes_profile_id` 链。
- 最终结果：7 files / 112 tests PASS；TypeScript `--noEmit` 与 build PASS；并发/崩溃 harness PASS；完整 `dist` 已重建；第二轮独立复核 P0=0/P1=0，结论为 Ready。

### 3.7 B 最小纵切 Task 5

- 新增纯函数 `buildReceiptData()`：只从冻结的餐食结果挑选可展示字段，固定按 `title → item... → issues（可选）→ progress` 排序；进度块永远最后。
- ReceiptData 不复制 event、batch、transaction、profile、snapshot、SQL、文件路径或技术日志；每个食物只保留名称、显式数量/单位、实际库存作用状态、估算字段和稳定 Issue code。
- 明确值不会被误标成估算；只有 `estimated_fields` 中列出的推断字段携带估算证据。
- 新增纯函数 `buildQuickPrompt()`：每组固定 3 个安全项（保持原样、稍后处理、自由文本），满足 2—4 项限制；支持组合和自然语言，最后一项与末行严格为“也可以直接说明实际情况，不必选择以上选项。”。
- 快捷提示不是模型临时生成。服务层先读取真实 `issues` 行，核对 issue ID/code/revision/open 状态/检测时间，再生成一小时有效的稳定绑定；EnvelopeFinalize 再在同一最终事务中逐项交叉核验。
- 回执没有第二张表。服务层把结构化回执和 quick prompts 放入 `FinalizeEnvelopeInput.payload`，EnvelopeFinalize 使用自己刚冻结的累计进度替换回执末尾进度块，然后与 finalization/idempotency 终态一起原子提交。
- 同一天第二餐的回执进度使用累计值，不会只显示第二餐增量；同键重试返回原始深冻结 ReceiptData/QuickPrompt，零新业务写入。
- `effects_pending` 的纯构建结果只有 pending 块，没有成功标题或进度；生产路径只有成功 EnvelopeFinalize 才持久化正式 ReceiptData。
- 最终结果：7 files / 115 tests PASS；TypeScript `--noEmit`、build、并发/崩溃 harness 和 `dist` 回执模块导入均 PASS；测试临时目录残留 0。

### 3.8 B 最小纵切 Task 6

- `correct_record` 与 `undo_record` 只追加纠正事实；原餐食事实不覆盖、不删除。对 inactive 餐再次 `undo_record` 会追加 `restore_event`，恢复也不是复活旧库存交易。
- 当前有效视图按 revision 顺序折叠已稳定的纠正链。pending correction 在 EffectBundle 成功前不可见，并阻止另一条纠正越过它；同 token 则可从原 checkpoint 继续。
- 纠正库存只补偿真实账本已发生的净扣减。多项目撤销逐项使用独立幂等权威；外食或原本跳过扣库的项目不会发明补偿交易。
- 若新增扣减超过当前真实库存，纠正事实、营养与进度仍生效，库存保持非负且不部分扣减；系统写入 `inventory_insufficient` Issue，并返回 `committed_with_issues`。
- 纠正后的营养快照与同日进度使用 before/after delta。EnvelopeFinalize 在自己的 `BEGIN IMMEDIATE` 内把 delta 应用到当时最新日进度，因此 EffectBundle 与 Finalize 之间插入另一餐也不会被覆盖。
- EffectBundle 完成但尚未 seal、以及已 `effects_stable` 但尚未 finalize 的两个崩溃窗口，均支持同 token 重试；不重复纠正事实、库存补偿、Issue 或最终化行。
- 最终器不信任调用方提交的纠正结果。它从 correction fact、标准四键 bundle、精确 outbox/progress effect、真实补偿交易和 Issue 重建权威结果，再核对 envelope、digest 和完整 item 字段。
- 最终结果：Task 6 focused 15/15 PASS；B 包 7 files / 130 tests PASS；TypeScript build、真实 SQLite worker 并发/崩溃 harness、OpenClaw 本地 plugin build/validate 均 PASS；独立复核 P0=0/P1=0、Ready。

### 3.9 B 最小纵切 Task 7

- 新增第一个受控多操作信封：只接受精确顺序 `[add_inventory, record_meal]`。购买效果先提交，餐食随后读取最新库存，因此 `CASE-MIXED-001` 能把 24 盒牛奶先入库、再饮用 1 盒，最终库存为 23。
- 两个子操作仍保留明确的 `sequence=0/1`、operation ID、状态和错误字段；每个子操作使用派生的作用域幂等键，避免购买与餐食的 effect/outbox/transaction ID 碰撞，同时不改变旧单操作非零 sequence 的历史 ID 公式。
- 父信封只 seal 一次、只写一条 finalization、只生成一个当日累计进度和一个回执进度块；最终状态由后一个餐食子项的真实业务状态决定。
- 后一个子项出现单位不兼容等业务 Issue 时，父结果为 `committed_with_issues`，但前一个已经提交的购买事实和 24 盒库存不会被撤销。
- 技术失败不会生成成功 finalization。若餐食派生写入事务回滚，购买事实与购买效果保留；同 token 重试后只补未完成部分，不重复入库。
- 新增两个持久边界恢复：餐食 EffectBundle 已提交但尚未 seal，以及已 `effects_stable` 但尚未 finalization。系统从 canonical checkpoint、outbox、营养快照、库存交易和 Issue 重建餐食权威结果，跳过已完成效果并只最终化一次。
- 新增 finalization 提交后响应丢失恢复：同 token 返回原始冻结结果，所有业务表零新增。
- EnvelopeFinalize 允许多 bundle 信封，但只从唯一 `daily_progress_contribution` 操作读取进度，并核对该操作在混合序列中的 sequence。
- 最终结果：Task 7 focused 8/8 PASS；B 包 7 files / 138 tests PASS；TypeScript `--noEmit`、build、真实 SQLite worker 并发/崩溃 harness、OpenClaw 本地 plugin build/validate 均 PASS；独立复核 P0=0/P1=0、Ready。

### 3.10 B 最小纵切 Task 8

- 冻结了精确 17 个 B-only 接受案例及固定顺序，覆盖混合操作、纠正、查询、EffectBundle 故障恢复、餐食、库存、营养、幂等、回执和同日进度。
- 8 个已经存在于公开案例目录的案例直接消费 `cases.json` 及 `core-v1.json` 解析后的 `CaseExecutionInput` 与夹具；另外 9 个案例使用独立、结构化、SHA 锁定的 `b-slice-inputs.json`，不再由测试矩阵伪造第二套案例权威。
- 每个案例使用新建的测试专属 SQLite 数据库并调用真实 `createDietDomainService()`；driver 只输出脱敏后的实际观察，不读取 expected Oracle，也不调用模型或远程 OpenClaw 测试平台。
- 同键重放不再用少量表行数近似判断零写入，而是对 migration 之外全部业务表做规范快照和 SHA 比较，可发现 INSERT、UPDATE 与 DELETE。
- 10 个 mutation 检查在真实 driver observation 边界注入错误，包括混合顺序、营养/库存混用、外食扣库、自动选择歧义库存、unknown 变 0、历史档案被改写、纠正覆盖原事实、重放写入、回执进度错误和失败效果伪成功。
- `CASE-EFFECT-001` 现在真正形成 `retryable_failed/effects_pending`：技术失败只保留 FactCommit，零营养/库存/进度/正式回执；同 token 重试按 `retryable_failed -> processing -> succeeded` 合法状态机恢复，attempt count 递增且成功时清除失败原因。
- `CASE-PROGRESS-010` 使用真实 `[add_inventory, record_meal]` 两子操作父信封。入库子项不产生饮食营养贡献，餐食子项提供实际贡献；系统只写 1 个 daily progress snapshot、1 条 finalization，并从唯一真实 ReceiptData progress 块核对冻结累计值。
- 验收修复采用 RED→GREEN：旧 synthetic authority 会拒绝真实公开 CASE-EFFECT 输入；旧进度案例缺少 child contribution/finalization 证据；旧 receipt observation 会读取顶层 100k 而漏掉真实回执块 200k。三处缺陷均由新回归精确捕获后修复。
- 最终结果：共享验收 22/22 PASS，其中精确 17 案全匹配且行为变异全部被拒；B 插件 7 files / 138 tests PASS；driver 严格 TypeScript、插件 TypeScript/build、真实 SQLite 并发 harness、OpenClaw 本地 plugin build/validate 均 PASS；独立复核 P0=0/P1=0/P2=0、Ready。

### 3.11 B 最小纵切 Task 9

- `CASE-EFFECT-001` 使用真实签名 preview 和 FactCommit 事务内故障点，证明提交失败后所有业务表仍为 0。唯一技术日志恰含 `phase/error_code/trace_id/input_digest` 四字段，不含原始文本、食物、数量、库存、SQL、secret 或绝对路径。
- 新增独立 Node 子进程 worker，只接受 `after_fact_commit`、`after_effect_bundle`、`after_finalize_before_reply` 三个精确 mode；不接受数据库路径，只能使用 harness 在系统 TEMP 直属创建且带 owner token 的测试根。
- FactCommit 后退出：崩溃时已有 1 个事实和 2 个 pending outbox，零营养快照；重启只应用 pending effects，不重复事实或 outbox。
- EffectBundle 后退出：事实和业务效果已稳定但尚未最终化；重启只写最终器拥有的进度、finalization 与 mixed rows，并把全部允许变化表按全行全列规范快照字面 SHA 精确核对。
- Finalize 提交后响应前退出：重启同 token 返回与持久化 payload 字节一致的 JSON，所有数据库表规范快照完全不变。
- 子进程有 30 秒硬上限和 kill signal；hang self-test 使用 200ms 上限并验证无存活 PID。父进程无论验证成功或失败都清理自己拥有的测试根。
- 删除前重新核对系统 TEMP 直属、根 dev/ino/birthtime、普通目录、owner marker 的 bytes/size/ctime/token；目录被替换时 fail closed，外部 sentinel 保留，不会递归删除新对象。
- harness self-test 覆盖挂死、根替换、异常清理和计数不变的字段篡改；三窗口正常路径结束时子进程、数据库和日志残留均为 0。
- 最终结果：Task 9 focused 1/1、crash package PASS、B 插件 138/138、TypeScript/build、真实 SQLite 并发 harness、OpenClaw 本地 build/validate 均 PASS；独立复核经过两轮修复后 P0=0/P1=0/P2=0、Ready。

## 4. 本轮新增开发内容

1. 新增九个 0.3 指定案例：明确克重、橙子可食部估算、半碗饭上界、公司苹果、库存多候选、库存不足、公共营养源、营养版本历史和同日进度。
2. 新增餐食 EffectBundle 的整体事务，库存扣减、营养档案/快照、Issue、outbox 状态和操作贡献一起成功或一起回滚。
3. 新增餐食与单日营养查询；查询在只读事务中执行，并用表快照证明不写业务数据。
4. 新增 `CASE-EFFECT-001` 对应故障回归：FactCommit 成功后注入营养写入失败，餐食事实保留，所有派生表为零、outbox 仍 pending、技术日志脱敏且无伪进度。
5. 新增中文食物名称回归；先复现 `DOMAIN_IDENTITY_INVALID:idempotency_key`，再改用规范摘要作为内部 ID 种子。
6. 新增操作贡献持久化断言；进度贡献现在进入 `effect_bundle_commits.payload_json` 的 `daily_progress_contribution` 效果项。
7. 新增非法自然日回归；`2026-02-31` 不再被 `Date.parse` 静默滚动到 3 月。
8. 新增营养基准与整数换算：例如每 100g 营养值按 150g 摄入换算为 1.5 倍，同时库存仍可只扣半碗；原始基准、摄入量、公式和结果同时留证。
9. 新增未知量回归：`null` 营养采用量产出未知营养，不产出伪 0；`null` 库存扣减量跳过 transaction 并生成稳定 Issue。
10. 新增旧版本兼容：旧 pending label intent 继续执行；旧 product nutrition profile 不被改写，新餐食快照仍保存本次 basis。
11. 新增严格 checkpoint/CAS：篡改 authority、漏 effect 或竞争更新都会 fail closed，不能形成“bundle 已成功、outbox 仍 pending”的半终态。
12. 新增同日累计：EffectBundle 只提交本操作贡献，EnvelopeFinalize 在写事务中形成累计快照；两餐各 100000 后，第二餐回执与查询均为 200000。
13. 新增真实跨 effect 回滚：第一项已写营养和库存后注入失败，所有派生表/projection 一起回滚，餐食事实和 pending outbox 保留。
14. 新增冻结重放：同一已完成餐食再次执行返回深冻结原结果、核对终态权威且零新行。
15. 新增进度贡献权威绑定：最终器逐项核对 bundle 与 outbox，阻止把 contribution 搬到营养或库存 effect 后伪造终态。
16. 新增乱序完成保护：请求接收时间早晚不再决定“最新”累计，较晚最终化的快照一定排在已提交快照之后。
17. 新增 `CASE-RECEIPT-001` 结构化多食物回执：逐食物块、显式数量证据、无内部 ID、最终进度块顺序均有精确断言。
18. 新增 `CASE-RECEIPT-003` 快捷提示：3 个稳定选项、至少一个安全出口、组合/自然语言标志和自由文本末行均被冻结。
19. 新增 pending 回执负向断言：技术待处理不能出现成功 title 或 progress。
20. 新增回执累计对账：第二餐最终器将本餐贡献改写为同事务累计结果，回执与 `daily_progress` 逐字段一致。
21. 新增 Issue 权威绑定：提示生成前读取真实 Issue 行，最终器再次核对真实行，不能把伪造 prompt 塞入最终回执。
22. 新增回执隐私投影：ReceiptData 只挑选面向用户的业务字段，machine prompt 绑定信息与显示块分离。
23. 新增 `CASE-CORR-001`：两枚鸡蛋改成三枚时只追加纠正事实，并只多扣真实的一枚；同 token 重放零新写入。
24. 新增完整纠正链：change、void、restore 按 revision 追加；多项目同批次逐项补偿，外食和原跳过扣库项目零伪交易。
25. 新增“实际账本而非声明快照”补偿：一次 2→12 因不足跳过后，再改 12→1 只返还真实一枚，随后撤销只返还剩余真实一枚。
26. 新增纠正故障恢复：FactCommit 后 pending、EffectBundle 已终态未 seal、`effects_stable` 未 finalize 三个窗口均可同 token 续跑。
27. 新增纠正最终器权威重建：伪造 envelope/digest、correction ID、target、revision、operation、交易 ID 或 Issue code 都会在最终写入前拒绝。
28. 新增纠正进度并发回归：纠正 effects 后插入另一餐并先 finalization，最终日进度仍保留另一餐并正确应用纠正 delta。
29. 新增 `CASE-MIXED-001`：同一信封按 sequence 先购买 24 盒牛奶、再饮用 1 盒，库存轨迹为 0→24→23。
30. 新增子操作 effect identity 隔离：混合操作使用派生子键，旧单操作仍保留 parent key + 原 sequence 的兼容 ID。
31. 新增混合业务 Issue 回归：后项 `committed_with_issues` 不撤销前项事实或库存。
32. 新增混合技术失败回归：餐食 effect 整体回滚、父信封不伪成功、购买 effect 不重复，同 token 可恢复。
33. 新增两个 durable-boundary 崩溃回归：meal EffectBundle→seal 和 seal→finalize 均可从数据库权威继续。
34. 新增混合 finalization 响应丢失重放：精确返回原结果并证明零新行。
35. 新增多 bundle 进度权威选择：只允许唯一 progress-producing operation 贡献累计进度。
36. 新增精确 17 案 B-only 接受驱动：每案新数据库、真实服务执行、固定 observation key 和确定性报告。
37. 新增公开案例/夹具权威解析：8 个既有案例直接绑定公开目录，9 个缺失案例使用独立结构化输入目录；四份输入文件均由 manifest SHA 锁定。
38. 新增全业务表重放指纹：同键执行前后对 migration 之外全部业务表做规范快照和 SHA 比较，不再只统计部分表行数。
39. 新增真实 observation mutation：十类禁止行为均修改实际 driver 输出或执行边界，不能靠修改 expected 克隆自我证明。
40. 新增技术效果恢复状态机：首次失败把 outbox 置为 `retryable_failed`，同 token 重试先 CAS 到 `processing`，再进入成功/业务跳过终态并记录 attempt count。
41. 新增真实多子操作进度验收：读取两个 child 槽位、唯一 progress effect、唯一 daily snapshot/finalization，以及 ReceiptData 内唯一 progress 块的六项累计值。
42. 新增 FactCommit 脱敏日志证据：技术失败可以产生四字段日志，但饮食记录、库存、营养、进度和正式回执保持零写入。
43. 新增三个真实进程崩溃窗口：子进程在持久检查点后以 73 退出，父进程重新打开同一测试数据库并只补未完成阶段。
44. 新增崩溃 harness 资源权威：固定 mode、owner token、根身份复核、超时终止、PID 存活检查和异常路径清理。
45. 新增全表 crash snapshot：非最终器表重启前后逐字节等价；最终器拥有的 5 张表也以独立字面 SHA 锁定全部列和行。

## 5. 发现问题与处理

| 发现 | 处理 |
|---|---|
| 初版只比较 token/调用参数中的 revision，未再次计算数据库当前 revision | 在 FactCommit 事务内计算服务器当前 revision；先 RED 后 GREEN |
| 初版 repository revision 漏掉 product/batch 主数据 | 新增独立 RED；把两类权威行纳入 revision |
| 只会执行指定 outbox，不足以证明重启恢复能发现 pending work | 新增确定性 pending/retryable 枚举与 reopen 测试 |
| 故障测试只覆盖事务末尾会漏掉中间半写 | 扩展到 FactCommit 7 个、Effect 5 个、Finalize 4 个逐写点 fault matrix |
| response loss 可能错误返回后来变化的当前投影 | replay 从原 inventory transaction payload 重建原结果 |
| 测试并发可能只是顺序调用 | 使用 worker threads、独立连接和共享启动屏障 |
| 运行环境自带 Node 24.14 低于当前 OpenClaw 要求 | 最终证据使用已核 SHA 的 Node 24.15；不修改系统环境 |
| 首版餐食用例只覆盖英文名称，中文食物名进入 ASCII 幂等键后失败 | 新增中文 RED；仅食物型营养档案改用 canonical SHA-256 种子，商品档案兼容公式不变 |
| 首版 EffectBundle 终态只有效果 ID/状态，未保存本操作进度贡献 | 新增精确 RED；把冻结贡献放入 `daily_progress_contribution` 效果项 |
| JavaScript 会把 2 月 31 日自动滚动到 3 月 | 查询前先做 UTC 自然日 roundtrip 校验 |
| 最初测试种子中的商品标签来源与餐食来源不一致，触发了正确的档案冲突 | 修正测试 fixture，不放宽生产冲突检查 |
| 同一固定时钟下连续餐食会碰撞进度生成时间唯一索引 | 在同一事务内选择第一个未占用毫秒，保持确定性和唯一性 |
| 营养来源保存的是每 100g/份原始值，初版却直接当成实际摄入 | 新增 basis 字段和 BigInt `round_half_up` 换算，快照同时保存 source/converted 两组数值与公式 |
| JavaScript `Number(null)` 会把未知采用量/扣减量变成 0 | 所有 nullable 数量显式分支；营养未知保持六项 `null`，扣减未知不写 transaction |
| 来源营养六项完整但本次采用量未知时，快照覆盖状态仍会误写成 `complete` | 新增精确 RED；营养档案继续按来源完整度，餐食快照改按换算后的实际营养值计算并写成 `partial` |
| Task 3 的旧 pending intent/旧 nutrition profile 不含 basis | 使用两套精确 shape 解析而非放宽任意字段；旧档案原样保留，新档案写 basis |
| 第二餐终态回执只包含本次 contribution | EffectBundle 只写 contribution；EnvelopeFinalize 在同一事务聚合前一累计快照并发布新累计终态 |
| finalized 餐食重放直接 `JSON.parse`，对象可变且未交叉核验终态 | 重放统一调用 repository finalizer replay，核对两份终态并递归冻结 |
| 餐食 checkpoint 只比较 `data_revision`，篡改/漏更新可能制造半终态 | 精确校验 checkpoint/outbox 全集合、终态 state 与每个 UPDATE 的 `changes()===1` |
| 单项故障用例未证明前序 inventory/Issue 写入会回滚 | 新增两项目晚故障 seam；已完成第一项派生写入后失败，整包表快照和 projection 恢复 |
| 最终器只比较“任意 effect 中的 contribution”，未绑定专用进度 outbox | 新增搬移贡献篡改 RED；按唯一 operation 的 outbox 全量核对 effect ID/kind/state/exact shape，仅成功的 `daily_progress_contribution` 可携带贡献 |
| 较早接收的请求若较晚完成，累计快照时间可能倒退并被查询层隐匿 | 新增乱序 finalize RED；新快照时间至少为 `max(finalizedAt, previous.generated_at + 1ms)`，再处理唯一值碰撞 |
| 初版回执若直接使用 EffectBundle 的本餐 contribution，第二餐会只显示本餐增量 | EnvelopeFinalize 在同一累计事务中重绑回执末尾 progress，禁止回执层额外查询或重算 |
| 仅从 `issue_codes` 自行拼提示无法证明提示来自真实 Issue | 服务层读取并核对真实 Issue 行；最终器按 envelope/item 顺序再次核对 ID、code、revision、状态和时间 |
| 机器提示必须绑定 Issue，但用户回执不得泄漏内部 ID | `quick_prompts` 保存机器绑定；ReceiptData 的 issues 块只保留 code、可读选项及自由文本出口 |
| 技术 pending 若复用成功回执 builder，可能误带成功标题或进度 | 单独冻结 pending 形状，只含 `{kind: pending, code: effects_pending}`；正式结果仍只由成功最终器持久化 |
| 初版纠正只处理首个餐食项目，同批多项还会共用库存幂等键 | undo/restore 为所有项目生成独立 effect 与 transaction 权威；新增同批双项目回归 |
| pending correction 的事实曾提前进入查询有效视图 | 只折叠终态 succeeded/applied 或 permanent_business_skip/applied_with_issues；其他纠正保持不可见并阻塞后续链 |
| 库存补偿最初按业务快照声明的 from/to 计算，连续纠正会凭空入库 | 改为汇总原始消费交易与所有已成功补偿交易的有符号净额，再计算 desired-actual |
| correction inventory 不足曾被当技术失败，事实会永久卡 pending | 安全跳过增量扣减、写稳定 Issue、继续营养/进度，并形成 `committed_with_issues` |
| EffectBundle 已提交未 seal、或 effects_stable 未 finalize 时，同 token 无法恢复 | 从存储权威重建纠正结果，按当前 envelope 阶段继续 seal/finalize，不重做业务效果 |
| 纠正最终器最初只绑定进度/status，伪造业务字段可被冻结 | 从 fact/outbox/progress/transaction/Issue 重建完整 item，并绑定 envelope ID 与 input digest 后逐字段比较 |
| 恢复实现一度把 `result` 加到标准 EffectBundle 顶层，破坏四键兼容 | 删除新增顶层字段；writer、reader、FactCommit、finalizer 全部继续锁定标准四键 |
| 混合操作最初沿用父幂等键派生每个 child effect，购买与餐食会发生 ID 碰撞 | 每个 child 使用稳定派生的作用域幂等键；parent token/digest 仍是唯一外部权威 |
| 子键接入后，旧非零 sequence 购买操作一度被误算成 sequence 0 | 新增 sequence 7 的兼容 RED；无子键时继续使用旧 parent key + sequence，只有混合子键使用 ordinal 0 |
| 混合餐食测试的营养来源与刚入库商品标签不同，触发正确的档案冲突 | 修正测试 fixture 使用同一标签权威，不放宽生产 nutrition profile 冲突检查 |
| meal result 曾固定返回 sequence 0 | 改为使用当前 operation sequence，混合父结果明确保持 0/1 顺序 |
| finalizer 最初假设每个信封只有一个 EffectBundle | 改为定位唯一 progress-producing operation，并把 bundle sequence 与 mixed item sequence 交叉核验 |
| meal EffectBundle 提交后 seal 前崩溃，重试会重复调用 pending-only 执行器 | 从终态 checkpoint、outbox 和业务派生表重建餐食结果；已完成效果不再执行 |
| seal 后 finalization 前崩溃会把信封永久卡在 `effects_stable` | mixed service 接受该稳定状态并直接进入唯一 finalizer；新增精确 RED→GREEN |
| 初版 17 案矩阵自己定义 requirement/setup，再验证自己的数据 | 8 个公开案例改为直接解析并 SHA 绑定公开案例与夹具；只有目录缺失的 9 案使用独立结构化权威输入 |
| 初版 mutation 只修改 expected 克隆，无法证明真实错误实现会失败 | 改为包装真实 driver 并在实际 observation/执行边界注入十类错误，每一类都必须使验收失败 |
| 初版同键零写只统计部分表行数，漏掉 UPDATE/DELETE 或其他业务表 | 动态枚举全部业务表，对列和行做规范快照并比较 SHA |
| 技术失败后 outbox 仍为 pending，且重试曾从 retryable_failed 直接跳终态 | 失败持久化 `retryable_failed`；重试严格经过 `processing`，递增 attempt count 并清理/冻结 reason |
| 初版 CASE-PROGRESS-010 只是一次 meal 中放两个 item | 改为真实 `[add_inventory, record_meal]` 两子操作父信封，并核 child contribution、唯一 snapshot/finalization 和父回执累计 |
| 进度验收一度从终态顶层复制 receipt_progress，没有读取真实回执块 | 新增可区分顶层 100k/回执块 200k 的 RED；只接受唯一精确形状的 ReceiptData progress 块 |
| Task 9 最初用 service preview 后强改固定 digest，先触发了正确的身份拒绝 | 改用公开测试 seam 创建固定 `A×64` digest 的真实签名 preview，不放宽生产身份校验 |
| crash harness 最初没有子进程超时，worker 卡死会让父进程永久等待 | 正常模式固定 30 秒，hang self-test 固定 200ms；超时后终止并验证无存活 PID |
| 清理最初只依赖内存路径集合，替换同名目录可能被误删 | 冻结根身份与 owner marker；删除前逐项复核，不匹配就 fail closed 并保留 sentinel |
| 重启证据最初只比较行数，抓不到 UPDATE 或删除再插入 | 对全部表做规范快照；所有允许变化表也使用独立字面 SHA 精确核所有单元格 |
| hardening 后 emergency cleanup 一度错误遍历 Map entries | 增加 verifier-failure self-test，改为遍历 `roots.keys()` 并证明失败路径残留为 0 |

Task 4 第一轮独立复核给出 6 项 P1，后续差量复核再发现 3 项 P1；9 项均已通过精确回归关闭。Task 8 独立复核发现输入权威、多操作进度与真实回执进度 3 项 P1，均经 scoped re-review 关闭；最终结论 P0=0、P1=0、P2=0、Ready。

## 6. 待优化（不阻断）

独立审查累计记录了 4 个 P2，按“非必要内容提前结束”原则不在本任务继续开发：

1. 删除 effect replay 中一次无效的预查询，保留唯一权威 transaction ID 路径。
2. 将 finalized 后的 FactCommit 重试从通用 fail-closed 错误改成更语义化的冲突码。
3. 未来真正引入持久 `retryable_failed` worker 时，定义跨回滚保存的 attempt count 规则。
4. Task 7 的 repository finalizer 当前按有序 operation ID 绑定 mixed rows；未来可再把 command type、child key、status 和 payload 与终态 effect 做逐字段二次绑定。当前服务层已从签名输入和权威终态读回构造这些行，因此它不阻断 Task 7。

其他后续优化：技术日志轮换/保留、性能压测、批量 effect 调度和运维指标。这些都不能进入饮食业务表，也不应阻塞当前安全门。

## 7. 已完成：`X-GATE-001`

这一步没有写新业务功能，只回答了一个问题：**B 的存储与事务证据是否足够进入最小纵向业务切片？**

最终结果：`pass_b_safety`。

1. 13/13 个 G1 存储责任通过，且没有借用 A/C 证据。
2. 69/69 项 B 测试、真实并发/崩溃、OpenClaw 本地插件和追踪门通过。
3. 写入失败零业务半记录、库存不为负、响应丢失/重启可恢复。
4. 保护路径改动、平台地址/令牌泄漏和测试残留均为 0。
5. 独立复核：P0=0、P1=0；两项文档类 P2 已记录但不阻断。

门只授权 `B-SLICE-001`；没有授权 G2/G3、安装、部署或完整 Skill 声明。

## 8. 后续待开发

按 `B-SLICE-001` 实施计划继续，不临时改路线：

1. Tasks 1—9 已完成：协议、repository、购买、餐食/库存/营养/累计查询、结构化回执、稳定快捷提示、追加式纠正/撤销/恢复、有序“购买后食用”混合编排、17 案 B-only 接受驱动，以及失败日志/三阶段真实崩溃恢复证据均已有可执行代码与回归测试。
2. Task 7 已完成同一信封内的有序多操作与单日累计；跨日期业务属于后续正式案例，不在本任务冒充完成。
3. 快捷提示的“展示”已完成，但真正执行选项、自然语言等价映射、过期拒绝和冲突组合拒绝属于后续 Issue 服务，当前不能冒充已完成。
4. Task 8 已完成：17 个 B-only 案例使用公开/独立结构化输入权威、真实服务与实际 observation 验收；这只是 B 纵切行为证据，不代表 G2 正式门或完整 Skill 已通过。
5. Task 9 已完成：技术失败日志脱敏、测试根隔离、三个真实进程崩溃窗口、重启权威与零测试污染均有自动化证据。
6. Task 10：下一步冻结候选证据、执行完整本地门与最终独立审查，更新 0.3 任务台账和 `X-GATE-002` 输入。
7. `B-FAULT-001`：随后扩展其他业务的 EffectBundle/seal/finalize、响应丢失与进程重启故障矩阵；纠正路径的两个窗口已在 Task 6 提前覆盖，但不冒充全部故障矩阵完成。
8. `X-GATE-002` 通过前不生成 selected-route-map，不做安装/发布；通过后再进入 PRODUCT-0.1 的 Skill 包装、OpenClaw 薄适配和干净环境安装。
9. PRODUCT-0.2 的模板、回顾、提醒和丰富导出继续留在 0.1 之后。

## 9. GitHub 与文档

- 公开仓库：<https://github.com/Aim996/diet-manager-b>
- B-STOR-002 draft PR：<https://github.com/Aim996/diet-manager-b/pull/8>
- X-GATE-001 draft PR：<https://github.com/Aim996/diet-manager-b/pull/9>
- 当前开发分支：`agent/b-slice-001-vertical`
- 当前纵切草稿 PR：<https://github.com/Aim996/diet-manager-b/pull/10>
- 已推送纵切基线：`1b9b3d0`（Tasks 1–3）
- Task 4 代码/测试/产物提交：`d9ccaeb`；最终验证 112/112、并发 harness、构建、产物导入及独立复核均通过
- Task 5 代码/测试/产物提交：`007afdc`；最终验证 115/115、TypeScript、构建、并发 harness 与回执产物导入均通过
- Task 6 代码/测试/产物提交：`1748548`；最终验证 130/130、Task 6 focused 15/15、TypeScript、并发 harness、OpenClaw 本地 plugin validate 与独立复核均通过
- Task 7 代码/测试/产物提交：`54af295`；最终验证 138/138、Task 7 focused 8/8、TypeScript、并发 harness、OpenClaw 本地 plugin validate 与独立复核均通过
- Task 8 代码/测试/产物提交：`4dad227`；最终验证共享验收 22/22（含 17 案）、B 插件 138/138、driver/插件 TypeScript、并发 harness、OpenClaw 本地 plugin validate 与独立复核均通过
- Task 9 初始崩溃证据提交：`569a3e3`；harness hardening：`c5ac0b9`；完整快照/清理修复：`37e3030`；最终验证 focused 1/1、crash package、138/138、TypeScript/构建、并发、OpenClaw 本地 validate、零残留和独立复核均通过
- 实施报告：[B-STOR-002-report.md](./work-items/B-STOR-002-report.md)
- 独立复核：[B-STOR-002-review.md](./work-items/B-STOR-002-review.md)
- 存储门报告：[X-GATE-001-report.md](./work-items/X-GATE-001-report.md)
- 存储门复核：[X-GATE-001-review.md](./work-items/X-GATE-001-review.md)
- 最新证据：[EV-20260812-030-x-gate-001.md](./evidence/EV-20260812-030-x-gate-001.md)

机器令牌、专用测试平台地址、本地依赖、真实数据库、饮食记录和临时日志一律不进入 GitHub。
