# 饮食管家开发进度

> 更新日期：2026-08-11
>
> 本文件是便于阅读的进度快照，不是第二份任务台账。任务状态、依赖、完成条件和执行顺序以项目根目录《总功能开发计划 0.3》和对应 work-item brief 为准。

## 最新简明进度

当前按 [Skill-First B 简明计划](./superpowers/plans/2026-08-11-skill-first-b-practical-plan.md) 执行。该计划只开发 B；Skill 是智能体入口，SQLite 后端是唯一业务实现，OpenClaw/MCP 只是适配器。

### 当前完成度（粗略估算）

| 范围 | 完成度 | 说明 |
|---|---:|---|
| 产品计划与边界 | 90% | B 单线、失败原子性、CONTRACT-v2 及 Skill/后端/适配器职责已冻结 |
| 必要安全底座 | 100%（当前范围） | `DEC-028` 有界退出验收通过；非必要 Windows 边角转入 deferred，不再阻塞产品 |
| B 业务产品 | 39% | 三份 v2 业务契约和营养来源契约已验收；确定性策略、模型和真实业务表尚未开始 |
| OpenClaw 实际集成 | 8% | 专用平台完成失败、忽略和澄清规则验证；插件尚未部署 |
| 整体可交付度 | 约 52% | 安全前置、三份 v2 契约和 GitHub 交付链路已收口；策略、数据模型和真实业务闭环尚未完成 |

### 已开发

- 新建 6 任务简明实施计划，明确先 Skill 合同、再 SQLite、再业务闭环、最后 OpenClaw。
- B 的可移植动作从 4 个扩展到 8 个：餐食、饮水、库存写入、库存查询、餐食查询、每日汇总、更正、撤销。
- 增加统一结果状态：开发期 `foundation_not_implemented`，以及业务态 `committed`、`committed_with_issues`、`needs_clarification`、`ignored`、`failed`。
- 增加结果一致性校验：`committed=false` 不能伪装成提交成功，失败结果不能携带业务 `record_id`。
- 当前 foundation 仍保持零写入；未部署时不生成 `operation_id` 或 `record_id`。
- Skill 已明确：技术日志不算饮食记录；工具失败时不得用记忆、便签、聊天历史或表格代写后再声称成功。
- Node 24 内置 `node:sqlite` 兼容门已通过：WAL、提交、回滚、关闭和 test-owned 零残留均有测试覆盖；没有加入第三方 SQLite 驱动。
- `SH-SAFE-BASE-001` 已按 `DEC-028` 完成有界退出验收：路径逃逸、物理残留、报告可信度、业务候选扫描和 4 项状态隔离全部通过；业务类文件与测试夹具残留均为 0。
- `shared/business-contract.md` 已冻结为 CONTRACT-v2：`FactCommit → EffectBundle → EnvelopeFinalize`、五种业务结果、幂等身份、服务端预览绑定，以及“FactCommit 失败可写独立技术日志但业务零数据”均通过机器校验和 OpenClaw 第二轮独立复核；`SH-CONTRACT-001` 已由 `EV-20260811-013` 完成。
- `SH-CONTRACT-002` 日期/回执 v2 已完成两轮 RED→GREEN、OpenClaw 独立语义复核和最终本地门禁；证据为 `EV-20260811-014`。
- `SH-CONTRACT-004` Issue/纠正 v2 已完成两轮语义 RED→GREEN；四状态 Issue、追加纠正、三阶段补偿、PRODUCT-0.2 批量预览和失败零业务数据均通过 OpenClaw 第二轮独立复核；证据为 `EV-20260811-015`。
- 已建立私有 GitHub 仓库 `Aim996/diet-manager-b`；受保护租约、业务数据、密钥、raw evidence 和废弃 A/C 实现均未上传，独立 clone 后三项 v2 验证器再次 PASS。

### 待开发

1. `SH-DECISION-001`：冻结候选、临期、模板容差、取整和 B-only 门条件的确定性策略表，关闭模型入口依赖。
2. 进入 M2：完成 Event/Amount/Product/Inventory、Nutrition/Progress、Issue/Correction/Mixed 三组模型与 Schema，再形成共同 SQLite mapping、harness 和 trace。
3. Task 3：入口门满足后，实现业务 SQLite repository 与独立技术日志边界，重点验证 `FactCommit` 失败时业务零数据。
4. Task 4：完成“记录 → 查询 → 更正 → 撤销”首个真实纵向闭环，并补饮水和每日汇总。
5. Task 5：构建并部署到专属 OpenClaw 测试平台，验证重启持久化、幂等和故障原子性。
6. Task 6：业务闭环稳定后再决定是否增加 MCP 薄适配器。

### 本轮新增

- 新计划：`docs/superpowers/plans/2026-08-11-skill-first-b-practical-plan.md`。
- `foundation.test.ts` 完成两轮真实 RED→GREEN：动作/提交一致性与非写入状态均已冻结；当前 13/13 PASS。
- TypeScript `tsc --noEmit` PASS。
- SQLite compatibility 从缺少运行时模块的 RED 进入 GREEN；当前 B 包共 2 个测试文件、14/14 PASS，TypeScript 检查 PASS。
- Skill 行为基线：未加载新规则时，测试 OpenClaw 把“喝水 500ml”写进每日记忆并称“记好了”；加入新规则后，它正确说明 `committed=false`、未形成正式饮食记录，也不再用记忆代写。
- 新状态行为基线：未给规则时，OpenClaw 对 `ignored + committed=false` 错误回答“记下了”；加入当前 Skill 规则后，它明确说明未来计划没有写入，`needs_clarification` 也会说明尚未记录并只追问缺少的食物和数量。
- CONTRACT-v2 机器校验完成两轮 RED→GREEN：旧契约缺机器块时稳定失败；补旧规则防退化测试后又以 `Legacy rule guards are missing` 失败，当前输出 `CONTRACT_V2|PASS|statuses=5|protocol=3|legacy_guards=10`。
- 已建立 CONTRACT-v2 专属 brief、实施报告和独立复核包；旧无 `-v2` 文件明确保留为 CONTRACT-v1 历史，不再冒充当前证据。
- OpenClaw 第一轮独立审阅发现 F1–F9 与 NUTR 优先级问题；逐项 TDD 修订后，第二轮独立复核 `PASS`，77 条需求、10 项旧规则和业务零数据边界全部确认，新证据为 `docs/evidence/EV-20260811-013-sh-contract-v2.md`。
- 日期/回执 v2 已冻结 `OccurredTime`、单日/跨日 `daily_progress_by_date[]`、六项十进制 `round_half_up`、字段级证据标签以及 pending/failed 无成功回执边界；11/11 当前需求 ID 唯一。
- OpenClaw 独立复核逐条确认 11 个范围并独立重算六项 golden；结论 `PASS`，完成标志 `Ready for SH-CONTRACT-002 completion: Yes`，新证据为 `docs/evidence/EV-20260811-014-sh-contract-002-v2.md`。
- Issue/纠正 v2 第二轮复核使用六项精确 ID 映射，结论 `PASS`，完成标志 `Ready for SH-CONTRACT-004 completion: Yes`；第一轮输入不完整，不作为关闭证据。
- 新证据 `docs/evidence/EV-20260811-015-sh-contract-004-v2.md` 冻结追加纠正、阶段所有权、库存不足、批量预览和失败零业务数据边界。
- GitHub 私有发布与下载回归已完成：`main` 可克隆，克隆提交与源提交一致，受保护文件为 0，工作区为 clean。
- Skill 元数据完成手工门禁：名称、触发描述、长度、默认提示和目录结构均符合当前技能规范；官方快速校验脚本因本机缺少 PyYAML 未能运行，未安装额外环境依赖。
- 新证据 `docs/evidence/EV-20260811-012-sh-safe-bounded-exit.md`：`RED-ROOT-005`、`RED-TEMP-001`、`RED-REPORT-005`、`RED-MANIFEST-001` 和 4/4 状态隔离全部 PASS；安全文件哈希未漂移。

### 发现问题

- 当前 OpenClaw 实例尚未安装 `diet-manager-b`，所以行为 GREEN 只是规则应用验证，不是插件部署验收。
- 三份 v2 契约已完成；确定性策略、模型、mapping、harness 和 trace 仍未完成，不能据此提前创建生产业务表或宣称产品可用。
- B 仍无业务 SQLite 表和真实饮食记录，实现不了真实“已记录”；任何 `foundation_not_implemented` 都必须明确回答未写入。
- 本机 Node 24.14 的 `node:sqlite` 仍输出实验性警告；官方 Node 文档显示 24.15 起进入 release-candidate 阶段，部署时必须核对 OpenClaw 实际 Node 版本。
- 当前工作区已经接入私有 GitHub 仓库；仍保留文件 SHA 和测试证据作为契约冻结依据，Git 提交不能替代独立复核。
- 旧安全 brief 对 Windows `conhost.exe` 的精确进程数量存在平台冲突；已按 `DEC-028` 转入 deferred，不再追加平台专用实现，除非新证据表明它会影响业务数据、路径边界或部署可用性。
- 总计划 0.3 规定正式 B repository 还依赖 `SH-MAP-001`、`SH-HARNESS-001`、`SH-TRACE-001`；这些输出尚未验收，因此仍未创建生产业务表。

### 待优化

- 安全测试改为风险分层：产品改动跑 focused；里程碑跑 release；不再为无关平台细节反复跑长矩阵。
- Skill 真正部署后，用新会话复测饮水、失败、重复提交和更正/撤销，避免旧聊天上下文影响结果。
- 为 Node 运行命令统一一个短入口，避免桌面 PowerShell PATH 不含 Node 时产生包装失败。

### 后续可增加

- MCP、其他智能体适配器、营养提供商、复杂库存、报表 UI 和性能优化。
- 这些内容都复用同一个 B 后端，不再复制第二套业务逻辑。

---

> 以下内容保留为安全底座技术附录，记录此前实现与验证细节；它不再代表当前产品执行顺序。当前顺序以上述简明计划为准。

## 一、当前结论

- 唯一产品主线是 B（Skill + 轻量插件后端 + SQLite）；A 只保留只读降级，C 停止独立产品开发，其安全控制以后并入 B。
- `SH-SAFE-BASE-001`、`SH-CONTRACT-001/002/003/004` 已完成；当前顺序进入 M1 的 `SH-DECISION-001`，随后完成模型、mapping、harness 和 trace。没有并行开发 A/B/C 三个产品版本。
- 当前交付物是必要 foundation 与规范契约，不是可安装的饮食管家产品。正式业务记录、库存、营养计算、纠正执行、查询、SQLite 存储、安装和发布均未开始。
- 48 项完整矩阵仍只保留为历史发布级证据；当前按 `DEC-028` 以关键 focused + 4 项状态隔离完成安全有界退出。完整矩阵只在后续 release 候选需要时重跑，不再阻塞日常产品开发。
- G1、G2、G3 均未通过；不得提前创建 `shared/selected-route-map.json`，也不得宣称 Skill 已可用。

## 二、总计划中的当前位置

```text
M0 foundation 历史基线
  ↓
M1 SH-SAFE-BASE-001（已按 DEC-028 验收）
  ↓
M1 SH-CONTRACT-001 共同契约 v2（已完成）
  → M1 SH-CONTRACT-002 日期/回执 v2（已完成）
  → M1 SH-CONTRACT-004 Issue/纠正/补偿 v2（已完成）
  → M1 SH-DECISION-001 确定性策略表（下一项）
  ↓
M2 模型 / Schema / 案例 / Harness / Trace
  ↓
M3 B SQLite + C 安全控制合并
  ↓
G1 B-only 存储硬安全门
  ↓
M4 B 最小纵向切片 + 故障验证
  ↓
G2 B-only 就绪与路径绑定
  ↓
M5–M6 B 的 PRODUCT-0.1 业务能力
  ↓
M7 安装 / 迁移 / 备份 / 恢复 / G3 发布
```

安全底座已经按业务风险完成有界退出，不再追求平台细节上的“优化到完美”。三份 CONTRACT-v2 已冻结；当前先完成总计划要求的确定性策略表，再进入模型与 SQLite mapping。

## 三、已开发

- 固定根矩阵、路径 containment、长路径、junction/reparse no-follow、handle-bound 创建/复制/清理和物理残留复核。
- 正式数据、项目候选、源码 dist、外部 guards 的前后观察、差异报告和失败路径继续审计。
- source runtime 身份、private runtime snapshot、phase-local ledger、命令级 immutable handle 生命周期和闭包核验。
- A 的固定 PowerShell 入口及 B/C 的 8 条 Node 命令 spec、clean-room 环境、staging allowlist 和 TypeBox pinned-copy。
- 冻结 Q policy bootstrap、policy/spawn/addon journal 的 DTO、物理文件复核基础与报告结构。
- 默认 publisher 的 CreateNew/原子发布、精确 DTO、动态 getter 拒绝和 partial 结果聚合。
- 四个临时根的 adapter 结果与独立物理残留分离；adapter 谎报零仍能发现 ordinary/reparse 残留。
- 内嵌原生 helper 已由运行时 `Add-Type` 改为冻结 DLL 字节的 `Assembly.Load(byte[])`，避免污染调用者 TEMP。
- native `CreateProcessW`/Job/completion/stream/timeout 基础实现已经落盘，尚未通过全部计划 Oracle。
- `path_security` 的 Windows handle-pin 账本、十类路径操作、命令引用及五个 `PathPhaseObserver` 阶段已经落盘，并保持正式入口不暴露测试 seam。

## 四、本轮新增开发内容

1. native runner 增加 parent process identity、Job completion telemetry、Job accounting 和 8 MiB 双流读取基础。
2. `RED-PROCESS-001/003` 增加真实 parent/descendant identity 与 Job 对账断言；当前它们仍处 RED 状态。
3. 默认 Manifest provider 增加私有 `ExcludedSubtrees` 扫描参数；公开请求仍保持冻结的 5 字段。
4. `validation_root` 删除前的默认审计只跳过本轮已注册的 `runtime-snapshot`；同级未知 junction 仍 fail-closed。
5. snapshot 排除 focused 回归已通过：业务候选仍被发现，注册 snapshot 内 junction 不被跟随，未知 junction 被拒绝，fixture 残留为 0。
6. Manifest provider 新增 `manifest-provider-dto/v1` 深拷贝边界：先检查普通 `NoteProperty` 元数据，再读取一次值；动态 getter 在执行前拒绝，顶层、容器和 nested root 的跨 scope 引用复用均 fail-closed。
7. 机器报告新增精确 `manifests.provider_dto` 与 `test_seams`；当前 production-style 验证中 fault injector/observer 均如实记录为 inactive，正式入口未暴露内部 seam。
8. `ProcessFaultInjector` 已按冻结的 11 个阶段接入真实 Win32/Job/双流执行路径；注入结果使用普通 DTO 深拷贝，动态 getter 在执行前拒绝，命令报告固定记录 `fault_injection`。
9. 内嵌 native DLL 已随 fault callback 契约重新冻结；加载仍使用内存字节、固定长度/SHA/MVID/API 契约，不在调用者 TEMP 生成编译文件。
10. 机器报告新增精确 `path_security`：固定 `windows-handle-pin/v1`、immutable/writable share mode、十类 operation、逐 pin volume/file identity、零 delete-share；每条命令通过 `path_operation_ids` 只引用已登记操作。
11. 五个 `PathPhaseObserver` 阶段已接入 root create、staging copy、runtime input pin/launch、evidence temp-write/rename、cleanup entry-pin/dispose；observer 请求为普通 DTO，异常统一归一为 `PATH_OPERATION_FAILED`。
12. 路径账本实现过程中发现 WinPS 5.1 将 scriptblock 转为泛型 comparer 可能触发调用者 TEMP 的瞬时写入；已改为 `List[string].Sort(StringComparer.OrdinalIgnoreCase)`，`RED-ROOT-005` 的物理 junction 目标零变化恢复通过。
13. 用户进一步澄清业务原子性：失败允许写入独立技术日志/审计，但`FactCommit`失败时饮食记录、库存、营养、Issue、业务outbox、进度和成功结果必须全部回滚；该日志不得进入业务查询或被解释为“已记录”。已登记`CHG-20260811-001`，不改变FactCommit成功后Effect待重试的既有分层语义。

## 五、已经通过的验证

### 当前候选上的新鲜 focused 结果

| 验证 | 结果 | 边界 |
|---|---|---|
| PowerShell Parser / ASCII / CR / NUL | PASS | core 与主测试当前修改后通过 |
| 默认 Manifest snapshot 私有排除 | PASS | 不启动 Node/OpenClaw；注册 snapshot 跳过、未知 junction 拒绝、fixture 0 |
| `RED-REPORT-005` | PASS | 当前候选 55.0 秒；内存/磁盘 `path_security`、十类 operation、命令引用、五个 observer phase、异常归一、provider DTO/test seams 与深层 JSON 通过，fixture 0 |
| `RED-ROOT-005` | PASS | 当前候选 14.3 秒；ancestor junction 物理目标摘要不变，路径拒绝稳定，fixture 0 |
| `RED-STAGING-002` | PASS | 当前候选 58.6 秒；production-exact TypeBox 缺失/错 junction/篡改均 fail-closed，fixture 0 |
| `RED-TEMP-001` | PASS | 当前候选 142.7 秒；四根 adapter lie-zero、ordinary/reparse 物理残留与 junction 外部 sentinel 保护通过，fixture 0 |
| `RED-MANIFEST-001` | PASS | 当前候选 23.7 秒；包含 snapshot 私有排除子变体，fixture 0 |
| `RED-MANIFEST-006` provider 子变体 | PASS | 11 个 identity/dynamic/nested reuse 变体 114.7 秒；ScriptProperty getter 命中 0，finally/audit/cleanup 继续，fixture 0 |
| `ProcessFaultInjector` 11 阶段窄矩阵 | PASS | 约 1.8 秒；11 个固定 phase 均命中，前五阶段未恢复目标进程，动态 getter 命中 0，fixture 0 |
| 4 项状态隔离 | PASS | 当前候选 4/4，75.6 秒；`RED-OPENCLAW-001/002`、`RED-ENV-001/002` 全部通过 |
| `RED-PROCESS-002` | PASS | 当前候选 2.7 秒；8 MiB 双流边界与收尾通过 |
| production entry seam 反射门 | PASS | 正式入口不公开或转发 `ProcessFaultInjector/PathPhaseObserver` |
| native argv/exit 基础 | RED | argv 和 parent exit 正确；严格 Job 集合含系统 `conhost.exe`，`RED-PROCESS-001` 首红为 expected parent-only / actual parent+conhost |
| `RED-PROCESS-003` | RED | 当前候选重跑 39.8 秒；11 个 fault phase、taskkill 故障后的 Job fallback 和双流收尾均已通过；唯一失败为 timeout 树 expected parent+worker、actual parent+worker+系统 conhost，fixture 0 |

### 较早候选的历史结果（必须重验）

- 48/48 主矩阵、4/4 状态隔离曾通过。
- `RED-CACHE-VITEST-001`、`RED-STAGING-002`、`RED-TEMP-001`、`RED-ROOT-005`、`RED-OPENCLAW-002`、`RED-REPORT-005` 曾分别 focused 通过。
- 上述结果不能替代当前修改后候选的完整重验，也不能关闭 `SH-SAFE-BASE-001`。

## 六、发现问题

| 问题 | 影响 | 当前处置 |
|---|---|---|
| 严格 `CREATE_SUSPENDED | CREATE_NO_WINDOW | CREATE_UNICODE_ENVIRONMENT` 在当前 Windows 11 上把系统 `conhost.exe` 计入 Job | brief 同时要求 Job 总数恰为 parent + Q 成功 child，因此 `RED-PROCESS-001/003` 出现额外 PID；血缘探针确认它是目标 PowerShell 的直接子进程，命令行为 `\\??\\C:\\WINDOWS\\system32\\conhost.exe 0x4`，系统文件签名有效 | 不过滤、不伪造。微软文档说明无 breakaway 时 child 默认进入 Job；启用 breakaway 会削弱边界。保持 RED，登记为 brief/平台冲突，继续完成不依赖该裁决的计划项 |
| 用 `DETACHED_PROCESS` 或 Job breakaway 规避 conhost | direct Win32 试验中 `DETACHED_PROCESS` 可令 Job total=1，但 Windows PowerShell payload 未执行；`SILENT_BREAKAWAY_OK` 会让全部后代逃离 Job | 两条均拒绝采用。前者破坏 A 命令语义，后者破坏 kill-on-close、completion 和后代对账；需要按变更流程裁决冻结 brief，而不是在实现中偷偷放宽 |
| 真实 Task 5 报告曾因临时审计扫描 `validation_root\runtime-snapshot` 内的合法 junction 而失败 | 安全 snapshot 被误当成未知临时 reparse | 已用默认 provider 私有排除修复并 focused 通过；仍需正式入口重验 |
| native runner 还未把物理 Q journal 与 completion/Job 数据完成最终对账 | 真实 Node 命令无法形成可信的 `expected_total_processes` 和角色集合 | 作为 Task 5 剩余实现继续推进，不扩大到新产品功能 |
| 路径账本最初使用 scriptblock 泛型 comparer 排序 | WinPS 5.1 在 poisoned TEMP/junction 场景产生瞬时目录变化，破坏 ROOT 零写保证 | 已改为 CLR `List[string]` + OrdinalIgnoreCase comparer；五阶段 observer 与 `path_security.operations[]` 已完成 focused 验证 |
| 两次 Task 5 真实运行均形成了失败证据 | 第一轮遇到 source runtime 漂移；第二轮执行到 A 后遇到 `PROCESS_JOB_ACCOUNTING_MISMATCH`，并暴露 snapshot 审计误扫合法 junction | source 身份须在下一次运行前重新冻结；snapshot 私有排除已 focused 修复；完成本地门禁后只重跑一次正式入口 |
| 当前测试/核心文件已在较早 48+4 后改变 | 旧完整 PASS 不再新鲜 | 完成剩余实现后只跑一次完整 48+4，再跑唯一真实入口 |
| 当前目录不是 Git 仓库 | 无提交级回退与差异追踪 | 按总计划接受 `DEBT-005`，以 SHA-256 和不可覆盖证据补偿 |

## 七、待开发

1. 完成 native runner 对物理 Q policy/spawn/addon journal 的严格 schema/identity/role/PID 对账；当前已接入基本 parent/child/Job 集合核对，还需真实 Node 证明。
2. 解决或由计划裁决 `conhost.exe` 与冻结 Job 计数 Oracle 的冲突；在此之前不把 process runner 标为 GREEN。
3. 重跑 `RED-PROCESS-001/002/003` 等受影响 focused 用例；`RED-MANIFEST-001` 当前已通过。
4. 在同一冻结候选上串行运行 48 项主矩阵和 4 项状态隔离，确认无 Node/npm/Vitest/TypeScript/OpenClaw 实体被测试套件启动。
5. 冻结 27 项 protected manifest，执行唯一正式入口的 Task 5 真实 PowerShell/Node/Vitest/TypeScript/OpenClaw 验证，并再次核对零正式数据差异和零临时残留。
6. 本地门禁通过后，在用户已登录的专属 OpenClaw 平台验证可观察结果；浏览器测试不替代正式入口。
7. 生成 raw 输出、机器 JSON、最终 EV 和独立复核；全部通过后才关闭 `SH-SAFE-BASE-001`。

## 八、待优化（不扩张当前 WIP）

- 长矩阵只在候选稳定后运行一次；失败只定位首个计划内阻断，不反复盲跑。
- 继续保持 structural fixture 仅用于非 STAGING 用例；STAGING-001/002 使用 production-exact 依赖树。
- 最终 EV 集中保存 focused、完整矩阵、状态隔离、真实入口和 OpenClaw 结果，减少碎片证据。
- 对 embedded native DLL 增加可复现生成说明，但不在当前阻断关闭前重构构建系统。

## 九、后续可增加的优化

- 为安全矩阵提供 smoke / focused / release 三层统一入口和机器进度事件。
- B SQLite 开发开始后，为 migration、事务故障和 Docker/OpenClaw 建短反馈测试层。
- B 的 Skill 命令契约、SQLite 服务边界和 G2/G3 发布门稳定后，可增加薄 MCP adapter；MCP 只暴露同一套 B 能力，不复制业务逻辑。当前仍优先开发 Skill。

## 十、当前文件锚点

| 文件 | SHA-256 | 说明 |
|---|---|---|
| `总功能开发计划0.3.md` | `A13468CCF04445935EB8F7A7386C8611FFFDF1E13AD97FA470C8E121C6E762F1` | 唯一总计划；`SH-CONTRACT-001/002/003/004`已完成，下一项为`SH-DECISION-001` |
| `shared/contracts/issue-correction-contract.md` | `41E4A18D4D72644641D66A58F918616EBB0A6189E7F0BE1E836741E057298FDB` | Issue/纠正 CONTRACT-v2；由 `EV-20260811-015` 完成独立复核 |
| `shared/tests/validate-issue-correction-contract-v2.ps1` | `5C719F1E169D5BA17A9C0160FBDA5410FB3F9C78A43672BE11C42DC3CE19DC2C` | Issue/纠正 v2 机器验证器 |
| `docs/work-items/SH-CONTRACT-004-v2-review.md` | `CFBCAA7029407AF20452704D45C9BACA2E7F12ADBFF498A9DDC495C43E455C5A` | OpenClaw 两轮独立复核记录；第二轮最终 PASS |
| `docs/evidence/EV-20260811-015-sh-contract-004-v2.md` | `5775CD3F9FF97AB22F3BA79818C422B04890CD0F7EDEC9BE8F5FD169B5E7A28C` | `SH-CONTRACT-004` TDD、独立复核、GitHub clone 与业务边界证据 |
| `shared/contracts/receipt-and-date-contract.md` | `F33E34D6B9EA9B1212208D75C5025FA86BB07923248E3B4929A1EF0BB7A375DD` | 日期/回执 v2 已由 `EV-20260811-014` 完成独立复核 |
| `docs/work-items/SH-CONTRACT-002-v2-review.md` | `880C13E9C30638F7BB05ACAADA90BE03FAF5AB37950D384F2820C3CAF9AEC7FF` | OpenClaw 独立复核记录；最终 PASS |
| `shared/tests/validate-receipt-and-date-contract-v2.ps1` | `DB7B9CD1BADF07CB4BDF14536CC7077276C16702E8040B9FD31D1E4AA6C6F0B7` | 日期/回执 v2 机器验证器 |
| `docs/evidence/EV-20260811-014-sh-contract-002-v2.md` | `59E2D29AF0F9E0CCC3B441FB617635BF809B7DFF5C3A237538A876AC798D8BF2` | `SH-CONTRACT-002` TDD、独立复核和最终门禁证据 |
| `docs/superpowers/plans/2026-08-11-skill-first-b-practical-plan.md` | `39E48E29635A6F97D9049B499208026446D4D85F9ED871D13A2A022D1B7876A3` | 当前简明执行路线；Task 1、Task 2已完成，Task 3还受mapping/harness/trace约束 |
| `docs/evidence/EV-20260811-012-sh-safe-bounded-exit.md` | `4E1AB059C0BA0D626B92DFBB2636D4E77EA05D60E60D8FEDC44CEFF343743DE2` | `SH-SAFE-BASE-001`按`DEC-028`完成必要范围验收的证据 |
| `docs/evidence/EV-20260811-013-sh-contract-v2.md` | `AFC3F3C160D310ACDBD767333DC34E48D3795D5A0D4732BF5D963C606BC28E61` | `SH-CONTRACT-001`机器门禁与OpenClaw第二轮独立PASS证据 |
| `shared/business-contract.md` | `632B2BBF8D0E6C655F4C0A47958828A86C67B3240065984CCC78A808E6F7072E` | 已冻结CONTRACT-v2；含三阶段、业务零数据边界和十项旧规则guard |
| `shared/tests/validate-business-contract-v2.ps1` | `2395D4B0A5806CDCE5FE1C313BE814CF473ABF65B3810E72DBC2BFFD20790589` | CONTRACT-v2机器协议、优先级、生命周期和旧规则防退化验证器 |
| `docs/work-items/SH-CONTRACT-001-v2-review-package.md` | `28628B5299B536156B2A759606FD4878CAF241C20E6C9B4A5AFAC4EF7744C4D0` | 第二轮独立契约复核输入 |
| `docs/work-items/SH-CONTRACT-001-v2-review.md` | `7F7953F7A1DF679CA3E6DE58A87BDD722B67B1188315B9F002D55D40D8A03902` | OpenClaw两轮复核记录；最终PASS |
| `version-b-lite-plugin/src/contracts.ts` | `4E5140E16180ED341F80536265F8E53A8273EF6F7B4A35001B8375550EF5EE99` | 8动作、6状态的可移植合同与结果一致性校验 |
| `version-b-lite-plugin/skills/diet-manager-b/SKILL.md` | `7D29519CC83D8B5CBA18CF1A9A21ACD4B6AF3900ADC47B9847DD25414D588623` | Skill结果处理规则；已做OpenClaw ignored/clarification正向验证 |
| `version-b-lite-plugin/src/storage/sqlite-runtime.ts` | `E1FE0DDF96625F6C874F761FA6A0991C0901E6CAAE52777BB50F72D5F399F7A8` | Node 24内置SQLite私有兼容运行时；尚无业务表 |
| `docs/work-items/SH-SAFE-BASE-001-brief.md` | `C25EEA39A4A778AC98F9BCDE17BED136D0BE3645153EBA7E1151205F10FE4441` | 当前任务冻结契约 |
| `shared/private/foundation-validation-core.ps1` | `E921FBB889C9AF46397E6DEDC918CACAC7AE68D1D2819DEB218A1B22DCFFBED7` | 当前工作候选；已含 ProcessFaultInjector、path_security 与五阶段 observer，未通过完整重验 |
| `shared/tests/validate-data-manifests.ps1` | `8309CBF6A92FDF17959071D3719189AED72E5F22439345C5C5DD1C2D35E4A591` | 当前 48 项主测试候选；新增 path_security/observer Oracle，未通过完整重验 |
| `shared/validate-foundations.ps1` | `2A6D5801952F3CF67E025B782716EE937AFAFF623FCCDC1106B9D9641D719F61` | 唯一正式入口 |
| `shared/tests/validate-foundations-state-isolation.ps1` | `574E4F67BCF75D74A57FA8805CAA1C90990E3DCA3D60C58CFB4424B83E5F11C9` | 4 项状态隔离测试 |

> 注意：core/test 会在本任务继续修改；每次稳定里程碑后应更新这里的 SHA。未生成有效最终 EV 前，不得把当前任务写成“已完成”。
