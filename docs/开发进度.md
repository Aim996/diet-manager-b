# 饮食管家开发进度

> 更新日期：2026-08-12
>
> 本文件是便于阅读的进度快照，不是第二份任务台账。任务状态、依赖、完成条件和执行顺序以项目根目录《总功能开发计划 0.3》和对应 work-item brief 为准。

## 最新简明进度

当前按 [Skill-First B 简明计划](./superpowers/plans/2026-08-11-skill-first-b-practical-plan.md) 执行。`SH-CASE-004` 已完成其 26 案/8 份黄金文本范围；`SH-HARNESS-001` 正在开发。首个 manifest RED 发现总计划要求的 `CASE-STORAGE-001` 尚未进入累计目录，现已保持原 26 案不变、复用既有 fixture，单独追加为第 27 案并增加反弱化 mutation。Skill 是智能体入口，未来 SQLite 后端是唯一业务实现，OpenClaw/MCP 只做薄适配器或隔离复核。当前仍未创建生产数据库、未写业务记录，也没有重开非必要安全底座。

### 当前完成度（粗略估算）

| 范围 | 完成度 | 说明 |
|---|---:|---|
| 产品计划与边界 | 93% | B 单线、失败原子性、三份 v2 契约、确定性策略及 Skill/后端/适配器职责已冻结 |
| 必要安全底座 | 100%（当前范围） | `DEC-028` 有界退出验收通过；非必要 Windows 边角不再阻塞业务开发 |
| 共享业务模型 | 100%（当前 M2 模型范围） | SH-MODEL-001/002/003 均有固定案例、mutation、独立复核和 GitHub clone 复验证据 |
| B 业务产品 | 71% | SH-MAP-001、27个累计共享案例和8份黄金回执已完成；harness 协议正在开发，repository、outbox worker 和真实业务纵切仍待开发 |
| OpenClaw 实际集成 | 8% | 专用平台完成规则与独立复核使用；正式插件仍未部署 |
| 整体可交付度 | 约 75% | 共享模型、mapping、27个累计 case 和8份黄金回执已冻结；harness、trace、B SQLite 与业务纵切尚未完成，产品仍不可安装 |

### 已开发

- B 的可移植 Skill 已有 8 个动作入口和五种业务结果；未接后端时保持 `committed=false`，不得伪称已记录。
- 必要安全底座已按业务风险有界收口，路径越界、业务候选污染、伪提交和清理逃逸已有门禁。
- CONTRACT-v2、日期/回执 v2、Issue/纠正 v2、营养来源契约和确定性策略已冻结并有独立复核证据。
- `SH-MODEL-001` 已冻结 Event/Amount/Product/Inventory：41 个固定用例、4 个 mutation，证据 `EV-20260811-017`。
- `SH-MODEL-002` 已冻结 Nutrition/Profile/Snapshot/GoalVersion/DailyProgress：42 个固定用例、4 个 mutation，证据 `EV-20260811-018`。
- `SH-MODEL-003` 已完成：12 个公共定义、65 个固定用例、10 个 semantic-only case、10 个跨字段语义不变量和 4 个反弱化 mutation；第二轮独立复核 P0=0/P1=0，证据 `EV-20260811-019`。
- `SH-MAP-001` 已完成：34 个定义字段全覆盖、20 张表、18 个索引、22 条外键、4 个完整事务边界和 18 个本地 mutation；第三轮独立复核 P0=0/P1=0、33/33 独立 mutation 被拒绝，证据 `EV-20260811-020`。
- `SH-CASE-001` 已完成：5 个正式 CASE-ID、3 个固定 fixture、8 个本地 mutation、20 个独立 mutation 和 7 项串行验证通过；独立复核 P0=0/P1=0，证据 `EV-20260811-021`。
- `SH-CASE-002` 已完成：新增 9 个正式 CASE-ID 和 9 个 `domain_scenarios`，累计 14 案；11 个本地 mutation、11 个独立 mutation、8 项串行验证和第二轮独立复核 P0=0/P1=0，证据 `EV-20260811-022`。
- `SH-CASE-003` 已完成：新增 6 个正式 CASE-ID、6 个 `ops_security_scenarios`，累计 20 案；12 个本地 mutation、九项串行验证和三组独立 checker 全部通过，复核 P0=0/P1=0/P2=2，证据 `EV-20260811-023`。
- `SH-CASE-004` 已完成：累计案例升级为 26 案，新增 6 个正式 CASE-ID，冻结 8 份独立 UTF-8/LF 黄金文本和 12 个本地 mutation；十项串行验证通过，公开候选独立复核 P0=0/P1=0/P2=2，证据 `EV-20260812-024`。
- GitHub 仓库已公开，分支 `agent/sh-case-004-golden-receipts` 和 draft PR `#3` 已建立；受保护文件、业务数据、密钥、OpenClaw 地址/令牌和废弃 A/C 实现均未上传。

### 正在开发

- `SH-HARNESS-001` 已进入实现：manifest 与输入哈希已冻结；计划缺失的 `CASE-STORAGE-001` 已按 TDD 追加为第27案；下一步是 A/B 适配器和确定性机器报告。
- 当前先建设共享 harness 与机器报告，不创建 SQLite 业务库、不实现 repository。
- OpenClaw 继续只承担隔离复核与未来薄适配验证，不成为第二套业务状态机。

### 待开发

1. 完成 `SH-HARNESS-001`，让同一 Oracle 可经 B adapter 执行，并固定 A 只读降级边界。
2. 完成 `SH-TRACE-001` 后，才开始真正的 `B-STOR-001` SQLite 实现。
3. OpenClaw/MCP 继续只做薄适配器；不得复制 Skill/后端的业务状态机。

### 本轮新增开发内容

- 新增 `SH-HARNESS-001` design、brief、TDD plan、manifest、plain TypeScript 协议和 Node 24 内建测试骨架。
- manifest RED 捕获总计划与累计目录不一致：`CASE-STORAGE-001` 被任务引用但目录缺失；已复用 `domain-idempotency-conflict-v1`，只追加该案并将目录升级为 `1.4.0 / 27`，旧26案与fixture字节不变。
- 四个接受验证器已同步新累计版本；domain validator 新增同幂等键/同输入不得重执行的 mutation。

- 新增 `SH-CASE-004` design、brief、TDD plan、候选报告、独立 review 和 `EV-20260812-024`。
- 把累计案例目录升级到 `1.3.0`：保持前20案逐值不变，追加6案，并以独立 SHA-256 锁定旧值。
- 冻结8份独立中文黄金文本，逐字节校验严格 UTF-8、LF、终端换行、长度、SHA-256和行数；结构化结果不得自生成期望文本。
- 冻结多菜、证据/估算、Issue快捷、安全退出、请求分析、单项、单日alias、跨日重放和finalizer pending语义。
- 新增12个真实反弱化 mutation；黄金/core/domain/ops四项接受验证与三份契约、三组模型验证共十项串行PASS。
- OpenClaw 02 仅执行一次公开候选独立复核，结论P0=0/P1=0/P2=2；隔离clone残留0，未把测试平台变成第二套业务实现。
- 新增 SH-CASE-001 设计、实施计划和 work-item brief，把任务严格限制为5个核心 CASE-ID。
- `shared/acceptance-cases/cases.json` 从12条无固定环境/Oracle的简化数组升级为版本化正式案例包。
- 新增 `fixtures/core-v1.json`，固定时钟、时区、locale、week_start、六项目标和查询生命周期数据。
- 新增独立 PowerShell 5.1 validator：先以旧数组取得 `CORE_CASE_SET_SHAPE_INVALID:root` RED，再达到5案例/3 fixture/8 mutation GREEN。
- 将“写入失败可以有独立脱敏日志，但饮食记录零新增”写成包级硬 Oracle；查询同样固定业务写入数为0。
- 完成 OpenClaw 独立复核：验证 151068 字节 review ZIP、12 个条目和 10 个声明哈希，独立 checker 的 20/20 mutation 全部被拒绝，P0=0/P1=0。
- 新增 `SH-CASE-001-review.md` 与 `EV-20260811-021`，明确本任务不等于 adapter、数据库、harness 或产品可安装。
- 新增 `SH-CASE-002` 设计、brief 与TDD实施计划：选定“单一累计catalog + 9个domain_scenarios”，拒绝第二套Oracle和提前SQL实现。
- 冻结9个新CASE-ID、9个scenario ID、14案累计顺序、12个累计fixture对象以及两层失败/幂等冲突的精确state vector与SHA-256。
- 新增 ASCII PowerShell 5.1 领域 validator；旧5案目录先稳定 RED，加入9案后达到 `cases=9|scenarios=9|mutations=11` GREEN，并保持原5案/8 mutation 不弱化。
- 冻结入库、库存多候选、精确标签营养、合并 Issue、追加纠正、mixed 顺序、EffectBundle 故障、最终器并发故障和幂等冲突九类行为。
- 第一轮独立复核捕获 `skipped_multiple_candidates` 与冻结契约 `skipped_ambiguous` 的 P1；经 focused RED 修正两案和字面断言后，第二轮独立复核 P0=0/P1=0、11/11 mutation 全捕获。
- 新增 `SH-CASE-002-review.md` 与 `EV-20260811-022`；再次明确写入失败可以产生日志，但饮食记录和全部业务对象必须零新增。
- 新增 `SH-CASE-003` design、brief、TDD plan、candidate report 和 review package，范围只包含隐私、foundation 零差异、干净安装、迁移失败、候选漂移和无效导出恢复六类静态 Oracle。
- 把累计案例目录升级到 `1.2.0`：原 14 个 case 与旧 fixture 字节保持，新加 6 个 CASE-ID 和 6 个 `ops_security_scenarios`，不创建 SQLite/JSONL 业务数据。
- 新增 `validate-ops-security-acceptance-cases.ps1`，固定六个案例、14 个类文件 fixture 的 Base64/长度/SHA-256、逻辑路径、时间戳和 12 个真实反弱化 mutation。
- 冻结外部营养请求六字段白名单、普通日志脱敏、技术日志与业务数据隔离、正式数据/sidecar 零差异、失败后扫描与清理。
- 冻结安装 preflight→包哈希→staging→隔离 smoke→原子 promotion，安装后正式 SQLite 为空、JSONL 和业务记录为零。
- 冻结迁移失败保持旧字节/版本/可读性、候选任意字节漂移使证据失效，以及最小导出不得冒充完整备份的拒绝顺序。
- GitHub 仓库转为 public；公开前执行 machine-path、OpenClaw 地址/令牌、密钥、业务载荷和受保护路径扫描，变更文件均通过。
- OpenClaw 02 通过无凭据 HTTPS partial clone 独立建立三组 checker；20 案顺序、6 个 fixture、旧数据字节保持、12/12 mutation 和公开卫生均通过，最终 P0=0/P1=0/P2=2。
- 临时只读 deploy key 已撤销；两次 OpenClaw review 临时根均按精确路径清理，最终残留为 0。
- 新增 `SH-CASE-003-review.md` 与 `EV-20260811-023`，明确本任务只完成静态 Oracle/fixture，不代表安装、迁移、恢复、删除或正式产品已经实现。

- 新增 SH-MAP-001 详细设计、实施计划和 work-item brief，明确“映射文档”和“数据库实现”是两个阶段。
- 新增 test-first 存储映射验证器，要求 34 个定义的全部字段恰好分配一次，不能遗漏、重复或从候选文档自适应生成期望。
- 固定未来 B 数据库文件名 `diet-manager-b.sqlite3`，数据库驱动方向为 `node:sqlite`；当前任务不创建该文件。
- 冻结 20 张逻辑表和 18 个关键索引的目标集合，用于事件、餐食顺序、商品/批次、库存流水、营养、目标/进度、Issue/纠正、outbox、最终器和 mixed 子结果。
- 冻结 fresh install、成功升级、失败回滚、恢复四类迁移/恢复行为；失败迁移不得推进 `user_version`。
- 冻结 A 只读、B 唯一写入、C 控制合并策略，避免再次出现 A/B/C 多套产品并行开发。
- 完成 SH-MAP-001 三轮独立复核闭环：第一轮修正 7 项 Schema 冲突，第二轮修正 EffectBundle/EnvelopeFinalize 表分类，第三轮 `P0=0/P1=0`。
- 把四个事务边界升级为 20 表完整互斥分区；EffectBundle 可推进信封到 `effects_stable`，但不得修改终态幂等记录。
- 本地 validator 从 9 个 mutation 扩展到 18 个，独立 reviewer 从 28 个扩展到 33 个变异。
- 生成 `EV-20260811-020`，并在私有 GitHub 与独立 clone 复现 mapping + 六项上游验证。

- 新增 SH-MODEL-003 设计、实施计划和工作说明三份文档。
- 新增 test-first 验证器与 65 个 test-owned 固定 case ID；期望不能从候选 Schema 或 fixture 自适应生成。
- 新增 `x-semantic-contract`：把 correction 身份/版本/摘要/日期、mixed 顺序/双身份、进度日期/别名相等等 10 项跨字段规则明确交给配套语义层和未来 B 后端事务执行。
- 新增 7 个第一轮复核驱动的反例：void/restore 生命周期、target/revision、mixed operation/idempotency 唯一性和单日 alias 不相等。
- 明确 `FactCommitResult` 失败对象只是命令响应，不是饮食业务行。
- 明确失败可以写独立脱敏技术日志，但该日志完全位于业务 Schema、业务查询和成功回执之外。
- 明确 `effects_pending` 不得携带 ReceiptData、进度或终态幂等结果；只有成功 EnvelopeFinalize 才能冻结成功输出。
- 明确 mixed 后项失败不回滚前项，并逐子操作保留原顺序和幂等身份。
- 新增 `EffectOutboxEntry.previous_state`，把允许的四类状态转换写入数据契约；终态重开由固定反例直接拒绝。
- 新增 `FactCommitResult` 严格失败分支：`failed_fact + failed + business_writes=0`，事件、纠正、outbox、Issue、营养、库存、进度、回执和终态幂等引用全部为空。
- 新增 `EffectBundleResult` 与 `EnvelopeFinalizeResult` 分层结果：效果失败保留事实并等待重试，但不得产生成功回执；仅成功最终器可输出 Receipt/Progress/冻结幂等结果。
- 新增 mixed 子项顺序与局部提交规则：`sequence == array index`，后项失败不回滚前项，失败/忽略/待澄清子项不得携带已提交事实或纠正引用。

### 发现问题

| 问题 | 当前影响 | 处置 |
|---|---|---|
| Issue/Correction/Mixed 物理 Schema 原先不存在 | mapping、共同案例和 SQLite mapping 无统一输入 | 已由 SH-MODEL-003 关闭；双层独立复核、六项回归和 GitHub clone 均已确认 |
| 只保存 outbox 当前状态无法证明“终态不得重开” | 错误实现可能把 succeeded/permanent skip 重新处理 | 已增加 `previous_state` 和精确转换矩阵；不另建第二套状态机 |
| 失败技术日志容易被误解为“产生了一条记录” | 可能导致写失败后出现半条饮食数据 | 技术日志排除在业务 Schema 外；failed_fact 分支强制 `business_writes=0` 并禁止所有业务引用 |
| 三阶段结果若只建终态对象，无法证明效果重试和最终器失败 | 可能伪造成功回执或重复事实 | Schema 同时冻结 FactCommit、EffectBundle、EnvelopeFinalize 和 outbox 状态 |
| 当前 B 仍无正式 SQLite 业务表 | 不能真实记录、查询、更正或撤销 | 等 SH-MAP/HARNESS/TRACE 完成后再进入 B storage |
| OpenClaw 当前只参与测试/独立复核 | 不能代表产品已部署 | 正式集成保持后置，不替代本地业务门 |
| 当前内置 Node/Python 运行时未提供 Ajv/jsonschema 包 | 本地第二实现不能直接启动 | 不把临时依赖写入项目；在隔离复核环境或 OpenClaw 侧运行第二实现，并记录版本/命令/结果 |
| 第一轮把所有 `expected_valid=false` 都当成 Schema 必须拒绝 | 标准 Schema 无法表达跨字段相等、递增、数组索引和成员字段唯一性，导致报告出现 52/58 假差异 | 已拆成 Schema 层和 mandatory semantic 层；fixture 精确列出 10 个 semantic-only case，文档不再夸大 Schema 能力 |
| 第一轮缺少可表达的 void/restore 与进度 alias cardinality 规则 | 错误 lifecycle 或单日缺 alias 可能被结构层放行 | Schema 已补 `if/then`；新增负例并由 validator 固定结构门 |
| Ajv 8.20.0 在本次独立加载方式下错误执行 `prefixItems + items:false` | 对合法 Nutrition/Progress 对象产生假阴性，不能作为唯一判定依据 | 以通过该官方 2020-12 语义用例的 Hyperjump 作为判定引擎；Ajv 仅保留兼容性观察，不改坏上游 Schema |
| SH-MAP 第一版测试只固定列名，未固定类型/可空性/默认值 | 未来 migration 可以在“同名列”下实现出不同物理语义 | 已在实现前扩展机器契约：每列精确固定 `name/type/not_null/default`，并保留主键、检查、外键和索引门 |
| `command_envelopes.operation_id` 若被单值化会阻断 mixed 命令 | 一个 envelope 可包含多个 operation，错误外键会使合法 mixed 写入不可达 | 已从 envelope 行移除单值 operation，并让 event 用 contextual `envelope_id` 指向 envelope 主键；operation 仍保存在各事实/效果子行 |
| 第一版抽取列的可空性与共享 Schema 有 22 个需复核项 | 合法 unknown、pending、failed 或无目标进度可能被 SQLite `NOT NULL` 误拒绝 | 已逐字段对照 required+nullable；17 项直接修正，余下 5 项均属于共享表的 subtype/终态条件，并用 `CHECK` 固定成立条件 |
| outbox 终态曾写成近义枚举 `permanently_skipped` | 与冻结 Schema 的 `permanent_business_skip` 不一致，未来写入必失败 | 已改为唯一冻结枚举并由物理契约摘要锁定 |
| 餐食子项只有 `item_id` 主键，未锁同一事件内顺序唯一 | 可出现同一餐两个 `item_order=0`，破坏回执和 mixed 顺序 | 新增 `ux_meal_item_event_order(event_id,item_order)`，索引总数由 17 调整为 18 |
| 第一轮独立复核发现 7 项物理枚举/类型/常量与 Schema 冲突 | 忠实 SQLite 实现会拒绝合法模型或接受旧值 | 已逐项 TDD 修复并增加 7 个回归 mutation |
| 第二轮发现 EffectBundle 没有合法写点推进信封到 `effects_stable` | 三阶段协议可能停在 `effects_pending` | 已允许 EffectBundle 写 `command_envelopes`、禁止写 `idempotency_records`，四边界完整分类 20 表 |
| 部分提取枚举列尚无 SQLite `CHECK` | 不与 Schema 冲突，但未来 DDL 的数据库内防御可更强 | 记为 P2；在 B-STOR/迁移任务结合兼容性统一补充，不继续扩大当前 mapping |
| 旧 `cases.json` 只有12条简化意图，没有时钟、fixture、expected/forbidden | 任意实现都可自行解释示例，无法证明业务正确性 | 已替换为5个正式 CASE-ID 和独立字面 validator；其余案例按 SH-CASE-002/003/004 分批迁移 |
| 案例术语 `nutritious_drink` 与 Schema 枚举 `nutrition_drink` 不同 | 未来 adapter 若暗中改写可能破坏 Oracle 所有权 | 记为 P2；在 SH-HARNESS-001 中定义显式单向映射并单独断言，不修改当前五案 Oracle |
| 单项餐案例没有重复列出全部回执公共禁项 | 单看该 case 的 forbidden 不代表完整输出安全门 | 当前由 CASE-RECEIPT-001 覆盖；未来 harness 集中执行公共输出禁项，避免各案复制后漂移 |
| SH-CASE-002 第一轮用了 `skipped_multiple_candidates` 作为库存影响状态 | 严格遵循冻结契约、输出 `skipped_ambiguous` 的正确 B 实现会被 Oracle 错拒 | 已用 TDD 对齐为 `skipped_ambiguous`；匹配语义仍为 `multiple`，Issue 代码仍为 `inventory_multiple_candidates`；第二轮独立复核关闭 P1 |
| SH-CASE-003 新 validator 未再复制旧 14 case/旧 fixture 摘要表 | 少一层独立 preservation defense，但旧值仍由 core/domain validator 逐值拥有 | 记为 P2；本次独立 base comparison 已证明字节保持。为避免第二套 Oracle，当前不复制摘要表，后续按 validator hardening 统一评估 |
| SH-CASE-003 早期 brief 曾包含工作区绝对路径 | 若直接公开会泄露本机目录结构 | 公开前已改为仓库相对路径并完成全变更集扫描；独立审查确认最终分支无本机路径、令牌或密钥，本轮 report 已补记该修正 |
| SH-CASE-004 新增六案尚无独立的逐案 Oracle key/source_text 字面锁 | 未来目录维护若只改 case 描述，额外 defense-in-depth 较弱；当前黄金字节、语义、shape 和 mutation 仍会阻止行为弱化 | 记为 P2；在 `SH-HARNESS-001` 或 CI 中评估独立字面锁，不得从候选文件自适应生成期望 |
| SH-CASE-004 候选阶段两份文档的 pending 状态措辞不同 | 只影响阅读，不影响代码、案例或审查判定 | 已在最终 review/evidence 中统一为 `independent_review_passed` |
| SH-HARNESS-001 所需 `CASE-STORAGE-001` 在旧26案目录中缺失 | harness 无法证明“同键同输入重试返回原结果且零业务写入”，总计划与物理目录不一致 | 已先 RED，再只追加该案为第27案；复用既有fixture并增加独立mutation，未创建数据库或业务数据 |

### 待优化

- 后续 DDL 必须延续独立复核确认的父键唯一性、可空性和四事务边界，不能只按表名生成弱化 SQL。
- SH-MAP 只固定物理契约；真正 migration 到来时必须从机器块生成或逐项比对 DDL，并新增真实 SQLite `foreign_key_check/integrity_check` 测试。
- 后续模型变更继续分别执行 Schema 结果和结构+语义结果，避免只信单一 PowerShell validator，也避免要求 Schema 完成它无法表达的比较。
- 将 semantic validator 保持为小函数和 test-owned 字面 Oracle，避免用候选对象生成期望。
- 完成模型后再统一整理三个 Schema 的公共 `$ref` 与运行时加载顺序，不在当前任务提前重构。
- 后续 runtime 测试补充响应丢失、重启恢复和同键并发，不把这些数据库行为伪装成当前 Schema 已实现。
- SH-HARNESS-001 必须显式验证 `nutritious_drink -> nutrition_drink` 适配，不允许 adapter 回写或改造共享 Oracle。
- 公共输出禁项应在 harness 形成一处可复用检查，同时保留每个案例自己的领域 forbidden。
- 在不建立第二套 Oracle 的前提下，评估为累计 case/fixture 增加一份独立 preservation digest；若增加，期望必须来自冻结 base，而不是候选文件自适应计算。

### 后续可增加优化

- 在不改变当前 20 表语义的前提下，后续可基于真实查询计划补充性能索引；不能凭猜测提前加入并把它伪装成正确性要求。
- 后续可增加 canonical JSON 编码器的跨语言一致性测试和历史 payload 升级工具。
- SQLite outbox worker 的退避、观察指标和人工重放工具。
- MCP、其他智能体和 UI 适配器；它们必须复用同一 B 后端，不复制业务逻辑。
- 更丰富的 Issue 搜索、批量纠错预览、运营审计界面和性能优化。
- 这些均不进入当前 SH-MODEL-003 完成条件。
- 可在 `SH-HARNESS-001` 或 CI 中复用九项允许范围验证和开源卫生扫描；不得把候选 PASS 文本变成自生成期望。

### 验证与证据

| 时间/阶段 | 验证 | 结果 |
|---|---|---|
| 2026-08-11 / SH-MODEL-003 RED | PowerShell Parser | `0 errors` |
| 2026-08-11 / SH-MODEL-003 RED | 新验证器执行 | `ISSUE_CORRECTION_MIXED_SCHEMA_FILE_MISSING`，exit 1，符合预期 |
| 2026-08-11 / SH-MODEL-003 GREEN 候选 | 严格 JSON / Parser / ASCII | Schema/fixture JSON 可解析；validator Parser `0 errors`；三文件 non-ASCII/CR/NUL 均为 `0` |
| 2026-08-11 / SH-MODEL-003 第一轮独立复核 | Ajv 8.20.0 + Hyperjump | `FAIL|p0=0|p1=3`；符合规范的判定引擎为 `52/58`，6 个均是未声明分层的 Schema 假接受；失败原子性三项 mutation 均被 Schema 拒绝 |
| 2026-08-11 / SH-MODEL-003 Round-2 RED | 新 Schema 分层门 | 第一版 Schema 缺 `x-semantic-contract`，稳定失败 `ISSUE_CORRECTION_MIXED_CASE_RESULT_INVALID:x-semantic-contract object` |
| 2026-08-11 / SH-MODEL-003 Round-2 GREEN | 65 个固定案例 | `ISSUE_CORRECTION_MIXED_SCHEMAS|PASS|version=1.0.0|cases=65|definitions=12|semantic_only=10|mutations=4` |
| 2026-08-11 / SH-MODEL-003 Round-2 GREEN | 文件身份 | Schema `EDBB15A3...B3B511F`；fixture `FAD2104B...8EE1F7`；validator `4E27245E...B35CC8` |
| 2026-08-11 / SH-MODEL-003 第二轮独立复核 | Ajv 8.20.0 + Hyperjump 1.17.8 + 独立语义 checker | `SH-MODEL-003-INDEPENDENT-REVIEW|PASS|p0=0|p1=0|cases=65|semantic_only=10|mutations=4`；另有 10/10 invariant 与 25/25 结构门通过 |
| 2026-08-11 / SH-MODEL-003 GitHub clone | 私有 `main` + 独立 clone | source/origin/clone=`cdacb042d20bc33b2e5bf5b05d91b47328f85bd7`；六项验证、严格 JSON、Parser、ASCII、零业务候选和 clean 状态全部 PASS |
| 2026-08-11 / 失败原子性 | failed FactCommit 正反例 | 成功拒绝 failed 分支中的 business ID/outbox 和 `business_writes>0`；技术日志字段未进入公共 Schema |
| 2026-08-11 / Task 3 mutations | 4 个反弱化检查 | failed fact 业务引用、outbox 终态重开、pending 回执、mixed 乱序均输出独立 `MUT-...|PASS` |
| 2026-08-11 / Task 3 上游回归 | 6 项串行验证 | CONTRACT-v2、Receipt/Date-v2、Issue/Correction-v2、Core Model、Nutrition/Progress、Mixed Model 全部 exit 0 |
| 2026-08-11 / Task 3 数据边界 | 工作区业务候选扫描 | `.jsonl/.sqlite/.sqlite3/.db/-wal/-shm/-journal` 新候选 `0`；差量只含计划内 validator/文档 |
| 2026-08-11 / Task 3 validator | SHA-256 | `4E27245EEF89966D04DEEDE628E2B2010DB3C746F9AE15B50801DDCE95B35CC8` |
| 2026-08-11 / SH-MAP-001 RED | 缺失映射契约 | Parser/ASCII/CR/NUL 均通过后稳定输出 `STORAGE_MAPPING_FILE_MISSING` |
| 2026-08-11 / SH-MAP-001 GREEN | 34 定义、20 表、18 索引 | `STORAGE_MAPPING|PASS|version=1.0.0|sources=4|definitions=34|tables=20|indexes=18|mutations=9` |
| 2026-08-11 / SH-MAP-001 mutations | 9 个反弱化检查 | 缺字段、A writer、日志入业务库、FactCommit 写终态、移除幂等索引、失败迁移推进版本、弱化列类型、弱化可空性、外键重定向均被拒绝 |
| 2026-08-11 / SH-MAP-001 上游回归 | 6 项串行验证 | CONTRACT-v2、Receipt/Date-v2、Issue/Correction-v2、Core Model、Nutrition/Progress、Mixed Model 全部 exit 0 |
| 2026-08-11 / SH-MAP-001 数据边界 | 业务候选扫描 | `.sqlite/.sqlite3/.db/-wal/-shm/-journal/.jsonl` 新候选 `0`；当前只有文档与验证器 |
| 2026-08-11 / SH-MAP-001 Round 1 | 独立 Schema/物理复核 | `FAIL|p0=0|p1=7`；7 项均先 RED 后修复并加入 mutation |
| 2026-08-11 / SH-MAP-001 Round 2 | 独立 checker + 28 mutations | `FAIL|p0=0|p1=1`；发现 EffectBundle 信封/幂等表未分类，另记录 finalize 的 goal_versions P2 |
| 2026-08-11 / SH-MAP-001 final GREEN | 18 个本地反弱化检查 | `STORAGE_MAPPING|PASS|version=1.0.0|sources=4|definitions=34|tables=20|indexes=18|mutations=18` |
| 2026-08-11 / SH-MAP-001 Round 3 | 独立 checker + 33 mutations | `SH-MAP-001-INDEPENDENT-REVIEW|PASS|p0=0|p1=0|definitions=34|tables=20|indexes=18|mutations=33` |
| 2026-08-11 / SH-MAP-001 GitHub clone | 私有 main + 独立 clone | candidate/origin/clone=`d8f612b5c2a7a556fc4f79c7f76414d4d70b78e3`；7 项验证、clean 状态与零业务候选均 PASS |
| 2026-08-11 / SH-CASE-001 RED | 旧12条简化数组 | validator Parser/ASCII/CR/NUL 通过后稳定拒绝 `CORE_CASE_SET_SHAPE_INVALID:root` |
| 2026-08-11 / SH-CASE-001 local GREEN | 5 cases / 3 fixtures / 8 mutations | `CORE_ACCEPTANCE_CASES|PASS|version=1.0.0|cases=5|fixtures=3|mutations=8` |
| 2026-08-11 / SH-CASE-001 regression | 新validator + 6项上游 | 7项串行验证全部 exit 0；未执行受保护validator，未创建业务数据库 |
| 2026-08-11 / SH-CASE-001 independent review | 独立 checker + 20 mutations | `SH-CASE-001-INDEPENDENT-REVIEW|PASS|p0=0|p1=0|cases=5|fixtures=3|mutations=20`；ZIP/12 entries/10 hashes/零受保护与零业务载荷通过 |
| 2026-08-11 / SH-CASE-001 evidence | 任务证据 | `EV-20260811-021`；仍不声明 adapter、SQLite、harness、安装或发布完成 |
| 2026-08-11 / SH-CASE-002 RED | 旧5案累计目录 | `DOMAIN_CASE_SET_VERSION_INVALID:expected=1.1.0:actual=1.0.0` |
| 2026-08-11 / SH-CASE-002 local GREEN | 9个新案例/9个场景/11 mutations | `DOMAIN_ACCEPTANCE_CASES|PASS|version=1.1.0|cases=9|scenarios=9|mutations=11`；原5案 validator 仍 `cases=5|fixtures=3|mutations=8` |
| 2026-08-11 / SH-CASE-002 Round 1 review | 独立 checker + 11 mutations | `FAIL|p0=0|p1=1`；捕获库存影响状态 token 漂移，11/11 mutation 均被拒绝 |
| 2026-08-11 / SH-CASE-002 token RED/GREEN | `skipped_ambiguous` 契约对齐 | 旧值稳定触发 `DOMAIN_CASE_VALUE_INVALID:CASE-INVENTORY-003:inventory`；修复后8项允许范围验证全部 PASS |
| 2026-08-11 / SH-CASE-002 Round 2 review | 177207-byte ZIP + 独立 checker | `SH-CASE-002-INDEPENDENT-REVIEW|PASS|p0=0|p1=0|cases=9|scenarios=9|mutations=11` |
| 2026-08-11 / SH-CASE-002 evidence | 任务证据 | `EV-20260811-022`；仍不声明 adapter、SQLite、harness、安装或发布完成 |
| 2026-08-11 / SH-CASE-002 GitHub clone | 私有 main + 独立 clone | candidate/source/origin/clone=`eedc1c367d6a0ab7abf84c107f50bb9606332f31`；8项允许范围验证全部 PASS，clone clean |
| 2026-08-11 / SH-CASE-003 TDD | ops/security validator | 旧 `1.1.0` 目录稳定 RED；最终 `OPS_SECURITY_ACCEPTANCE_CASES|PASS|version=1.2.0|cases=6|scenarios=6|mutations=12` |
| 2026-08-11 / SH-CASE-003 regression | 九项 Windows PowerShell 5.1 串行验证 | ops/core/domain + 三份 v2 契约 + 三组模型验证全部 exit 0；strict JSON、Parser、ASCII、CR/NUL 和 `git diff --check` PASS |
| 2026-08-11 / SH-CASE-003 public review | GitHub public HTTPS partial clone + 三组独立 checker | reviewed HEAD=`1139dd59a590e9fb78a49efd471c52e4bba8a958`；`SH_CASE_003_REVIEW|PASS|P0=0|P1=0|P2=2` |
| 2026-08-11 / SH-CASE-003 cleanup | GitHub key 与 OpenClaw 临时根 | 临时 deploy key 撤销后残留0；`SH_CASE_003_PUBLIC_REVIEW_CLEANUP|PASS|residual=0` |
| 2026-08-11 / SH-CASE-003 evidence | 任务证据 | `EV-20260811-023`；只证明静态 Oracle/fixture，不声明产品安装、迁移、恢复或删除实现完成 |
| 2026-08-12 / SH-CASE-004 regression | 十项 Windows PowerShell 5.1 串行验证 | golden/core/domain/ops + 三份 v2 契约 + 三组模型验证全部 exit 0；8资产/26累计case/12黄金mutation通过 |
| 2026-08-12 / SH-CASE-004 public review | GitHub public HTTPS isolated clone | reviewed HEAD=`072c1fb9ed4242a39b7b1aeaf1c134d5d1c33508`；`SH_CASE_004_REVIEW|PASS|P0=0|P1=0|P2=2|cleanup=1` |
| 2026-08-12 / SH-CASE-004 evidence | 任务证据 | `EV-20260812-024`；只证明黄金Oracle，不声明renderer、SQLite、adapter、installer或可安装Skill已实现 |
| 上一稳定里程碑 | SH-MODEL-001 | `CORE_MODEL_SCHEMAS|PASS|version=1.0.0|cases=41|event_defs=6|inventory_defs=6|mutations=4` |
| 上一稳定里程碑 | SH-MODEL-002 | `NUTRITION_PROGRESS_SCHEMAS|PASS|version=1.0.0|cases=42|definitions=10|mutations=4` |

`EV-20260812-024` 已生成；SH-CASE-004 已按黄金 Oracle 范围完成，但不代表 renderer、B adapter、SQLite、installer、migration、restore、runtime 或产品可安装。

### 下一步

下一步继续完成 `SH-HARNESS-001` 的 A/B 适配器、共享比较器和机器报告。当前仍不创建生产业务库，也不重开非必要安全底座功能。

---

> 以下内容保留为安全底座技术附录，记录此前实现与验证细节；它不再代表当前产品执行顺序。当前顺序以上述简明计划为准。

## 一、当前结论

- 唯一产品主线是 B（Skill + 轻量插件后端 + SQLite）；A 只保留只读降级，C 停止独立产品开发，其安全控制以后并入 B。
- `SH-SAFE-BASE-001`、`SH-CONTRACT-001/002/003/004`、`SH-DECISION-001`、`SH-MODEL-001/002/003`、`SH-MAP-001` 和 `SH-CASE-001/002/003/004` 已完成；下一项为 `SH-HARNESS-001`，随后完成 trace。没有并行开发 A/B/C 三个产品版本。
- 当前交付物是必要 foundation 与规范契约，不是可安装的饮食管家产品。正式业务记录、库存、营养计算、纠正执行、查询、SQLite 存储、安装和发布均未开始。
- 48 项完整矩阵仍只保留为历史发布级证据；当前按 `DEC-028` 以关键 focused + 4 项状态隔离完成安全有界退出。完整矩阵只在后续 release 候选需要时重跑，不再阻塞日常产品开发。
- G1、G2、G3 均未通过；不得提前创建 `shared/selected-route-map.json`，也不得宣称 Skill 已可用。

## 二、总计划中的当前位置

```text
M0 foundation 历史基线
  ↓
M1 SH-SAFE-BASE-001（已按 DEC-028 验收）
  ↓
M1 SH-CONTRACT-001 共同契约 v2（已完成）
  → M1 SH-CONTRACT-002 日期/回执 v2（已完成）
  → M1 SH-CONTRACT-004 Issue/纠正/补偿 v2（已完成）
  → M1 SH-DECISION-001 确定性策略表（已完成）
  ↓
M2 SH-MODEL-001 Event / Amount / Product / Inventory（已完成）
  → SH-MODEL-002 Nutrition / Progress（已完成）
  → SH-MODEL-003 Issue / Correction / Mixed（已完成）
→ SH-MAP-001（已完成）→ SH-CASE-001/002/003/004（已完成）→ SH-HARNESS-001（进行中）→ Trace
  ↓
M3 B SQLite + C 安全控制合并
  ↓
G1 B-only 存储硬安全门
  ↓
M4 B 最小纵向切片 + 故障验证
  ↓
G2 B-only 就绪与路径绑定
  ↓
M5–M6 B 的 PRODUCT-0.1 业务能力
  ↓
M7 安装 / 迁移 / 备份 / 恢复 / G3 发布
```

安全底座已经按业务风险完成有界退出，不再追求平台细节上的“优化到完美”。共同模型、SQLite mapping、27 个累计静态案例和 8 份黄金回执已冻结；当前完成 harness/trace 后，再开始真正的 B SQLite 实现。

## 三、已开发

- 固定根矩阵、路径 containment、长路径、junction/reparse no-follow、handle-bound 创建/复制/清理和物理残留复核。
- 正式数据、项目候选、源码 dist、外部 guards 的前后观察、差异报告和失败路径继续审计。
- source runtime 身份、private runtime snapshot、phase-local ledger、命令级 immutable handle 生命周期和闭包核验。
- A 的固定 PowerShell 入口及 B/C 的 8 条 Node 命令 spec、clean-room 环境、staging allowlist 和 TypeBox pinned-copy。
- 冻结 Q policy bootstrap、policy/spawn/addon journal 的 DTO、物理文件复核基础与报告结构。
- 默认 publisher 的 CreateNew/原子发布、精确 DTO、动态 getter 拒绝和 partial 结果聚合。
- 四个临时根的 adapter 结果与独立物理残留分离；adapter 谎报零仍能发现 ordinary/reparse 残留。
- 内嵌原生 helper 已由运行时 `Add-Type` 改为冻结 DLL 字节的 `Assembly.Load(byte[])`，避免污染调用者 TEMP。
- native `CreateProcessW`/Job/completion/stream/timeout 基础实现已经落盘，尚未通过全部计划 Oracle。
- `path_security` 的 Windows handle-pin 账本、十类路径操作、命令引用及五个 `PathPhaseObserver` 阶段已经落盘，并保持正式入口不暴露测试 seam。

## 四、本轮新增开发内容

1. native runner 增加 parent process identity、Job completion telemetry、Job accounting 和 8 MiB 双流读取基础。
2. `RED-PROCESS-001/003` 增加真实 parent/descendant identity 与 Job 对账断言；当前它们仍处 RED 状态。
3. 默认 Manifest provider 增加私有 `ExcludedSubtrees` 扫描参数；公开请求仍保持冻结的 5 字段。
4. `validation_root` 删除前的默认审计只跳过本轮已注册的 `runtime-snapshot`；同级未知 junction 仍 fail-closed。
5. snapshot 排除 focused 回归已通过：业务候选仍被发现，注册 snapshot 内 junction 不被跟随，未知 junction 被拒绝，fixture 残留为 0。
6. Manifest provider 新增 `manifest-provider-dto/v1` 深拷贝边界：先检查普通 `NoteProperty` 元数据，再读取一次值；动态 getter 在执行前拒绝，顶层、容器和 nested root 的跨 scope 引用复用均 fail-closed。
7. 机器报告新增精确 `manifests.provider_dto` 与 `test_seams`；当前 production-style 验证中 fault injector/observer 均如实记录为 inactive，正式入口未暴露内部 seam。
8. `ProcessFaultInjector` 已按冻结的 11 个阶段接入真实 Win32/Job/双流执行路径；注入结果使用普通 DTO 深拷贝，动态 getter 在执行前拒绝，命令报告固定记录 `fault_injection`。
9. 内嵌 native DLL 已随 fault callback 契约重新冻结；加载仍使用内存字节、固定长度/SHA/MVID/API 契约，不在调用者 TEMP 生成编译文件。
10. 机器报告新增精确 `path_security`：固定 `windows-handle-pin/v1`、immutable/writable share mode、十类 operation、逐 pin volume/file identity、零 delete-share；每条命令通过 `path_operation_ids` 只引用已登记操作。
11. 五个 `PathPhaseObserver` 阶段已接入 root create、staging copy、runtime input pin/launch、evidence temp-write/rename、cleanup entry-pin/dispose；observer 请求为普通 DTO，异常统一归一为 `PATH_OPERATION_FAILED`。
12. 路径账本实现过程中发现 WinPS 5.1 将 scriptblock 转为泛型 comparer 可能触发调用者 TEMP 的瞬时写入；已改为 `List[string].Sort(StringComparer.OrdinalIgnoreCase)`，`RED-ROOT-005` 的物理 junction 目标零变化恢复通过。
13. 用户进一步澄清业务原子性：失败允许写入独立技术日志/审计，但`FactCommit`失败时饮食记录、库存、营养、Issue、业务outbox、进度和成功结果必须全部回滚；该日志不得进入业务查询或被解释为“已记录”。已登记`CHG-20260811-001`，不改变FactCommit成功后Effect待重试的既有分层语义。

## 五、已经通过的验证

### 当前候选上的新鲜 focused 结果

| 验证 | 结果 | 边界 |
|---|---|---|
| PowerShell Parser / ASCII / CR / NUL | PASS | core 与主测试当前修改后通过 |
| 默认 Manifest snapshot 私有排除 | PASS | 不启动 Node/OpenClaw；注册 snapshot 跳过、未知 junction 拒绝、fixture 0 |
| `RED-REPORT-005` | PASS | 当前候选 55.0 秒；内存/磁盘 `path_security`、十类 operation、命令引用、五个 observer phase、异常归一、provider DTO/test seams 与深层 JSON 通过，fixture 0 |
| `RED-ROOT-005` | PASS | 当前候选 14.3 秒；ancestor junction 物理目标摘要不变，路径拒绝稳定，fixture 0 |
| `RED-STAGING-002` | PASS | 当前候选 58.6 秒；production-exact TypeBox 缺失/错 junction/篡改均 fail-closed，fixture 0 |
| `RED-TEMP-001` | PASS | 当前候选 142.7 秒；四根 adapter lie-zero、ordinary/reparse 物理残留与 junction 外部 sentinel 保护通过，fixture 0 |
| `RED-MANIFEST-001` | PASS | 当前候选 23.7 秒；包含 snapshot 私有排除子变体，fixture 0 |
| `RED-MANIFEST-006` provider 子变体 | PASS | 11 个 identity/dynamic/nested reuse 变体 114.7 秒；ScriptProperty getter 命中 0，finally/audit/cleanup 继续，fixture 0 |
| `ProcessFaultInjector` 11 阶段窄矩阵 | PASS | 约 1.8 秒；11 个固定 phase 均命中，前五阶段未恢复目标进程，动态 getter 命中 0，fixture 0 |
| 4 项状态隔离 | PASS | 当前候选 4/4，75.6 秒；`RED-OPENCLAW-001/002`、`RED-ENV-001/002` 全部通过 |
| `RED-PROCESS-002` | PASS | 当前候选 2.7 秒；8 MiB 双流边界与收尾通过 |
| production entry seam 反射门 | PASS | 正式入口不公开或转发 `ProcessFaultInjector/PathPhaseObserver` |
| native argv/exit 基础 | RED | argv 和 parent exit 正确；严格 Job 集合含系统 `conhost.exe`，`RED-PROCESS-001` 首红为 expected parent-only / actual parent+conhost |
| `RED-PROCESS-003` | RED | 当前候选重跑 39.8 秒；11 个 fault phase、taskkill 故障后的 Job fallback 和双流收尾均已通过；唯一失败为 timeout 树 expected parent+worker、actual parent+worker+系统 conhost，fixture 0 |

### 较早候选的历史结果（必须重验）

- 48/48 主矩阵、4/4 状态隔离曾通过。
- `RED-CACHE-VITEST-001`、`RED-STAGING-002`、`RED-TEMP-001`、`RED-ROOT-005`、`RED-OPENCLAW-002`、`RED-REPORT-005` 曾分别 focused 通过。
- 上述结果不能替代当前修改后候选的完整重验，也不能关闭 `SH-SAFE-BASE-001`。

## 六、发现问题

| 问题 | 影响 | 当前处置 |
|---|---|---|
| 严格 `CREATE_SUSPENDED | CREATE_NO_WINDOW | CREATE_UNICODE_ENVIRONMENT` 在当前 Windows 11 上把系统 `conhost.exe` 计入 Job | brief 同时要求 Job 总数恰为 parent + Q 成功 child，因此 `RED-PROCESS-001/003` 出现额外 PID；血缘探针确认它是目标 PowerShell 的直接子进程，命令行为 `\\??\\C:\\WINDOWS\\system32\\conhost.exe 0x4`，系统文件签名有效 | 不过滤、不伪造。微软文档说明无 breakaway 时 child 默认进入 Job；启用 breakaway 会削弱边界。保持 RED，登记为 brief/平台冲突，继续完成不依赖该裁决的计划项 |
| 用 `DETACHED_PROCESS` 或 Job breakaway 规避 conhost | direct Win32 试验中 `DETACHED_PROCESS` 可令 Job total=1，但 Windows PowerShell payload 未执行；`SILENT_BREAKAWAY_OK` 会让全部后代逃离 Job | 两条均拒绝采用。前者破坏 A 命令语义，后者破坏 kill-on-close、completion 和后代对账；需要按变更流程裁决冻结 brief，而不是在实现中偷偷放宽 |
| 真实 Task 5 报告曾因临时审计扫描 `validation_root\runtime-snapshot` 内的合法 junction 而失败 | 安全 snapshot 被误当成未知临时 reparse | 已用默认 provider 私有排除修复并 focused 通过；仍需正式入口重验 |
| native runner 还未把物理 Q journal 与 completion/Job 数据完成最终对账 | 真实 Node 命令无法形成可信的 `expected_total_processes` 和角色集合 | 作为 Task 5 剩余实现继续推进，不扩大到新产品功能 |
| 路径账本最初使用 scriptblock 泛型 comparer 排序 | WinPS 5.1 在 poisoned TEMP/junction 场景产生瞬时目录变化，破坏 ROOT 零写保证 | 已改为 CLR `List[string]` + OrdinalIgnoreCase comparer；五阶段 observer 与 `path_security.operations[]` 已完成 focused 验证 |
| 两次 Task 5 真实运行均形成了失败证据 | 第一轮遇到 source runtime 漂移；第二轮执行到 A 后遇到 `PROCESS_JOB_ACCOUNTING_MISMATCH`，并暴露 snapshot 审计误扫合法 junction | source 身份须在下一次运行前重新冻结；snapshot 私有排除已 focused 修复；完成本地门禁后只重跑一次正式入口 |
| 当前测试/核心文件已在较早 48+4 后改变 | 旧完整 PASS 不再新鲜 | 完成剩余实现后只跑一次完整 48+4，再跑唯一真实入口 |
| 当前目录不是 Git 仓库 | 无提交级回退与差异追踪 | 按总计划接受 `DEBT-005`，以 SHA-256 和不可覆盖证据补偿 |

## 七、待开发

1. 完成 native runner 对物理 Q policy/spawn/addon journal 的严格 schema/identity/role/PID 对账；当前已接入基本 parent/child/Job 集合核对，还需真实 Node 证明。
2. 解决或由计划裁决 `conhost.exe` 与冻结 Job 计数 Oracle 的冲突；在此之前不把 process runner 标为 GREEN。
3. 重跑 `RED-PROCESS-001/002/003` 等受影响 focused 用例；`RED-MANIFEST-001` 当前已通过。
4. 在同一冻结候选上串行运行 48 项主矩阵和 4 项状态隔离，确认无 Node/npm/Vitest/TypeScript/OpenClaw 实体被测试套件启动。
5. 冻结 27 项 protected manifest，执行唯一正式入口的 Task 5 真实 PowerShell/Node/Vitest/TypeScript/OpenClaw 验证，并再次核对零正式数据差异和零临时残留。
6. 本地门禁通过后，在用户已登录的专属 OpenClaw 平台验证可观察结果；浏览器测试不替代正式入口。
7. 生成 raw 输出、机器 JSON、最终 EV 和独立复核；全部通过后才关闭 `SH-SAFE-BASE-001`。

## 八、待优化（不扩张当前 WIP）

- 长矩阵只在候选稳定后运行一次；失败只定位首个计划内阻断，不反复盲跑。
- 继续保持 structural fixture 仅用于非 STAGING 用例；STAGING-001/002 使用 production-exact 依赖树。
- 最终 EV 集中保存 focused、完整矩阵、状态隔离、真实入口和 OpenClaw 结果，减少碎片证据。
- 对 embedded native DLL 增加可复现生成说明，但不在当前阻断关闭前重构构建系统。

## 九、后续可增加的优化

- 为安全矩阵提供 smoke / focused / release 三层统一入口和机器进度事件。
- B SQLite 开发开始后，为 migration、事务故障和 Docker/OpenClaw 建短反馈测试层。
- B 的 Skill 命令契约、SQLite 服务边界和 G2/G3 发布门稳定后，可增加薄 MCP adapter；MCP 只暴露同一套 B 能力，不复制业务逻辑。当前仍优先开发 Skill。

## 十、当前文件锚点

| 文件 | SHA-256 | 说明 |
|---|---|---|
| `总功能开发计划0.3.md` | `EF48E14407CFD6A8C623DD8162591656973C0F38009FDB6BA3BA9132F8B9CEC0` | 唯一总计划；`SH-DECISION-001`已完成，下一项为`SH-MODEL-001` |
| `shared/policies/decision-thresholds.json` | `8B15BAF36474B10E0F4FD6CA925B3E30A85FC1058E5A870E6E9EBF2585DDBCC7` | 确定性策略v1；54项固定用例和OpenClaw独立复核通过 |
| `shared/tests/validate-decision-thresholds.ps1` | `A41F61FC1DCA5FC13BEB2DF42747B16D4F38E2165FB512B57BA44B6E8DD94C11` | 策略shape、边界、hash和4项mutation验证器 |
| `docs/work-items/SH-DECISION-001-review.md` | `BDDDEA26B04D349CAF10CEB963CCEF817640069CEBEA0FBA61A2D61A984BBA89` | OpenClaw独立语义复核记录；最终PASS |
| `docs/evidence/EV-20260811-016-sh-decision-001.md` | `E5B3E09B845CF0E5BD8C47F5EB67841E45B6C81C2946266A2C22871C5997E30F` | 策略TDD、独立复核、GitHub交付和独立下载复验证据 |
| `shared/contracts/issue-correction-contract.md` | `41E4A18D4D72644641D66A58F918616EBB0A6189E7F0BE1E836741E057298FDB` | Issue/纠正 CONTRACT-v2；由 `EV-20260811-015` 完成独立复核 |
| `shared/tests/validate-issue-correction-contract-v2.ps1` | `5C719F1E169D5BA17A9C0160FBDA5410FB3F9C78A43672BE11C42DC3CE19DC2C` | Issue/纠正 v2 机器验证器 |
| `docs/work-items/SH-CONTRACT-004-v2-review.md` | `CFBCAA7029407AF20452704D45C9BACA2E7F12ADBFF498A9DDC495C43E455C5A` | OpenClaw 两轮独立复核记录；第二轮最终 PASS |
| `docs/evidence/EV-20260811-015-sh-contract-004-v2.md` | `5775CD3F9FF97AB22F3BA79818C422B04890CD0F7EDEC9BE8F5FD169B5E7A28C` | `SH-CONTRACT-004` TDD、独立复核、GitHub clone 与业务边界证据 |
| `shared/contracts/receipt-and-date-contract.md` | `F33E34D6B9EA9B1212208D75C5025FA86BB07923248E3B4929A1EF0BB7A375DD` | 日期/回执 v2 已由 `EV-20260811-014` 完成独立复核 |
| `docs/work-items/SH-CONTRACT-002-v2-review.md` | `880C13E9C30638F7BB05ACAADA90BE03FAF5AB37950D384F2820C3CAF9AEC7FF` | OpenClaw 独立复核记录；最终 PASS |
| `shared/tests/validate-receipt-and-date-contract-v2.ps1` | `DB7B9CD1BADF07CB4BDF14536CC7077276C16702E8040B9FD31D1E4AA6C6F0B7` | 日期/回执 v2 机器验证器 |
| `docs/evidence/EV-20260811-014-sh-contract-002-v2.md` | `59E2D29AF0F9E0CCC3B441FB617635BF809B7DFF5C3A237538A876AC798D8BF2` | `SH-CONTRACT-002` TDD、独立复核和最终门禁证据 |
| `docs/superpowers/plans/2026-08-11-skill-first-b-practical-plan.md` | `39E48E29635A6F97D9049B499208026446D4D85F9ED871D13A2A022D1B7876A3` | 当前简明执行路线；Task 1、Task 2已完成，Task 3还受mapping/harness/trace约束 |
| `docs/evidence/EV-20260811-012-sh-safe-bounded-exit.md` | `4E1AB059C0BA0D626B92DFBB2636D4E77EA05D60E60D8FEDC44CEFF343743DE2` | `SH-SAFE-BASE-001`按`DEC-028`完成必要范围验收的证据 |
| `docs/evidence/EV-20260811-013-sh-contract-v2.md` | `AFC3F3C160D310ACDBD767333DC34E48D3795D5A0D4732BF5D963C606BC28E61` | `SH-CONTRACT-001`机器门禁与OpenClaw第二轮独立PASS证据 |
| `shared/business-contract.md` | `632B2BBF8D0E6C655F4C0A47958828A86C67B3240065984CCC78A808E6F7072E` | 已冻结CONTRACT-v2；含三阶段、业务零数据边界和十项旧规则guard |
| `shared/tests/validate-business-contract-v2.ps1` | `2395D4B0A5806CDCE5FE1C313BE814CF473ABF65B3810E72DBC2BFFD20790589` | CONTRACT-v2机器协议、优先级、生命周期和旧规则防退化验证器 |
| `docs/work-items/SH-CONTRACT-001-v2-review-package.md` | `28628B5299B536156B2A759606FD4878CAF241C20E6C9B4A5AFAC4EF7744C4D0` | 第二轮独立契约复核输入 |
| `docs/work-items/SH-CONTRACT-001-v2-review.md` | `7F7953F7A1DF679CA3E6DE58A87BDD722B67B1188315B9F002D55D40D8A03902` | OpenClaw两轮复核记录；最终PASS |
| `version-b-lite-plugin/src/contracts.ts` | `4E5140E16180ED341F80536265F8E53A8273EF6F7B4A35001B8375550EF5EE99` | 8动作、6状态的可移植合同与结果一致性校验 |
| `version-b-lite-plugin/skills/diet-manager-b/SKILL.md` | `7D29519CC83D8B5CBA18CF1A9A21ACD4B6AF3900ADC47B9847DD25414D588623` | Skill结果处理规则；已做OpenClaw ignored/clarification正向验证 |
| `version-b-lite-plugin/src/storage/sqlite-runtime.ts` | `E1FE0DDF96625F6C874F761FA6A0991C0901E6CAAE52777BB50F72D5F399F7A8` | Node 24内置SQLite私有兼容运行时；尚无业务表 |
| `docs/work-items/SH-SAFE-BASE-001-brief.md` | `C25EEA39A4A778AC98F9BCDE17BED136D0BE3645153EBA7E1151205F10FE4441` | 当前任务冻结契约 |
| `shared/private/foundation-validation-core.ps1` | `E921FBB889C9AF46397E6DEDC918CACAC7AE68D1D2819DEB218A1B22DCFFBED7` | 当前工作候选；已含 ProcessFaultInjector、path_security 与五阶段 observer，未通过完整重验 |
| `shared/tests/validate-data-manifests.ps1` | `8309CBF6A92FDF17959071D3719189AED72E5F22439345C5C5DD1C2D35E4A591` | 当前 48 项主测试候选；新增 path_security/observer Oracle，未通过完整重验 |
| `shared/validate-foundations.ps1` | `2A6D5801952F3CF67E025B782716EE937AFAFF623FCCDC1106B9D9641D719F61` | 唯一正式入口 |
| `shared/tests/validate-foundations-state-isolation.ps1` | `574E4F67BCF75D74A57FA8805CAA1C90990E3DCA3D60C58CFB4424B83E5F11C9` | 4 项状态隔离测试 |

> 注意：core/test 会在本任务继续修改；每次稳定里程碑后应更新这里的 SHA。未生成有效最终 EV 前，不得把当前任务写成“已完成”。
