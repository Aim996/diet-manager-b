# 饮食管家开发进度

> 更新日期：2026-08-11
> 本文件是详细、可阅读的进度快照。任务依赖、完成条件和唯一状态以根目录《总功能开发计划 0.3》及对应 work-item brief 为准。

## 当前结论

- B 是唯一产品主线；A/C 不再并行开发。
- 当前整体可交付度约 69%，但这不是“可正式使用”的比例。
- 当前 B 插件状态为 `foundation_development_only`：可以构建、测试、验证和在专用 OpenClaw 环境加载，尚不能写真实饮食数据。
- 当前写动作必须返回 `foundation_not_implemented`、`committed=false`。
- 正式 SQLite repository、业务纵向切片、安装迁移和 PRODUCT-0.1 发布门仍未完成。
- `SH-HANDOFF-001` 的本地门和 OpenClaw 02/03/04 独立交接验证已完成；当前只剩现有 GitHub PR 的证据提交与审查收口。下一产品任务恢复为 `SH-CASE-003`。

### 分范围完成度

| 范围 | 粗略完成度 | 当前事实 |
|---|---:|---|
| 产品计划与边界 | 93% | B 单线、失败原子性、业务阶段和 Skill/后端/适配器职责已冻结 |
| 必要安全底座 | 100%（当前必要范围） | 非必要 Windows 边角已停止扩张，不再阻塞业务开发 |
| 共享业务模型与 mapping | 100%（当前 M2 范围） | Event/Amount/Product/Inventory、Nutrition/Progress、Issue/纠正与 SQLite mapping 已完成独立复核 |
| 共同案例 | 进行中 | SH-CASE-001/002 已完成，累计 14 个正式案例；SH-CASE-003/004 待开发 |
| B 业务产品 | 62% | 契约、模型、mapping、核心/领域案例已有；repository、outbox worker 和真实纵切未实现 |
| OpenClaw 实际集成 | 进行中 | 已用于独立复核；本轮建设 GitHub 独立拉取和开发安装链 |
| PRODUCT-0.1 发布 | 0% | M7 安装、迁移、备份、恢复和不可变候选尚未开始 |

## 已开发

1. **Skill 与工具边界**
   - `diet-manager-b` Skill 已定义饮食、饮水、库存、查询、更正和撤销意图。
   - `diet_manager` 提供 8 个动作入口。
   - 未接后端时保持非写入，不使用聊天记忆或旁路文件伪造记录。

2. **共同契约**
   - CONTRACT-v2 已冻结 FactCommit、EffectBundle、EnvelopeFinalize 三阶段。
   - FactCommit 失败时允许独立技术日志，但所有业务表、业务 outbox 和成功结果必须零写入。
   - 日期/回执、Issue/纠正、营养来源和确定性策略已完成独立复核。

3. **共享模型与 SQLite mapping**
   - SH-MODEL-001/002/003 已完成。
   - SH-MAP-001 已完成 34 个定义字段、20 张表、18 个索引、22 条外键和事务边界映射。
   - `node:sqlite` 兼容运行时已有提交、回滚、关闭和测试清理验证，但尚无正式业务表。

4. **共同案例**
   - SH-CASE-001：5 个核心案例完成。
   - SH-CASE-002：新增 9 个领域/故障案例，累计 14 个正式案例完成。
   - 案例具备固定 fixture、确定性环境、mutation 和独立复核证据。

5. **必要安全与 GitHub 基础**
   - 路径越界、验证污染、伪提交和清理逃逸已有必要门禁。
   - 私有 GitHub 仓库、独立 clone 复验和受保护文件排除已建立。
   - 真实饮食数据、数据库、令牌、依赖目录和 A/C 实现不会上传。

## 正在开发

### SH-HANDOFF-001：GitHub → OpenClaw 开发交接

目标是让陌生智能体只依据仓库内容完成私有 GitHub 拉取、环境预检、依赖安装、测试、构建、插件校验、Skill/插件安装和零写入冒烟。

当前已完成：

- 交接设计与逐步实施计划。
- 独立 feature branch 和隔离 Git worktree。
- 机器交接契约 RED→GREEN。
- Node 下限从错误的 24.14.0 收紧到 OpenClaw 2026.7.1 实际接受的 24.15.0。
- pnpm 11 五项依赖构建脚本显式白名单。
- 机器可读交接清单和只读验证器。
- 人工入口文档和 GitHub 协作文件。
- 本地完整交接门、18 项测试、构建、插件校验和泄漏扫描。
- 私有 GitHub 分支、draft PR 和同一提交的三环境拉取。
- OpenClaw 02 完整安装门、03 八动作真实工具零写入冒烟、04 幂等/卸载/清理验证。
- 三环境临时仓库、私钥和 GitHub 只读 deploy key 清理。

当前待完成：

- 把三环境证据提交到现有 PR，并完成 PR 审查收口。
- 是否合并 main 由当前开发交接 PR 的最终审查决定；不得把合并解释成 PRODUCT 发布。

## 待开发

1. `SH-CASE-003`：隐私、foundation、迁移、安装、恢复、删除等运维/安全 Oracle。
2. `SH-CASE-004`：黄金回执和跨日展示案例。
3. `SH-HARNESS-001` 与 `SH-TRACE-001`：共同执行 harness 和需求—案例—任务—证据追踪。
4. `B-STOR-001/002`：正式 SQLite Schema、migration、repository、事务、幂等、并发、WAL 和恢复。
5. `B-MERGE-C-001`：把服务端权威 preview/state 控制合并到 B。
6. G1/G2：B 存储硬安全门和真实纵向切片/故障门。
7. M5/M6：饮食、饮水、库存、营养、Issue、纠正、进度、查询和黄金回执。
8. M7：唯一正式安装器、迁移、备份、恢复、导出、卸载和 G3 发布。
9. MCP：只在 B Skill/后端契约稳定后增加薄适配器，不复制业务逻辑。

## 本轮新增开发内容

- 新增 `delivery/openclaw-development-handoff.json` 机器交接清单。
- 新增 `scripts/validate-openclaw-development-handoff.mjs` 跨平台只读验证器。
- 新增 handoff contract Vitest。
- 新增 `pnpm-workspace.yaml`，显式允许五个已知依赖执行安装构建脚本，拒绝交互式全量批准。
- 新增 GitHub/OpenClaw 设计、实施计划和 work-item brief。
- 重新生成并纳入完整 `dist`，使 GitHub 安装包包含 8 个动作、结果校验和 SQLite 兼容运行时，而不是旧的 4 动作产物。
- 重写 README、START-HERE 和本进度文件，移除三路线并行与旧安全任务的混合状态。
- 新增 OpenClaw 02/03/04 分工、安装、行为、安全和清理交接文档。
- 新增未来开源所需的贡献、安全报告和 PR 检查入口；许可证仍待用户选择。

## 发现问题

| 问题 | 影响 | 当前处理 |
|---|---|---|
| README 与 START-HERE 状态冲突 | 新智能体无法判断 B-only 和是否可安装 | 本轮统一为 B-only、开发交接、非 PRODUCT |
| Node 24.14.0 不满足 OpenClaw 2026.7.1 | 旧命令在干净环境直接失败 | 交接和 package engine 收紧为 `>=24.15.0 <25` |
| pnpm 11 默认拒绝依赖构建脚本 | `pnpm install --frozen-lockfile` 首次失败 | 仓库内固定五项 allowBuilds，禁止 `--all` |
| 私有 GitHub 需要授权 | 专用 OpenClaw 可能无法直接 clone | 02 环境首先验证；缺权限明确报 `GITHUB_AUTH_REQUIRED`，不泄漏凭据 |
| 当前插件无真实业务实现 | 加载成功可能被误解为可记录 | 文档、清单、测试和行为冒烟共同锁定 `committed=false` |
| Git 中原有 `dist` 落后于 TypeScript 源码 | GitHub 直接安装会得到旧的 4 动作插件，并缺少 SQLite 运行时产物 | 完整构建已刷新产物；handoff contract 新增 8 动作、结果校验和 storage 产物防退化 |
| 当前没有开源许可证 | 即使代码可见也不构成合法开源授权 | 保持私有；公开前由用户选择许可证并重跑历史/密钥复核 |
| 干净 OpenClaw 没有系统 `ssh`，且 22 端口受阻 | 私有 GitHub clone 无法直接执行 | 临时只读 deploy key + GitHub 官方 SSH 443 验证通过；密钥已全部撤销 |
| 安装后当前 Gateway 可能未注册新工具 | plugin 显示 loaded 但工具暂不可调用 | 新会话/完整激活后再核 `diet_manager`，不允许直接 import 源码冒充工具调用 |
| 热重载不保证卸载旧工具对象 | 磁盘已清理但进程内工具仍可能存活 | 卸载门新增完整 Gateway 进程重启复核；03 的该行为已作为已知运行时事实记录 |

## 待优化

- 把 GitHub/OpenClaw 交接验证接入未来 CI，但本轮不增加未经选择的云服务或密钥。
- 缩短 OpenClaw 首次依赖构建时间，同时保持 lockfile 和构建白名单。
- 为 OpenClaw Skill workspace 增加官方可逆卸载命令；当前 2026.7.1 CLI 没有通用 `skills uninstall`。
- 在不削弱零写入门的前提下，把三环境证据收敛为统一机器报告。

## 后续可增加的优化

- 选择许可证后补 `LICENSE`、公开路线图、Code of Conduct 和公开 issue 模板。
- 增加 GitHub Actions 的 Linux/Windows Node 24.15+ 矩阵。
- PRODUCT-0.1 候选形成后增加签名、SBOM、逐文件 SHA-256 和不可变 release asset。
- B 业务稳定后提供 MCP 薄适配器和更多智能体平台适配，但保持同一后端。
- 为 Docker/OpenClaw 干净环境增加正式安装、升级、恢复和卸载 E2E；当前只做开发交接。

## 验证与 GitHub 状态

| 项目 | 状态 |
|---|---|
| 仓库 | `Aim996/diet-manager-b`，当前私有 |
| 主分支 | `main` 保持稳定开发基线 |
| 当前分支 | `agent/github-openclaw-development-handoff` |
| 隔离工作区 | 已建立，不直接在 main 开发 |
| handoff contract | PASS：本地与三套独立 OpenClaw 环境均出现冻结成功行 |
| Node 版本门 | PASS：最低版本规则为 24.15.0，三环境实际为 24.16.0 |
| 完整 pnpm test/build/plugin validate | PASS：18/18 测试、构建和插件校验均通过 |
| OpenClaw 02 | PASS：完整交接门、安装发现、零业务写入、最终清理 |
| OpenClaw 03 | PASS：8/8 真实工具动作均不提交；磁盘/配置/Skill/私钥清理完成，热重载运行时行为已记录 |
| OpenClaw 04 | PASS：幂等安全拒绝、真实调用零写入、精确卸载、完整重启后无残留 |
| 临时 GitHub deploy key | 02/03/04 均已撤销；仓库最终为 0 把 deploy key |
| 正式 PRODUCT release | 未开始，禁止创建 |

本轮完成后只表示“GitHub 开发交接可复现”；不会把 PRODUCT-0.1、真实数据库或正式安装任务提前标记完成。
