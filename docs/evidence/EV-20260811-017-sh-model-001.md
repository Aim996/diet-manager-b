# EV-20260811-017 — SH-MODEL-001 Core Model v1

## Result

`SH-MODEL-001` is complete for its frozen scope: Event/Amount/Product/Inventory facts, immutable batch input, rebuildable projection and inventory transactions.

This evidence does not claim a production database, repository, runtime schema engine, route binding, G1/G2/G3, installable Skill or product readiness.

## Frozen deliverables

| File | SHA-256 |
|---|---|
| `shared/schemas/event-and-amount.schema.json` | `FD5F2B44C5AC1B8295F54774AA3425DD2DB4BA16915111A3E1B241104CEE47CA` |
| `shared/schemas/product-inventory.schema.json` | `681551FA18759AE3F993B0951C3A650FA8ABE16B28D7A9E7223E24F5E9B6613F` |
| `shared/tests/fixtures/core-model-cases.json` | `3FF78E234D063E86294143BD2D91765E96E6569D53704EECEE4A83F29945AA39` |
| `shared/tests/validate-core-model-schemas.ps1` | `486CF6D9783E8FF750115EF49C9C2CA7F6DF3B0C2D608310E490B06B8E30120C` |

## TDD evidence

1. Missing-schema RED: `MODEL_SCHEMA_FILE_MISSING`.
2. Initial GREEN: 33 fixed cases.
3. Test-quality RED: bidirectional adjustment/correction and required provenance were missing.
4. Review RED: failed business result, standard cross-resource refs, strict extra fields, nonempty provenance and current-message authorization were incomplete.
5. Final GREEN:

```text
CORE_MODEL_SCHEMAS|PASS|version=1.0.0|cases=41|event_defs=6|inventory_defs=6|mutations=4
```

The final corpus pins its exact bytes and keeps expected case IDs/order/uniqueness as test-owned literals. Four independent fail-closed mutations cover schema identity, JSON-number quantities, generic status and missing fixture coverage.

## Business invariants proved

- business objects are completed and `result_status=committed`; failed writes may enter only an independent technical log and create zero dietary data;
- generic `status` is not a public model field;
- time preserves raw text, interval, precision, timezone, basis, anchor and resolver version;
- quantities are canonical decimal strings and unknown values are not invented as zero;
- meal input keeps stated/nutrition/inventory/package amounts distinct;
- ProductIdentity, immutable InventoryBatch and rebuildable InventoryBatchProjection have separate ownership;
- aliases require explicit user evidence and product/batch field provenance is nonempty;
- inventory transactions enforce decimal before/delta/after, nonnegative after, direction/sign and event/reason coherence;
- partial deduction authorization is tied to the current source message and related event.

## Independent review

Two OpenClaw read-only review rounds returned `PASS` and the exact completion marker `Ready for SH-MODEL-001 completion: Yes`.

The final reviewer also used Ajv 8.20.0 plus `ajv-formats` as an independent draft 2020-12 implementation. It reported `20/20` expected-valid schema results and all `21` expected-invalid outcomes correctly split between schema-level and semantic-only rejection.

The reviewer confirmed that a third-party runtime is not required to close this schema-freeze work item. Before production writes, a route-neutral runtime validator must load both schemas by `$id`, enforce formats and preserve the independent cross-field semantic rules.

## Fresh local gates

```text
CONTRACT_V2|PASS|id=diet-manager/contract-v2|statuses=5|protocol=3|legacy_guards=10
RECEIPT_DATE_V2|PASS|id=diet-manager/receipt-date-contract-v2|metrics=6|trace=11|legacy_guards=10
ISSUE_CORRECTION_V2|PASS|id=diet-manager/issue-correction-contract-v2|statuses=4|codes=23|operations=13|trace=6|legacy_guards=12
DECISION_THRESHOLDS|PASS|id=diet-manager/decision-thresholds/v1|version=1.0.0|cases=54|expiry_rules=8|gates=2|mutations=4
CORE_MODEL_SCHEMAS|PASS|version=1.0.0|cases=41|event_defs=6|inventory_defs=6|mutations=4
MODEL_PARSER_ERRORS=0
VALIDATOR_NONASCII=0
RELATIVE_EVENT_REFS=0
ABSOLUTE_EVENT_REFS=7
PROTECTED_TRACKED=0
BUSINESS_CANDIDATES=0
```

## Deferred, not blocking

- distinct fixtures for the remaining inventory event branches and additional time/amount boundaries;
- arbitrary-precision decimal and stricter decimal canonicalization in the production validator;
- real runtime format assertion and custom annotation registration;
- SQLite mapping, repository, transaction fault tests and Skill/OpenClaw deployment.

## GitHub delivery

Pending private-main commit, push and independent-clone reproduction. This section is updated only after the remote and clone gates pass.
