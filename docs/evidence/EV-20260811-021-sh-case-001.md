# EV-20260811-021 — SH-CASE-001 核心共同案例

## 结果

`SH-CASE-001` 已完成：5 个正式核心 CASE-ID、3 个固定 fixture、8 个本地反弱化 mutation、20 个独立 mutation、7 项串行验证和独立 clone 复现共同构成首组 B-only 共享 Oracle。

本证据不声明 B adapter 已执行这些案例，不声明已创建 SQLite 业务数据库、repository、outbox worker、OpenClaw/MCP 生产适配器或可安装 Skill，也不构成 G1/G2/G3 通过。

## 冻结交付物

| 文件 | SHA-256 |
|---|---|
| `shared/acceptance-cases/cases.json` | `4972830F746ED630FF31897D2E958451077DD602EEA1C5510CE0B3432FDD38D7` |
| `shared/acceptance-cases/fixtures/core-v1.json` | `720A8DB76CB00C034164E401B5B45C415D79218C88BCEF87F42399B2DC0CF2CB` |
| `shared/tests/validate-core-acceptance-cases.ps1` | `F0247742B91400E22AFE117C239AAC91D2B78D0024F59B315B9E8585ABCB4262` |
| `docs/superpowers/specs/2026-08-11-sh-case-001-design.md` | `FFC1BDD89E0DA117A1D27578A2E05313607AE846D0FBE88CB9BF29E445AE268F` |

计划、brief、report、review package、review 和本 EV 属于治理记录；它们在收尾提交中更新，因此不在本文件内自引用最终哈希。

## TDD 与本地验证

旧的 12 条简化意图数组被新 validator 稳定拒绝：

```text
CORE_CASE_SET_SHAPE_INVALID:root
```

实现五案和 fixture 后，8 个本地 mutation 全部被拒绝：

```text
CORE_ACCEPTANCE_CASES|PASS|version=1.0.0|cases=5|fixtures=3|mutations=8
```

串行回归同时通过：

```text
CONTRACT_V2|PASS|id=diet-manager/contract-v2|statuses=5|protocol=3|legacy_guards=10
RECEIPT_DATE_V2|PASS|id=diet-manager/receipt-date-contract-v2|metrics=6|trace=11|legacy_guards=10
ISSUE_CORRECTION_V2|PASS|id=diet-manager/issue-correction-contract-v2|statuses=4|codes=23|operations=13|trace=6|legacy_guards=12
CORE_MODEL_SCHEMAS|PASS|version=1.0.0|cases=41|event_defs=6|inventory_defs=6|mutations=4
NUTRITION_PROGRESS_SCHEMAS|PASS|version=1.0.0|cases=42|definitions=10|mutations=4
ISSUE_CORRECTION_MIXED_SCHEMAS|PASS|version=1.0.0|cases=65|definitions=12|semantic_only=10|mutations=4
```

额外门禁包括：严格 JSON、精确案例/fixture ID、PowerShell Parser=0、validator ASCII、CR/NUL=0、`git diff --check`、受保护路径差量 0、业务数据候选 0。

## 已证明的业务不变量

- 多项和单项 meal 共用 `items[]` 与 `meal_id`，不制造单项特例 Schema。
- 牛奶是 meal 中的营养饮品，不计入 plain-water；500 ml 白水精确且只产生一个 WaterEvent。
- 回执结构由同一 EnvelopeFinalize 结果拥有，进度块最后，结构 Oracle 与黄金文本职责分离。
- 当前日查询采用用户时区自然日半开区间、active-only、稳定排序和零业务写入。
- 写入失败可以留下独立脱敏技术日志，但饮食记录及八类业务对象全部零新增，不产生半条记录。

## 独立复核

OpenClaw 在隔离目录验证 151068 字节 review ZIP、SHA-256、12 个条目、10 个声明文件哈希、0 个受保护文件和 0 个业务数据库载荷；随后用自行编写的 checker 执行 20 项 mutation，未执行候选 validator 生成期望。

```text
SH-CASE-001-INDEPENDENT-REVIEW|PASS|p0=0|p1=0|cases=5|fixtures=3|mutations=20
```

P2 仅记录 `nutritious_drink`/`nutrition_drink` 的未来 adapter 显式映射，以及把公共输出禁项集中到 harness；二者不阻塞本任务，也不授权修改当前 Oracle。

## GitHub 与独立 clone

冻结候选已推送到私有 `origin/main`，候选提交为 `b37eaf66676fd01f25cc9ee530cae1e84f7f447d`。独立 clone `E:\codx\skill\github\diet-manager-b` fast-forward 到同一提交并重新执行上述 7 项验证，全部 PASS、工作区 clean。包含本 EV 和独立复核记录的收尾提交随后同样由 source/origin/clone 三方一致门交付；最终交接消息记录实际收尾提交值，本 EV 不自引用包含自身的提交哈希。

## 下一任务

下一项是 `SH-CASE-002`：冻结入库、库存、营养、Issue、纠正、混合、Effect 和最终器故障 Oracle。它仍只建设共享案例/fixture，不提前创建生产数据库或复制 OpenClaw/MCP 业务逻辑。
