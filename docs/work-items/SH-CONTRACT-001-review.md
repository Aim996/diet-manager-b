PASS

# SH-CONTRACT-001 独立审阅报告

## 1. 结论

候选 `shared/business-contract.md` 的文件、哈希、77 个任务范围 REQ ID 集合、追踪表单例性、Markdown 结构、被替代规则隔离和业务数据卫生检查均通过；但逐条对照总计划第 2—9 节后，发现 5 项规范性缺口，其中包含规则强度弱化、纠错副作用不完整和动态保质重算被降为可选。候选目前不能作为 A/B/C、Schema 和验收案例共同实现的 CONTRACT-v1。

## 2. 阻断发现

### F-001｜高｜规范词定义允许未经决策的 SHOULD 偏离，并弱化硬规则回执

- 精确位置：`E:\codx\skill\饮食管家\shared\business-contract.md:7`、`:77`；规范来源 `E:\codx\skill\饮食管家\总功能开发计划0.2.md:73`、`:207`。
- 受影响 REQ ID：`REQ-EVENT-010`、`REQ-MEAL-011`、`REQ-PANTRY-018`、`REQ-NUTR-002`、`REQ-NUTR-003`；同一定义还会影响候选中以“应”表达的 `REQ-NUTR-007`、`REQ-NUTR-008`。
- 计划中的正确语义：`应当` 只有存在“已登记且生效的例外决定”才能偏离；`REQ-EVENT-010` 位于“不可突破的业务与数据规则”，总回执必须逐项说明状态。
- 当前候选的问题：第 7 行把 SHOULD 的例外门槛写成任意“可追溯理由”，低于计划要求；第 77 行又把 mixed 总回执逐项状态写成“应”，使硬规则可被该宽松例外绕过。追踪表虽指向 §10，但没有消除强度差异。
- 最小修复：把第 7 行改为“SHOULD 只有存在已登记且生效的例外决定时才可偏离”；把第 77 行的逐项状态要求改为 MUST。随后逐一复核候选中所有 `SHOULD/应`，确保计划中的硬约束未被降级；无需改变字段 Schema 或文案格式。

### F-002｜高｜追加纠错/冲销没有冻结库存补偿、营养重算和进度联动

- 精确位置：`E:\codx\skill\饮食管家\shared\business-contract.md:30`、`:77`、`:86`；规范来源 `E:\codx\skill\饮食管家\总功能开发计划0.2.md:265-273`。
- 受影响 REQ ID：`REQ-SCOPE-012`、`REQ-EVENT-003`、`REQ-MEAL-018`。
- 计划中的正确语义：定位既有活动记录后追加 `CorrectionEvent`；库存补偿与营养重算必须原子执行，更新有效视图及受影响日期的 `daily_progress`，原始记录保持可审计。冲销同样不能留下与有效事实不一致的库存/营养/进度副作用。
- 当前候选的问题：第 30 行只规定 append-only 和历史可审计，并把有效视图重算写成“可以”；第 77 行只冻结首次饮食与扣减的事务边界；第 86 行把载体交给 companion contract，却没有保留上述共同业务结果。实现可据此追加 correction/void 而不补偿库存、不重算营养或不更新受影响日期进度。
- 最小修复：在 §4 或 §10 增加一条共同语义：任何改变有效摄入/数量/组成/库存效果的 correction/void，MUST 在单一可靠事务边界追加更正事件、执行相应库存补偿和营养重算，并更新受影响日期的有效进度；原事件和旧快照仍保留。继续把载体字段与展示交给 companion contract。

### F-003｜高｜位置或开封变化后的保质估算重算被降为 MAY

- 精确位置：`E:\codx\skill\饮食管家\shared\business-contract.md:63`；规范来源 `E:\codx\skill\饮食管家\总功能开发计划0.2.md:602`。
- 受影响 REQ ID：`REQ-PANTRY-022`。
- 计划中的正确语义：位置或开封状态变化后重新计算当前有效估算，并保留旧估算审计记录。
- 当前候选的问题：第 63 行写成“位置或开封变化 MAY 重算”，允许继续展示基于旧位置或旧开封状态的剩余天数；这是对精确 REQ 的直接强度弱化。
- 最小修复：将该处 MAY 改为 MUST，并明确重算的是当前有效估算、旧估算及依据继续保留审计；不需要冻结具体算法。

### F-004｜中｜入库位置的自然语言纠正与单品/多品回执边界遗漏

- 精确位置：`E:\codx\skill\饮食管家\shared\business-contract.md:61`、`:84`；规范来源 `E:\codx\skill\饮食管家\总功能开发计划0.2.md:569`、`:605-628`。
- 受影响 REQ ID：`REQ-PANTRY-017`、`REQ-PANTRY-018`。
- 计划中的正确语义：入库回执显示实际采用的位置，用户可用自然语言纠正；单商品可详细展示，多类同时入库时每项一行简洁展示；快捷位置选项只在多个合理位置会实质影响结果时出现。精确中文格式可由 receipt/date contract 冻结。
- 当前候选的问题：第 61 行只保留“显示位置”，漏掉自然语言纠正；第 84 行把精确文案交给 companion contract 是正确的，但未保留单品详细/多品逐项简洁这一共同回执边界。三条路线可能因此对同一多商品入库返回不可比较的聚合结果，或无法纠正位置。
- 最小修复：在 §8/§11 补充“位置 MUST 可由用户自然语言纠正；单商品 MAY 详细回执，多商品 MUST 每项一行简洁反映各自结果”，仍不规定逐字文案。

### F-005｜中｜营养基准与计算覆盖语义不完整

- 精确位置：`E:\codx\skill\饮食管家\shared\business-contract.md:70`、`:72`；规范来源 `E:\codx\skill\饮食管家\总功能开发计划0.2.md:682-706`、`:730-764`。
- 受影响 REQ ID：`REQ-NUTR-005`、`REQ-NUTR-010`、`REQ-NUTR-011`（并涉及计划 §9.3、§9.5、§9.6 中未单独编号但属于本次规范来源的共同语义）。
- 计划中的正确语义：每份营养资料除基准量和单位外，还要表达份名与每包装份数；内部计算使用稳定精度，展示统一舍入，展示后的舍入值不得反向参与累计；`complete/partial/estimated/unknown` 的覆盖含义必须一致，存在未计入项目时只能称“已知部分合计”。
- 当前候选的问题：第 70 行只写“计量基准及换算依据”，没有保留份名和每包装份数；第 72 行要求快照保存原始/舍入值和覆盖状态，却没有禁止展示舍入值回流累计，也未定义 `complete`，第 23 行也只解释了 `partial/estimated/unknown`。这些遗漏会让 Schema、计算器和 Oracle 对同一包装与覆盖状态产生不同解释。
- 最小修复：在 §9 补充份名与每包装份数的可表达性；明确内部稳定精度、仅展示层舍入且舍入值不得回流累计；定义四个覆盖状态，保留现有“部分覆盖不得冒充完整总量”规则。字段名和数值精度可继续留给 Schema/实现契约。

## 3. 77 条逐语义审阅结果

状态含义：`通过` 表示正文语义、强度及跨节一致性均满足；`不通过` 表示被上述发现影响；`直接语义通过/章节缺口` 表示该 REQ 行本身已覆盖，但同一规范来源章节的共同语义仍不完整。

| REQ ID | 结果 | 说明 |
| --- | --- | --- |
| REQ-SCOPE-001 | 通过 | 仅当前用户本人。 |
| REQ-SCOPE-002 | 通过 | 家庭库存为本人默认库存来源。 |
| REQ-SCOPE-003 | 通过 | 非本人事实不入本人账本且不建他人档案。 |
| REQ-SCOPE-004 | 通过 | 排除 0.3 健康助手。 |
| REQ-SCOPE-005 | 通过 | 排除医疗诊疗和用药建议。 |
| REQ-SCOPE-006 | 通过 | 仅已配置目标用于进度，不虚构目标/百分比/评价。 |
| REQ-SCOPE-007 | 通过 | 后续候选能力不进入 0.1。 |
| REQ-SCOPE-008 | 通过 | 排除评分、改善及减重方案。 |
| REQ-SCOPE-009 | 通过 | 未授权分享/上传禁止且最小披露。 |
| REQ-SCOPE-010 | 通过 | 禁止无来源联网补数，来源可追溯/版本化。 |
| REQ-SCOPE-011 | 通过 | 不无依据拆解复合食品。 |
| REQ-SCOPE-012 | 不通过 | append-only 已覆盖，但 correction/void 联动副作用不完整，见 F-002。 |
| REQ-EVENT-001 | 通过 | 仅已发生事实进入正式账本。 |
| REQ-EVENT-002 | 通过 | 未发生 ignored；已记录删除走 void。 |
| REQ-EVENT-003 | 不通过 | fact-first 通过；纠错后的可靠副作用一致性缺口见 F-002。 |
| REQ-EVENT-004 | 通过 | 原话、来源、时间、版本和稳定 ID 完整。 |
| REQ-EVENT-005 | 通过 | 推导来源和规则版本完整。 |
| REQ-EVENT-006 | 通过 | 命令结果与记录生命周期分离。 |
| REQ-EVENT-007 | 通过 | 同幂等键返回原结果且无重复副作用。 |
| REQ-EVENT-008 | 通过 | 不同幂等键仅提示疑似重复。 |
| REQ-EVENT-009 | 通过 | mixed 按叙述顺序、逐事件原子。 |
| REQ-EVENT-010 | 不通过 | 后项不回滚已覆盖；逐项总回执被弱化，见 F-001。 |
| REQ-EVENT-011 | 通过 | 测试使用隔离数据根且不触碰正式数据。 |
| REQ-EVENT-012 | 通过 | 存储未持久化必须 failed。 |
| REQ-MEAL-001 | 通过 | 餐次原文、规范值、来源分开。 |
| REQ-MEAL-002 | 通过 | 用户明确优先，推断标识来源。 |
| REQ-MEAL-003 | 通过 | 餐次和时刻分离。 |
| REQ-MEAL-004 | 通过 | 多项共享 meal_event_id 并保序。 |
| REQ-MEAL-005 | 通过 | 要求已发生。 |
| REQ-MEAL-006 | 通过 | 至少一个可识别食物。 |
| REQ-MEAL-007 | 通过 | 保留原话和原始食物名。 |
| REQ-MEAL-008 | 通过 | 用户时间或带来源默认时间。 |
| REQ-MEAL-009 | 通过 | 数量/餐次/库存/营养独立不完整。 |
| REQ-MEAL-010 | 通过 | 模糊/缺失仍保存事实并建问题。 |
| REQ-MEAL-011 | 不通过 | upper_plausible_bound 条件正确，但 SHOULD 例外治理过宽，见 F-001。 |
| REQ-MEAL-012 | 通过 | 合理上界而非理论极值，来源/version 保留。 |
| REQ-MEAL-013 | 通过 | 摄入、营养采用量、扣减量、包装规格隔离。 |
| REQ-MEAL-014 | 通过 | 无可信范围保持 unknown。 |
| REQ-MEAL-015 | 通过 | 分数、小数、范围、近似及自然单位可保留。 |
| REQ-MEAL-016 | 通过 | 整菜/组件二选一，不重复计算。 |
| REQ-MEAL-017 | 通过 | 模板组成、采用量、版本和来源可追溯。 |
| REQ-MEAL-018 | 不通过 | 组成纠正重算已写，但通用 correction 联动不完整，见 F-002。 |
| REQ-MEAL-019 | 通过 | 未知套餐事实保留、营养 unknown、建问题。 |
| REQ-PANTRY-001 | 通过 | home_default 默认读、关联并在条件满足时扣减。 |
| REQ-PANTRY-002 | 通过 | 明确外食同时跳过读取和扣减。 |
| REQ-PANTRY-003 | 通过 | 明确只记录/别扣时同时跳过读和扣。 |
| REQ-PANTRY-004 | 通过 | 跳过原因是稳定状态。 |
| REQ-PANTRY-005 | 通过 | 同商品多批次不视为商品歧义。 |
| REQ-PANTRY-006 | 通过 | 指定批次优先。 |
| REQ-PANTRY-007 | 通过 | FEFO 后 FIFO。 |
| REQ-PANTRY-008 | 通过 | 允许跨批次逐事件原子分配。 |
| REQ-PANTRY-009 | 通过 | 过期批次不静默自动选，显式选择留证。 |
| REQ-PANTRY-010 | 通过 | 并发不得超扣。 |
| REQ-PANTRY-011 | 通过 | 批次关系及时间/来源锚点可表达。 |
| REQ-PANTRY-012 | 通过 | 数量类型、包装层级与换算语义可表达。 |
| REQ-PANTRY-013 | 通过 | 原始量/余量分开，unknown 不等于 0。 |
| REQ-PANTRY-014 | 通过 | 来源、推导、质量、规则版本与问题可追溯。 |
| REQ-PANTRY-015 | 通过 | 每次购买新批次且不覆盖。 |
| REQ-PANTRY-016 | 通过 | 位置推断保留来源/version。 |
| REQ-PANTRY-017 | 不通过 | 显示位置已覆盖，漏自然语言纠正，见 F-004。 |
| REQ-PANTRY-018 | 不通过 | 触发条件基本覆盖，但受宽松 SHOULD 定义及回执边界影响，见 F-001/F-004。 |
| REQ-PANTRY-019 | 通过 | 估算标识、锚点、时区、位置和规则版本完整。 |
| REQ-PANTRY-020 | 通过 | 不编造天数，显示最有价值锚点。 |
| REQ-PANTRY-021 | 通过 | 剩余天数动态展示，不改写原批次。 |
| REQ-PANTRY-022 | 不通过 | 必须重算被降为 MAY，见 F-003。 |
| REQ-PANTRY-023 | 通过 | 临期提示不作安全保证。 |
| REQ-NUTR-001 | 通过 | 来源优先级及不得低级覆盖完整。 |
| REQ-NUTR-002 | 不通过 | 主动查询语义存在，但 SHOULD 例外治理过宽，见 F-001。 |
| REQ-NUTR-003 | 不通过 | 精确资料优先语义存在，但 SHOULD 例外治理过宽，见 F-001。 |
| REQ-NUTR-004 | 通过 | 禁止通用资料套明显不同加工饮料。 |
| REQ-NUTR-005 | 直接语义通过/章节缺口 | unknown 和缺失不补 0 正确；营养基准/覆盖共同语义缺口见 F-005。 |
| REQ-NUTR-006 | 通过 | 明确量、标签、确定换算不标估算。 |
| REQ-NUTR-007 | 不通过 | 标签语义存在，但“应”受 F-001 的宽松例外定义影响。 |
| REQ-NUTR-008 | 不通过 | 标签语义存在，但“应”受 F-001 的宽松例外定义影响。 |
| REQ-NUTR-009 | 通过 | 公共库资料简洁回执可不标估算且详情可追溯。 |
| REQ-NUTR-010 | 直接语义通过/章节缺口 | 字段级采用值/依据正确；计算精度缺口见 F-005。 |
| REQ-NUTR-011 | 直接语义通过/章节缺口 | 只标推定字段正确；覆盖状态定义缺口见 F-005。 |

逐条结果：77/77 已审阅；其中 65 条通过，9 条不通过，3 条精确 ID 语义通过但受同章节未编号共同语义缺口影响。不存在额外任务范围 ID。

## 4. 必查问题结论

1. 仅本人、家庭库存默认来源：通过。
2. 未恢复 0.3、医疗建议、主动饮食评价或虚构目标：通过。
3. fact-first 对数量/库存/营养异常不丢事实：通过；纠错后的附加效果一致性见 F-002。
4. 明确外食跳过库存读取和扣减、无证据 home_default：通过。
5. 用户摄入量、营养采用量、库存扣减量、包装规格隔离：通过。
6. upper_plausible_bound 仅用于营养合理范围、不自动扣库存：通过。
7. 多候选、不足、不可换算、过期批次安全降级且不负库存：通过。
8. 同商品多批次按指定/FEFO/FIFO/跨批次原子分配：通过。
9. ProductIdentity、NutritionProfile、InventoryBatch、NutritionSnapshot 分离，unknown != 0：通过。
10. 入库最低信息、包装、位置、开封、保质和动态天数：主体通过；位置纠正、回执边界和重算强度见 F-003/F-004。
11. 营养优先级、主动查、精确资料、最小披露、快照：主体通过；基准/精度/覆盖缺口见 F-005。
12. append-only、结果/生命周期、幂等、mixed、per-event atomic：主体通过；纠错事务联动和总回执强度见 F-001/F-002。
13. companion contract 边界：分工方向正确，但不能把 F-002/F-004 的共同结果语义一并下放。
14. 规范强度：不通过，见 F-001/F-003。
15. 追踪表：77 个 ID 均单例且指向真实小节；但指针不能补救 F-001—F-005 的语义缺口。
16. 被替代规则：只存在于明确非规范性 superseded 段，现行正文未发现旧规则复活。
17. 可实施性：结构清晰且未冻结新的产品目标；上述缺口修复前，跨路线实现仍可能分叉。

## 5. 独立只读验证

### 5.1 文件、哈希、集合、结构和业务数据

执行要点（PowerShell，只读）：

`Test-Path` 检查候选/计划/审阅文件；`Get-FileHash -Algorithm SHA256` 复核候选与计划；按五个显式范围构造 77 个期望 ID，与候选正则提取的唯一 ID 比较；从 §13 起逐 ID 统计；统计 H1、标题和 Markdown 围栏；递归计数 `.jsonl/.sqlite/.sqlite3/.db`。

结果：

- 候选存在：`True`；计划存在：`True`；审阅报告写入前存在：`False`。
- 候选 SHA256：`B072AA837F9626298986C68A860893E71E5B29C69DE87CECB779FBC5489669FA`，与审阅包一致。
- 当前计划 SHA256：`EC28F7E0E2A39760D753CD0F9DD3E4B444C8DBA46170D6A85F6A87DE000D8EF8`，与审阅包一致。
- 期望 ID：77；候选唯一任务范围 ID：77；缺失：0；额外：0。
- §13 追踪表非单例 ID：0。
- H1：1；全部标题：14；围栏：0；围栏成对：`True`。
- 审阅写入前业务数据文件数：0。

### 5.2 旧规则冲突检索

将候选 §12 之前的现行规范正文与 10 类旧语义模式分别检索：仅在家才读/扣库存、白水出范围、禁止纠错、模糊量强制 preview、库存异常阻断饮食、禁止主动查营养、估算不得计算、旧四级来源、同 SKU 多批次即 multiple、营养估算量自动扣库存。

结果：10 类现行正文命中均为 0；`home_default` 现行命中 3；`skip inventory read/deduct` 现行命中 1；§12 标题明确为“非规范性审计”且正文明确标记 `superseded`。

### 5.3 写入后复核

- 审阅报告存在：`True`；第一行：`FAIL`；发现标题数：5。
- 候选 SHA256 写入后复核：`B072AA837F9626298986C68A860893E71E5B29C69DE87CECB779FBC5489669FA`，保持不变。
- 业务数据写入后计数：0，与写入前一致。

## 6. 非阻断建议

无单独非阻断建议。建议只按 F-001—F-005 做最小规范修复，不扩展产品范围，不在本契约冻结字段名、展示逐字文案或路线实现细节。

---

## 7. 修复候选独立复审

### 7.1 复审结论

复审仍为 **FAIL**。候选哈希与审阅包一致，F-003、F-005 已关闭；F-001、F-002、F-004 仅部分修复。尤其是 §11 的“本契约仅要求/仅规定”再次缩窄 §4 已冻结的纠错副作用和 §8 已冻结的回执边界。此外，全量 77 条回归审阅发现库存默认扣减仍被写成可选，并发现修复将两个默认行为过度提升为 MUST。

### 7.2 初审发现关闭状态

| 初审发现 | 状态 | 独立复审结论 |
| --- | --- | --- |
| F-001 | 未关闭 | SHOULD 定义及 mixed 回执已修复；但 `REQ-MEAL-011` 被从计划的默认行为过度提升为 MUST，`REQ-NUTR-003` 在 §6 与 §9 出现 MUST/SHOULD 强度冲突，见 R-004。 |
| F-002 | 未关闭 | §4 已补齐事务、库存补偿、营养重算、进度和审计语义；§11 又用“本契约仅规定……”将共同边界缩回追加与审计，见 R-001。 |
| F-003 | 已关闭 | 位置或开封变化后已改为 MUST 重算当前有效估算，并保留旧估算及依据。 |
| F-004 | 未关闭 | §8 已补自然语言位置纠正和单品/多品回执边界；§11 又声称本契约“仅要求”状态、位置和覆盖，见 R-002。 |
| F-005 | 已关闭 | 基准种类、基准量/单位、份名、每包装份数、稳定精度、展示舍入不回流及覆盖状态均已补齐，未发现跨节削弱。 |

### 7.3 复审剩余与新增发现

#### R-001｜高｜§11 再次削弱 §4 的 correction/void 共同副作用

- 精确位置：`E:\codx\skill\饮食管家\shared\business-contract.md:30`、`:87`；规范来源 `E:\codx\skill\饮食管家\总功能开发计划0.2.md:265-273`。
- 受影响 REQ ID：`REQ-SCOPE-012`、`REQ-EVENT-003`、`REQ-MEAL-018`。
- 计划中的正确语义：correction/void 采用追加模型；适用的库存补偿和营养重算在可靠事务边界执行，更新有效视图与受影响日期的 `daily_progress`，原事实与旧快照保持可审计。这些是共同业务结果；companion contract 只冻结载体、字段和展示。
- 当前候选的问题：第 30 行已正确补入全部共同结果，但第 87 行随后宣称“本契约仅规定问题必须可登记、纠错/冲销追加且审计可追溯”，从字面排除了同一契约第 30 行的库存补偿、营养重算、进度更新和事务要求。实现无法判断“仅规定”还是 §4 的 MUST 优先，F-002 因跨节冲突未关闭。
- 最小修复：将第 87 行的“本契约仅规定”改为明确引用 §4：companion 只冻结问题分类、载体和展示，不得削弱 §4 已冻结的追加、事务、库存补偿、营养重算、进度更新及审计结果。

#### R-002｜中｜§11 再次削弱 §8 的入库回执共同边界

- 精确位置：`E:\codx\skill\饮食管家\shared\business-contract.md:61-62`、`:85`；规范来源 `E:\codx\skill\饮食管家\总功能开发计划0.2.md:566-570`、`:605-628`。
- 受影响 REQ ID：`REQ-PANTRY-017`、`REQ-PANTRY-018`。
- 计划中的正确语义：回执显示实际采用的位置且位置可自然语言纠正；单商品可详细，多商品同时入库时每项一行简洁；companion contract 只冻结精确中文格式、日期格式和快捷选项文案。
- 当前候选的问题：第 61—62 行已正确冻结共同语义，但第 85 行又说“本契约仅要求回执忠实反映实际状态、位置和覆盖”，没有保留自然语言纠正及多商品逐项一行的 MUST。该“仅要求”与 §8 直接冲突，F-004 未真正关闭。
- 最小修复：第 85 行明确声明 receipt/date contract 只冻结精确格式和文案，必须遵守 §8 的位置纠正及单品/多品结构边界；删除或改写“本契约仅要求”这一缩窄表述。

#### R-003｜高｜home_default 下的可靠库存扣减仍被写成可选

- 精确位置：`E:\codx\skill\饮食管家\shared\business-contract.md:50-52`；规范来源 `E:\codx\skill\饮食管家\总功能开发计划0.2.md:432`、`:465-471`。
- 受影响 REQ ID：`REQ-PANTRY-001`、`REQ-EVENT-003`。
- 计划中的正确语义：无非家庭证据时默认读取库存并尝试关联和扣减；匹配为唯一、单位可处理且库存充足时执行扣减，其他状态才保存饮食并跳过相应库存副作用。
- 当前候选的问题：第 50 行只要求默认读取和尝试关联，第 52 行写成满足 `unique`、可换算且足量时“`MAY` 扣减”。这允许三条路线在所有前置条件满足时仍无理由完全不尝试扣减，与“家庭库存是默认库存来源”和 `REQ-PANTRY-001` 不一致。
- 最小修复：改为 home_default 下 MUST 尝试扣减；唯一、可靠可换算且足量时 MUST 扣减，前置条件不满足时才按已列稳定状态跳过。保留并发、原子和不负库存约束。

#### R-004｜中｜修复把默认行为过度提升为 MUST，并造成 NUTR-003 跨节强度不一致

- 精确位置：`E:\codx\skill\饮食管家\shared\business-contract.md:40`、`:45`、`:69`；规范来源 `E:\codx\skill\饮食管家\总功能开发计划0.2.md:73`、`:402`、`:410-417`、`:649`。
- 受影响 REQ ID：`REQ-MEAL-011`、`REQ-NUTR-003`。
- 计划中的正确语义：`upper_plausible_bound` 是存在可靠合理范围时的默认采用值，按计划规范词属于可由“已登记且生效的例外决定”偏离的默认行为；精确包装/精加工/高度自定义食品应优先精确资料或请求配方，仍是默认优先策略。强制边界是不得用明显不适用的通用资料冒充，且无可靠资料时保持 unknown。
- 当前候选的问题：第 40 行把默认上界改为无例外的 MUST；第 45 行把精确资料优先和请求配方改为 MUST，而第 69 行针对同一 `REQ-NUTR-003` 又保留 SHOULD，形成跨节强度冲突。这不仅未精准修复 F-001，还冻结了计划未决定的零例外行为。
- 最小修复：第 40 行将“可靠范围时 MUST 使用”改为在第 7 行严格例外治理下的 SHOULD；第 45 行与第 69 行统一为 SHOULD 的精确资料/配方优先策略，同时保留不适用通用资料的 MUST NOT、无可靠资料 unknown 及 fact-first。

### 7.4 77 条全量回归审阅

已重新逐项对照总计划第 2—9 节，不沿用初审追踪表结论。复审结果为 69 条通过、8 条受剩余/新增发现影响：

| 需求族 | 逐项复审结论 |
| --- | --- |
| REQ-SCOPE-001—012 | `001`—`011` 通过；`012` 因 R-001 的 §11 缩窄不通过。 |
| REQ-EVENT-001—012 | `001`—`002`、`004`—`012` 通过；`003` 因 R-001/R-003 不通过。`010` 的 mixed 顺序、per-event atomic、前项不回滚及逐项回执已修复。 |
| REQ-MEAL-001—019 | `001`—`010`、`012`—`017`、`019` 通过；`011` 因 R-004 过度提升强度不通过；`018` 因 R-001 跨节冲突不通过。 |
| REQ-PANTRY-001—023 | `002`—`016`、`019`—`023` 通过；`001` 因 R-003 不通过；`017`、`018` 因 R-002 不通过。`022` 已按计划强度修复。 |
| REQ-NUTR-001—011 | `001`—`002`、`004`—`011` 通过；`003` 因 §6/§9 强度冲突及过度冻结不通过。F-005 涉及的最低基准、快照、精度和覆盖语义均无退化。 |

其余高风险语义未发现退化：仅本人、home_default/明确外食跳过读扣、fact-first、四类数量隔离、upper bound 不自动扣库存、异常安全降级、指定批次/FEFO/FIFO/跨批次原子分配、三层实体、unknown != 0、来源优先级/最小披露/不可变快照、幂等和存储失败语义均仍存在。追踪表 77 个指针仍为单例，但 R-001—R-004 表明“有指针”不等于语义完整一致。

### 7.5 独立只读复审证据

复审者独立执行 PowerShell 只读检查：`Test-Path`；`Get-FileHash -Algorithm SHA256`；显式构造五族 77 个期望 ID 并与候选唯一集合比较；从 §13 起逐 ID 计数；统计 H1、标题和 Markdown 围栏；将 §12 之前的现行正文对 10 类旧冲突模式逐类检索；递归扫描 `.jsonl/.sqlite/.sqlite3/.db`。

结果摘要：

- 候选与计划存在；复审更新前本审阅报告存在。
- 候选 SHA256：`D7024EAAD3514EFEEF4DF4A9C988FD32F52E216481073376598A2413F33B515B`，与审阅包一致。
- 当前计划 SHA256：`EC28F7E0E2A39760D753CD0F9DD3E4B444C8DBA46170D6A85F6A87DE000D8EF8`，与审阅包一致。
- 期望任务范围 ID：77；候选唯一任务范围 ID：77；缺失 0；额外 0；追踪表非单例 0。
- H1 计数 1；标题计数 14；围栏计数 0；围栏成对 `True`。
- 10 类旧冲突在 §1—§11 的模式命中均为 0；§12 明确标为非规范性审计且包含 `superseded`。
- `home_default` 现行正文命中 3；`skip inventory read/deduct` 命中 1。
- 复审报告更新前业务数据文件计数：0。

复审报告写入后只读复核：报告第一行精确为 `FAIL`；候选 SHA256 仍为 `D7024EAAD3514EFEEF4DF4A9C988FD32F52E216481073376598A2413F33B515B`；业务数据文件计数仍为 0；初审发现标题 5 个、复审剩余/新增发现标题 4 个。

---

## 8. 第二次独立复审（最终结论）

### 8.1 结论

**PASS。** 已重新完整阅读当前审阅包、候选契约和更新后的实施报告，逐项复核 R-001—R-004、回归 F-001—F-005，并重新对照总计划第 2—9 节审阅全部 77 条任务范围需求及候选中每一处 MUST、MUST NOT、SHOULD、MAY。未发现剩余阻断问题、需求退化、跨节强度冲突、旧规则复活或未经决定的新产品语义。

本节为当前结论；前文的 FAIL、F-001—F-005 和 R-001—R-004 均作为前两轮历史审计保留，不代表当前候选仍失败。

### 8.2 R-001—R-004 关闭核验

| 发现 | 状态 | 独立语义核验 |
| --- | --- | --- |
| R-001 | 已关闭 | `business-contract.md:30` 完整冻结追加、单一事务、适用库存补偿、营养重算、受影响日期进度更新和旧依据审计；`:87` 明确 companion 只冻结载体/字段/展示并 MUST 遵守 §4，不再缩窄共同结果。 |
| R-002 | 已关闭 | `business-contract.md:61-62` 冻结位置自然语言纠正及单品 MAY 详细/多品 MUST 逐项一行；`:85` 明确 receipt/date contract 只冻结精确格式与文案并 MUST 遵守 §8。 |
| R-003 | 已关闭 | `business-contract.md:50` 要求 home_default 默认读取、尝试关联并尝试扣减；`:52` 要求当且仅当 unique、可靠可换算且足量时 MUST 扣减，其他前置条件不满足时提交饮食、稳定跳过并登记问题，fact-first 与不负库存均未受损。 |
| R-004 | 已关闭 | `business-contract.md:40` 将可靠合理范围的 upper_plausible_bound 恢复为 SHOULD；`:45` 与 `:69` 对精确包装/精加工/高度自定义食品均统一为 SHOULD，偏离只能走 `:7` 的已登记且生效例外决定；不适用通用资料的 MUST NOT、无可靠资料 unknown 和 fact-first 保留。 |

### 8.3 F-001—F-005 无退化核验

| 初审发现 | 当前状态 | 回归结论 |
| --- | --- | --- |
| F-001 | 已关闭、无退化 | SHOULD 例外门槛严格；mixed 汇总回执逐项状态为 MUST；默认策略未再被无依据升降级。 |
| F-002 | 已关闭、无退化 | §4 的 correction/void 共同事务与副作用完整，§11 明确服从而非覆盖。 |
| F-003 | 已关闭、无退化 | 位置或开封变化后 MUST 重算当前有效估算并保留旧估算及依据。 |
| F-004 | 已关闭、无退化 | 位置自然语言纠正、单品/多品回执边界完整，§11 未缩窄。 |
| F-005 | 已关闭、无退化 | 营养基准、份名/每包装份数、稳定精度、展示舍入不回流、不可变快照和四类覆盖状态完整。 |

### 8.4 77 条逐语义全量回归

本轮不是关键词抽样。复审者按计划原行逐条重新核对正文语义、规范强度、相邻小节和 §11 companion 边界，结果为 **77/77 通过**：

| 需求族 | 数量 | 全量结论 |
| --- | ---: | --- |
| REQ-SCOPE-001—012 | 12 | 逐条通过：仅本人、家庭库存默认来源、非本人排除、产品定位/非目标、目标边界、隐私/来源/复合食品/历史审计均一致。 |
| REQ-EVENT-001—012 | 12 | 逐条通过：已发生事实、ignored/void、fact-first、事件证据、结果/生命周期、幂等、mixed 顺序/per-event atomic、隔离测试和存储失败均一致。 |
| REQ-MEAL-001—019 | 19 | 逐条通过：餐次、最低事实、数量四层隔离、upper plausible bound、unknown、自然单位、组合菜去重/模板/纠正/套餐异常均一致。 |
| REQ-PANTRY-001—023 | 23 | 逐条通过：home_default 与外食跳过、稳定状态、唯一匹配、指定批次/FEFO/FIFO/跨批次原子、并发、入库最低信息、位置/开封/保质/动态天数/回执均一致。 |
| REQ-NUTR-001—011 | 11 | 逐条通过：来源优先级、普通食材主动查、精加工精确资料、unknown != 0、估算标识、字段级依据、不可变快照和部分覆盖均一致。 |

关键交叉语义再次确认：数量、库存或营养异常只降级附加效果并登记问题，不丢已发生且食物可识别的饮食事实；明确外食/只记录同时跳过库存读取和扣减；营养估算采用量绝不自动成为库存扣减量；过期批次不静默自动选择；同商品多批次不误判为商品歧义；部分营养覆盖不冒充完整总量。

### 8.5 MUST / SHOULD / MAY 全量跨节审计

逐行提取候选 §1—§11 中全部规范词后核对如下：

| 规范词位置 | 计划语义 | 结论 |
| --- | --- | --- |
| `:7` | MUST/不得、SHOULD 严格例外、MAY 许可的全局定义 | 通过。 |
| `:40` | upper_plausible_bound 是可靠范围下的默认行为 | SHOULD，且受 `:7` 严格治理；通过。 |
| `:45`、`:69` | 精确包装/精加工/高度自定义食品优先精确资料或请求配方 | 两节均为 SHOULD，无冲突；通过。 |
| `:35`、`:39`、`:45`、`:53`、`:61-63`、`:72`、`:85` 的 MAY | 缺时间可用接收时间、可靠换算许可、常见食物资料/模板许可、跨批次许可、位置推断/快捷选项/单品详细/开封推断、公共库简洁标识许可及 §11 同语义引用 | 均与计划的“可以/允许/仅在条件下可”一致；通过。 |
| 其余 MUST/MUST NOT | 事实、审计、隔离、强制默认、禁止条件、事务、快照、隐私和回执硬边界 | 逐项与计划硬规则一致，未发现降级或过度冻结。 |

### 8.6 独立只读验证

独立执行的 PowerShell 只读检查包括：

- `Test-Path` 检查候选、计划和审阅报告；`Get-FileHash -Algorithm SHA256` 复核候选与计划。
- 由五个明确范围构造 77 个期望 ID，与候选唯一集合比较；从 §13 起逐 ID 统计追踪表单例性。
- 统计 H1、全部标题和 Markdown 围栏。
- 将 §12 前的现行正文对 10 类旧规则逐类正则检索。
- 递归扫描项目目录中的 `.jsonl`、`.sqlite`、`.sqlite3`、`.db`。

写入本轮审阅结论前的结果：

- 候选存在 `True`；计划存在 `True`；审阅报告存在 `True`。
- 候选 SHA256：`CFECD8F825DB07E9283723D79A17C43CD41EEF42F01FB7083C22A14E5B7BC60D`，与二次复审期望值一致。
- 当前计划 SHA256：`EC28F7E0E2A39760D753CD0F9DD3E4B444C8DBA46170D6A85F6A87DE000D8EF8`，与审阅包一致。
- 期望 ID 77；候选唯一任务范围 ID 77；缺失 0；额外 0；追踪表非单例 0。
- H1 计数 1；标题计数 14；围栏计数 0；围栏成对 `True`。
- 10 类旧冲突在现行正文中的命中均为 0；§12 明确为非规范性审计且标记 `superseded`。
- 业务数据文件写入前计数：0。

### 8.7 非阻断建议

候选契约本身无非阻断建议。实施报告为保留多轮审计，在前部仍将初审候选 `B072AA...` 标作“最终 CONTRACT-v1 SHA256”，后续章节已明确记录当前 `CFECD8...`；建议未来将前者标为“初审候选 SHA256”以降低阅读歧义。这不影响当前候选契约的规范合规性。

本轮审阅写入后只读复核：报告第一行精确为 `PASS`；候选 SHA256 仍为 `CFECD8F825DB07E9283723D79A17C43CD41EEF42F01FB7083C22A14E5B7BC60D`；业务数据文件计数仍为 0；第二次独立复审终局小节恰有 1 个，且报告明确记录 `77/77 通过`。
