# PRODUCT v1.0 OpenClaw 业务测试记录（2026-08-14）

## 测试边界

- 候选源码提交：`1a2784dffeab2de64c8ec007a521b48ddce45cf8`
- 运行环境：OpenClaw 实例 02–07，均使用各自独立的 `diet-manager-b-data` 测试目录。
- 所有业务操作均通过运行中注册的 `diet_manager` 工具发起，不以数据库脚本或 CLI 代替。
- 本轮只做代表性业务验收，不执行全量单元/故障/并发测试。
- 本记录不保存 Gateway 密钥、私钥内容或用户正式业务数据。

## 业务结果

| 实例 | 场景 | 结果 | 业务证据 |
|---|---|---|---|
| 02 | 记录早餐 | 通过（带问题提交） | `committed_with_issues`；记录 `event-3b5caa11f42d49ddb76330a8e3e2a841`；识别鸡蛋 2 个、面包 2 片、牛奶 250ml。未配置营养数据源，因此营养为 `unknown`，库存为 `skipped_insufficient`。 |
| 02 | 查询当天餐食 | 通过 | 只读 `query_meals` 返回 1 条；时间、早餐时段、地点及三项食物数量与写入一致；`committed=false`、`reason_code=read_only_result`。 |
| 03 | 记录 500ml 白水 | 通过 | `committed`；记录 `event-8b0a6f392f91c0f1925bcfecfc3d150e`。 |
| 03 | 查询当天汇总 | 通过 | 只读汇总返回 `water.count=1`、`plain_water_ml_milli=500000`（500ml），与写入一致。 |
| 04 | 查询空库存 | 通过 | `batches=[]`；只读返回，没有业务写入。 |
| 05 | 购买两箱牛奶（每箱 12 盒，每盒 250ml） | 通过 | `committed`；记录 `event-b04b3f20e17107a67cd7c8fe1dcc2a15`。 |
| 05 | 查询库存 | 通过 | 返回 1 个 milk 批次；`quantity_microunits=24000000`、`unit=carton`、状态 active/available、位置 refrigerator；与 24 盒的采购解析一致。 |
| 06 | “明天准备吃鸡蛋” | 通过（零写保护） | `ignored`、`committed=false`、`reason_code=future_plan`，没有生成 record_id，测试目录没有数据库文件。 |
| 07 | “喝了豆浆和白水”且不提供数量 | **发现缺陷** | 返回 `committed_with_issues`，只记录 soy_milk（数量 unknown），白水未记录，也没有 clarification。记录 `event-b6184b2bc0153df6476c5812f3b0ae5b`。 |

## 第二轮循环测试

| 实例 | 场景 | 结果 |
|---|---|---|
| 02 | 完全相同的餐食请求再次提交 | 通过；返回与首次相同的 `event-3b5caa11f42d49ddb76330a8e3e2a841`，没有产生第二条事实。 |
| 03 | “今天没喝水” | 通过零写保护；`committed=false`、`ACTION_CONFLICT`，没有新增饮水事实。 |
| 04 | “买了2盒鸡蛋和3袋面包” | 未实现；`ACTION_CONFLICT`、零写、无 clarification。库存复查仍为空。 |
| 05 | “喝了一盒牛奶” | 通过；生成 `event-cd67bb2ed72b07e09df3a660068ae575`，唯一匹配既有 milk 批次并扣减 1 carton；只读复查从 24 变为 23。 |
| 06 | “买了牛奶”（无数量） | 数据安全通过、交互不足；`ACTION_CONFLICT`、零写，库存复查为空，但没有返回可读 clarification。 |

第二轮证明了幂等、否定句零写、唯一库存匹配扣减和失败请求零半写均能工作；同时新增两个产品缺口：多商品采购未实现，以及缺数量采购没有可读追问。

## 发现的问题

### MIXED-LIQUID-001：同一句豆浆与白水会静默丢失白水

当前 `record_meal` 对“喝了豆浆和白水。”只提交豆浆事实；白水既未转换为 water event，也未触发明确澄清。用户看到 `committed_with_issues`，但如果不阅读详细 receipt，容易误以为整句话都已记录。

建议后续修复满足以下任一稳定语义：

1. 单次请求原子地产生 meal + water 两个记录，并返回 `record_ids`；或
2. 在写入前返回 `needs_clarification`，明确要求分别说明豆浆与白水数量，保持零写。

在修复前，不应宣称混合饮品一句话可以完整记录。

### PURCHASE-MULTI-001：多商品采购被整体拒绝

清晰输入“买了2盒鸡蛋和3袋面包”返回 `ACTION_CONFLICT`，没有写入也没有澄清。零写是安全的，但完整 0.1 的采购能力尚不能处理一句多商品。

### CLARIFICATION-001：缺数量采购只有错误码

输入“买了牛奶”时没有编造数量、也没有写入，这是正确的；但公开 outcome 只有 `ACTION_CONFLICT`，缺少“买了多少/什么规格”的自然语言追问。

### CORE-UNDO-001：权威 v1.0 要求的追加式撤销尚未公开实现

本地覆盖审计确认公开 `undo_record` 当前返回未实现。该项属于 v1.0 `I-2` / `REQ-CORE-004` 的完整 0.1 能力，不属于本轮已通过功能。

## 部署观察

- 实例 02、03 曾保留旧版同名工具的内存注册；仅热重载不足，完整 Gateway 容器进程重启后恢复为新 schema。
- 实例 02–07 的插件均已加载并注册 `diet_manager`。
- 实例 02、03、04、05、07 已创建独立 SQLite 测试库；实例 06 的未来计划被零写拦截，因此其测试目录仍为空。
- 未配置 USDA/FDC 数据源与凭据，所以营养字段保持 unknown；这是当前部署配置限制，不是本轮写入失败。

## 本轮结论

餐食写入与回读、饮水写入与汇总、采购入库与库存查询、未来计划零写保护均已在真实 OpenClaw 运行进程中通过。混合饮品场景存在一项明确业务缺陷，当前候选应标记为“核心链可试用，混合饮品未完成”，而不是全功能完成。

## 2026-08-15 修复部署与日常口语测试

### MIXED-LIQUID-001 修复复测

- 修复提交：源码 `01b1f8e`，构建产物 `5ac410f`。
- 02–07 均从同一最小包更新，包 SHA-256 为 `CCF655285EFEE89ACDD630D0B2830501E9470919D51D4405F1B493AF2E94B615`。
- 仅做 SIGUSR1 热重载时，07 仍运行旧模块，继续把豆浆单独写入；这证明热重载不足以清除同路径 ESM 缓存。
- 完整重启容器主进程后，同一句“喝了豆浆和白水。”返回 `needs_clarification`、`committed=false`、`reason_code=unsupported_command`，没有 `record_id`，豆浆和白水均未写入。修复在线通过。
- 02–06 随后也完成完整进程重启，避免实例间混用旧缓存。

### 全新会话日常口语 RED

这组测试没有给动作名、参数名或调用指令，只使用普通用户表达。六个会话均为新会话。

| 实例 | 用户原话 | 实际结果 |
|---|---|---|
| 02 | 早餐吃了一个鸡蛋和一片面包。 | 模型选择了 `diet_manager`，但漏传完整调用元数据，返回 `APPLICATION_AUTHORITY_REQUIRED`；随后擅自估算约 150–180 千卡。 |
| 03 | 刚刚喝了500毫升水。 | 同样缺调用元数据而未记录；随后错误地用“今日笔记”兜底写入。 |
| 04 | 我今天没吃午饭。 | 没有产生饮食事实，但回答混入跨日猜测和建议，没有明确复述工具零写结果。 |
| 05 | 家里还剩多少牛奶？ | 未正确构造公开查询请求，错误声称没有权限/没有记录，未返回既有 23 盒库存。 |
| 06 | 明天早餐想吃鸡蛋。 | 未写入饮食事实，但回答“记下了”，容易把聊天记忆与业务记录混淆。 |
| 07 | 我刚才喝了豆浆和白水。 | 未写入，但因缺调用元数据得到 `APPLICATION_AUTHORITY_REQUIRED`，没有到达已修复的业务澄清分支。 |

根因不是业务解析器，而是模型可见的工具 Schema/description 只把 `action` 标为结构必填，且简短 description 没有给出自然会话所需的完整七字段配方。Skill 虽已有禁止兜底与禁止编造规则，但全新会话没有稳定遵守它。

本轮已先以失败测试冻结以下约束，再做最小修复：

- 工具 description 明示每次业务调用必须发送七个字段，并从当前 OpenClaw 消息/会话元数据取得时间与标识。
- 六个核心字段分别带有模型可见的 operational-required 说明，同时保留 `required=[action]` 的旧调用兼容边界。
- 工具失败时直接说明未记录，不得写便签、记忆或其他替代记录。
- 工具未返回营养数值时，不得自行估算热量或营养。

修复后的本地边界测试 `tests/foundation.test.ts` 为 11/11 通过，`tsc --noEmit` 通过。下一步是把该接入说明更新到 02–07，并重复同一组全新会话日常口语测试。

## 2026-08-15 集中问题采样（修复前冻结）

按“先收集一批问题、再集中修复、最后统一更新测试”的节奏，本轮未修改源码，使用 02–07 的隔离测试数据连续执行 18 个日常口语场景。每个场景只给用户原话，不提供动作名、字段名或调用指令。出现重复工具循环后人工停止，避免继续产生无意义调用。

### 第一批：常见省略、模糊数量、计划、查询与混合饮品

| 实例 | 用户原话 | 实际结果 | 判断 |
|---|---|---|---|
| 02 | 午饭吃了米饭和鸡胸肉。 | 调用 `record_meal`，但返回 `ignored/non_self_subject`，零写。 | **缺陷**：中文对话常省略“我”，不应在本人私聊里自动判成他人饮食。 |
| 03 | 刚刚喝了两杯水。 | 调用 `record_water`，返回 `ACTION_CONFLICT`，零写，并建议“再发一次”。 | **缺陷**：杯量不明确时应追问杯容量，而不是冲突或盲目重试。 |
| 04 | 明天想买两箱牛奶。 | 没有写入饮食库存，但未经请求创建了次日 10 点提醒，并声称“记下了”。 | **严重交互缺陷**：未来计划不得擅自转成其他工具写入。 |
| 05 | 牛奶还剩多少？ | 正确只读返回冰箱 23 盒。 | 业务通过；回复随后夹带无关的起名、身份档案和记忆邀请。 |
| 06 | 买了两箱牛奶和一袋鸡蛋。 | 两次工具活动后 `ACTION_CONFLICT`、零写；回复误导用户改说“喝了多少/吃了几个”。 | **缺陷**：多商品采购未给采购规格澄清，指导方向错误。 |
| 07 | 喝了500毫升水和250毫升牛奶。 | 触发 14 次工具活动，多次 Diet Manager 错误并搜索容器文件；未形成稳定结果，人工停止。 | **严重缺陷**：混合饮品失败后重复调用和越界排错，未遵守单次失败即如实返回。 |

### 第二批：否定句、明确白水、采购数量、汇总与营养饮品

| 实例 | 用户原话 | 实际结果 | 判断 |
|---|---|---|---|
| 02 | 我没吃早饭。 | `ignored/non_self_subject`、零写；回复明确没记录。 | 数据安全通过；`reason_code` 语义不准确，应是明确否定而不是非本人。 |
| 03 | 我刚刚喝了500毫升白水。 | 完整七字段调用 `record_water`，仍返回 `ACTION_CONFLICT`、零写。 | **缺陷**：中文单位“毫升”未进入已支持的 `ml` 白水路径。 |
| 04 | 刚买了一盒牛奶。 | 模型额外传旧 `items` 字段，后端按原话解析后 `ACTION_CONFLICT`、零写。 | **缺陷**：自然数量词/单位未稳定进入采购解析；回复错误猜测“可能已登记”。 |
| 05 | 我买了牛奶。 | `ACTION_CONFLICT`、零写。 | 安全通过、交互失败：应直接追问数量和规格；仍夹带无关开户式对话。 |
| 06 | 今天的饮食和喝水情况怎么样？ | 只读汇总正确返回饮食 0、饮水 0。 | 通过；末尾仍主动建议创建提醒，属于轻度噪声。 |
| 07 | 我中午喝了250毫升豆浆。 | `needs_clarification/amount_ambiguous`、零写，追问无糖/甜豆浆。 | **缺陷**：数值已明确，真正差异是“毫升”相对 `ml` 的单位解析；追问了错误维度。 |

### 第三批：撤销、跨会话查询、明确采购、位置纠正与控制组

| 实例 | 用户原话 | 实际结果 | 判断 |
|---|---|---|---|
| 02 | 撤销刚才那条早餐记录。 | 9 次工具活动，多次 Diet Manager 错误并搜索插件源码；人工停止。 | **已知未完成功能 + 严重循环**：公开追加式撤销尚未实现，且失败后没有立即收口。 |
| 03 | 今天喝了多少水？ | 正确只读返回 0 条、0 毫升，与本轮两次失败写入一致。 | 通过；证明失败请求没有半写。 |
| 04 | 我买了两箱牛奶，每箱12盒，每盒250ml。 | 完整七字段 `add_inventory` 仍 `ACTION_CONFLICT`、零写；模型能算出 24 盒/6 升。 | **缺陷**：中文数词“两”或自然采购句式未被公开解析器稳定支持。 |
| 05 | 把牛奶从冰箱改到常温。 | 多次 Diet Manager/Memory/Search/Exec 调用并搜索插件文件；人工停止。 | **严重缺陷**：位置纠正的自然语言入口不稳定，失败后越界排错。 |
| 06 | 我没买牛奶。 | 没有 Diet Manager 业务写入，但搜索既有会话并提议写备注/提醒。 | 零库存写通过；否定句不应扩展成记忆或提醒。 |
| 07 | 我中午喝了250ml豆浆。 | 成功记录豆浆，营养保持 unknown，未编造热量。 | 控制组通过；与“250毫升豆浆”形成清晰单位差异。 |

### 集中问题清单（尚未开始修复）

| 优先级 | 编号 | 问题 | 建议修复边界 |
|---|---|---|---|
| P1 | NATURAL-UNIT-001 | `毫升`、`杯`、中文数词“两”未稳定映射到现有 `ml`/数量路径。 | 解析器补自然单位与数词；杯量未知必须澄清，不得填零或猜容量。 |
| P1 | SUBJECT-ELLIPSIS-001 | “午饭吃了……”在私聊中被判 `non_self_subject`。 | 仅在可信的一对一用户上下文采用本人默认；显式他人主体继续零写。 |
| P1 | TOOL-LOOP-001 | 混合饮品、撤销、位置纠正失败后出现 9–14 次工具/文件搜索循环。 | Skill/工具说明要求同一消息最多一次 Diet Manager；失败直接返回，禁止搜索插件源码、写便签、记忆或提醒兜底。 |
| P1 | PLAN-SIDE-EFFECT-001 | “明天想买……”未经请求创建提醒。 | 计划/否定句默认只说明未记录；只有用户明确要求提醒时才允许另一个工具。 |
| P1 | PURCHASE-NATURAL-001 | 明确单品/多品采购自然话术仍 `ACTION_CONFLICT`。 | 支持中文数词与包装单位；多商品不具备原子提交能力时返回规格化 clarification。 |
| P1 | CORRECTION-NATURAL-001 | “把牛奶从冰箱改到常温”未形成一次稳定纠正调用并进入循环。 | 冻结唯一公开纠正说法和所需目标记录选择；缺标识时给可读选项，零写。 |
| P1 | CORE-UNDO-001 | “撤销刚才记录”尚未公开实现。 | 按 v1.0 追加 void/correction 事实，不删除原事实；本轮可先确保稳定未实现响应且不循环。 |
| P2 | RESPONSE-NOISE-001 | 正确查询/提交后夹带起名、身份档案、记忆和提醒推销。 | Skill 限制饮食场景只回答本次结果和一个必要追问。 |
| P2 | REASON-CODE-001 | 明确否定句被标成 `non_self_subject`。 | 保持零写，改成与否定/计划一致的稳定原因。 |

### 下一轮执行方式

1. 先修共同根因：自然单位/数词、单次调用收口、禁止未请求的提醒/记忆兜底。
2. 再修自然采购和位置纠正入口；撤销作为独立较大功能，不与解析小修混写。
3. 开发期间每个根因只保留一个 RED、一个 focused GREEN 和一条记录；不跑全量。
4. 同批源码冻结后统一运行 parser/foundation/OpenClaw focused、`tsc --noEmit`，只构建一次。
5. 先更新一个实例复测，稳定后一次性更新 02–07，再重复上述 18 个自然场景的代表子集。

## 2026-08-15 集中修复批次 1（源码候选）

本批只处理多个实例共同出现的根因，没有实现撤销，也没有扩展数据库或迁移。

- RED：新增 5 个真实解析回归，分别冻结“午饭省略本人”“500毫升白水”“250毫升豆浆”“两杯水需询问容量”“我买了两箱牛奶”。旧实现为 5/5 失败，表现与 OpenClaw 实测一致。
- GREEN：解析器只新增 `毫升` 到既有 ml 单位映射；杯/口/瓶水只标记为未知容量而不猜毫升；仅新增“刚刚/午饭”两个省略主体前缀；仅给冻结的两箱牛奶采购句式增加显式“我”。
- 回归收窄：首次统一 parser 测试发现“其他时候吃了……”被意外放宽；随后撤回泛化，只保留“刚刚/午饭”，原未批准前缀继续零写。
- 模型调用 RED：真实实例已出现 9–14 次 Diet Manager/Exec/Read/Memory 重复调用，以及未来计划未经请求创建提醒。
- Skill/工具说明 GREEN：一条入站消息最多调用一次 Diet Manager；非提交结果立即收口；禁止读取插件文件、运行命令、Memory/便签/提醒兜底；计划和否定不调用工具并明确未记录；提醒必须由用户明确提出；业务回复不追加起名或身份初始化。
- 统一本地验证：`core-parser` 199/199、`foundation` 11/11，共 210/210；`tsc --noEmit` 通过；`git diff --check` 通过。
- scoped review 结论为 P0/P1/P2=0/0/0、READY；随后只执行一次 emit build。候选源码提交 `af446ff`，构建产物提交 `8132f06`。

## 2026-08-15 实例 03 更新与代表性复测

- 最小安装包：`diet-manager-b-min-8132f06.zip`。
- SHA-256：`3905F3A14C39CA5E396397318702EEE8C47305D9262507F2D613CB9B6964AA18`。
- 03 号实例内执行插件安装成功（exit 0），随后终止 Gateway 主进程；Docker `restart: unless-stopped` 自动恢复，重启后运行时间从 33 分钟重置为 6 秒。
- 本轮只更新 03，没有更新 02、04–07。

| 用户原话 | 03 更新后实际结果 | 判断 |
|---|---|---|
| 我刚刚喝了500毫升白水。 | 单次 Diet Manager 调用，成功记录 500 毫升。 | **通过**；中文“毫升”写入路径已恢复。 |
| 刚刚喝了两杯水。 | 单次调用，询问两杯合计毫升或杯容量；随后查询仍只有已提交的 500 毫升。 | 数据零写 **通过**；回复开头“记下了你刚喝了水”不准确，登记 `RESPONSE-TRUTH-001`。 |
| 明天想买两箱牛奶。 | 0 次工具调用；明确说明计划未记入饮食记录；未创建提醒。 | **通过**；仍主动询问是否需要提醒，属于轻度交互噪声。 |
| 今天喝了多少水？ | 单次只读调用，返回 1 次、500 毫升。 | **通过**；同时证明杯量不明请求未产生半写。 |
| 午饭吃了米饭和鸡胸肉。 | 单次调用并成功记录午饭，数量未知保持未知。 | **通过**；省略本人主语已恢复。 |
| 我买了两箱牛奶，每箱12盒，每盒250ml。 | 单次调用并成功加入 24 盒 / 6000ml。 | **通过**；明确主语采购路径已恢复。 |
| 库存里现在有多少牛奶？ | 单次只读调用，返回 24 盒。 | **通过**；采购写入与查询一致。 |
| 我喝了500ml白水和250ml牛奶。 | 单次调用后明确“没有写入”，请求用户决定是否分开记录；没有 Exec/Read/Memory 循环。 | **通过**；原 14 次循环已关闭。 |
| 我中午喝了250毫升豆浆。 | 单次调用并成功记录 250ml 豆浆；营养保持 unknown。 | **通过**；没有编造营养。 |

### 更新后仍待集中处理

1. `RESPONSE-TRUTH-001`：`needs_clarification`/零写结果的自然语言回复不得以“记下了”开头，必须先明确“还没有写入”。已在集中修复批次 2 收紧工具说明，待实例复测。
2. `RESPONSE-NOISE-001`：成功查询/提交后仍有午夜提醒、继续加油、补录能力等非必要话语。已在集中修复批次 2 收紧回复边界，待实例复测。
3. 撤销、位置纠正、多商品采购仍是原计划中的独立功能，不因本轮解析修复而宣称完成。

## 2026-08-15 集中修复批次 2（回复真实性）

- RED：`foundation.test.ts` 新增两条模型可见说明约束，旧 description 1/11 失败：缺少 `committed=false` 禁止“recorded/noted/saved/updated”和禁止鼓励、开户、能力推销、提醒建议的明确措辞。
- GREEN：只修改 `diet_manager` 的模型可见 description；要求 `committed=false` 首句先说明未记录，只保留本次结果和一个必要追问。
- focused：`foundation` 11/11 通过；`tsc --noEmit` 与 `git diff --check` 通过。
- 未修改解析器、数据库、业务事件、Skill 文件或测试数据；待一次构建后只更新 03，复测“刚刚喝了两杯水”和“今天喝了多少水”。

### 批次 2 实例复测结果

- 源码提交 `4393c37`，唯一一次 emit build 后产物提交 `14cb8c0`。
- 安装包 `diet-manager-b-min-14cb8c0.zip`，SHA-256 `058C6A4BCBAF0B2EDBC0E7132D0B0D9D39C399005CB8B3260125354D981C8A31`。
- 03 号安装 exit 0；Gateway 自动重启后 PID 从 9 变为 8、运行时间重置为 1 分钟。
- “刚刚喝了两杯水。”：只调用一次 Diet Manager；回复首句明确“这次没有记录成功”，只询问杯容量或总毫升，`RESPONSE-TRUTH-001` 已关闭。
- “今天喝了多少水？”：只调用一次只读查询；一段回复直接给出 1 次、500 毫升以及 2 餐，没有提醒、鼓励或能力推销，`RESPONSE-NOISE-001` 在该代表场景关闭。
- 当前仍只更新 03；批量更新 02、04–07 前不再改源码。
