# 饮食管家 · 开发约束与需求文档 v1.0（权威版）

> 本文档是**唯一权威版本**，取代 `总功能开发计划` 0.1 / 0.2 / 0.3 / 0.4。
> 历史稿移入 `archive/`，**任何开发与验收均不得引用历史稿**。
> 本文档既给人看，也给 AI 执行；所有可校验条目都以「机器可检验」为第一优先级。

---

## 0. 文档治理

| 项 | 规则 |
|---|---|
| 权威性 | 本文档为唯一事实来源（Single Source of Truth） |
| 版本 | 语义化版本，当前 `v1.0`；每次变更必须 bump 版本并写 CHG 记录 |
| 修改权限 | 只有两类变更可改本文档：**DEC（决策）** 与 **CHG（变更）**；REQ/CASE 只能被它们引用，不得直接增删 |
| 稳定 ID | `REQ-*` / `CASE-*` / `DEC-*` / `I-*`（不变式）/ `CHG-*` / `EV-*`（证据）/ `RISK-*` / `DEBT-*`。ID 一旦分配**永不重命名、永不复用** |
| 证据 | 每个 EV 指向一个不可变产物（commit SHA / 测试输出文件），不复制「已通过」结论 |
| 历史稿 | `archive/` 只读，禁止引用 |

---

## 1. 产品定义

### 1.1 PRODUCT-0.1 —— 可靠日常饮食账本

核心承诺：**记录真实发生的事，且记录过程可靠、可追溯、可恢复。**

### 1.2 发布分层（关键）

| 版本 | 范围 | 目标 |
|---|---|---|
| **0.1.0 首发子集** | 最小闭环（见下） | 先跑通并真正可安装 |
| **0.1.x 完整版** | 全部 REQ-CORE/PANTRY/NUTR/WATER/PURCHASE/CORRECTION/PROGRESS/QUERY/INSTALL | 完整账本 |

**0.1.0 首发子集（最小闭环，冻结）**，只含 5 步：

1. 记录一餐（含原话）
2. 营养解析（可 `unknown`，**不得凭空生成**）
3. 库存唯一匹配扣减（唯一才扣，多候选不自动扣）
4. 生成回执（原话 + 解析结果 + 附加效果状态）
5. 查询当日进度

> 首发子集的意义：**先证明「记录 → 解析 → 扣减 → 回执 → 查询」这条链路可靠**，再谈完整版。完整 0.1 的所有其余能力都在 0.1.0 验证通过后才展开。

### 1.3 PRODUCT-0.2 —— 记录与回顾助手（仅占位，本版不实现）

营养来源研究流水线、跨周期回顾等**只做接口预留，不进入 0.1 实现范围**。

---

## 2. 硬约束（Invariants）

> 这是全文**约束力最强**的部分。每条不变式都必须有可检验方式；任何提交违反不变式即视为失败（Red）。

### I-1 事实优先
- **陈述**：饮食事实（谁、何时、吃了什么、原话）是最高优先级数据。营养解析失败、库存不足、来源缺失，都**只能降级附加效果，不得丢弃或篡改饮食事实**。
- **校验**：单元测试断言——营养失败时，餐次记录仍存在且完整。

### I-2 未发生不写入
- **陈述**：计划、意图、否定（「我没吃」）、取消、未发生的事件**不得写入事实表**；已写入事实的取消，走**追加式 `void_event`**，绝不物理删除或就地改写。
- **校验**：测试断言否定/计划输入不产生事实行；`void_event` 后原事实行仍在且标记 void。

### I-3 未知 ≠ 0
- **陈述**：字段未知（如纤维未提供）存储为 `unknown`，**展示「未知」而非 0**；0 只表示「明确为 0」。
- **校验**：schema 约束（`NULL` 语义区分）+ 测试断言序列化不输出假 0。

### I-4 库存非负 + 唯一匹配
- **陈述**：库存**不得为负**；存在多个候选食品时**不自动扣减**，必须要求用户澄清或标记待定。
- **校验**：schema `CHECK (quantity >= 0)` + 测试断言多候选场景不产生扣减。

### I-5 原话保留
- **陈述**：用户原始输入与结构化推导**分开存储**；估算/推导只做**字段级标记**（`derived: true`），不污染原话。
- **校验**：schema 分列（`raw_text` 与派生字段隔离）+ 测试断言原话字节级不变。

### I-6 幂等
- **陈述**：同一逻辑事件的重复提交（重试/重复消息）**只生效一次**；幂等键由业务语义（非随机）生成。
- **校验**：测试断言重复提交产生相同结果、不产生重复事实行。

### I-7 原子性（有界）
- **陈述**：饮食事实写入 + 其**必要**附加效果（库存扣减）在同一事务提交，任一失败则整体回滚；**非必要**附加效果（营养来源、进度重算）允许降级为异步/跳过，不回滚事实。
- **校验**：故障注入测试——库存失败时，事实与库存均回滚，且事实侧记录 `skipped_inventory`。

### I-8 可追溯
- **陈述**：所有数据变更可追溯到事件；营养事实可追溯到**来源**；库存变化可追溯到**事实**。
- **校验**：每个表含 `source_event_id` / `source_url` 外键或引用列，schema 强制非空（事实/库存链）。

---

## 3. 架构决策（冻结，不可再改除非新 DEC 推翻）

| ID | 决策 | 约束力 |
|---|---|---|
| **DEC-027** | 路线 **B（轻量插件 + SQLite `node:sqlite`）为唯一主线**；A 只读对照、C 语义并入 B | 冻结 |
| **DEC-028** | 安全底座**按业务风险有界收口**，不做金融级保证 | 冻结 |
| **DEC-023** | 分层事务 `FactCommit → EffectBundle → EnvelopeFinalize` **有界启用**（见下） | 冻结 |
| **DEC-029**（新增） | 营养事实**只能来自数据源白名单**（见 §9），AI **禁止凭空生成营养数值** | 冻结 |

### 3.1 分层事务的有界启用（DEC-023 落地）

| 场景 | 使用层 | 说明 |
|---|---|---|
| 纯查询、白水、单条营养标注 | **仅 FactCommit**（单层 SQLite 事务） | 不启用 EffectBundle |
| 饮食写入 + 库存唯一匹配扣减 | FactCommit + **EffectBundle** | 跨对象原子性，必要场景 |
| 需要外部投递/回执保证的场景 | 完整 EnvelopeFinalize | **0.1 默认不启用**，仅当确有跨系统投递需求才加 |

> 规则：**能用单层就用单层**。EffectBundle 只服务于「事实 + 必要附加效果」的原子性；EnvelopeFinalize 是最后手段，进入实现前必须先有一条新 DEC 说明必要性。

---

## 4. 需求（REQ）

> 每条需求：稳定 ID + 描述 + **Given/When/Then 验收**（必须可转成测试）。按领域分块，**每块可独立切片给 AI 执行**。

### 4.1 REQ-CORE —— 记录与事实模型
- **REQ-CORE-001 记录餐次**：接受自然语言，写入事实。
  - G/W/T：给定「我吃了两个鸡蛋」→ 当解析成功 → 则事实行含 `raw_text`、时间、`food=鸡蛋`、`qty=2`。
- **REQ-CORE-002 否定/计划不写入**：见 I-2。
- **REQ-CORE-003 幂等写入**：见 I-6。
- **REQ-CORE-004 追加式撤销**：`void_event` 语义见 I-2。

### 4.2 REQ-PANTRY —— 库存
- **REQ-PANTRY-001 唯一匹配扣减**：唯一候选才扣减。
- **REQ-PANTRY-002 多候选不自动扣**：标记待定，返回澄清问题。
- **REQ-PANTRY-003 负库存禁止**：见 I-4，`CHECK` 约束 + 测试。
- **REQ-PANTRY-004 库存可追溯**：扣减记录引用 `source_event_id`。

### 4.3 REQ-NUTR —— 营养（**最高风险域**）
- **REQ-NUTR-001 营养解析可 unknown**：字段级 unknown（I-3）。
- **REQ-NUTR-002 来源白名单**：营养事实只能引用 §9 白名单数据源；AI 不得生成数值。
- **REQ-NUTR-003 来源可追溯**：每条营养事实带 `source_url`/`source_id`。
- **REQ-NUTR-004 估算标记**：估算/推导字段 `derived: true`（I-5）。

### 4.4 REQ-WATER —— 白水
- **REQ-WATER-001 白水与食物分离记录**：单层 FactCommit。

### 4.5 REQ-PURCHASE —— 购买
- **REQ-PURCHASE-001 购买入库**：购买 → 库存增加，幂等，可追溯。

### 4.6 REQ-CORRECTION —— 纠正与撤销
- **REQ-CORRECTION-001 纠正不物理删除**：追加式修正，保留历史链。

### 4.7 REQ-PROGRESS —— 日进度
- **REQ-PROGRESS-001 六项日进度**：餐次/白水/营养/库存/购买/纠正 的当日汇总。
- **REQ-PROGRESS-002 进度只读重算**：进度是派生视图，可随时重算，不落事实表。

### 4.8 REQ-QUERY —— 查询
- **REQ-QUERY-001 事实查询**：按时间/食品/餐次查询。

### 4.9 REQ-INSTALL —— 安装/备份/恢复
- **REQ-INSTALL-001 一键安装**；**REQ-INSTALL-002 备份**；**REQ-INSTALL-003 恢复**（恢复后不变式全满足）。

---

## 5. 任务切片（Slices）

> AI 执行时**一次只注入一个切片**，不喂整份文档。每个切片自包含：范围、依赖、涉及的 REQ/I、验收、证据要求。

| 切片 | 内容 | 依赖 | 完成产出 |
|---|---|---|---|
| **SLICE-0 骨架** | 插件骨架 + SQLite schema（含 I-3/I-4 的 CHECK 约束）+ 迁移框架 | 无 | 可安装、可跑空库 |
| **SLICE-1 事实写入** | REQ-CORE-001/002/003/004 + I-1/I-2/I-6 | SLICE-0 | 写入 + 幂等 + void 测试通过 |
| **SLICE-2 营养解析** | REQ-NUTR 全部 + I-3/I-5/I-8 | SLICE-1 | unknown/白名单/来源/估算测试通过 |
| **SLICE-3 库存扣减** | REQ-PANTRY 全部 + I-4/I-7 | SLICE-1 | 唯一/多候选/负库存/回滚测试通过 |
| **SLICE-4 回执** | 事实 + 解析 + 附加效果状态回执 | SLICE-2,3 | 回执字段断言通过 |
| **SLICE-5 日进度** | REQ-PROGRESS | SLICE-1..4 | 六项汇总 + 只读重算测试 |
| **SLICE-6 白水/购买/纠正** | REQ-WATER/PURCHASE/CORRECTION | SLICE-1 | 各域测试通过 |
| **SLICE-7 查询/安装/备份/恢复** | REQ-QUERY/INSTALL | SLICE-1..6 | 恢复后 I-1..I-8 全满足 |

> **0.1.0 首发子集 = SLICE-0 → SLICE-5**。SLICE-6/7 属于完整 0.1。

---

## 6. 完成定义（DoD，工具强制）

一个任务**只有同时满足全部 8 条**才算完成，每条对应一个可执行检查：

1. 涉及的所有 REQ 的 Given/When/Then 已转为测试且**通过**（CI Green）。
2. 涉及的所有 I（不变式）有断言且通过。
3. 代码已合并主线，commit 可定位（记录 SHA）。
4. 证据（EV）指向真实产物（测试输出/commit），**不复制「已通过」结论**。
5. 台账状态更新为 `DONE`，且**未超过 WIP=1**（见 §7）。
6. 无新增 `DEBT-*`，或新增 DEBT 已登记并注明偿还计划。
7. 相关 `RISK-*` 已复核，风险未升级。
8. 文档零变更（除非本次同时提交 DEC/CHG）。

---

## 7. 约束执行机制（关键：不是 prompt 自觉，是工具强制）

| 约束 | 执行方式 |
|---|---|
| 验收用例 | 存为**测试文件**，CI 自动跑，失败 = Red，阻止合并 |
| WIP=1 | 台账文件加锁，`pre-commit`/脚本校验同时只有一个 `IN_PROGRESS` |
| 完成定义 8 条 | 提交前 hook 检查 DoD 清单是否勾选齐全 |
| 营养禁造数 | 白名单数据源硬编码为只读引用；AI 无写入口径 |
| 版本漂移 | 仓库根只留本文件；CI 检查 `archive/` 被引用即失败 |
| 证据不可伪造 | EV 必须指向不可变产物哈希，不指向可编辑文本 |

---

## 8. 里程碑与门禁

| 里程碑 | 内容 | 门禁 |
|---|---|---|
| **M0** 骨架与 schema | SLICE-0 | G0：空库可安装、不变式约束落地 |
| **M1** 事实写入 | SLICE-1 | G1：I-1/I-2/I-6 测试通过 |
| **M2** 营养解析 | SLICE-2 | G2：I-3/I-5/I-8 + 白名单生效 |
| **M3** 库存扣减 | SLICE-3 | G3：I-4/I-7 + 回滚故障注入通过 |
| **M4** 回执 | SLICE-4 | 回执字段断言 |
| **M5** 日进度（=0.1.0 完成） | SLICE-5 | **首发子集可安装、可端到端跑通** |
| **M6–M9** 完整 0.1 | SLICE-6/7 | 各域完成 + 恢复后全不变式满足 |

---

## 9. 数据源白名单（营养）

> **本版最关键的防线。** 营养事实只能来自下列来源；**AI 作为开发者不得生成任何营养数值**，只能实现「匹配 + 引用」逻辑。

| 优先级 | 来源类型 | 示例 | 规则 |
|---|---|---|---|
| 1 | 权威数据库 | 中国食物成分表（CFCT）、USDA FoodData Central | 优先引用，带 source_id |
| 2 | 用户自建库 | 用户自己录入的食品营养 | 标记 `source=user`，可编辑 |
| 3 | 包装标签 OCR | 用户拍照/上传营养标签 | 走 OCR 流水线，结果标记 `source=label`，需人工确认 |
| — | **禁止** | AI 凭常识/记忆生成数值 | 一律 `unknown` |

> 0.1 阶段：白名单**至少接入一个权威库（优先级 1）**；其余优先级可作为完整版扩展。解析不到 → `unknown`，**绝不编**。

---

## 10. 变更记录（CHG）

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-14 | 从 0.4 重构：合并四版为单一权威源；新增 I-*（不变式）机器校验层；新增 DEC-029（营养白名单）；明确 0.1.0 首发子集 = SLICE-0..5；事务模型有界启用落地 |
